# CLAUDE.md — shhh!

이 파일은 Claude Code가 매 세션 반드시 알아야 할 **현재 규칙만** 담는다. 과거 결정과 시행착오는
`docs/history/`에 보존하지만 현재 지침으로 사용하지 않는다.

## 1. 현재 상태와 출시 통제

- 실제 스택: Vanilla HTML/CSS/JS PWA + Cloudflare Pages Functions + D1.
- 제품: 연인·친구가 실제 한국수어 단어를 함께 배우는 `shhh!`.
- 현재 단계: **앱스토어 미출시, 내부 개발용 웹/PWA staging**.
- **2단계(회원가입·개인정보·계정 정책) 설계 완료 2026-08-17** — `docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md`.
  설계만 확정됐다. 법률 검토·구현·출시는 각각 별개다(같은 문서 §1).
- `shhh-app.pages.dev`는 인터넷에서 열리지만 OAuth 제공자는 비활성이라 계정 기능은 준비되지 않았다.
- 현재 허용 범위: 로컬 개발, 읽기 전용 점검, 테스트, 빌드, 계정 없는 내부 사용.
- **비공개 계정 테스트 전 P0를 해결해야 하며, 공개 웹/PWA 베타는 현재 No-Go다.**

### 2026-08-12 사용자 결정

- P0 인증·계정 격리·동기화 문제를 고치고 회귀 테스트를 통과하기 전까지 OAuth 시크릿을 넣지 않는다.
- 개인 복구 링크와 개인 식별 정보는 Git에서 제외된 `CLAUDE.local.md`에만 둔다. 사용 후 삭제한다.
- 이번 정리는 문서 구조만 다룬다. 애플리케이션 코드·DB·배포는 별도 승인 후 작업한다.

## 2. 지침의 우선순위와 진실의 원본

- 사용자 전역 `~/.claude/CLAUDE.md`는 적용한다.
- 홈 디렉터리 `/Users/bu/CLAUDE.md`는 이 프로젝트와 무관하므로
  `.claude/settings.local.json`에서 **이 프로젝트에 한해 제외**한다.
- 이 파일은 현재 운영 규칙의 원본이다. `docs/history/CLAUDE_LEGACY_2026-08-12.md`는 참고 기록일 뿐이다.
- 완료 여부는 설명문이 아니라 현재 코드, 회귀 테스트, 배포 환경 실측으로 판정한다.
- 문서와 코드가 다르면 추측하지 말고 차이를 보고한 뒤 코드와 재현 결과를 우선한다.
- `docs/SECURITY_RELEASE_CHECKLIST.md`의 과거 “공개 베타 가능” 판정은 재감사 전까지 유효하지 않다.

## 3. 제품에서 절대 지킬 것

1. **실제 한국수어만 보여준다.** 뜻, 수형, 정확도를 추측하거나 단정하지 않는다.
2. 미검증 데이터는 미검증이라고 표시한다. Claude/AI는 후보를 좁힐 수 있지만 최종 수어 판정을 하지 않는다.
3. 이 앱은 한국어 입력에 대응하는 **수어 단어·합성 후보**를 보여준다. 한국수어 문장 번역기라고 주장하지 않는다.
4. 수어 습득 속도, 유창성, 정확성을 마케팅 문구로 보장하지 않는다.
5. 농인·한국수어를 비장애인 커플의 장식이나 “비밀 놀이”로만 소비하지 않는다.

수어 데이터 작업의 세부 규칙은 `.claude/rules/data-quality.md`를 따른다.

## 4. 사용자 최종 결정권

Claude가 자율적으로 할 수 있는 일:

- 파일 읽기, 검색, Git 상태 확인, 로컬 테스트, 빌드, 정적 분석
- 승인된 범위 안의 되돌릴 수 있는 작은 수정과 회귀 테스트
- 사실관계를 바꾸지 않는 문서 정리

반드시 `분석 → 자기검토 → 대안 비교 → 권고 → 사용자 승인` 후 진행할 일:

- 인증·인가·세션 구조 또는 계정 전환 정책
- 동기화 충돌·병합 정책
- DB 스키마, 데이터 마이그레이션, 백업·복원
- 개인정보 수집, 미성년자 정책, 계정 삭제 정책
- 기능 삭제, 제품 핵심 방향, 결제·외부 유료 서비스
- OAuth 시크릿, Cloudflare 설정, 원격 D1, 캐시 퍼지, 배포, Git push

사용자 선택에 보안 위험이 남으면 그대로 동의하지 않는다. 가능한 공격·데이터 손실, 허용 가능한 공개 범위,
필요한 임시 비활성화 조치를 설명한 뒤 사용자가 최종 결정하게 한다.

## 5. 현재 열린 출시 차단 항목

아래는 “완료”로 바꾸기 전에 공격 흐름 재현과 회귀 테스트가 필요한 열린 항목이다.

| 단계 | 항목 | 완료 조건 |
|---|---|---|
| ~~P0~~ ✅ | OAuth 로그인 브라우저 결속 | **완료 2026-08-12.** `/login` 이 심은 표(`shh_t`)를 `/cb`·`/exchange` 가 code 교환 전에 대조 (`test-friends` 76~84) |
| ~~P0~~ ✅ | 계정별 로컬 상태 격리 | **완료 2026-08-12.** 로그아웃이 `ACCOUNT_KEYS` 를 지우고, 계정 판정을 `remote.me` 수신 후로 (`test-auth` 13~17) |
| ~~P0~~ ✅ | 시계 독립 동기화 | **완료 2026-08-12.** `syncPlan` 이 `dirty`·`base` 만 본다. 시계 인자가 아예 없다 (`test-auth` 3~8·16) |
| ~~P0~~ ✅ | 친구 관계 쌍 유일성 | **완료 2026-08-12.** `pair_key` UNIQUE + 같은 문장의 `ON CONFLICT` (`test-friends` 85~89) |
| ~~P0~~ ✅ | **실패를 성공이라 말하던 화면** | **완료 2026-08-14.** 로그아웃·계정 삭제·친구 철회가 서버 2xx 를 확인한 뒤에만 성공 처리. 404 는 "이미 그 상태"라 성공 수렴 (`test-client`) |
| ~~P0~~ ✅ | **저장 완료 판정** | **완료 2026-08-14.** `localRevision`(이 탭의 편집)과 `serverVersion`(서버가 센 번호)을 분리. 두 번째 409 도 저장 실패 (`test-client`) |
| ~~P0~~ ✅ | **로그인 버튼 fail-open** | **완료 2026-08-14.** 서버에 못 물어봤으면 버튼을 안 그리고 이유를 말한다 (`test-client`) |
| ~~P1~~ ✅ | 남용 방어 | **완료 2026-08-14.** D1 카운터가 실제로 막는다(`limited`). 키가 해시라 원문 IP·uid 를 안 남긴다 (`test-friends` 84~88). **WAF 는 여전히 더 좋은 답** — 도메인이 붙는 날 옮긴다 |
| ~~P1~~ ✅ | **원격 D1 이전 적용** | **완료 2026-08-14.** 백업 후 `0001`·`0002`·`0003` 적용. 적용 전 실측 전 테이블 0행. 그전엔 이력 없이 스키마만 올라가 있었다 |
| ~~P1~~ ✅ | 친구 상한 경합 · 세션 정리 · Origin | **완료 2026-08-14.** 상한을 문장 안에서 세고, 죽은 세션 행을 다음 로그인에 지우고, Origin 없는 상태 변경을 막는다 |
| ~~P1~~ ✅ | readiness | **완료 2026-08-12.** `/ready` 가 `SELECT 1` 을 실제로 던지고, `providers` 가 id·secret 쌍이 맞는 것만 센다(카카오만 secret 선택). `test-friends` 69b~69d |
| ~~P1~~ ✅ | 운영 안전 | **완료 2026-08-14.** 버전형 migration · 백업·복원 시험 · 배포 후 smoke test(`docs/HANDOFF.md` §4-4) · 롤백 절차(§4-3) |
| ~~P0~~ ✅ | **프로덕션 재배포** | **완료 2026-08-14.** 배포 `774015e9` (Production/main, source `cd767ee`). `/api/ready` 에 `db` 확인. Origin 없는 `POST /friends` 가 라이브에서 403 |
| ~~P0~~ ✅ | **친구 목록 무한 로딩** | **완료 2026-08-16.** 사용자가 겪은 그 증상. 옛 세대 `renderFriends` 에 실패 분기가 아예 없었다. 상태를 loading/error/success 로 가르고, 스키마를 경계에서 검증하고, 재시도 버튼을 준다 (`test-client`) |
| ~~P0~~ ✅ | **서비스워커 세대 혼합** | **완료 2026-08-16.** 빌드가 선캐시 자산 해시를 캐시 이름에 박는다(`shhh-v11-<해시12>`). activate 가 지운 뒤 claim 하고, 새 세대가 붙으면 화면을 한 번 다시 읽는다 (`test-sw`·`test-dist`, 실브라우저 확인) |
| ~~P0~~ ✅ | **탭 간 편집 세대 유실** | **완료 2026-08-16.** 편집 세대를 `shh-rev`(localStorage)로 옮겼다. 한 탭의 저장 성공이 다른 탭의 미저장 편집을 "저장 끝"으로 지우지 않는다 (`test-client`) |
| ~~P0~~ ✅ | **리미터 DoS 증폭** | **완료 2026-08-16.** 한도에 닿으면 카운터를 더 안 쓴다. 없는 경로는 안 센다. `auth`/`login` 이중 집계 제거. 바인딩 예외도 fail-open (`test-friends` 89~95) |
| ~~P1~~ ✅ | **인증 요청 무한 대기** | **완료 2026-08-16.** `apiExchange`·`apiLogoutRaw` 에 시간 제한. 없을 때는 응답이 안 오면 **앱 초기화가 통째로 멈췄다** (`test-client`) |
| ~~P1~~ ✅ | **readiness 의미** | **완료 2026-08-16.** `configReady`/`db` 를 갈랐다. 전에는 OAuth 미설정이면 DB 를 안 보고 `db:false` 라 했고, 그래서 진짜 DB 장애와 구분이 안 됐다 (`test-friends` 69e) |
| ~~P0~~ ✅ | **다중 탭 계정 전환 교차 저장** | **완료 2026-08-16.** 문지기 둘 — 저장이 `me`(자기가 믿는 계정)를 싣고 서버가 대조해 409 `accountChanged` (`test-friends` 96~100) · 계정 미확인 탭은 자동 저장을 안 보내고, 다른 탭의 계정 변경·로그아웃을 `storage` 로 감지해 멈춘다 (`test-client`). **`me` 를 안 보낸 옛 화면은 그대로 받는다** — 막으면 업데이트 전 사용자의 저장이 통째로 죽는다 |
| ~~P1~~ ✅ | **친구 UI 경합** | **완료 2026-08-16.** 수락·회전 중복 클릭 잠금, 친구 A/B 단어장 응답 역전 방지 (`test-client`) |
| ~~P1~~ ✅ | **초대 코드 동시성** | **완료 2026-08-16(결정 B).** `0004` 부분 유니크 인덱스가 **사용자당 활성 코드 1개를 DB 가 강제**한다. 충돌은 "누가 이미 만들었다"로 읽어 두 탭이 같은 코드를 받는다. 읽기(`myCode`)는 더 이상 회전을 부르지 않는다 — 화면을 여는 것만으로 이미 보낸 링크가 죽지 않는다 (`test-friends` 107~111 · `test-migrations` 8~9). **원격 D1 적용은 아직** |
| ~~P1~~ ✅ | **계정 삭제 중간 실패** | **완료 2026-08-16(결정 A).** 재현해 보니 500 인데 세션만 죽고 계정·단어장·친구관계가 전부 남아 **친구에게 별명·개수가 계속 보였다.** 이제 `DELETE FROM users` **한 문장**이라 중간이 없고, 실패하면 세션이 살아 있어 그 자리에서 재시도된다. `users` 참조 테이블의 CASCADE 를 스키마에서 전수 검사한다 (`test-friends` 124~129 · `test-migrations` 6~7) |
| **P1** | **옛 엣지 캐시에 내부 파일 7개** | **열림(자동 만료 대기).** `shhh-app.pages.dev` 의 canonical 주소가 레포 루트 배포 시절 응답을 아직 준다. **오리진은 깨끗하다**(배포 고유 주소·쿼리 붙인 주소는 폴백). `*.pages.dev` 는 퍼지가 안 되므로 **2026-08-18 경 자동 소멸**. 비밀값 0건, 옛 `CLAUDE.md` 에 개인 복구 링크 5곳. 자세한 건 `docs/HANDOFF.md` §4-5 |
| ~~P1~~ ✅ | **레이트리밋 키 재식별** | **완료 2026-08-16(결정 A).** 평문 `SHA-256(용도\|IP\|창)` 은 **실측 43회 대입으로 원본 IP 가 복원됐다.** 이제 `RL_KEY` HMAC 이고, 키가 없으면 평문으로 되돌아가지 않고 세지 않으며 `/ready` 가 503 이다 (`test-friends` 116~120). **`RL_KEY` 시크릿 등록은 배포 때** |
| ~~P1~~ ✅ | **초대 링크 회전 쓰기 증폭** | **완료 2026-08-16(결정 A).** 회전이 `write`(120/분)에만 묶여 계정 하나가 **하루 34만 D1 쓰기**(한도 10만)를 태울 수 있었고 폐기 행이 영구히 쌓였다. 전용 버킷 5회/분 + 회전할 때 자기 폐기 행 정리 (`test-friends` 112~115) |
| ~~P1~~ ✅ | **로그인 한도 표기 불일치** | **완료 2026-08-16(결정 A).** "분당 10회"가 실제로는 **완전한 로그인 5회**였다(`/login`+`/cb` 이중 계수). 이제 세션이 실제로 생기는 `/cb`·`/exchange` 만 센다 (`test-friends` 121~123) |
| ~~P1~~ ✅ | **OAuth 응답 무제한 적재** | **완료 2026-08-16.** `text()` 에 상한이 없어 **1MB 응답으로도 로그인이 성공했다.** 64KB 상한(정상 최대 2~4KB의 16배). 제공자 오류 문자열도 80자로 자른다 (`test-friends` 101~106) |
| ~~P1~~ ✅ | **SW activate 청소 실패** | **완료 2026-08-16.** `Promise.all` 이라 캐시 하나를 못 지우면 `claim` 까지 건너뛰어 **떠 있는 탭이 옛 세대로 남았다**(세대 혼합 재발 경로). `allSettled` (`test-sw` 11~12). ⚠️ `skipWaiting()` 위치와 선캐시 실패 동작은 **지금이 맞아서 안 고쳤다** |
| ~~P1~~ ✅ | **개인정보 — 정책 설계** | **완료 2026-08-17.** 가입 화면 구성·`policy_events` v2·`document_version` 해시 규칙·삭제 표식·백업·KV 폐기 순서를 확정 (`docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md`) |
| **P0** | **개인정보 — 외부 법률 검토** | **열림.** 처리 근거가 제15조 제1항 **제1호(동의)** 인지 **제4호(계약 이행)** 인지, 국외 이전이 제28조의8 **제1호** 인지 **제3호** 인지 미확정. **전달 자료는 `docs/PRIVACY_LEGAL_REVIEW_PACKET.md`** (질문 L1~L10). **Claude 가 판정하지 않는다** |
| **P1** | **개인정보 — 3단계 구현** | **열림. 상세 설계는 완료 2026-08-17**(`docs/STAGE3_SIGNUP_SECURITY_DESIGN.md`) — **사용자 결정 6건 대기.** 가입 UI·API·`policy_events` 테이블·migration·`scripts/test-signup.mjs`·`test-policies.mjs`·`test-deletion-ledger.mjs` 전부 **없다.** 위 법률 근거가 확정된 뒤에 시작한다 — SQLite 는 `CHECK` 를 `ALTER` 로 못 바꿔서, 먼저 만들면 쓰지도 않은 테이블에 재생성 migration 을 쓰게 된다 |
| **P1** | **`privacy.html` 불일치 7건** | **열림.** 특히 `:126-127` 이 "로그인하면 동의한 것으로 본다"고 하는데 **게이트 화면에 방침 링크가 없다**(`js/auth.js:501-532` · `css/style.css:223` 이 푸터를 가린다). 목록은 같은 문서 §14. 고치는 시점은 **가입 화면 구현과 같은 변경 단위** |
| **P1** | **Time Travel 복원이 탈퇴 계정을 되살린다** | **열림.** D1 Time Travel 은 끌 수 없고 운영자가 `restore` 를 실행할 수 있다. 복원하면 삭제된 계정·단어장·친구 관계가 돌아온다 — `privacy.html:73` 의 "그 자리에서 사라집니다"가 그 순간 거짓이 된다. **삭제 표식 재적용 절차 없이는 복원 금지.** 요구사항은 같은 문서 §9, **saga·실패 매트릭스·복원 8단계는 `docs/STAGE3_SIGNUP_SECURITY_DESIGN.md` §10** — 설계만이고 구현은 없다 |
| **P1** | **legacy KV 폐기** | **방향 승인, 실행은 별도 승인.** 6단계 순서는 같은 문서 §11. 현재 5개(`b:1 c:1 s:2 u:1`) 살아 있다. **1번(롤백 금지 명시)을 먼저 하지 않고 바인딩부터 떼지 않는다** |
| **P1** | **원격 D1 에 `0004` 미적용** | **열림.** 로컬·테스트에서만 확인했다. 적용 전에는 활성 초대 코드 제약이 **운영에 없다.** 원격에 중복 활성 코드가 있는지도 조회 권한이 없어 미확인. 배포·원격 D1 은 사용자 승인 사항 |
| **P1** | **`RL_KEY` 시크릿 미등록** | **열림.** 없으면 리미터가 세지 않고 `/ready` 가 503 이다. **OAuth 를 켜는 날 같이 넣어야 한다** |

**2026-08-16 배포 완료(2회)**
- `acdecfa2` (Production/main, source `586cc86`) — 다중 탭 계정 보호·친구 UI 경합. **현재 라이브.**
- `fa7d8ef0` (Production/main, source `468d858`) — 친구 목록·서비스워커·리미터·readiness. **롤백 대상.**

> ⚠️ **위 ✅ 중 2026-08-16 1단계 작업분은 아직 배포되지 않았다.** 로컬 테스트만 통과한 상태다.
> 배포하려면 **세 가지를 같은 창에서** 해야 한다 — 하나만 빠지면 라이브가 더 나빠진다:
> ① `RL_KEY` 시크릿 등록(안 하면 리미터가 아예 안 센다) ② 원격 D1 에 `0004` 적용
> ③ 배포. 순서와 절차는 `release` skill 과 `docs/HANDOFF.md` §4-3.

검증 결과는 `docs/HANDOFF.md` §4-5·§4-6.

경로별 세부 보안 규칙은 `.claude/rules/security.md`와 `.claude/rules/privacy.md`를 따른다.

## 6. 구조와 경계

이 프로젝트는 Vanilla JS이므로 다음 세 층을 사용한다.

| 역할 | 현재 파일 |
|---|---|
| 사용자 입력·화면 | `js/app.js`, `js/auth.js`, `js/friends.js` |
| 핵심 판단 | `syncPlan()` 등 화면·저장소 없이 테스트 가능한 순수 함수 |
| 데이터·외부 접근 | `js/authApi.js`, `worker/index.js`, D1 |

- 브라우저 API 호출은 `js/authApi.js`에 모은다. 화면 코드가 직접 `fetch()`하지 않는다.
- Pages 진입점 `functions/api/[[path]].js`에는 로직을 복사하지 않는다.
- DB 행을 화면에 그대로 노출하지 않고 API 경계에서 응답 형태를 제한한다.
- 새 구조 계층이 단순 통과만 한다면 만들지 않는다.
- 주요 경로는 다음과 같다.

| 경로 | 역할 |
|---|---|
| `index.html`, `css/`, `js/` | PWA 화면과 클라이언트 로직 |
| `worker/index.js` | OAuth, 세션, 단어장, 친구 API |
| `worker/schema.sql` | 현재 D1 기준 스키마; 운영 migration 파일이 아님 |
| `functions/api/[[path]].js` | Pages Functions 연결부 |
| `scripts/` | 빌드·수집·검증·회귀 테스트 |
| `scripts/build.mjs` | `dist/` 배포 allowlist의 원본 |
| `data/` | 사전·합성·사람 검증 데이터 |
| `docs/` | 현재 절차와 참고 문서 |

## 7. 작업 순서

1. `git status --short`로 다른 작업과 사용자 파일을 확인한다. 관련 없는 변경은 건드리지 않는다.
2. `package.json`, 관련 코드, 관련 `.claude/rules/*.md`를 실제로 읽는다.
3. 문제를 재현하거나 현재 동작을 먼저 측정한다.
4. 중요한 설계는 두 가지 이상의 대안과 권고를 제시하고 사용자 결정을 받는다.
5. 승인된 최소 범위의 회귀 테스트를 먼저 만들고 코드를 수정한다.
6. 관련 테스트와 전체 `npm test`를 실행한다.
7. 화면 변경은 브라우저·실기기 조건에서 전체 흐름을 다시 본다.
8. `git diff --check`와 전체 diff로 의도하지 않은 변경을 확인한다.
9. 문서의 상태·개수·URL이 실제 결과와 맞는지 갱신한다.

같은 오류를 두 번 만나면 같은 시도를 반복하지 말고 측정 방법을 의심한 뒤 원인 가설 두 개를 비교한다.

## 8. 명령어

```bash
# 정적 화면 개발
python3 scripts/serve.py 8000

# Pages Functions + 로컬 D1
npm run build
npx wrangler pages dev dist

# 전체 회귀 테스트와 의존성 점검
npm test
npm audit
```

- `npm test`가 빌드와 `dist/` 공개 파일 검사까지 수행한다. 16개 스위트다.
- **`scripts/test-client.mjs` 는 화면 코드를 실제로 실행한다.** `js/authApi.js`·`js/auth.js`·`js/friends.js`
  를 index.html 과 같은 순서로 이어 붙여 Node 에서 돌리고 진짜 클릭 처리기를 부른다.
  화면·API 실패 처리를 고칠 때는 **여기에 먼저 빨간 테스트를 만든다** — 정규식으로 소스를 훑는
  검사는 "코드가 이렇게 생겼나"를 잴 뿐 "이렇게 도나"를 재지 못한다.
- 테스트 통과만으로 보안 완료를 선언하지 않는다. 실제 공격 순서가 테스트에 포함됐는지 확인한다.
- 원격 작업과 배포 명령은 여기 두지 않는다. 사용자가 배포를 요청하면 프로젝트 `release` skill을 사용한다.

## 9. 배포·비밀값 안전선

- 승인 없이 deploy, 원격 D1 실행, 시크릿 변경, 캐시 퍼지, DNS 변경, Git push를 하지 않는다.
- **D1 Time Travel `restore` 와 백업 복원은 삭제 표식 재적용 절차 없이 실행하지 않는다.**
  복원은 탈퇴한 사람의 계정·단어장·친구 관계를 되살린다. 절차는
  `docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md` §9, 아직 **설계만 있고 구현이 없다** — 즉 지금은 복원 금지다.
- **legacy KV 삭제는 방향만 승인됐다.** 실제 `kv namespace delete` 는 6단계(같은 문서 §11)를
  차례로 마치고 **파괴적 작업 최종 승인**을 다시 받은 뒤에만 한다.
- 비밀값·세션·개인 UID·복구 링크를 명령 출력, 로그, 커밋, **Git 추적 문서**에 남기지 않는다.
- `--commit-dirty=true`로 프로덕션 배포하지 않는다.
- 스키마 변경은 `worker/schema.sql` 직접 재실행이 아니라 버전형 migration으로 설계한다.
- OAuth 시크릿은 위 P0 완료 및 사용자 승인 전까지 비활성으로 둔다.
- **`RL_KEY` 는 이제 필수 시크릿이다.** 없으면 레이트리밋이 세지 않고 `/api/ready` 가 503 이다.
  값을 명령 출력·로그·커밋에 남기지 않는다. `STATE_KEY`·OAuth secret 을 돌려 쓰지 않는다 —
  용도가 다른 비밀값을 겸용하면 하나를 교체할 때 다른 하나가 같이 무너진다.

## 10. 문서 지도

- `docs/history/README.md`: 역사 자료의 위치와 사용 규칙
- `docs/history/CLAUDE_LEGACY_2026-08-12.md`: 이전 1,212줄 원문에서 개인 복구 정보를 제거한 기록
- `docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md`: **2단계 최종 결정서.** 회원가입 화면 정책, `policy_events` 설계,
  법률 검토 질문, 삭제 표식·백업·KV 폐기 절차, 3단계 진입 조건, 공개 출시 No-Go 조건
- `docs/STAGE3_SIGNUP_SECURITY_DESIGN.md`: **3단계 상세 설계서(2026-08-17).** 가입 상태 머신,
  가입 시점 3안 비교와 권고, 정책 문서 무결성, 삭제 saga·복원 절차, 위협 모델 25건, 테스트 명세,
  사용자 결정 6건. **설계만이고 코드는 0줄이다**
- `docs/PRIVACY_LEGAL_REVIEW_PACKET.md`: **외부 법률 검토에 그대로 전달할 사실 자료.**
  법률 결론이 없다. 질문 L1~L10 과 답에 따라 바뀌는 코드·DB·UI 지점
- `docs/SECURITY_RELEASE_CHECKLIST.md`: 기존 체크리스트. P0 재감사 후 판정을 갱신해야 함
- `docs/D1_MIGRATION.md`: KV→D1 당시 절차와 배경. 현재 상태 판단에는 코드와 실측을 우선
- `CLAUDE.local.md`: 이 기기만의 개인 복구 메모. Git 커밋 금지

작업이 끝나면 코드 파일 목록이 아니라 **왜 그 결정을 했는지**를
`~/Desktop/claude/claude_brain/01-projects/shhh!.md`의 결정 로그에 한 줄 남긴다.
