# CLAUDE.md — shhh!

이 파일은 Claude Code가 매 세션 반드시 알아야 할 **현재 규칙만** 담는다. 과거 결정과 시행착오는
`docs/history/`에 보존하지만 현재 지침으로 사용하지 않는다.

## 1. 현재 상태와 출시 통제

- 실제 스택: Vanilla HTML/CSS/JS PWA + Cloudflare Pages Functions + D1.
- 제품: 연인·친구가 실제 한국수어 단어를 함께 배우는 `shhh!`.
- 현재 단계: **앱스토어 미출시, 내부 개발용 웹/PWA staging**.
- `shhh-app.pages.dev`는 인터넷에서 열리지만 OAuth 제공자는 비활성이라 계정 기능은 준비되지 않았다.
- 현재 허용 범위: 로컬 개발, 읽기 전용 점검, 테스트, 빌드, 계정 없는 내부 사용.
- **비공개 계정 테스트 전 P0를 해결해야 하며, 공개 웹/PWA 베타는 현재 No-Go다.**

### 1-1. 단계 정의와 현재 위치 (2026-08-19)

단계 번호는 이 정의 하나만 쓴다. 다른 문서가 다르게 부르면 여기가 이긴다.

<!-- 현재상태:시작 -->

| 단계 | 무엇 | 상태 |
|---|---|---|
| 1단계 | 보안 기준선 코드 수정 및 로컬 회귀 검증 | **완료 2026-08-19.** 배포 `f72f5225`(source `cba3d3a`) · `RL_KEY` 등록 · 원격 `0004` 적용 · smoke test 통과. **마지막까지 열려 있던 옛 엣지 캐시 7개도 닫혔다** — 2026-08-19 18:15 KST 실측(쿼리 없는 canonical GET, 읽기 전용): **7개 전부 SPA 폴백** — `text/html` · **9,994바이트** · sha256 `7d809fa2268d…` 로 `dist/index.html` 과 바이트 동일 · `cf-cache-status` 헤더 **없음** · `cache-control: public, max-age=0, must-revalidate`. 만료 예상 시각이 아니라 이 실측이 근거다(`docs/HANDOFF.md` §4-5) |
| 2단계 | 회원가입·개인정보·계정 **정책 결정** | **정책 결정 완료 2026-08-17 · 처리 근거·국외 처리·연령·CASCADE 는 2026-08-18 프로젝트 결정으로 확정 · 외부 법률 검토 미완료** → `docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md` §21 |
| 3단계 | 회원가입·보안·삭제·복원 **기술 상세 설계** | **완료 2026-08-20(8판)** → `docs/STAGE3_SIGNUP_SECURITY_DESIGN.md`. 6판·7판의 완료 판정은 각각 다음 재감사로 **철회**됐고, 8판이 위협 48~51 · T61~T64 를 더해 다시 닫았다(§0-8) — **세 판 연속 같은 무늬였다**. 6판은 **복원 gate 를 「사전점검」으로 정정**했다 — 앱 코드는 `wrangler d1 time-travel restore` 를 막지 못한다(§10-8-0) |
| 4단계 | **코드·DB·테스트 구현** | **로컬 구현 완료 2026-08-20 · 원격 미반영.** 가입/로그인 분리 · 정책 번들 · `policy_events` · 가입 state AEAD · 소비 표식 · 삭제 표식 ledger · 유지보수 3상태 · 정리 Worker · **남용 방어 계약(없으면 계정 라우트 fail-closed)** · **T1~T64**. **2026-08-18 로컬 구현 → 08-19 자체 재감사 4건(위협 39~42) → 08-19 독립 재감사 5건(위협 43~47) → 08-20 마감 감사 4건(위협 48~51)** 을 차례로 닫았다. ⚠️ **아홉 건 전부 그때의 스위트가 전부 통과하는 상태에서 재현됐다** — 모드 전환의 옆문 · 호출자 함수가 정한 삭제 증거 · 안 세어진 pending · 잘못된 키로 살아 있는 계정 승격 · 인증 전 쓰기 증폭(`scripts/test-reaudit.mjs` R1~R5) · stale 표식 제거의 경합 · `drained:true` 뒤의 주 D1 쓰기 · 엣지 방어 없이 열린 계정 경로 · 삭제 키 어긋남이 readiness 에 안 나옴(`scripts/test-stage34-closeout.mjs` T61~T64). **테스트 개수와 문서 일치는 완료의 근거가 아니다.** **전역 user-data drain 은 결정 A′ 로 구현 완료 2026-08-18 — HTTP 요청과 정리 크론 둘 다**(크론은 처음에 빠져 있었다 · T47b) · **정리 실패가 Cron 실행 실패로 남고 pending·연속 실패가 `/api/ready` 의 `cleanupAlert` 로 나온다**(T49) · 배포 0건 · 원격 D1 쓰기 0건 |
| 남용 방어 | 엣지 레이트리밋(WAF·Turnstile·Workers 바인딩) | **미구성.** Pages Functions 에는 그 바인딩이 없고 `*.pages.dev` 에는 WAF 규칙을 못 건다. 그래서 **`RL` 바인딩이 없으면 계정 라우트가 DB 를 만지기 전에 503** 이다(위협 50 · T63). 무엇을 붙일지는 **사용자 결정** — 선택지·비용은 `docs/OPS_RUNBOOK.md` §13 |
| 이후 | 운영 반영·배포·출시 검증 | 미착수. **공개 OAuth·계정 출시는 No-Go** |

<!-- 현재상태:끝 -->

> **주 D1 `time-travel restore` 와 백업 복원은 운영상 금지다.** 해제 조건은 **9개**이고
> (`docs/STAGE3_SIGNUP_SECURITY_DESIGN.md` **§10-8-0**), **2026-08-19 기준 셋이 미충족**이다 —
> ⑤ 지금 이 순간 임차증 0건(질의해야 안다) · ⑦ 옛 배포 차단 증명(D1~D12 · §10-8-1) · ⑨ T8-b.
> ⚠️ **앱 코드가 복원을 막는다고 말하지 않는다** — `worker/ops.js` 의 gate 는 **사전점검**이고
> `wrangler d1 time-travel restore` 는 계정 권한으로 실행되어 이 코드를 지나지 않는다.
> 반환값은 언제나 `restoreAllowed:false` 다. 실제 통제는 권한·승인 절차·옛 배포 제거다.
> drain 3종(①②④)은 **결정 A′ 로 구현됐다.**
> ⚠️ **`user-data drain` 이라 부른다** — 「write drain」이라는 이름이 4판에서 읽기를 빠뜨리게 했다.

### 2026-08-18 사용자 결정 — 개인정보·회원가입 (프로젝트 방침)

아래 다섯은 **사용자가 공식 자료와 실제 사례를 검토해 내린 프로젝트 운영 결정**이다.
**외부 법률 검토 완료가 아니고, 법적 적합성 보증도 아니다.** 법령 변경·사업 확장·유료 결제·
민감정보 수집·아동 대상 서비스가 생기면 재검토한다.

| # | 결정 | 그래서 코드가 이렇게 된다 |
|---|---|---|
| A | 서비스 제공에 필수인 처리의 근거는 「개인정보 보호법」 **제15조 제1항 제4호(계약의 이행)** | 가입 화면에 **개인정보 수집·이용 동의 체크박스를 두지 않는다.** `policy_events` 의 `privacy` action 이 `accepted` 가 아니라 **`presented`** 다 |
| B | `policy_events` 는 계정과 함께 삭제(**ON DELETE CASCADE**) | 탈퇴자 가명키 보존 구조를 만들지 않는다. `test-migrations` 의 「users 참조는 전부 CASCADE」 전수 검사와 맞는다 |
| C | Cloudflare 국외 처리위탁·보관의 근거는 **제28조의8 제1항 제3호(계약 이행에 필요한 처리위탁·보관)** | **국외 이전 별도 동의 체크박스를 두지 않는다.** 대신 `privacy.html` 이 법정 공개사항을 표로 적는다 |
| D | 만 14세 미만은 **가입 대상에서 제외** | 필수 진술 1개. 법정대리인 동의 절차·생년월일·본인확인을 만들지 않는다. **「연령 인증 완료」라고 쓰지 않는다** — 허위 진술은 기술로 못 막는다 |
| E | 외부 법률 검토(L1~L15)는 **4단계 착수 조건이 아니다** | `docs/PRIVACY_LEGAL_REVIEW_PACKET.md` 는 역사·전달 자료로 보존한다. **공개 출시 조건에는 그대로 남는다** |

⚠️ `requiredPolicyKinds` 는 정확히 셋이다 — `terms/accepted` · `privacy/presented` · `age14/attested`.
개수는 `REQUIRED_POLICY_EVENTS = requiredPolicyKinds.length` 로 계산한다. **숫자 3을 하드코딩하지 않는다.**

### 2026-08-12 사용자 결정

- P0 인증·계정 격리·동기화 문제를 고치고 회귀 테스트를 통과하기 전까지 OAuth 시크릿을 넣지 않는다.
- 개인 복구 링크와 개인 식별 정보는 Git에서 제외된 `CLAUDE.local.md`에만 둔다. 사용 후 삭제한다.
- 이번 정리는 문서 구조만 다룬다. 애플리케이션 코드·DB·배포는 별도 승인 후 작업한다.

## 2. 지침의 우선순위와 진실의 원본

- 사용자 전역 `~/.claude/CLAUDE.md`는 적용한다.
- 홈 디렉터리 `/Users/bu/CLAUDE.md`는 이 프로젝트와 무관하므로
  `.claude/settings.local.json`에서 **이 프로젝트에 한해 제외**한다.
- 이 파일은 현재 운영 규칙의 원본이다. `docs/history/CLAUDE_LEGACY_2026-08-12.md`는 참고 기록일 뿐이다.
- 완료 여부는 설명문이 아니라 현재 코드, 회귀 테스트, 배포 환경 실측으로 판정한다.
- 문서와 코드가 다르면 추측하지 말고 차이를 보고한 뒤 코드와 재현 결과를 우선한다.
- `docs/SECURITY_RELEASE_CHECKLIST.md`의 과거 “공개 베타 가능” 판정은 재감사 전까지 유효하지 않다.

## 3. 제품에서 절대 지킬 것

1. **실제 한국수어만 보여준다.** 뜻, 수형, 정확도를 추측하거나 단정하지 않는다.
2. 미검증 데이터는 미검증이라고 표시한다. Claude/AI는 후보를 좁힐 수 있지만 최종 수어 판정을 하지 않는다.
3. 이 앱은 한국어 입력에 대응하는 **수어 단어·합성 후보**를 보여준다. 한국수어 문장 번역기라고 주장하지 않는다.
4. 수어 습득 속도, 유창성, 정확성을 마케팅 문구로 보장하지 않는다.
5. 농인·한국수어를 비장애인 커플의 장식이나 “비밀 놀이”로만 소비하지 않는다.

수어 데이터 작업의 세부 규칙은 `.claude/rules/data-quality.md`를 따른다.

## 4. 사용자 최종 결정권

Claude가 자율적으로 할 수 있는 일:

- 파일 읽기, 검색, Git 상태 확인, 로컬 테스트, 빌드, 정적 분석
- 승인된 범위 안의 되돌릴 수 있는 작은 수정과 회귀 테스트
- 사실관계를 바꾸지 않는 문서 정리

반드시 `분석 → 자기검토 → 대안 비교 → 권고 → 사용자 승인` 후 진행할 일:

- 인증·인가·세션 구조 또는 계정 전환 정책
- 동기화 충돌·병합 정책
- DB 스키마, 데이터 마이그레이션, 백업·복원
- 개인정보 수집, 미성년자 정책, 계정 삭제 정책
- 기능 삭제, 제품 핵심 방향, 결제·외부 유료 서비스
- OAuth 시크릿, Cloudflare 설정, 원격 D1, 캐시 퍼지, 배포, Git push

사용자 선택에 보안 위험이 남으면 그대로 동의하지 않는다. 가능한 공격·데이터 손실, 허용 가능한 공개 범위,
필요한 임시 비활성화 조치를 설명한 뒤 사용자가 최종 결정하게 한다.

## 5. 현재 열린 출시 차단 항목

아래는 “완료”로 바꾸기 전에 공격 흐름 재현과 회귀 테스트가 필요한 열린 항목이다.

| 단계 | 항목 | 완료 조건 |
|---|---|---|
| ~~P0~~ ✅ | OAuth 로그인 브라우저 결속 | **완료 2026-08-12.** `/login` 이 심은 표(`shh_t`)를 `/cb`·`/exchange` 가 code 교환 전에 대조 (`test-friends` 76~84) |
| ~~P0~~ ✅ | 계정별 로컬 상태 격리 | **완료 2026-08-12.** 로그아웃이 `ACCOUNT_KEYS` 를 지우고, 계정 판정을 `remote.me` 수신 후로 (`test-auth` 13~17) |
| ~~P0~~ ✅ | 시계 독립 동기화 | **완료 2026-08-12.** `syncPlan` 이 `dirty`·`base` 만 본다. 시계 인자가 아예 없다 (`test-auth` 3~8·16) |
| ~~P0~~ ✅ | 친구 관계 쌍 유일성 | **완료 2026-08-12.** `pair_key` UNIQUE + 같은 문장의 `ON CONFLICT` (`test-friends` 85~89) |
| ~~P0~~ ✅ | **실패를 성공이라 말하던 화면** | **완료 2026-08-14.** 로그아웃·계정 삭제·친구 철회가 서버 2xx 를 확인한 뒤에만 성공 처리. 404 는 "이미 그 상태"라 성공 수렴 (`test-client`) |
| ~~P0~~ ✅ | **저장 완료 판정** | **완료 2026-08-14.** `localRevision`(이 탭의 편집)과 `serverVersion`(서버가 센 번호)을 분리. 두 번째 409 도 저장 실패 (`test-client`) |
| ~~P0~~ ✅ | **로그인 버튼 fail-open** | **완료 2026-08-14.** 서버에 못 물어봤으면 버튼을 안 그리고 이유를 말한다 (`test-client`) |
| ~~P1~~ ✅ | 남용 방어 | **완료 2026-08-14.** D1 카운터가 실제로 막는다(`limited`). 키가 해시라 원문 IP·uid 를 안 남긴다 (`test-friends` 84~88). **WAF 는 여전히 더 좋은 답** — 도메인이 붙는 날 옮긴다 |
| ~~P1~~ ✅ | **원격 D1 이전 적용** | **완료 2026-08-14.** 백업 후 `0001`·`0002`·`0003` 적용. 적용 전 실측 전 테이블 0행. 그전엔 이력 없이 스키마만 올라가 있었다 |
| ~~P1~~ ✅ | 친구 상한 경합 · 세션 정리 · Origin | **완료 2026-08-14.** 상한을 문장 안에서 세고, 죽은 세션 행을 다음 로그인에 지우고, Origin 없는 상태 변경을 막는다 |
| ~~P1~~ ✅ | readiness | **완료 2026-08-12.** `/ready` 가 `SELECT 1` 을 실제로 던지고, `providers` 가 id·secret 쌍이 맞는 것만 센다(카카오만 secret 선택). `test-friends` 69b~69d |
| ~~P1~~ ✅ | 운영 안전 | **완료 2026-08-14.** 버전형 migration · 백업·복원 시험 · 배포 후 smoke test(`docs/HANDOFF.md` §4-4) · 롤백 절차(§4-3) |
| ~~P0~~ ✅ | **프로덕션 재배포** | **완료 2026-08-14.** 배포 `774015e9` (Production/main, source `cd767ee`). `/api/ready` 에 `db` 확인. Origin 없는 `POST /friends` 가 라이브에서 403 |
| ~~P0~~ ✅ | **친구 목록 무한 로딩** | **완료 2026-08-16.** 사용자가 겪은 그 증상. 옛 세대 `renderFriends` 에 실패 분기가 아예 없었다. 상태를 loading/error/success 로 가르고, 스키마를 경계에서 검증하고, 재시도 버튼을 준다 (`test-client`) |
| ~~P0~~ ✅ | **서비스워커 세대 혼합** | **완료 2026-08-16.** 빌드가 선캐시 자산 해시를 캐시 이름에 박는다(`shhh-v11-<해시12>`). activate 가 지운 뒤 claim 하고, 새 세대가 붙으면 화면을 한 번 다시 읽는다 (`test-sw`·`test-dist`, 실브라우저 확인) |
| ~~P0~~ ✅ | **탭 간 편집 세대 유실** | **완료 2026-08-16.** 편집 세대를 `shh-rev`(localStorage)로 옮겼다. 한 탭의 저장 성공이 다른 탭의 미저장 편집을 "저장 끝"으로 지우지 않는다 (`test-client`) |
| ~~P0~~ ✅ | **리미터 DoS 증폭** | **완료 2026-08-16.** 한도에 닿으면 카운터를 더 안 쓴다. 없는 경로는 안 센다. `auth`/`login` 이중 집계 제거. 바인딩 예외도 fail-open (`test-friends` 89~95) |
| ~~P1~~ ✅ | **인증 요청 무한 대기** | **완료 2026-08-16.** `apiExchange`·`apiLogoutRaw` 에 시간 제한. 없을 때는 응답이 안 오면 **앱 초기화가 통째로 멈췄다** (`test-client`) |
| ~~P1~~ ✅ | **readiness 의미** | **완료 2026-08-16.** `configReady`/`db` 를 갈랐다. 전에는 OAuth 미설정이면 DB 를 안 보고 `db:false` 라 했고, 그래서 진짜 DB 장애와 구분이 안 됐다 (`test-friends` 69e) |
| ~~P0~~ ✅ | **다중 탭 계정 전환 교차 저장** | **완료 2026-08-16.** 문지기 둘 — 저장이 `me`(자기가 믿는 계정)를 싣고 서버가 대조해 409 `accountChanged` (`test-friends` 96~100) · 계정 미확인 탭은 자동 저장을 안 보내고, 다른 탭의 계정 변경·로그아웃을 `storage` 로 감지해 멈춘다 (`test-client`). **`me` 를 안 보낸 옛 화면은 그대로 받는다** — 막으면 업데이트 전 사용자의 저장이 통째로 죽는다 |
| ~~P1~~ ✅ | **친구 UI 경합** | **완료 2026-08-16.** 수락·회전 중복 클릭 잠금, 친구 A/B 단어장 응답 역전 방지 (`test-client`) |
| ~~P1~~ ✅ | **초대 코드 동시성** | **완료 2026-08-16(결정 B).** `0004` 부분 유니크 인덱스가 **사용자당 활성 코드 1개를 DB 가 강제**한다. 충돌은 "누가 이미 만들었다"로 읽어 두 탭이 같은 코드를 받는다. 읽기(`myCode`)는 더 이상 회전을 부르지 않는다 — 화면을 여는 것만으로 이미 보낸 링크가 죽지 않는다 (`test-friends` 107~111 · `test-migrations` 8~9). **원격 D1 적용도 완료 2026-08-17** — 아래 `0004` 항목 |
| ~~P1~~ ✅ | **계정 삭제 중간 실패** | **완료 2026-08-16(결정 A).** 재현해 보니 500 인데 세션만 죽고 계정·단어장·친구관계가 전부 남아 **친구에게 별명·개수가 계속 보였다.** 이제 `DELETE FROM users` **한 문장**이라 중간이 없고, 실패하면 세션이 살아 있어 그 자리에서 재시도된다. `users` 참조 테이블의 CASCADE 를 스키마에서 전수 검사한다 (`test-friends` 124~129 · `test-migrations` 6~7) |
| ~~P0~~ ✅ | **옛 엣지 캐시에 내부 파일 7개** | **완료 2026-08-19.** 2026-08-19 18:15 KST 실측(쿼리 없는 canonical GET, 읽기 전용): **7개 전부 SPA 폴백** — `text/html` · **9,994바이트** · sha256 `7d809fa2268d…` 로 `dist/index.html` 과 바이트 동일 · `cf-cache-status` 헤더 **없음** · `cache-control: public, max-age=0, must-revalidate`. 즉 그 주소들이 주는 것은 폴백 하나이고 내부 파일 원문이 아니다. `s-maxage=604800` 짜리 옛 캐시 항목이 사라졌다. **만료 예상 시각을 근거로 쓰지 않고 실제 응답을 다시 측정해서 닫았다.** ⚠️ **그 파일들이 한동안 공개돼 있었다는 사실은 남는다** — 옛 `CLAUDE.md` 안의 개인 복구 링크 5건은 이미 복사됐을 수 있으므로 **쓰지 않는다**(`CLAUDE.local.md` 정리 대상). 비밀값은 0건이었다. 아래는 그때의 기록이다 — **2026-08-18 10:37 KST 측정에서는 7개 전부 원문 200** 이었다(`/CLAUDE.md` 106,598B · `/worker/index.js` 33,307B · `/scripts/test-friends.mjs` 23,749B · `/wrangler.jsonc` 1,256B · `/package.json` 403B · `/package-lock.json` 3,085B · `/docs/API_KEY_GUIDE.md` 3,002B) — 전부 `cf-cache-status: HIT` · `s-maxage 604800`. `*.pages.dev` 는 우리 존이 아니라 퍼지 API 를 못 썼고, 배포 3회로도 지워지지 않았다. 자세한 건 `docs/HANDOFF.md` §4-5 |
| ~~P1~~ ✅ | **레이트리밋 키 재식별** | **완료 2026-08-16(결정 A).** 평문 `SHA-256(용도\|IP\|창)` 은 **실측 43회 대입으로 원본 IP 가 복원됐다.** 이제 `RL_KEY` HMAC 이고, 키가 없으면 평문으로 되돌아가지 않고 세지 않으며 `/ready` 가 503 이다 (`test-friends` 116~120). **`RL_KEY` 시크릿도 등록 완료 2026-08-17** — 아래 `RL_KEY` 항목 |
| ~~P1~~ ✅ | **초대 링크 회전 쓰기 증폭** | **완료 2026-08-16(결정 A).** 회전이 `write`(120/분)에만 묶여 계정 하나가 **하루 34만 D1 쓰기**(한도 10만)를 태울 수 있었고 폐기 행이 영구히 쌓였다. 전용 버킷 5회/분 + 회전할 때 자기 폐기 행 정리 (`test-friends` 112~115) |
| ~~P1~~ ✅ | **로그인 한도 표기 불일치** | **완료 2026-08-16(결정 A).** "분당 10회"가 실제로는 **완전한 로그인 5회**였다(`/login`+`/cb` 이중 계수). 이제 세션이 실제로 생기는 `/cb`·`/exchange` 만 센다 (`test-friends` 121~123) |
| ~~P1~~ ✅ | **OAuth 응답 무제한 적재** | **완료 2026-08-16.** `text()` 에 상한이 없어 **1MB 응답으로도 로그인이 성공했다.** 64KB 상한(정상 최대 2~4KB의 16배). 제공자 오류 문자열도 80자로 자른다 (`test-friends` 101~106) |
| ~~P1~~ ✅ | **SW activate 청소 실패** | **완료 2026-08-16.** `Promise.all` 이라 캐시 하나를 못 지우면 `claim` 까지 건너뛰어 **떠 있는 탭이 옛 세대로 남았다**(세대 혼합 재발 경로). `allSettled` (`test-sw` 11~12). ⚠️ `skipWaiting()` 위치와 선캐시 실패 동작은 **지금이 맞아서 안 고쳤다** |
| ~~P1~~ ✅ | **개인정보 — 정책 설계** | **완료 2026-08-17.** 가입 화면 구성·`policy_events` v2·`document_version` 해시 규칙·삭제 표식·백업·KV 폐기 순서를 확정 (`docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md`) |
| **P1** | **개인정보 — 외부 법률 검토** | **열림 · 4단계 착수 게이트에서는 제외(2026-08-18 프로젝트 결정 E).** 처리 근거는 제15조 제1항 **제4호**, 국외 처리위탁·보관은 제28조의8 제1항 **제3호** 를 프로젝트 방침으로 채택했다. **이것은 외부 법률 검토 완료가 아니다.** 전달 자료 `docs/PRIVACY_LEGAL_REVIEW_PACKET.md`(**L1~L15**)는 그대로 보존한다. **공개 출시 조건에는 남는다** — Claude 가 법적 적합성을 판정하지 않는다 |
| ~~P1~~ ✅ | **개인정보 — 4단계 구현** | **로컬 구현 완료 2026-08-18.** 가입/로그인 분리(`POST /api/signup/start` · `GET /api/policies`) · 정책 번들 `policies/`(pv **`6c02f2903c0c`** — 2026-08-19 `privacy` 판이 하나 늘었다. 옛 판 `privacy-c77d72a0ea60.html` 은 **지우지 않는다**: 그때 그 문서를 본 사람의 기록이 그 해시를 가리킨다) · migration `0005`(`policy_events` CASCADE · `consumed_signup_states`) · 가입 state **AES-256-GCM** · 소비 표식 **TOMBSTONE_KEY HMAC** · 삭제 표식 ledger + `restore_closed` 3상태 · 정리 Worker(`worker/cleanup/`) · 테스트 4종 신설(T1~T49 매핑은 설계서 §13-6). **`internalUid()` 는 삭제했다** — 조회와 생성을 같이 하던 함수라 로그인만으로 계정이 생겼다. ⚠️ **원격은 아무것도 안 했다**: 배포 0건 · 원격 D1 쓰기 0건 · ledger D1 미생성 · 새 시크릿 미등록. **전역 user-data drain 은 결정 A′ 로 구현했다** — HTTP 요청 하나에 임차증 하나, **정리 크론 실행 하나에 하나**(크론은 나중에 편입했다 · T47b·T47d). 주 D1 접근 경로 분류표는 설계서 §10-9-6. **정리 크론의 실패·경보도 마감했다**(2026-08-18): `tick()` 이 실패를 기록한 뒤 **예외를 삼켜서** 3회 연속 실패가 Cron Trigger 에 성공 세 줄로 남았고, `/api/ready` 는 `open_pending`·`fail_streak` 을 **읽고 버렸다** — 이제 실패한 회차는 `ctx.waitUntil()` Promise 를 거부하고, 경보는 `cleanupAlert` **boolean 하나**로 나간다(개수·오류 문자열 비공개 · 못 읽으면 `true` · T49) |
| ~~P1~~ ✅ | 개인정보 — 3단계 설계 | **완료 2026-08-17(5판)**(`docs/STAGE3_SIGNUP_SECURITY_DESIGN.md`). 결정 1·2·5(조건부)·6 과 **A(가입 state AEAD 암호화) · B(정리 전용 Scheduled Worker) · C(옛 배포 차단, 조건부) · D(drain 은 법률 검토 후) · E(보유기간 임시 기술값)** 확정, **결정 3·4 는 법률 검토 종속.** **당시 기록(2026-08-17, 설계를 끝낸 날): 위 대상은 전부 없었다(코드 0줄) — 가입 UI·API·`policy_events`·`consumed_signup_states`·migration·`restore_closed`·삭제 표식 D1·정리 Worker·`user-data drain`·테스트 4종. 그날 명세는 T1~T48 이었다.** 그때 「법률 근거 확정 뒤에 시작한다」고 적은 이유는 SQLite 가 `CHECK` 를 `ALTER` 로 못 바꿔서, 먼저 만들면 쓰지도 않은 테이블에 재생성 migration 을 쓰게 되기 때문이다. ⚠️ **현재 상태는 위 4단계 행과 §1-1 이 말한다** — 그 대상은 2026-08-18 에 전부 만들어졌고 명세는 **T1~T49**(설계서 §13-5·§13-6)다 |
| ~~P0~~ ✅ | **`LEDGER` 없으면 fail-open** | **완료 2026-08-19.** 재현: `LEDGER` 바인딩을 빼면 `readMode()` 가 **`open`** 을 돌려주고 임차증 획득은 `if (env.LEDGER && …)` 로 통째로 건너뛰어, **게이트도 추적도 없이 `GET /book`·`PUT /book`·로그인 콜백이 전부 200** 이었다(삭제만 503 이었다). 그 배포는 삭제 표식도 못 남기고 drain 도 못 센다 — 사용자 데이터를 만지면 안 되는 배포다. 이제 `readMode()` 가 **`unbound`** 를 돌려주고 `maintenanceAllows()` 가 **아는 모드만** 연다(모르는 모드의 기본값이 「읽기 허용」이었다). `/health` 는 `providers: []`·`ledgerBound:false` 로 답해 **눌러도 503 인 버튼을 안 그린다**. 두 자물쇠를 **계층별로** 잰다 (`test-friends` T8-a·T8-a1·T8-a2 · 위협 39) |
| ~~P0~~ ✅ | **없는 주소·로그인 시작의 쓰기 증폭** | **완료 2026-08-19.** 임차증을 **라우트 판정보다 먼저** 따서, `POST /없는주소` 하나가 ledger 에 INSERT+DELETE 를 남겼다 — 인증도 필요 없었다. `GET /login/kakao` 도 주 D1 을 한 줄도 안 만지면서 같은 쓰기를 냈다. 아무나 D1 무료 한도(하루 10만 쓰기)를 태울 수 있었고, 바닥나면 **정상 사용자의 저장이 먼저 죽는다**. 이제 순수 함수 `routeFor()` 가 **게이트·임차증·리미터보다 앞에서** 판정하고, 모르는 경로·method 는 **어느 DB 도 만지지 않고** 404 다. 버킷도 **라우트 하나에 하나**로 바꿨다(`POST /friends` 가 `write`+`friends` 로 두 번 세고 있었다) (`test-friends` T50~T53·T55 · 위협 40) |
| ~~P1~~ ✅ | **미등록 레이트리밋 버킷** | **완료 2026-08-19.** `RL_MAX[bucket] ?? RL_MAX.write` 라, 오타 하나 또는 새 버킷 하나가 **아무도 모르게 한도 120**(가장 넉넉한 값)을 물려받았다 — 실제로 `auth` 버킷이 그랬고 한 번도 먼저 막은 적이 없었다. `[]` 인덱싱이라 `__proto__`·`constructor` 도 값을 내놓아 비교가 늘 거짓이 된다(= 무제한). 이제 `Object.hasOwn` 으로 자기 속성만 보고 **모르면 던진다** (`test-friends` T54 · 위협 41) |
| ~~P0~~ ✅ | **복원 사전점검이 인자로 우회됨** | **완료 2026-08-19.** `restoreGate(state)` 가 **임의의 객체**를 그대로 믿어 아홉을 전부 `true` 로 적은 객체 하나면 통과했고, **테스트가 정확히 그것을 「gate 가 열린다」로 확인**하고 있었다. `reopenReport(env, { targets: [] })` 도 빈 목록이면 잔여 0·살아 있는 계정 0 이라 **`canReopen:true`** 였다 — 아무것도 확인하지 않고 읽기를 다시 여는 길이다. 이제 **코드가 소유한 조건은 인자로 못 바꾸고**, 재개방 판정은 대상 집합을 **주 D1 과 ledger 를 읽어 스스로 만들며**, `setMode(open)` 은 보고서를 받지 않고 그 자리에서 다시 판정한다. ⚠️ **이 코드는 복원을 막지 못한다** — 사전점검이고 `restoreAllowed` 는 언제나 `false` 다 (`test-deletion-ledger` T7-b·H1-b · 위협 42) |
| ~~P0~~ ✅ | **`restore_closed` → `maintenance` 옆문** | **완료 2026-08-19.** 재현: 복원으로 되살아난 탈퇴 계정이 있는 채로 `setMode(env,"maintenance")` 를 부르면 **검증 없이 통과**했고, `maintenance` 는 읽기를 허용하는 상태라 **지웠던 단어장이 200 으로 그대로 읽혔다**. 자물쇠가 `restore_closed → open` **한 전환에만** 있었다 — 위협 36 과 같은 결과가 자물쇠를 부수지 않고 재현된다. 이제 판정 기준이 **「목적지가 사용자 데이터를 읽게 하는가」**이고, `restore_closed` 에서 나가는 길은 **검증된 `open` 하나**다(`maintenance` 로 가려면 `open` 을 거친다). 전환은 **CAS**(판정에 쓴 `mode`·`epoch`)라 판정 뒤 다른 전환이 끼어들면 닫는 쪽으로 실패한다 (`test-reaudit` R1 · **T56** · 위협 43) |
| ~~P0~~ ✅ | **호출자 함수가 삭제 증거를 정했다** | **완료 2026-08-19.** `reopenReport(env,{markFns})` 에 `() => "언제나같은값"` 하나를 넘기면 재삭제 대상이 **0** 이 되고, 대상이 0 이니 잔여도 0·살아 있는 계정도 0 이라 **`canReopen:true`** 였다 — `setMode("open")` 까지 그대로 통과했다. ⚠️ 6판 주석은 「틀린 함수를 주면 대상이 **늘어나지 줄지 않는다**」였고 **정확히 반대**였다. 이제 인자를 없앴고 키 재료는 레지스트리(`DELETION_MARKERS`)가 소유한다. 모르는 `key_version` 이 하나라도 있으면 자동 판정을 **거부**한다 (`test-reaudit` R2 · **T57** · 위협 44) |
| ~~P1~~ ✅ | **경보 전 pending 을 아무 데서도 안 셌다** | **완료 2026-08-19.** `openPendingCount()` 하나가 **경보**와 **복원 안전조건**을 겸했고 정의는 `pending_alert_at < now` 였다 — 방금 실패한 삭제(경보는 24시간 뒤)가 **「미확정 0건」**으로 보였고 그 값을 근거로 재개방·복원 사전점검이 통과했다. 이제 **`pendingTotalCount`**(안전 조건 · 시각을 안 본다)와 **`pendingAlertCount`**(경보 · 엄격한 `<`)로 갈랐다 (`test-reaudit` R3 · **T58** · 위협 45) |
| ~~P0~~ ✅ | **잘못된 키로 살아 있는 계정이 승격됐다** | **완료 2026-08-19.** `DELETION_KEY` 가 바뀌고 버전은 그대로면, 살아 있는 사용자들의 표식이 통째로 어긋나 **계정이 멀쩡히 있는 pending 이 confirmed 로 승격**됐다. **되돌릴 수 없다** — 그 뒤로 그 사람은 「지워진 사람」이고 복원 절차는 그를 재삭제 대상으로 삼는다. HMAC 은 역산할 수 없어 표식만으로는 키를 검증할 수 없으므로, 표식을 처음 남길 때 **키 검사값**을 함께 기록한다(`deletion_keys` · ledger migration **`0002`**). `deletionEvidenceUsable()` 이 reconcile·재개방·stale 제거 **앞에서** 대조하고 안 맞으면 아무것도 하지 않는다 (`test-reaudit` R4 · **T59** · 위협 46) |
| ~~P0~~ ✅ | **인증 없는 요청의 지속 저장소 쓰기에 상한이 없었다** | **완료 2026-08-19.** 임차증을 **인증보다도 리미터보다도 먼저** 땄고, 인증이 필요한 읽기 라우트 넷은 **버킷이 아예 없었다.** 그래서 쿠키 없는 요청·매번 다른 위조 쿠키·틀린 콜백이 하나에 ledger 쓰기 둘을 냈고 **요청 수에 상한이 없었다**(실측 익명 200회 → ledger 400 쓰기 · 5만 회면 D1 무료 한도가 바닥나고 그러면 정상 사용자의 저장이 먼저 죽는다). ⛔ **옛 T53 은 「요청당 정확히 둘」을 확인하고 그것을 「유한한 상한」이라 적었다** — 요청 수가 무제한이면 요청당 상수는 상한이 아니다. 이제 리미터가 **임차증보다 앞**이고 임차증을 드는 라우트는 **전부 버킷을 갖는다**(`read` 신설 · 분당 120). 세는 신원은 **IP** 다. ⚠️ **여러 IP 로 나눠 오는 분산 요청은 앱 코드로 못 막는다** — 엣지(WAF·Turnstile)의 일이고 도메인이 붙어야 쓴다. ⚠️ 대가: 공유 IP(CGNAT)는 같은 버킷을 나눠 쓴다 (`test-reaudit` R5 · **T60** · 위협 47) |
| ~~P2~~ ✅ | **관측할 수 없는 「두 번째 자물쇠」** | **완료 2026-08-19(제거).** 임차증 블록의 `if (!env.LEDGER) return 503` 은 **도달할 수 없는 코드**였다 — 바인딩이 없으면 `readMode()` 가 `unbound` 를 돌려주고 `lease:true` 라우트는 게이트가 전부 503 이다. 전체 스위트를 계측해 **도달 0회**를 확인하고 지웠다. **관측할 수 없는 방어를 통과 항목으로 세지 않는다** — 세는 순간 「자물쇠가 둘」이라는 거짓 안전감만 남는다. 같은 보장을 **관측 가능한 형태로** `test-friends` T8-a1 이 전수로 잰다(라우트 표의 `lease:true` 전부에 대해 `maintenanceAllows(unbound,…) === false`). workerd 실측으로도 확인했다 |
| ~~P1~~ ✅ | **운영 절차서 부재 · 설정 계약** | **완료 2026-08-19.** 설정이 **없는 파일 둘**을 가리키고 있었고(`docs/OPS_RUNBOOK.md`·`worker/wrangler.jsonc`), 배포 가능한 설정에 `<TODO>` 가 남아 있었고, 필수가 된 시크릿 셋과 `LEDGER` 바인딩이 **어느 문서에도** 없었다. `docs/OPS_RUNBOOK.md` 를 실제 절차로 쓰고, 정리 Worker 설정을 **템플릿(`wrangler.example.jsonc`)과 실제 설정으로 갈랐다**(실제 설정은 `.gitignore`). `scripts/test-config.mjs` 가 **코드에서 `env.*` 를 읽어** 문서 네 곳과 대조한다 — 사람이 세는 목록은 반드시 낡는다 |
| ~~P2~~ ✅ | **서비스워커의 거짓 오프라인 단언** | **완료 2026-08-19.** 테스트가 국립국어원 이미지 응답을 `{ok:true, type:"basic"}` 으로 **가정**해 「캐시된다」를 통과시키고 있었다. 실제로 `<img src>` 로 받는 바깥 origin 응답은 **opaque**(`ok:false`·`status:0`)라 성공인지 404 인지 알 수 없고, `cacheable` 은 처음부터 그것을 거부하고 있었다 — **오프라인 손그림은 한 번도 동작한 적이 없다.** 코드에 opaque 거부를 **이유와 함께** 명시하고, 테스트를 실제 응답 모양으로 바꿨다. 허용 범위도 호스트+경로+확장자 셋으로 좁혔다. ⚠️ **고치려면 그림을 우리 origin 으로 옮기거나 CORS 를 요청해야 한다** — 이번 범위 밖이라 `docs/OPS_RUNBOOK.md` §12 에 남긴다 |
| ~~P1~~ ✅ | **`privacy.html` 불일치 8건** | **완료 2026-08-18.** 「로그인하면 동의한 것으로 본다」를 제거하고 처리 근거를 제15조 제1항 제4호로 바꿨다. 게이트 화면 안에 **방침·약관 링크**를 넣었다(`.gate-docs` · `test-client` 가 잰다). 비로그인 요청도 `rate_limits` 행을 만든다는 사실, 국립국어원 서버에 IP 가 남는다는 사실, Cloudflare Pages/Functions/D1 처리, **계정 삭제와 Time Travel 복구본의 차이**를 각각 적었다. 「그 자리에서 사라집니다」 단정과 「미국을 포함한 글로벌 네트워크 국가」를 없앴다. **37일을 법정 보유기간으로 적지 않았다** — 확정 전에는 숫자 자체를 안 적는다 |
| **P1(부분)** | **복원이 탈퇴 계정을 되살린다 — 쓰기뿐 아니라 읽기까지** | **읽기 차단·전역 user-data drain 구현 완료 2026-08-18**(`restore_closed` 3상태 · `test-friends` T40~T43 · **정리 크론 편입 `test-cleanup` T47b·T47d**). **복원 금지는 그대로 유지된다** — **9개** 조건 중 **⑤ 지금 임차증 0건(질의)·⑦ 옛 배포 차단·⑨ T8 이 미충족**이고, 그 상태를 `worker/ops.js` 의 `restoreGate()` 가 **실행되는 검사로** 막는다(T7). 아래는 남은 사실이다 — D1 Time Travel 은 끌 수 없고 운영자가 `restore` 를 실행할 수 있다. 복원하면 삭제된 계정·단어장·친구 관계가 돌아오고 `privacy.html:73` 의 "그 자리에서 사라집니다"가 그 순간 거짓이 된다. ⚠️ **5판에서 더 나쁜 것이 드러났다** — 4판 설계는 복원 중 `GET /api/book`·`/api/me`·`/api/friends/:id/book` 을 **허용**하고 있었다("DB 를 안 쓴다"는 이유). 재삭제 전까지 **되살아난 탈퇴자의 단어장이 그대로 읽히고 세션까지 부활한다**(위협 36 · Critical). **주 D1 복원은 운영상 금지**이고 해제 조건은 **9개다**(설계서 **§10-8-0**) — `restore_closed` 3상태(§10-7) · **`user-data drain`**(읽기 포함) · **옛 배포 차단 D1~D12**(§10-8-1) · **ledger 제자리 restore 폐지**(§10-6-0, 게이트·epoch·lease 가 함께 과거로 간다 — 위협 37) 전부 필요하다. ⚠️ **deletion lease 0건은 전역 정지의 증거가 아니었다** — 그래서 A′ 는 **삭제 saga 가 아니라 사용자 데이터를 만지는 요청 전부**에 임차증을 건다(§10-9-6·§10-9-8) |
| **P1** | **legacy KV 폐기** | **방향 승인, 실행은 별도 승인.** 6단계 순서는 같은 문서 §11. 현재 5개(`b:1 c:1 s:2 u:1`) 살아 있다. **1번(롤백 금지 명시)을 먼저 하지 않고 바인딩부터 떼지 않는다** |
| ~~P1~~ ✅ | **원격 D1 에 `0004` 적용** | **완료 2026-08-17.** 백업·복원 시험 후 적용. 적용 후 `invite_codes_active` 인덱스 존재 · 중복 활성 코드 **0건** · `users` **0명 무변화** |
| ~~P1~~ ✅ | **`RL_KEY` 시크릿 등록** | **완료 2026-08-17.** production 에 등록(값 미출력). 라이브 실측으로 **실제로 센다** — `/api/cb/kakao` 12회 중 11번째부터 429. 증폭 방어도 확인: 12회 요청인데 카운터는 **11 에서 멈춘다**. ⚠️ **preview 환경에는 아직 없다** |
| ~~P1~~ ✅(로컬) | **보유기간을 약속했는데 지우는 사람이 없다** | **로컬 구현 완료 2026-08-18 · 배포 안 함.** `worker/cleanup/` 의 정리 전용 Worker(`0 * * * *`)가 만료된 소비 표식·세션·`rate_limits`·**확정된** 삭제 표식을 대상별 `LIMIT` 으로 지운다(대상 **4가지**). ⛔ **`write_leases` 는 정리 대상이 아니다** — 해제가 **행 DELETE**(결정 A′)라 표에 남은 행은 전부 「진행 중이거나 stale」이고, 둘 다 「아직 안 끝났다」는 증거다. 시간이 지났다는 이유로 지우면 그 증거가 사라져 복원 금지가 저절로 풀린다. **pending 표식도 지우지 않고 세어서 알린다.** `/api/ready` 가 둘을 갈라 말한다 — **`cleanupStale`** = 마지막 성공이 주기의 2배(2시간)보다 오래됐나(정리가 **제 시간에 돌고 있나**) · **`cleanupAlert`** = 확정 안 된 삭제 표식이 있거나 연속 실패가 **3회**에 닿았거나 **정리 상태를 아예 못 읽나**(사람이 **봐야 할 것이 쌓였나**). 둘 다 boolean 만 나가고 개수·실패 횟수·오류 문자열은 안 나간다. 둘 다 `ready` 를 내리지 않는다 — 크론 하나로 사이트를 503 으로 만들지 않는다. 실패한 회차는 `ctx.waitUntil()` Promise 를 **거부해** Cron Trigger 에 실패로 남는다. ⚠️ **배포하지 않았으므로 운영에서는 여전히 아무도 안 치운다.** 아래는 그때의 기록이다 — 세션·`rate_limits` 정리가 **다음 로그인**에만 붙어 있어(`worker/index.js:256-261`) 아무도 로그인하지 않으면 영원히 남는다. 삭제 표식·소비 표식도 같은 무늬다. **정리 전용 Scheduled Worker** 를 4단계에서 만든다(설계서 §10-5-2 · 위협 38). Cron 은 **무료 계정당 5개**, CPU **10ms** 제약 |
| ~~P1~~ ✅ | **`/api/ready` 의 거짓 정상 판정** | **완료 2026-08-18.** 재현: `policy_events`·`consumed_signup_states`·`deletions` 를 통째로 지워도 **200 · ready:true** 였다 — migration 을 빠뜨린 배포가 smoke test 를 통과하고 사용자의 첫 가입에서 500 이 났다. 이제 주 D1 질의가 신규 두 표를, `ledgerAnswers()` 가 ledger 네 표를 실제로 건드린다. 표 이름·SQL·오류 문자열은 응답에 안 실린다 (`test-friends` 69f) |
| ~~P2~~ ✅ | **`signupReady:false` 인데 가입 버튼이 보임** | **완료 2026-08-18.** `apiHealth()` 가 `signupReady` 를 버려서, 가입 전용 키만 빠진 상태에서 「카카오로 가입하기」를 그려 놓고 누르면 503 이었다. 이제 로그인 가능/가입 가능을 갈라 말한다 — 기존 사용자는 로그인하고, 가입 화면은 약관을 받아 오지도 않고 이유를 말한다 (`test-client`) |
| ~~P2~~ ✅ | **정리 Worker 의 공개 `/status`** | **완료 2026-08-18.** 인증 없이 `cleanup_runs` 전체(실패 사유·삭제 건수·pending 수)를 돌려줬고, `workers_dev` 기본값이 `true` 라 배포하면 `workers.dev` 로 열렸다. **`fetch` 핸들러를 없앴고** `workers_dev:false` 를 얹었다. 상태는 `/api/ready` 의 `cleanupStale`·observability·D1 직접 조회로 본다 (`test-cleanup`) |
| ~~P2~~ ✅ | **가입 화면 위쪽이 잘려 못 읽힘** | **완료 2026-08-18(실브라우저).** `.gate` 가 `justify-content: center` 라 내용이 넘치면 **제목과 요약 첫 줄이 스크롤로도 닿지 않았다**(390×844 실측). `safe center` + `overflow-y:auto`. 같은 화면에서 `signup_required` 안내가 약관 뒤(974자 중 918번째)로 밀리던 것도 고쳤다 (`test-client`) |
| ~~P1~~ ✅ | **가입 정보가 제공자 URL 을 평문으로 지나간다** | **완료 2026-08-18.** `makeState()` 는 **서명일 뿐 암호화가 아니다**(`b64u` 는 인코딩). 4판 설계는 그 자리에 약관 수락·연령 진술·행위 시각을 넣게 되어 있었고, 그 값은 카카오·네이버·구글 로그와 브라우저 기록을 **읽을 수 있는 상태로** 지나간다. 5판에서 **AES-GCM(AEAD) + 전용 `SIGNUP_STATE_KEY`** 로 바꿨다(설계서 §5-4). 소비 표식도 단순 SHA-256 → **`TOMBSTONE_KEY` HMAC**(§5-3-5). **법적 판단은 하지 않았다** — 법률 자료 `L15` |

**현재 라이브 (2026-08-18 09:07 KST 재확인)**
- **배포 `f72f5225`** — Production / branch `main` / source `cba3d3a`. 2026-08-16 1단계 작업분이
  여기 들어 있다. `--commit-dirty=true` 를 쓰지 않았다.
- **롤백 후보**: `acdecfa2` (Production/main, source `586cc86`) — 그 앞은 `fa7d8ef0`(source `468d858`).

> **당시 사실 — 2026-08-16.** 그날 배포 두 건(`acdecfa2` · `fa7d8ef0`) 시점에는 1단계 작업분이
> 아직 나가지 않았고, 배포에 **세 가지를 같은 창에서** 하는 것이 조건이었다 — ① `RL_KEY` 시크릿
> 등록 ② 원격 D1 에 `0004` 적용 ③ 배포. **셋 다 2026-08-17 에 끝났다.** 절차 원본은
> `release` skill 과 `docs/HANDOFF.md` §4-3 이다.

검증 결과는 `docs/HANDOFF.md` §4-5·§4-6.

경로별 세부 보안 규칙은 `.claude/rules/security.md`와 `.claude/rules/privacy.md`를 따른다.

## 6. 구조와 경계

이 프로젝트는 Vanilla JS이므로 다음 세 층을 사용한다.

| 역할 | 현재 파일 |
|---|---|
| 사용자 입력·화면 | `js/app.js`, `js/auth.js`, `js/friends.js` |
| 핵심 판단 | `syncPlan()` 등 화면·저장소 없이 테스트 가능한 순수 함수 |
| 데이터·외부 접근 | `js/authApi.js`, `worker/index.js`, `worker/ledger.js`, D1 · ledger D1 |
| 운영 명령(요청 처리 아님) | `worker/ops.js` — 유지보수 전환·drain 확인·reconciliation·ledger 병합·**복원 사전점검**(막는 장치가 아니다) |
| 크론(별도 배포) | `worker/cleanup/index.js` — 만료 데이터 정리. **Pages Functions 에 넣지 않는다** |

- **어떤 주소가 실재하는지는 `worker/index.js` 의 `ROUTES` 표 하나가 정한다.** 라우트를 추가하면
  거기에 한 줄 적는다 — 안 적으면 404 다(기본값이 안전한 쪽). 그 줄이 **임차증을 드는지**와
  **어느 레이트리밋 버킷인지**도 함께 정한다. 버킷은 라우트 하나에 하나다.
- **임차증(`lease:true`)을 드는 라우트에는 버킷이 반드시 있다.** 없으면 그 라우트가 곧 증폭
  통로다 — 인증 없는 요청 하나가 ledger 쓰기 둘을 내고 요청 수에 상한이 없다(위협 47).
  `test-friends` T55 가 표를 훑어 이 규칙을 강제한다.
- **레이트리밋은 임차증보다 먼저 돈다.** 뒤에 두면 429 를 돌려주는 요청도 임차증을 쓴다.
  그래서 세는 신원은 uid 가 아니라 **IP** 다(그 자리에서는 아직 누구인지 모른다).
  `scripts/test-docs.mjs` 가 소스에서 두 자리의 **순서를 직접 잰다**.
- **삭제 증거를 만드는 함수를 인자로 받지 않는다.** `reopenReport`·`reconcile`·`removeStalePending`
  은 `worker/ledger.js` 의 `DELETION_MARKERS` 레지스트리에서 키 재료를 만든다. 호출자가 넘긴
  함수가 증거를 정하면 상수 하나로 「다 지워졌다」가 된다(위협 44).
- **`restore_closed` 에서 나가는 길은 검증된 `open` 하나다.** `maintenance` 도 읽기를 허용하는
  상태이므로 그리로 곧장 가면 자물쇠를 지나지 않는다(위협 43).
- 브라우저 API 호출은 `js/authApi.js`에 모은다. 화면 코드가 직접 `fetch()`하지 않는다.
- Pages 진입점 `functions/api/[[path]].js`에는 로직을 복사하지 않는다.
- DB 행을 화면에 그대로 노출하지 않고 API 경계에서 응답 형태를 제한한다.
- 새 구조 계층이 단순 통과만 한다면 만들지 않는다.
- 주요 경로는 다음과 같다.

| 경로 | 역할 |
|---|---|
| `index.html`, `css/`, `js/` | PWA 화면과 클라이언트 로직 |
| `worker/index.js` | OAuth, 세션, 단어장, 친구 API |
| `worker/schema.sql` | 현재 D1 기준 스키마; 운영 migration 파일이 아님 |
| `functions/api/[[path]].js` | Pages Functions 연결부 |
| `scripts/` | 빌드·수집·검증·회귀 테스트 |
| `scripts/build.mjs` | `dist/` 배포 allowlist의 원본 |
| `scripts/fixtures/` | **운영 코드가 아닌 고정 fixture.** `legacy-worker.mjs` 는 옛 배포 세대(T8-b) — 고치지 않는다 |
| `worker/cleanup/wrangler.example.jsonc` | 정리 Worker 설정 **템플릿**. 실제 설정(`wrangler.jsonc`)은 `.gitignore` 이고 `docs/OPS_RUNBOOK.md` §3 이 만든다 |
| `policies/` | 가입 화면이 보여주는 **불변** 정책 문서와 `manifest.json`. 덮어쓰지 않고 추가만 한다 |
| `policies-src/` | 그 문서의 **살아 있는 원본**. 배포되지 않는다 (`privacy` 의 원본은 `privacy.html`) |
| `scripts/policies.mjs` | 원본 → 불변 사본 · manifest · `worker/policies.js` · SW 선캐시 목록을 한 번에 만든다 (`npm run policies`) |
| `migrations-ledger/` | 삭제 표식 ledger D1 의 migration. **주 D1 과 다른 데이터베이스다.** `0001` 초기 스키마 · **`0002` 삭제 키 검사값**(`deletion_keys` · 2026-08-19 · 위협 46) |
| `data/` | 사전·합성·사람 검증 데이터 |
| `docs/` | 현재 절차와 참고 문서 |

## 7. 작업 순서

1. `git status --short`로 다른 작업과 사용자 파일을 확인한다. 관련 없는 변경은 건드리지 않는다.
2. `package.json`, 관련 코드, 관련 `.claude/rules/*.md`를 실제로 읽는다.
3. 문제를 재현하거나 현재 동작을 먼저 측정한다.
4. 중요한 설계는 두 가지 이상의 대안과 권고를 제시하고 사용자 결정을 받는다.
5. 승인된 최소 범위의 회귀 테스트를 먼저 만들고 코드를 수정한다.
6. 관련 테스트와 전체 `npm test`를 실행한다.
7. 화면 변경은 브라우저·실기기 조건에서 전체 흐름을 다시 본다.
8. `git diff --check`와 전체 diff로 의도하지 않은 변경을 확인한다.
9. 문서의 상태·개수·URL이 실제 결과와 맞는지 갱신한다.

같은 오류를 두 번 만나면 같은 시도를 반복하지 말고 측정 방법을 의심한 뒤 원인 가설 두 개를 비교한다.

## 8. 명령어

```bash
# 정적 화면 개발
python3 scripts/serve.py 8000

# Pages Functions + 로컬 D1
npm run build
npx wrangler pages dev dist

# 전체 회귀 테스트와 의존성 점검
npm test
npm audit
```

- `npm test`가 빌드와 `dist/` 공개 파일 검사까지 수행한다. **24개 스위트**다.
- **`scripts/test-config.mjs` 는 설정·바인딩·시크릿 계약만 본다.** 코드에서 `env.*` 를 읽어
  `README.md`·`worker/SETUP.md`·`wrangler.jsonc`·`docs/OPS_RUNBOOK.md` 와 대조하고, 설정이
  가리키는 파일이 실재하는지·배포 가능한 설정에 placeholder 가 없는지를 본다.
  **개수를 문서에 손으로 적지 않는다** — `RL_KEY` 때 한 번, 가입·삭제 키 셋 때 또 한 번 낡았다.
- **`scripts/test-docs.mjs` 는 문서 일관성만 본다.** 낡은 문구·죽은 `§` 참조·번호 연속성·
  선언된 개수·판 번호·필수 절·완료 선언의 범위·미확정 보유기간 단정을 검사한다.
  **런타임 테스트가 아니다** — 4단계 기능이 도는지는 재지 않는다. 이 둘을 섞지 않는다.
  낡은 문구를 새로 금지하려면 `FORBIDDEN` 배열에 한 줄 더한다. **과거 오류를 설명하는
  문장은 통과한다**(정정 맥락을 앞뒤 2줄에서 찾는다) — 기록은 지우면 안 되기 때문이다.
- **`scripts/test-client.mjs` 는 화면 코드를 실제로 실행한다.** `js/authApi.js`·`js/auth.js`·`js/friends.js`
  를 index.html 과 같은 순서로 이어 붙여 Node 에서 돌리고 진짜 클릭 처리기를 부른다.
  화면·API 실패 처리를 고칠 때는 **여기에 먼저 빨간 테스트를 만든다** — 정규식으로 소스를 훑는
  검사는 "코드가 이렇게 생겼나"를 잴 뿐 "이렇게 도나"를 재지 못한다.
- 테스트 통과만으로 보안 완료를 선언하지 않는다. 실제 공격 순서가 테스트에 포함됐는지 확인한다.
- 원격 작업과 배포 명령은 여기 두지 않는다. 사용자가 배포를 요청하면 프로젝트 `release` skill을 사용한다.

## 9. 배포·비밀값 안전선

- 승인 없이 deploy, 원격 D1 실행, 시크릿 변경, 캐시 퍼지, DNS 변경, Git push를 하지 않는다.
- **주 D1 의 Time Travel `restore` 와 백업 복원은 지금 금지다.** 해제 조건은 **9개**이고
  (설계서 §10-8-0) **셋이 미충족**이다 — ⑤ 지금 이 순간 임차증 0건(**질의해야 안다**) ·
  ⑦ 옛 배포 차단 증명(D1~D12) · ⑨ T8. 복원은 탈퇴한 사람의 계정·단어장·친구 관계를 되살리고,
  drain 없이 복원하면 **진행 중이던 쓰기가 복원본에 섞인다**(로그아웃 무효화·단어장 소실).
  전역 user-data drain 은 A′ 로 구현했다 — **HTTP 요청 하나에 임차증 하나, 정리 크론 실행
  하나에 하나.** ⚠️ **「임차증 0건」을 코드가 아니라 `drainReport()` 의 질의 결과로만 읽는다.**
  ⚠️ **크론은 처음에 빠져 있었다** — 게이트만 읽고 임차증 없이 주 D1 을 지워서, 지우는 도중에
  `drainState()` 가 `drained:true` 라고 답했다(재현 · T47b). 「HTTP 요청만 세면 된다」로 읽지 않는다.
  복원이 꼭 필요하면 임의 실행하지 말고 **사고 대응으로 승격**해 별도 승인을 받는다.
- ⚠️ **`worker/ops.js` 가 복원을 막는다고 쓰지 않는다.** 그것은 **사전점검**이고
  `wrangler d1 time-travel restore` 는 계정 권한으로 실행되어 이 코드를 지나지 않는다.
  반환값은 언제나 `restoreAllowed:false` 다. 실제 통제는 권한·승인 절차·옛 배포 제거(§10-8-1)다.
- **legacy KV 삭제는 방향만 승인됐다.** 실제 `kv namespace delete` 는 6단계(같은 문서 §11)를
  차례로 마치고 **파괴적 작업 최종 승인**을 다시 받은 뒤에만 한다.
- **`RESTORE_STATE.noActiveLeases` 를 손으로 `true` 로 적지 않는다.** 그 값은 `drainReport()` 의
  질의 결과여야 한다 — 코드에 적은 참은 증거가 아니다.
- 비밀값·세션·개인 UID·복구 링크를 명령 출력, 로그, 커밋, **Git 추적 문서**에 남기지 않는다.
- `--commit-dirty=true`로 프로덕션 배포하지 않는다.
- 스키마 변경은 `worker/schema.sql` 직접 재실행이 아니라 버전형 migration으로 설계한다.
- OAuth 시크릿은 위 P0 완료 및 사용자 승인 전까지 비활성으로 둔다.
- **새 시크릿 3개가 필수가 됐다**(2026-08-18, 아직 **등록하지 않았다**): `SIGNUP_STATE_KEY`(32바이트,
  base64) · `TOMBSTONE_KEY` · `DELETION_KEY`. 없으면 가입은 503, 계정 삭제는 503 이고 `/api/ready` 가
  그 사실을 말한다. **서로도, 기존 키와도 겸용하지 않는다** — 용도가 다른 비밀값을 겸용하면 하나를
  교체할 때 다른 하나가 같이 무너진다.
- **`LEDGER` D1 바인딩이 필수다. 없으면 사용자 데이터 API 가 전부 503 이다**(2026-08-19 · 위협 39).
  계정 삭제만이 아니다 — 게이트도 임차증도 없는 배포는 삭제 표식을 못 남기고 drain 도 못 센다.
  `/health` 가 `providers: []`·`ledgerBound:false` 로 답해 **버튼도 안 그린다.**
  **아직 만들지 않았다** — 생성·바인딩·배포 순서는 `docs/OPS_RUNBOOK.md` 이고 전부 별도 승인 대상이다.
  ⚠️ Pages 프로젝트에 Wrangler 설정 파일이 있으면 **그 파일이 바인딩의 원본**이고 대시보드에서
  같은 항목을 편집할 수 없다(공식 문서) — 그래서 `LEDGER` 도 `wrangler.jsonc` 에 적는다.
- **`RL_KEY` 는 이제 필수 시크릿이다.** 없으면 레이트리밋이 세지 않고 `/api/ready` 가 503 이다.
  값을 명령 출력·로그·커밋에 남기지 않는다. `STATE_KEY`·OAuth secret 을 돌려 쓰지 않는다 —
  용도가 다른 비밀값을 겸용하면 하나를 교체할 때 다른 하나가 같이 무너진다.

## 10. 문서 지도

- `docs/history/README.md`: 역사 자료의 위치와 사용 규칙
- `docs/history/CLAUDE_LEGACY_2026-08-12.md`: 이전 1,212줄 원문에서 개인 복구 정보를 제거한 기록
- `docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md`: **2단계 최종 결정서.** 회원가입 화면 정책, `policy_events` 설계,
  법률 검토 질문, 삭제 표식·백업·KV 폐기 절차, 3단계 진입 조건, 공개 출시 No-Go 조건
- `docs/STAGE3_SIGNUP_SECURITY_DESIGN.md`: **3단계 기술 상세 설계서(2026-08-19, 6판) — 설계 완료.** 가입 상태 머신,
  가입 시점 3안 비교(C안 확정), **가입 state AEAD 암호화(§5-4)**, **소비 표식 HMAC(§5-3·§5-3-5)**,
  정책 문서 무결성, 정책 행위 시각, 삭제 saga·**유지보수 3상태 전수 표(§10-7 `restore_closed`)**·
  **주 D1 접근 경로 전수 분류표(온라인 workload · 운영 명령 · 공개 상태 확인 · §10-9-6)**·**restore 금지 gate 9조건(§10-8-0)**·**옛 배포 차단 D1~D12(§10-8-1)**·
  **ledger 재작성 13단계(§10-6-0)**·**정리 Worker(§10-5-2)**, 위협 모델 **42건**,
  테스트 명세 **T1~T55**(§13-5)과 **구현 매핑표(§13-6)**. 설계 근거는 그대로 두고,
  실제로 무엇이 만들어졌는지는 §13-6 이 말한다.
  2판 9건 §0-2 · 3판 6건 §0-3 · 4판 8건 §0-4 · 5판 11건 §0-5 · **6판 4건 §0-6**
- `docs/PRIVACY_LEGAL_REVIEW_PACKET.md`: **외부 법률 검토에 그대로 전달할 사실 자료.**
  법률 결론이 없다. 질문 **L1~L15**(L9 는 하위 질문 L9-1~L9-6 · L12 소비 표식 · **L13 제공자 식별자 성질**
  · **L14 백업과 표식 보유기간의 동기화** · **L15 가입 정보의 제공자 URL 통과**)와
  답에 따라 바뀌는 코드·DB·UI 지점
- `scripts/test-docs.mjs`: **문서 일관성 자동 검사**(`npm test` 에 포함). 낡은 문구·죽은 `§` 참조·
  번호 연속성·선언된 개수·판 번호·필수 절·완료 선언 범위·미확정 보유기간 단정을 본다.
  **런타임 테스트가 아니다** — 4단계 기능이 도는지는 재지 않는다
- `docs/OPS_RUNBOOK.md`: **원격 작업 절차서(2026-08-19 신설).** ledger D1 생성 → 시크릿 →
  주 D1 `0005` → 바인딩 → 배포 → `/api/ready` 기대값 → 크론 확인. **중단 기준과 롤백 기준**,
  복원 금지, 옛 배포 정리, 남은 외부 작업. `scripts/test-config.mjs` 가 이 문서와 코드를 대조한다
- `docs/SECURITY_RELEASE_CHECKLIST.md`: 기존 체크리스트. P0 재감사 후 판정을 갱신해야 함
- `docs/D1_MIGRATION.md`: KV→D1 당시 절차와 배경. 현재 상태 판단에는 코드와 실측을 우선
- `docs/OAUTH_REAPPROVAL_RUNBOOK.md`: 네이버·카카오 재승인 **순서**. 무엇을 적을지는 `brand/*-REVIEW.md`
- `brand/NAVER-REVIEW.md` · `brand/KAKAO-REVIEW.md`: 제공자별 검수 자료. **제출 0건**
- `CLAUDE.local.md`: 이 기기만의 개인 복구 메모. Git 커밋 금지

작업이 끝나면 코드 파일 목록이 아니라 **왜 그 결정을 했는지**를
`~/Desktop/claude/claude_brain/01-projects/shhh!.md`의 결정 로그에 한 줄 남긴다.
