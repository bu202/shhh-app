{
  "name": "shh-ksl",
  "private": true,
  "type": "module",
  "description": "shhh! — 개발용 스크립트 전용. 배포되는 앱(index.html/js/css)은 의존성 0.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "policies": "node scripts/policies.mjs stamp",
    "test": "for t in auth client book fingers compounds match assemble knn screen home meaning policies signup friends deletion-ledger cleanup migrations hash sw dist docs; do node scripts/test-$t.mjs || exit 1; done"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "wrangler": "4.123.0"
  }
}
