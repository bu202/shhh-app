# 3단계 상세 설계 — 회원가입 · 정책 기록 · 삭제 복원 안전성

**작성 2026-08-17 · 개정 2026-08-22(11판) · 브랜치 `cf-pages`**

**상태: 3단계 기술 상세 설계 11판 완료 · 4단계 로컬 구현 완료(2026-08-22 전체 재검증 + 독립 검토 보완) ·
원격은 **안전 동기화만 실행**(배포 `19e69dee` · `READY_KEY` · 옛 배포 15개 삭제)이고 **계정 인프라는 미실행** · 전역 user-data drain 은 결정 A′ 로 구현 완료 · 외부 법률 검토 미완료 ·
주 D1 restore 금지 유지 · 엣지 방어는 **A(도메인+WAF)로 확정됐고 아직 구성 전**이라 계정
라우트는 fail-closed · 공개 OAuth·계정 출시 No-Go.**

> ⚠️ **9판은 사용자 결정으로 닫혔고, 10판은 전체 재검증 4건을, 11판은 독립 검토 3건을 닫았다.**
> 아래 철회 기록은 그 앞의 사실이다.
>
> ⛔ **8판의 「3단계 완료 · 4단계 완료」 판정은 철회됐다(2026-08-22).** 남용 방어 독립 재감사가
> **스위트 24개가 전부 통과하는 상태에서** 결함 5건을 재현했다 — truthy 하나로 방어 있음 판정,
> 단일 바인딩으로 낼 수 없는 6개 한도, 다중 IP 최초 요청, 코드가 열지 못하는 선택지 A·C,
> 공개 `/ready` 의 무제한 DB 조회. 목록은 §0-9 이고 위협 52~56 · **T65~T69** 가 그것을 잰다.
> **네 판 연속 같은 무늬다.** 재판정 조건은 §21-0.

> ⚠️ **7판의 「완료」 판정도 같은 무늬로 철회됐다.** 마감 감사(2026-08-20)가 **스위트 23개가
> 전부 통과하는 상태에서** 결함 4건을 재현했다 — 삭제 증거를 지우는 경합, `drained:true` 뒤의
> 주 D1 쓰기, 엣지 방어 없이 열려 있는 계정 경로, 삭제 키가 어긋나도 200 인 readiness.
> 목록은 §0-8 이고 위협 48~51 · **T61~T64** 가 그것을 잰다. **세 판 연속 같은 일이 일어났다.**

> ⚠️ **6판의 「완료」 판정은 철회됐다.** 독립 재감사(2026-08-19)가 **당시의 스위트 22개가 전부 통과하는
> 상태에서** 우회 5건을 실제로 재현했다 — 모드 전환의 옆문, 호출자 함수가 정한 삭제 증거,
> 안 세어진 pending, 잘못된 키로 살아 있는 계정 승격, 인증 전 쓰기 증폭. 목록은 §0-7 이고
> 위협 43~47 · **T56~T60** 이 그것을 잰다. **테스트 개수와 문서 일치는 완료의 근거가 아니다.**

> ⚠️ **6판은 「복원을 막는다」는 표현을 걷어냈다.** `worker/ops.js` 의 gate 는 **사전점검**이고
> `wrangler d1 time-travel restore` 를 막지 못한다 — 그 명령은 계정 권한으로 실행되고 앱 코드를
> 지나지 않는다. 조건을 코드에 두는 것은 그대로 옳지만, **막는다고 말하지 않는다**(§10-8-0).

> ⚠️ **이 문서는 설계서다.** 「이번 단계에서는 만들지 않는다」류의 문장은 **3단계 시점의 서술**이고,
> 그 대상은 대부분 2026-08-18 에 만들어졌다. 무엇이 실제로 있는지는 §13-6 의 T1~T49 매핑표와
> `CLAUDE.md` §1-1 이 원본이다. 설계 근거는 지우지 않고 그대로 둔다 — **왜 그렇게 만들었는지가
> 코드에는 안 남기 때문이다.**

## 개정 이력

| 판 | 무엇이 바뀌었나 |
|---|---|
| 1판 | 최초 설계 제출 |
| **2판** | 사용자 검토에서 지적된 **설계 결함 9건**을 고쳤다. 목록은 §0-2. **결정 1·2·5·6 은 확정**(§17)되었고 3·4 는 법률 검토 대기로 남았다 |
| **3판** | **quiescence 증명의 범위를 정정**하고(§10-9) **주 D1 restore 를 운영상 금지**했다(§10-8-0). promote-only 확정(§10-4) · ledger 병합·키 회전 규칙 완성(§10-6-1·§10-6-2) · 정책 이벤트 수 파라미터화(§8-0) · state 재사용 사실 정정(§4-A·I8) · 위협 32·33 추가 · 4단계 테스트 명세 신설(§13-5) · 단계 명칭 통일. 목록은 §0-3 |
| **5판** | **결정 A~E 반영 + 자기검토 결함 2건.** ① 가입 state 를 **AEAD 로 암호화**(§5-4) — 4판까지 약관 수락·연령 진술이 **평문으로 제공자 URL 을 지나갔다** ② 소비 표식을 **전용 키 HMAC** 으로(§5-3-5) ③ **`restore_closed` 상태 신설**(§10-7) — 복원 중 **읽기·세션 인증까지** 막는다. 4판은 쓰기만 막아 **되살아난 탈퇴자의 단어장이 읽혔다**(위협 36) ④ **ledger 제자리 restore 폐지**(§10-6-0) — 게이트·epoch·lease 가 함께 과거로 갔다(위협 37). **재작성 13단계**로 교체 ⑤ **정리 전용 Scheduled Worker**(§10-5-2) — 「언젠가 치운다」를 없앴다(위협 38) ⑥ **옛 배포 차단 D1~D12**(§10-8-1) · **T8 합격을 「거부됨」만으로** ⑦ 보유기간 산식을 **`max(Time Travel, 수동 백업) + 여유`** 로 ⑧ R5 의 「가능한 한」 제거 ⑨ 죽은 절 참조·낡은 질문 범위 정정 ⑩ **불변식 I18 재정의** — 4판 표현대로면 정상 가입 두 건이 위반으로 잡히고 잘못된 전역 `UNIQUE` 를 부른다(I18-b · T48). 목록은 §0-5 |
| **6판** | **4단계 구현 재감사에서 나온 결함 4건.** ① **`LEDGER` 미바인딩이 fail-open 이었다**(위협 39 · High) — `readMode()` 가 `open` 을 돌려줘 바인딩 없는 배포가 게이트도 임차증도 없이 읽고 썼다. 이제 `unbound` 이고 사용자 데이터 라우트는 전부 503 ② **인증 없는 임의 경로가 두 DB 에 쓰기를 유발**했다(위협 40 · High) — 임차증을 라우트 판정 **전에** 땄다. 라우트 분류를 앞으로 옮겨 없는 주소·로그인 시작은 쓰기 0 ③ **미등록 레이트리밋 버킷이 조용히 `write`(120)로 축소**됐다(위협 41 · M) — 이제 fail-fast ④ **복원 gate 가 인자로 우회됐다**(위협 42 · High) — 임의 상태 객체·빈 `targets` 로 통과했다. 코드가 소유한 조건은 인자로 못 바꾸고, 재개방 판정은 대상 집합을 **스스로 만든다**. 목록은 §0-6 |
| **7판** | **독립 재감사에서 재현된 우회 5건.** 당시의 스위트 22개가 **전부 통과하는 상태에서** 나왔다 — 그래서 이 판의 첫 결론은 「테스트 개수는 완료의 근거가 아니다」이다. ① **`restore_closed → maintenance` 옆문**(위협 43 · Critical) — 자물쇠가 `→ open` 에만 있었는데 `maintenance` 도 읽기를 허용하는 상태라, 재삭제 없이 한 칸 옆으로 옮기면 되살아난 탈퇴자의 단어장이 200 으로 읽혔다 ② **호출자가 넘긴 mark 함수가 삭제 증거를 정했다**(위협 44 · Critical) — 상수를 돌려주는 함수 하나면 재삭제 대상이 0 이 되어 `canReopen:true` ③ **경보 시각 전의 pending 을 안 셌다**(위협 45 · High) — 안전 조건과 경보가 같은 함수를 써서, 방금 실패한 삭제가 「미확정 0건」으로 보였다 ④ **잘못된 키로 살아 있는 계정이 승격됐다**(위협 46 · Critical) — 되돌릴 수 없는 판정이다 ⑤ **인증 없는 요청의 지속 저장소 쓰기에 상한이 없었다**(위협 47 · High) — 옛 T53 은 「요청당 둘」을 확인하고 그것을 「유한한 상한」이라 적었다. 목록은 §0-7 |
| **8판** | **마감 감사에서 재현된 결함 4건.** 스위트 **23개가 전부 통과하는 상태**에서 나왔다. ① **stale pending 제거와 진행 중인 삭제의 경합**(위협 48 · Critical) — 확인은 시작할 때 한 번이고 `DELETE` 는 아무것도 다시 안 봤다. `users=0 && deletions=0` 인 「증거 없는 삭제」가 만들어졌다 ② **`drained:true` 뒤의 주 D1 쓰기**(위협 49 · High) — 리미터 카운터가 임차증 **밖**이었다. 카운터를 **ledger D1 로 옮겨** 주 D1 을 예외 없이 임차증 안에 넣었다 ③ **엣지 남용 방어 없이 열려 있던 계정 경로**(위협 50 · High) — IP·분당 한도는 분산 IP 에 곱해진다. 실측 861 쓰기/분/IP. 앱 코드로 못 막으므로 **막지 못하는 채로 열지 않는다**(fail-closed) ④ **삭제 키가 어긋나도 `/ready` 200**(위협 51 · High) — 검사는 있었는데 readiness 가 안 불렀다. 목록은 §0-8 |
| **9판** | **남용 방어 독립 재감사에서 재현된 결함 5건.** 스위트 **24개가 전부 통과하는 상태**에서 나왔다 — **네 판 연속**이다. ① **truthy 하나로 「방어 있음」 판정**(위협 52 · Critical) — 문자열·빈 객체·던지는 `limit()` 이 전부 `abuseReady:true` 였고 익명 요청이 진행됐다 ② **단일 바인딩으로 6개 한도를 낼 수 없다**(위협 53 · High) — 한도는 설정에 고정이고 `rlMax()` 는 엣지 갈래에서 안 쓰였다. 실측 rotate 5/분이 20회 통과 ③ **다중 IP 최초 요청**(위협 54 · High) — IP 100개 × 1회에 429 0건 · ledger 쓰기 200건 ④ **선택지 A·C 가 코드에서 문을 못 열었다**(위협 55 · High) — 코드는 `env.RL` 하나만 알았다 ⑤ **공개 `/ready` 의 무제한 DB 조회**(위협 56 · High) — 요청당 주 D1 1 · ledger 5. 목록은 §0-9 |
| **10판** | **공식 4단계 전체 재검증에서 재현된 결함 4건.** 스위트 **25개가 전부 통과하는 상태**에서 나왔다 — **다섯 판 연속**이다. ① **부분 시크릿으로도 가입이 끝까지 간다**(위협 57 · Critical) — `SESSION_ENVELOPE_KEY` 만 빠진 배포에서 `signupReady:true` 였고, 콜백이 계정·정책 기록·세션을 만들고 제공자를 2회 불렀다. 그 세션은 다음 요청부터 전부 503 이라 **쓸 수 없는 계정**이 남는다 ② **Turnstile 검증이 `success` 만 본다**(위협 58 · High) — `action`·`hostname` 을 안 봐서 다른 자리·다른 별칭의 토큰이 통과한다 ③ **비밀값 비교가 원문을 인덱싱한다**(위협 59 · Medium) ④ **리미터 저장소 고장의 fail-closed 를 아무도 안 쟀다**(위협 60 · High) — `catch` 를 `return OK` 로 되돌리는 돌연변이가 **스위트 25개를 전부 통과했다.** 목록은 §0-10 |
| **11판** | **독립 검토가 10판의 판정을 셋 뒤집었다.** ① **비밀값 비교가 런타임 API 를 안 썼다**(위협 62 · Medium) — 10판은 요약을 만든 뒤 JS XOR 로 비교하고 「Node 에 `timingSafeEqual` 이 없어서」라고 적었는데, 그건 **테스트 환경의 결핍이지 운영 구현을 약하게 둘 근거가 아니다** ② **제공자별 부분 시크릿**(위협 61 · High) — `NAVER_ID` 만 있고 secret 이 없어도 `/signup/start` 가 200 이었고 **Turnstile 검증을 1회 호출**한 뒤 OAuth 주소를 발급했다. 「쌍이 맞아야 제공자로 친다」가 **화면에만** 있었다 ③ **돌연변이 검증이 재현 불가능했다**(위협 63) — 10판은 「34종 중 33종 사망」이라 적었지만 목록도 명령도 로그도 저장소에 없었다. 지금은 `scripts/mutations.mjs` + `node scripts/mutate.mjs` 로 **다시 돌릴 수 있다**. 목록은 §0-11 |
| **4판** | **가입 state 의 동시 재사용으로 한 번의 약관 수락이 여러 계정의 증거가 되는 경로**를 찾아 닫았다(§4-B·§5-3·위협 34). **실패한 삭제 요청의 지연 재실행 금지**(§10-5-1·위협 35) · reconciliation 과 restore 의 quiescence 범위 분리(§10-4·Q7) · **37일 산식 통일**(§10-5) · **D1 restore 의 in-flight 취소 동작을 공식 문서대로 정정**(위협 32) · 4단계 착수 정책 통일(§20) · 법률 패킷 과장·법률 결론 제거. 목록은 §0-4 |

**3단계는 11판으로 완료다**(9판의 사용자 결정 0~7 + 10판 재검증 4건 + 11판 독립 검토 3건 · 2026-08-22). 4단계 로컬 구현도 그 결정의 코드까지
끝났다. 원격은 **안전 동기화만 실행**됐고(배포 `19e69dee` · `READY_KEY` 등록 · 옛 배포 15개 삭제 · preview `8e16c92e`) **계정 인프라는 하나도 실행하지 않았다.**

> **당시 사실(8판, 2026-08-20):** 그날 이 자리에는 「3단계는 8판으로 완료다」라고 적혀 있었다.

> **당시 사실(5판, 2026-08-17):** 그날 이 자리에는 「3단계는 5판으로 완료다. 4단계(코드·DB·테스트
> 구현)는 착수하지 않았다」고 적혀 있었다. 그 문장은 그날의 사실이었다.
설계가 끝났다는 말이지 구현·법률 검토·출시가 끝났다는 말이 아니다.

이 문서는 **설계와 결정 요청**이다. 애플리케이션 코드·migration·정적 정책 파일·원격 리소스는
이번 단계에서 하나도 만들지 않았다. 2단계 결정서(`docs/STAGE2_ACCOUNT_PRIVACY_DECISIONS.md`)를
뒤집는 항목은 없고, 사실 정정 3건은 §0 에 적었다.

**법률 판단은 이 문서 어디에도 없다.** 검토용 사실 자료는 `docs/PRIVACY_LEGAL_REVIEW_PACKET.md` 다.

---

## 0. 이 단계에서 고친 사실 오류 3건

| # | 어디 | 전 | 후 |
|---|---|---|---|
| 1 | `docs/HANDOFF.md` §2 | "라이브 `/api/ready` 응답에 `db` 항목이 없다" | 실측(2026-08-17): HTTP **503** · `{"ok":true,"configReady":false,"db":true,"providers":[],"ready":false}`. `db` 는 **이미 있다.** 503 은 OAuth·`RL_KEY` 미비로 인한 **의도된 fail-closed** 다. 라이브가 1단계 로컬 커밋보다 오래된 것은 그대로 사실이다 |
| 2 | 2단계 결정서 §3 가입 화면 | "계정 삭제로 전부 지울 수 있음" | 같은 문서 §8·§9(복원 시 부활)와 충돌. "주 서비스 DB 에서는 즉시 삭제 / 복구 이력 보유기간과 재삭제 절차는 방침에 안내"로 정정. **최종 법률 문구가 아님**을 함께 표시 |
| 3 | `HANDOFF.md` §2 · 2단계 §13 | D1 조회 `7403` 을 "간헐적 실패"로 단정 | "한 실행은 7403 권한 오류였고 이후 실행은 성공했다. 인증 컨텍스트·권한·Cloudflare 측 상태 중 무엇이 원인인지는 확인되지 않았다. 0명은 성공한 실행의 값이다" |

`CLAUDE.md` 에는 같은 단정이 없었다(검색으로 확인). 문서 지도에 이 문서와 법률 자료를 더했다.

---

## 0-2. 2판에서 고친 설계 결함 9건

1판은 **설계 자체에 결함이 있었다.** 사실 오류(§0)와 달리 이것들은 그대로 구현했으면
실제로 데이터가 사라지거나 되살아났을 항목이다.

| # | 결함 | 어디를 고쳤나 |
|---|---|---|
| 1 | **삭제 표식 DB 자체의 Time Travel 위험을 안 봤다.** 표식 DB 를 과거로 되돌리면 옛 표식이 부활할 뿐 아니라 **그 시점 이후의 표식이 사라진다** — 확정된 삭제가 통째로 증발한다 | §10-1 규칙 · §10-6 · 위협 26 · 테스트 |
| 2 | **reconciliation 경합.** 살아 있는 계정을 보고 pending 을 지운 **직후** 그 계정의 삭제가 성공하면 **표식 없는 삭제**가 된다 | §10-4 · 위협 27 · 테스트 |
| 3 | **유지보수 모드를 HTTP 메서드로 막으려 했다.** `GET /cb`·`GET /exchange`·`GET /friends`(myCode)·`limited()` 가 전부 GET 인데 DB 를 쓴다 | §10-7 전수 표 · 위협 28 |
| 4 | **`expires_at = pending_at + 30일` 은 틀렸다.** pending 정리 기한과 confirmed 보존 기한은 성격이 다르다. 「정리한다」만 적고 **실행 시점을 안 적었다.** `attempts` 는 갱신 자리가 없는 죽은 컬럼이었다 | §10-1 · §10-5 |
| 5 | **`GET /friends` 를 읽기로 분류했다.** 초대 코드가 없으면 **INSERT 한다** | §1 · §10-7 · §14 |
| 6 | **정책 행위 시각을 한 값으로 뭉갰다.** 체크한 시각과 DB 기록 시각은 OAuth 왕복만큼 벌어진다 | §7-4 · 법률 자료 L11 |
| 7 | **state 재사용 설명이 틀렸다.** 같은 `code` 재교환은 막히지만 **같은 authorize URL 을 다시 실행해 새 `code` 를 받으면** 같은 state 가 10분 안에 다시 쓰인다 | §4-A · 위협 2 · 테스트 |
| 8 | **`RL_MAX.signup` 추가로 끝내려 했다.** fallback 이 남아 있으면 **다음 버킷에서 같은 사고가 또 난다** | §12 |
| 9 | **표현 과장 5건** — KV 쓰기 확인 · 배포 세대 · manifest 증명력 · `verifyProvider` 부작용 없음 · 별도 D1 장점 | §1 · §7-2 · §9 · §5-2 · §13 |

---

## 0-3. 3판에서 고친 것 6건

2판의 가장 큰 문제는 **증명의 범위를 넓게 말한 것**이었다. 삭제 saga 하나만 추적하는 장치를
「모든 쓰기가 멈췄다」는 증거로 썼다.

| # | 무엇이 틀렸나 | 어디를 고쳤나 |
|---|---|---|
| 1 | **deletion lease 0건을 전역 쓰기 drain 의 증거로 썼다.** lease 는 삭제 saga 에만 붙는데, 복원과 경쟁하는 쓰기는 OAuth 콜백·세션·단어장·친구·리미터까지 **11개 경로**다 | §10-9-6 전수 표 · §10-9-7 범위 결정 8건 · 위협 32 |
| 2 | **「11단계를 지키면 지금도 복원할 수 있다」고 읽히게 썼다.** 전역 drain 이 구현되지 않은 지금 그 절차는 **실행 가능한 절차가 아니다** | §10-8-0 **restore 금지 gate** · §10-8 을 「조건부 절차」로 강등 |
| 3 | **주 D1 복원과 ledger 복원을 같은 말로 섞어 썼다.** 둘은 위험 방향이 반대인데 절차 이름이 없어 참조가 흔들렸다 | §10-6 를 **「ledger 복원 9단계」**로, §10-8 을 **「주 D1 조건부 복원 11단계」**로 명명 |
| 4 | **살아 있는 계정에 붙은 pending 을 방치하는 규정이 없었다.** 자동 삭제를 금지한 것은 맞지만, 그러면 **영원히 남는다** | §10-5-1 pending SLA · 법률 자료 L9-3·L9-4 |
| 5 | **`policy_events` 개수를 3으로 고정해 적었다.** 그 3은 법률 결정에 종속된 값이다 | §8-0 · 문서 전역 `N` 치환 |
| 6 | **state 재사용 결론이 낡아 있었다.** 「1회용이라 재생 불가」와 「새 code 면 통과」가 한 문서에 공존했다 | §4-A · I8 · 테스트 24~29 |

---

## 0-4. 4판에서 고친 결함 8건

3판까지의 문제는 **세션 소유권만 보고 「증거의 귀속」을 안 본 것**이었다. 세션은 늘 그 왕복에서
인증된 사람 것이라 안전해 보였지만, **같은 약관 수락이 두 계정의 증거가 되는 경로**는 그 판정
밖에 있었다.

| # | 결함 | 심각도 | 어디를 고쳤나 |
|---|---|---|---|
| 1 | **가입 state 의 동시 재사용으로 정책 증거가 여러 계정에 귀속된다.** 한 번의 약관 수락·연령 진술이 계정 A·B 양쪽의 증거로 기록될 수 있었다 | **High** | §4-B · §5-3 **소비 표식** · I17~I21 · 위협 34 · 테스트 D 묶음 |
| 2 | **정상 브라우저의 순차 재사용을 「10분간 계속 통과」로 과장했다.** 실제로는 성공 응답의 `clearTxn()`(`worker/index.js:766`·`:809`)이 `shh_t` 를 지우므로 **보통 `bound()` 에서 막힌다.** 남는 것은 **동시 요청**과 **쿠키 삭제를 무시하는 클라이언트**다 | Medium | §4-A |
| 3 | **「가입 재시도는 `UNIQUE` 때문에 언제나 계정 하나로 수렴한다」는 잘못된 일반화.** `UNIQUE(provider, provider_subject)` 는 **같은 제공자 계정**일 때만 수렴시킨다. **다른 제공자 계정이면 계정이 둘 생긴다** | **High** | §4-A · §4-B · T27 정정 |
| 4 | **실패한 삭제 요청의 지연 재실행을 허용했다.** 「지우지 못했어요」를 받고 계속 쓰던 사용자의 계정을 24시간 뒤 운영자가 옛 pending 만 보고 지울 수 있었다 | **High** | §10-5-1 S3~S7 · I22~I25 · 위협 35 |
| 5 | **reconciliation 의 quiescence 범위가 문서 안에서 충돌했다.** §10-4 는 "모든 쓰기 중단", Q7 은 "deletion lease 0건이면 충분" | High | §10-4 규정 · Q7 · §10-9-7 |
| 6 | **37일 산식이 문서마다 달랐다.** 설계서 `30+7`, 법률 패킷 `7+30`, L9-2 `Time Travel 기간+30일` | Medium | §10-5 · 법률 패킷 §12-2 |
| 7 | **D1 restore 의 in-flight 동작을 부정확하게 적었다.** "복원 도중 쓰기가 그대로 착지한다"고 했으나, 공식 문서는 **D1 에 이미 들어간 query 는 취소**된다고 말한다. 진짜 위험은 **아직 D1 에 안 간 요청과 복원 후에 시작되는 쓰기**다 | Medium | 위협 32 · §10-9-1 · §19 |
| 8 | **4단계 착수 조건이 "코드 한 줄도 금지"인데 전역 drain 은 먼저 해도 된다고 말했다.** 승인받지 않은 권고를 확정처럼 적었다 | Medium | §20 · §21 |

## 0-5. 5판에서 고친 결함 11건

4판을 「완료」로 선언한 뒤 독립 재감사에서 나온 것들이다. **앞의 넷은 4판이 「안전하다」고
판정하고 넘어간 자리**이고, 그중 둘은 Critical 이다.

| # | 무엇이 틀렸나 | 심각도 | 고친 곳 |
|---|---|---|---|
| 1 | **복원 중 읽기를 열어 두었다.** §10-7 표가 `GET /api/book`·`GET /api/me`·`GET /api/friends/:id/book` 을 「DB 를 안 쓴다」는 이유로 허용했다. **쓰기만 본 것이 잘못이다** — 복원 직후 되살아난 탈퇴자의 단어장·별명이 재삭제 전까지 그대로 읽히고, 되살아난 `sessions` 행으로 **로그인 상태까지 부활**한다 | **Critical** | **§10-7 `restore_closed` 신설** · 위협 **36** · **T40~T43** |
| 2 | **ledger 를 제자리 복원하는 절차를 그대로 뒀다.** 그 DB 에는 `deletions` 만 있는 게 아니라 **게이트(`maintenance`)·`epoch`·`write_leases`** 가 같이 있다. 4판 절차의 **1번(유지보수 진입)을 4번(복원)이 취소**했다 | **Critical** | **§10-6-0 신설**(G1~G3) · **ledger 재작성 13단계** · 위협 **37** |
| 3 | **가입 정보를 평문으로 제공자 URL 에 실어 보내는 설계였다.** `makeState()` 는 서명일 뿐 암호화가 아닌데(`b64u` 는 인코딩이다), 4판은 그 자리에 약관 수락·연령 진술·`occurred_at` 을 넣게 되어 있었다 | **High** | **§5-4 신설**(AEAD · S1~S14) · 법률 `L15` · `worker/index.js` 주석 |
| 4 | **T8 의 합격 조건이 「거부됨 **또는 탐지됨**」이었다.** 탐지는 이미 데이터가 나간 뒤라 복원 허용 근거가 못 된다. 그런데 복원 금지 해제 조건은 「옛 배포가 게이트를 무시할 수 없어야 함」이었다 — **문서 안에서 충돌** | **High** | **§10-8-1 신설**(D1~D12) · T8 · 위협 33 |
| 5 | **보유기간을 약속하면서 정리 수단이 없었다.** §10-5 가 「삭제가 한 번도 안 일어나면 정리도 안 돈다 … **언젠가 치운다**」고 스스로 인정하고 있었다 | Medium | **§10-5-2 신설**(Scheduled Worker · C1~C14) · 위협 **38** · **T44~T47** |
| 6 | **보유기간 산식이 Time Travel 만 봤다.** 수동 백업도 계정을 되살리는데 세지 않았다 — 표식보다 오래된 백업 하나가 산식 전체를 무의미하게 만든다 | Medium | §10-5 **`max(...)` 산식** · B1·B2 · 법률 `L14` |
| 7 | **R5 가 「가능한 한 같은 batch」였다.** 한 단어가 **필수 원자성을 구현자 재량으로 낮춘다** | Medium | R5 |
| 8 | **소비 표식이 단순 SHA-256 이었다.** 제공자 로그에서 `state` 를 본 사람이 **그 값을 직접 해시해 DB 행을 지목**할 수 있다 | Medium | **§5-3-5**(`TOMBSTONE_KEY` HMAC) |
| 9 | **존재하지 않는 절 번호 22 를 참조했다**(이 문서의 마지막 절은 §21) · 법률 질문 범위 표기가 낡아 있었다(L7·L11 에서 멈춘 채) | Low | §20-1 · 법률 패킷 §12 |
| 11 | **법률 자료가 「로그인하지 않으면 서버에 저장되는 값이 없다」고 단정했다.** 로그인 콜백 주소를 직접 열면 리미터가 **제공자 확인보다 먼저** 돌아(`worker/index.js:787`, `uid=null`) **IP 기준 요약값 한 줄이 `rate_limits` 에 저장된다.** ⛔ 5판은 여기 「지금은 `RL_KEY` 미등록이라 안 일어난다」고 적었으나 **`RL_KEY` 는 2026-08-17 production 에 등록됐고 지금은 실제로 일어난다** | Medium | 법률 패킷 §2-2 · 불일치 목록 8번 |
| 10 | **불변식 I18 이 정상 가입을 위반으로 판정했다.** 「같은 `pv`·`occurred_at` 이 서로 다른 `user_id` 에 붙지 않는다」고 **전역으로** 단정했는데, `pv` 는 문서가 안 바뀌면 **모두 같고** `occurred_at` 은 두 사람이 같은 밀리초에 누르면 같아진다. 그대로 구현하면 `UNIQUE(pv, occurred_at)` 를 넣게 되고 **같은 순간에 가입한 두 번째 사람이 막힌다** | **High** | **I18 재정의**(식별자는 `state`) · **I18-b 신설**(전역 UNIQUE 금지) · **T48** |

**코드 주석 3건도 같이 고쳤다**(런타임 동작은 바꾸지 않았다):
`worker/index.js:165-167`(「state 재사용은 code 가 1회용이라 무해」 — 실제로 막는 것은 `clearTxn()` 이고
남는 길은 동시 요청·쿠키 삭제 무시 둘이다) · `:501`(「제공자 ID 는 앱마다 다른 값」 — 구글 `sub` 는
공식 문서상 그렇지 않다) · `wrangler.jsonc`(시크릿 목록에 **`RL_KEY` 누락**, 8개→9개).

---

## 0-6. 6판에서 고친 결함 4건 (2026-08-19)

4단계 로컬 구현을 「완료」로 선언한 뒤의 재감사에서 나온 것들이다. **네 건 모두 「이미 만든
방어가 실제로는 돌지 않는 자리」**다 — 설계가 빠진 것이 아니라 구현이 설계보다 느슨했다.

| # | 무엇이 틀렸나 | 심각도 | 고친 곳 |
|---|---|---|---|
| 1 | **`LEDGER` 바인딩이 없으면 fail-open 이었다.** `readMode()` 가 바인딩이 없을 때 **`open`** 을 돌려줬고, 임차증은 `if (env.LEDGER && …)` 로 감싸여 있었다 — 즉 **바인딩을 빠뜨린 배포는 게이트도 임차증도 없이 사용자 데이터를 읽고 썼다**(실측: `GET /book`·`PUT /book` 둘 다 200). 그 배포는 삭제 표식도 못 남기고 drain 도 못 센다 | **High** | `readMode()` → **`unbound`** · `maintenanceAllows()` 의 **기본값 차단** · **위협 39** · **T8-a·T8-a1·T8-a2** |
| 2 | **인증 없는 임의 경로가 두 DB 에 쓰기를 유발했다.** 임차증을 **라우트 판정 전에** 땄기 때문에 `POST /없는주소` 하나가 ledger 에 INSERT+DELETE 를 남겼고, `GET /login/kakao` 도 주 D1 을 한 줄도 안 만지면서 같은 쓰기를 냈다. 리미터가 아니라 **임차증이 증폭기**였다 | **High** | **라우트 분류표를 게이트 앞으로**(`routeFor()`) · **위협 40** · **T50~T53·T55** |
| 3 | **등록되지 않은 레이트리밋 버킷이 조용히 `write`(120)로 축소됐다.** `RL_MAX[bucket] ?? RL_MAX.write` — 오타 하나 또는 새 버킷 하나가 아무도 모르게 **가장 넉넉한 한도**를 물려받는다(실제로 `auth` 버킷이 그랬고 한 번도 먼저 막은 적이 없었다). `[]` 인덱싱이라 `__proto__` 같은 이름도 통과한다 | Medium | `rlMax()` **fail-fast** + `Object.hasOwn` · **위협 41** · **T54** |
| 4 | **복원 gate 가 인자로 우회됐다.** `restoreGate(state)` 가 **임의의 객체**를 그대로 믿어, 아홉을 전부 `true` 로 적은 객체 하나면 통과했다 — 그리고 **테스트가 정확히 그것을 「gate 가 열린다」로 확인하고 있었다.** `reopenReport(env, { targets: [] })` 도 빈 목록이면 잔여 0·살아 있는 계정 0 이라 **`canReopen: true`** 였다 | **High** | 코드 소유 조건은 인자로 못 바꾼다 · **재개방 판정이 대상 집합을 스스로 만든다** · `setMode(open)` 이 그 자리에서 다시 판정 · **위협 42** · **T7-b·H1-b** |

**표현도 같이 고쳤다.** `worker/ops.js` 의 머리말은 「문서가 아니라 실행되는 검사다」라고만
적어 이 함수가 복원을 **막는다**고 읽히게 두었다. 막지 못한다 — `wrangler d1 time-travel
restore` 는 계정 권한으로 실행되고 앱 코드를 지나지 않는다. 이제 반환값이 언제나
`restoreAllowed: false` 이고 그 이유를 함께 돌려준다(§10-8-0).

**설정·문서 계약도 자동 검사로 옮겼다**(`scripts/test-config.mjs`): 설정이 가리키는 파일이
실재하나 · 배포 가능한 설정에 placeholder 가 없나 · **코드가 요구하는 `env.*` 가 문서 네 곳에
전부 적혀 있나**. 사람이 세는 목록은 반드시 낡는다 — `RL_KEY` 때 한 번, 가입·삭제 키 셋 때 또 한 번
그렇게 됐다.

---

## 0-7. 7판에서 고친 결함 5건 (2026-08-19 · 독립 재감사)

**6판의 「4단계 로컬 구현 완료」 판정은 이 재감사로 철회됐다.** 다섯 건 모두 **당시의 스위트 22개가
전부 통과하는 상태에서 실제로 재현**됐다. 그것이 이 판의 첫 결론이다 — **테스트가 몇 개
통과하는지, 문서가 서로 맞는지는 완료의 근거가 아니다.** 재현은
`scripts/test-reaudit.mjs`(R1~R5)에 **먼저 빨간 상태로** 들어갔고 그 뒤에 코드를 고쳤다.

| # | 무엇이 틀렸나 | 심각도 | 고친 곳 |
|---|---|---|---|
| 1 | **`restore_closed → maintenance` 로 걸어 나갈 수 있었다.** `setMode()` 의 자물쇠가 **`→ open` 에만** 있었다. 그런데 `maintenance` 도 `GET /book`·`GET /me`·`GET /friends/:id/book` 을 **허용하는 상태**다(§10-7) — 그래서 재삭제도 잔여 확인도 없이 한 칸 옆으로 옮기면 **되살아난 탈퇴자의 단어장이 그대로 200 으로 읽혔다.** 위협 36 이 막으려던 그 결과가 자물쇠를 부수지 않고 **옆문으로** 재현된다 | **Critical** | 판정 기준을 「목적지가 `open` 인가」 → **「목적지가 사용자 데이터를 읽게 하는가」**(`READS_USER_DATA`) · `restore_closed` 에서 나가는 길을 **검증된 `open` 하나로** · 전환에 **CAS**(mode·epoch) · **위협 43** · **T56** |
| 2 | **호출자가 넘긴 함수가 삭제 증거를 정했다.** `reopenReport(env, { markFns })`·`reconcile(env, { mark })` 가 **임의의 실행 함수**를 받았다. 옛 주석은 「틀린 함수를 주면 대상이 늘어나지 줄지 않는다」고 적혀 있었는데 **정확히 반대**였다 — 표식이 안 맞으면 교집합이 비어 `targets: 0` 이 되고, 잔여도 0·살아 있는 계정도 0 이라 **`canReopen: true`** 였다. `() => "언제나같은값"` 하나로 `setMode("open")` 까지 통과했다 | **Critical** | 인자 제거 · 키 재료를 **레지스트리**(`DELETION_MARKERS`)가 소유 · 모르는 `key_version` 이 하나라도 있으면 **자동 판정 거부** · **위협 44** · **T57** |
| 3 | **경보 시각이 안 된 pending 을 아무 데서도 안 셌다.** `openPendingCount()` 하나가 **경보**와 **복원 안전조건**을 겸했고 그 정의는 `pending_alert_at < now` 였다. 그래서 **방금 실패한 삭제**(경보는 24시간 뒤)가 「미확정 0건」으로 보였고, 그 값을 근거로 재개방과 복원 사전점검이 통과했다. 「아직 경보할 때가 아니다」와 「아직 아무 문제 없다」는 다른 말이다 | **High** | 함수 분리 — **`pendingTotalCount`**(안전 조건 · 시각을 안 본다) / **`pendingAlertCount`**(경보 · 엄격한 `<`) · **위협 45** · **T58** |
| 4 | **잘못된 키로 살아 있는 계정의 표식이 승격됐다.** 인자를 막아도 **`env.DELETION_KEY` 자체가 다른 배포**에서는 살아 있는 사용자들의 표식 집합이 통째로 어긋나, 계정이 멀쩡히 있는 pending 이 「삭제는 됐고 기록만 실패했다」로 읽혀 **confirmed 로 승격**됐다. **되돌릴 수 없다** — 그 뒤로 그 사람은 「지워진 사람」이고 복원 절차는 그를 다시 지운다. HMAC 은 역산할 수 없어 **표식만으로는 키를 검증할 수 없다** | **Critical** | 표식을 처음 남길 때 **키 검사값**을 함께 기록(`deletion_keys` · ledger `0002`) · `deletionEvidenceUsable()` 이 reconcile·재개방·stale 제거 앞에서 대조 · **위협 46** · **T59** |
| 5 | **인증 없는 요청의 지속 저장소 쓰기에 상한이 없었다.** 임차증을 **인증보다도 리미터보다도 먼저** 땄다. 쿠키 없는 요청·매번 다른 위조 쿠키·틀린 OAuth 콜백이 하나에 ledger 쓰기 둘(INSERT+DELETE)을 냈고 **요청 수에 상한이 없었다.** 실측: 익명 요청 200회 → ledger 400 쓰기, 5만 회면 D1 무료 한도(하루 10만)가 바닥나고 그러면 **정상 사용자의 저장이 먼저 죽는다.** ⛔ 옛 **T53** 은 「요청당 정확히 둘」을 확인하고 그것을 **「유한한 상한」이라 적었다** — 요청 수가 무제한이면 요청당 상수는 상한이 아니다 | **High** | 리미터를 **임차증보다 앞으로** · 임차증을 드는 라우트는 **전부 버킷을 갖는다**(`read` 신설) · T53 을 **총량 불변식**으로 교체 · **위협 47** · **T60** |

### 0-7-1. 이 판에서 **지운** 방어 하나

`worker/index.js` 의 임차증 블록에 `if (!env.LEDGER) return 503` 이 「두 번째 자물쇠」라는
이름으로 있었다. **도달할 수 없는 코드였다** — 바인딩이 없으면 `readMode()` 가 `unbound` 를
돌려주고 `lease:true` 인 라우트는 위 게이트가 전부 503 이다(`ALWAYS_OPEN` 셋은 `lease:false`).
전체 스위트를 계측해 **도달 0회**를 확인하고 지웠다.

**왜 지우는 쪽이 맞나:** 관측할 수 없는 방어를 통과 항목으로 세면 「자물쇠가 둘」이라는 거짓
안전감만 남는다. 같은 보장은 **관측 가능한 형태로** T8-a1 이 전수로 잰다 — 라우트 표의
`lease:true` 인 항목 **전부**에 대해 `maintenanceAllows(unbound, …) === false`. 바인딩이 없는 채로
그 자리에 오면 `acquireLease` 가 던지고 같은 503 이 나간다(동작은 동일하다).

### 0-7-2. 남은 것 — R5 의 상한은 **IP·분당**이다

7판의 상한은 「인증되지 않았거나 형식적으로 무효한 요청이 **반복돼도** 쓰기가 무제한으로 늘지
않는다」까지다. 요청 수를 2배로 늘려도 쓰기가 같다는 것은 잰다(T60). **여러 IP 로 나눠 오는
분산 요청은 앱 코드로 막지 못한다** — 그건 엣지(WAF·Rate Limiting·Turnstile)의 일이고
도메인이 붙어야 쓸 수 있다(§12-1 의 ①). 그 사실을 축소하지 않는다.

또한 **인증된 계정 하나**가 읽기를 반복하는 것은 `read` 버킷(IP·분당 120)까지만 좁혀졌다.
계정을 만들려면 OAuth 를 통과해야 하고 지금 OAuth 는 꺼져 있다.

---

## 0-8. 8판에서 고친 결함 4건 (2026-08-20 · 마감 감사)

⚠️ **넷 다 스위트 23개가 전부 통과하는 상태에서 재현됐다.** 6판·7판에 이어 **세 번째**다.
번호는 위협 48~51 · **T61~T64**(`scripts/test-stage34-closeout.mjs`)이고,
**프로덕션 코드를 고치기 전에 먼저 빨갛게 만들어 그 출력을 기록했다.**

| # | 무엇 | 심 | 왜 여태 안 잡혔나 | 어떻게 닫았나 |
|---|---|---|---|---|
| 1 | **stale pending 제거의 경합 — 「증거 없는 삭제」**(위협 48) | C | **T39 가 두 작업을 차례로만 돌렸다.** 선행조건은 시작할 때 한 번 보고, 실제 `DELETE FROM deletions` 는 아무것도 다시 확인하지 않았다 | 제거를 **조건부 한 문장**으로 바꿨다 — 판정에 쓴 `mode`·`epoch` 이 그대로이고 **그 순간 임차증이 0건**일 때만 지워진다. 아니면 0행(fail-closed) |
| 2 | **`drained:true` 뒤의 주 D1 쓰기**(위협 49) | H | **R5-f 는 이미 `restore_closed` 인 상태에서 시작한 요청만 쟀다.** 게이트를 통과한 뒤 리미터 UPSERT 직전에 멈춘 요청은 **어느 임차증에도 없다** | 리미터 카운터를 **ledger D1 로 옮기고** 게이트를 같은 문장의 `WHERE` 에 넣었다. 이제 **주 D1 은 예외 없이 임차증 안에서만** 만져진다 |
| 3 | **엣지 방어 없이 열려 있던 계정 경로**(위협 50) | H | 아무 테스트도 **여러 IP** 를 쓰지 않았다. IP·분당 한도는 분산 IP 에 그대로 곱해진다 — 실측 **861 쓰기/분/IP** | 앱 코드로는 못 막는다. 그래서 **막지 못하는 채로 열지 않는다** — 남용 방어 바인딩이 없으면 계정 라우트는 **DB 를 만지기 전에** 503 이고 `/health` 가 `providers: []` 로 답한다 |
| 4 | **삭제 키가 어긋나도 `/ready` 200**(위협 51) | H | 검사(`deletionEvidenceUsable()`)는 7판에서 만들었는데 **readiness 가 그것을 부르지 않았다** | `/ready` 가 `deletionEvidence` 를 실제로 판정하고, 거짓이면 **503 · `ready:false`**. 값·검사값·표 이름은 안 싣는다 |

### 0-8-1. 리미터 카운터를 ledger 로 옮긴 결정 — 버린 대안

**요구**: 「주 D1 은 임차증 안에서만 만진다」에 예외가 없어야 한다. 리미터는 임차증보다
**앞**에 있어야 하고(위협 47), 임차증보다 앞이면 그 쓰기는 추적 밖이다 — 두 요구가 정면으로
부딪힌다. 한쪽을 포기하지 않는 유일한 길은 **그 쓰기를 주 D1 밖으로 빼는 것**이다.

| 대안 | 왜 안 골랐나 |
|---|---|
| 리미터를 임차증 **뒤로** 옮긴다 | 위협 47 이 그대로 되살아난다 — 익명 요청 하나가 ledger 쓰기 둘을 내고 요청 수에 상한이 없다. `test-reaudit` R5 와 `test-docs` 의 순서 검사가 함께 막는다 |
| 리미터 쓰기를 그대로 두고 **문서에 남는 위험으로 적는다** | 그 창을 근거로 복원을 시작하면 사용자 데이터가 섞인다. 「적어 두면 된다」로 넘길 수 있는 종류가 아니다 |
| 주 D1 카운터를 **KV** 로 되돌린다 | 무료 KV 는 하루 1,000 writes 라 리미터 자체가 서비스 거부 수단이 된다(2026-08-11 에 이미 되돌린 길이다) |
| 주 D1 의 `rate_limits` 표를 **DROP** 한다 | 파괴적 원격 migration 이라 별도 승인이 필요하고, **안 쓰는 표는 해가 없다.** 표는 남기고 코드만 옮긴다 — 되돌릴 때도 코드만 되돌리면 된다 |

**고른 것**: ledger D1 에 `rate_limits` 를 만들고(migration `migrations-ledger/0003`),
UPSERT 를 게이트와 **한 문장**으로 묶는다 —
`... WHERE EXISTS (SELECT 1 FROM maintenance WHERE id=1 AND mode IN ('open','maintenance'))`.
전환 뒤에 깨어난 요청은 **0행을 쓰고 429 로 끝난다**(닫는 쪽 실패).

⚠️ **이것으로 줄어드는 것은 「추적 밖 쓰기」이지 「쓰기 총량」이 아니다.** 총량은 위협 50 이
다루고, 그 답은 코드 밖에 있다.

### 0-8-2. 이 판이 **하지 못한 것**

- **분산 IP 남용을 막지 못한다.** 앱 코드가 셀 수 있는 신원은 IP 하나뿐이고, IP 를 나누면
  한도는 곱해진다. 막는 것은 엣지의 일이다(§12-2) — **그래서 계정 경로를 fail-closed 로 뒀다.**
- **운영자의 직접 DB 조작을 막지 못한다.** 위협 48 의 방어는 앱의 명령 경로에만 걸린다.
- **복원을 막지 못한다.** 6판에서 정정한 그대로다(§10-8-0).

## 0-9. 9판에서 다시 연 결함 5건 (2026-08-22 · 남용 방어 독립 재감사)

⛔ **8판의 「위협 50 완료」 판정을 철회한다.** 8판이 실제로 닫은 것은 **「방어가 없을 때 닫힌다」
하나**이고, **「방어가 있다고 판정되는 조건」은 아무것도 검증하지 않았다.** `abuseGuard()` 는
`env.RL` 이 truthy 이기만 하면 `"edge"` 라고 답했고, 그 뒤의 `limit()` 호출 실패는 통과였다.
다섯 다 **스위트 24개가 전부 통과하는 상태에서** 재현됐다 — **네 판 연속 같은 무늬다.**

| # | 무엇 | 심 | 8판 테스트가 놓친 이유 | 이 판의 답 |
|---|---|---|---|---|
| 1 | **truthy 하나로 「방어 있음」 판정**(위협 52) | C | T63-c 가 **동작하는 mock** 하나만 넣어 「열린다」를 확인했다. 고장 난 바인딩·가짜 값은 아무도 안 넣어 봤다 | 모드를 **운영자가 선언**하고(`EDGE_GUARD`) 선언된 모드마다 **실재를 검증**한다. 호출 실패는 계정 경로에서 **503**(fail-closed) |
| 2 | **단일 바인딩으로 6개 한도를 낸다는 계약**(위협 53) | H | 어느 테스트도 **엣지 갈래에서 버킷별 한도**를 재지 않았다. 그래서 `rlMax()` 값이 그 갈래에서 **한 번도 쓰이지 않는다**는 사실이 안 드러났다 | 문서의 6개 한도는 **우리 카운터가 유일한 집행자**다(전역 일관). 엣지 바인딩은 **볼류메트릭 사전 거름**이고 그 한도는 설정에 산다 — 두 숫자를 섞지 않는다 |
| 3 | **다중 IP 의 최초 요청**(위협 54) | H | T63-a 는 **닫힌 상태**만 쟀다. **열린 상태에서** 여러 IP 가 한 번씩 오는 경우는 아무도 안 쟀다 | 막는 부분과 못 막는 부분을 **수치로 가른다**(§12-3-3). 못 막는 잔여 위험은 사용자 승인 대상이다 |
| 4 | **선택지 A·C 가 코드에서 계정 경로를 열 수 없었다**(위협 55) | H | 대안 표는 문서에만 있었고 **코드는 `env.RL` 하나만** 알았다. WAF·Turnstile 을 실제로 붙여도 `abuseReady` 는 거짓이다 | 모드를 셋으로 갈랐다 — `waf`(호스트 잠금 · 런타임이 증명 못 한다) · `ratelimit`(바인딩 검증) · Turnstile 은 **보조**(가입·로그인만) |
| 5 | **공개 `/ready` 의 무제한 DB 조회**(위협 56) | H | 「방어가 없으면 D1 이 탈 일이 없다」를 **계정 경로만** 재고 결론으로 적었다. 실측: `/ready` 한 번이 주 D1 1 · ledger 5 질의 | 진단은 **운영자 키**를 요구한다. 키 없는 호출은 **어느 DB 도 만지지 않는다** |

### 0-9-1. 고치기 전 실측 (2026-08-22 · HEAD `3a12dd9` · 프로덕션 코드 수정 전)

| 넣은 것 | `abuseGuard()` | `/ready` | `abuseReady` | 익명 `GET /book` | ledger 쓰기 |
|---|---|---|---|---|---|
| `RL = "configured-but-not-a-binding"` | `edge` | **200** | **true** | 401(진행됨) | **2** |
| `RL = {}` | `edge` | **200** | **true** | 401(진행됨) | **2** |
| `RL.limit()` 이 동기 예외 | `edge` | **200** | **true** | 401(진행됨) | **2** |
| `RL.limit()` 이 reject | `edge` | **200** | **true** | 401(진행됨) | **2** |
| `RL.limit()` 이 `{nope:1}` | `edge` | **200** | **true** | **429**(전부) | 0 |

- **`rotate` 한도 5/분** 인데 한도 100 짜리 바인딩 mock 으로 **20회 연속 통과**(429 0건).
  `limited()` 는 `rlMax(bucket)` 을 계산해 놓고 엣지 갈래에서 **쓰지 않는다.**
- **IP 100개 × 익명 `GET /book` 1회**(엣지 mock 은 전부 허용): 429 **0** · 401 **100** ·
  ledger 쓰기 **200**. IP 별 리미터는 **같은 IP 의 반복**만 센다.
- **WAF·Turnstile 만 설정된 `env`**: `guard=none` · `providers=0` · 계정 경로 **503**.
  즉 A·C 를 실제로 구성해도 코드는 문을 열지 못했다.
- **`guard=none` 에서 공개 `/ready` 10회**: 주 D1 **10 질의** · ledger **50 질의**
  (요청당 1 + 5). 「닫혀 있으니 D1 이 탈 일 없다」는 **계정 경로에만** 참이었다.

⚠️ 다섯째 줄(`{nope:1}`)이 **전부 429** 인 것도 방어가 아니다 — 고장 난 바인딩이 **전체 서비스
거부**가 된 것이고, 그동안 `/ready` 는 **200** 이라 운영자가 원인을 볼 수 없다.

### 0-9-2. 버린 대안

| 대안 | 왜 안 골랐나 |
|---|---|
| 버킷마다 `ratelimits` 바인딩 6개(`RL_LOGIN`…) | 한도가 **colo 별이고 eventual consistent** 라(공식 문서) 6개를 만들어도 **문서의 숫자가 전역으로 지켜지지 않는다.** 「6개 한도를 엣지가 집행한다」는 문장은 그때도 거짓이다. 게다가 namespace 6개를 운영자가 손으로 만들어야 한다 |
| 엣지 바인딩이 있으면 우리 카운터를 **끈다**(8판 동작) | 그 순간 `rlMax()` 의 숫자가 **장식**이 된다 — 실측에서 rotate 5/분이 20회 통과했다 |
| 공개 `/ready` 에 **짧은 TTL 캐시** | Workers 캐시는 colo 별이라 분산 요청에는 히트가 안 난다. 「비용이 준다」를 증명할 수 없는 방어다 |
| 공개 `/ready` 를 **정적 503** 으로 고정 | 운영자가 배포 직후 무엇이 덜 됐는지 볼 수단이 사라진다 — `docs/OPS_RUNBOOK.md` 의 절차가 통째로 `/api/ready` 위에 서 있다 |
| `/ready` 를 **레이트리밋 버킷**으로 막는다 | 그 카운터가 곧 ledger 쓰기다. **분산 IP 에는 다시 무제한**이라 순환이다 |

**고른 것**: ① `EDGE_GUARD` 선언 + 모드별 실재 검증 ② 우리 카운터가 **항상** 돌아 버킷별 한도의
유일한 집행자가 된다 ③ 진단은 `READY_KEY` 를 요구하고, 키 없는 `/ready` 는 **DB 를 안 만진다.**

⚠️ **이 판도 분산 IP 를 막지 못한다.** 줄어드는 것은 「열려 있을 때의 잔여 위험」이 아니라
**「열 수 있다고 잘못 판정하는 경로」**다.
⚠️ **정정(2026-08-22):** 이 줄은 「무엇을 붙일지는 그대로 사용자 결정이다」로 끝났었는데, 그 결정은
**같은 날 §12-3-4 에서 A안으로 내려졌다** — 남은 것은 구성이지 결정이 아니다.

---

## 0-10. 10판에서 다시 연 결함 4건 (2026-08-22 · 공식 4단계 전체 재검증)

9판을 닫은 **같은 날**, 4단계 구현 전체를 코드와 공격 시나리오로 다시 훑었다. 네 건 다
**스위트 25개가 전부 통과하는 상태**에서 나왔다 — 다섯 판 연속 같은 무늬다.

⚠️ **이번 넷은 성격이 앞과 다르다.** 앞의 판들은 「설계가 요구한 것을 구현이 반쯤 했다」였는데,
이번 넷 중 둘(위협 57·60)은 **「설계가 요구하지 않아서 아무도 안 본 자리」**다:
시크릿을 **부분만** 넣은 상태, 그리고 방어 코드의 **실패 갈래**.

| # | 무엇 | 심 | 그때 테스트가 놓친 이유 | 이 판의 답 |
|---|---|---|---|---|
| 1 | **부분 시크릿에서 가입이 끝까지 간다**(위협 57) | **C** | 테스트 환경은 늘 **전부 있거나 하나만 없거나**였고, 없을 때 재는 자리는 `/health` 와 `/signup/start` **둘뿐**이었다. 콜백은 `TOMBSTONE_KEY` 하나만 봤다 | 조건을 **공유 predicate 둘**(`loginPossible`·`signupPossible`)로 모으고 **화면·시작·콜백·로그인 시작** 넷이 같은 함수를 본다. 콜백 검사는 **제공자 호출 앞**이다 |
| 2 | **Turnstile 이 `success` 만 검증됐다**(위협 58) | **H** | T70 이 「거절을 통과로 바꾸지 않는다」만 쟀다. **성공 응답의 나머지 필드**는 아무도 안 봤다 | `action === "signup"` 과 `hostname === APP_ORIGIN 의 호스트` 를 함께 본다. **필드가 없는 응답도 거부**다. 화면은 같은 상수를 위젯에 싣고, 쓴 토큰은 **버리고 위젯을 되돌린다** |
| 3 | **비밀값 비교가 원문 문자열을 인덱싱한다**(위협 59) | **M** | 「끝까지 비교한다」는 성질만 쟀다. 문자열 표현에 따라 시간이 달라질 수 있다는 것은 **우리가 정하지 못하는 성질**이라 잴 수도 없었다 | 두 값의 **SHA-256 요약 32바이트**를 비교한다. 길이가 달라도 하는 일이 같다. `crypto.subtle.timingSafeEqual` 은 **쓰지 않는다**(Workers 확장이고 Node 에 없다 — 이 스위트는 Node 에서 돈다) |
| 4 | **리미터 저장소 고장의 fail-closed 가 관측되지 않았다**(위협 60) | **H** | `RL_KEY` 부재는 쟀지만(test-friends 120) **저장소가 던지는 경우**는 아무도 안 쟀다. 그래서 `catch → return OK` 돌연변이가 **스위트 25개를 전부 통과**했다 | `T76` 이 **버킷 6개 전부**에 대해 카운터 문장만 던지게 만들고 **503 · 주 D1 쓰기 0** 을 잰다. 문서가 선언한 **순서**(엣지 → 게이트 → 리미터 → 임차증)도 셋 다 잰다 — 전에는 둘만 쟀다 |

### 0-10-1. 고치기 전 실측 (2026-08-22 · HEAD `7f9078a` · 프로덕션 코드 수정 전)

| 뺀 시크릿 | `signupReady` | `POST /signup/start` | 콜백 결과 | 남은 것 |
|---|---|---|---|---|
| `SESSION_ENVELOPE_KEY` | **true** | **200** | **`#login=ok`** | users **1** · policy_events **3** · sessions **1** · 제공자 호출 **2** |
| `DELETION_KEY` | **true** | **200** | **`#login=ok`** | 위와 같다. ⚠️ 그 계정은 **지울 수도 없다** |
| `STATE_KEY` · `RL_KEY` | **true** | **200** | 진행 | 같다 |

- Turnstile: `{"success":true,"action":"contact","hostname":"evil.test"}` 로 **가입이 시작됐다.**
- `countVerdict` 의 `catch` 를 `return OK` 로 바꾸는 돌연변이: **스위트 25개 전부 통과**(exit 0).
- 게이트를 리미터 뒤로 옮기는 재배치: `test-docs` 의 순서 검사가 **주석으로만** 그 순서를 말하고
  실제로는 재지 않아 **통과**했다(다른 스위트가 잡아 주긴 했다).

### 0-10-2. 고치면서 **틀렸다고 확인한 옛 주석 둘**

| 어디 | 옛 주석 | 실제 |
|---|---|---|
| `sign()` · `envelopeSign()` | 「키가 없으면 `ENC.encode(undefined)` 가 문자열 `"undefined"` 를 서명 키로 쓴다 — 누구나 아는 값이다」 | **사실이 아니다.** `TextEncoder.encode()` 의 인자 기본값은 빈 문자열이라 결과는 **길이 0** 이고, `importKey` 가 `Zero-length key is not supported` 로 던진다(Node·workerd 실측). 그러므로 그 `if` 는 **두 번째 자물쇠가 아니고** 얻는 것은 이름이 적힌 오류 하나다. 실제로 막는 것은 `loginPossible()` 이다 |
| §13-6 「72건 전부 실행 가능한 단언으로 연결됐다」 | 개수와 연결만 말했다 | **연결됐다 ≠ 실패 갈래까지 쟀다.** 위협 60 이 그 증거다 — 돌연변이 하나가 25개를 전부 통과했다 |

⚠️ **이 판도 「이제 다 봤다」가 아니다.** 이번에 드러난 무늬는 「설계 문서가 요구하지 않은 자리는
아무도 안 본다」이고, 그 무늬는 문서를 더 쓴다고 사라지지 않는다. 줄이는 방법은 하나뿐이다 —
**돌연변이로 확인한다.** 10판의 판정 근거는 통과 개수가 아니라 돌연변이 결과였고, 살아남은 하나
(§0-10-2 첫 줄)는 왜 살아남아도 되는지를 위에 적었다.
⚠️ **정정(11판):** 그 숫자(34종 중 33종)는 **저장소에서 재현할 수 없었다** — 목록도 명령도 로그도
없었기 때문이다(위협 63). 지금 근거는 `node scripts/mutate.mjs` 로 **다시 돌릴 수 있는 결과**다.

---

## 0-11. 11판에서 뒤집힌 판정 3건 (2026-08-22 · 독립 검토)

10판을 닫은 **같은 날** 독립 검토가 셋을 지적했고, 셋 다 맞았다.
⚠️ **이번 셋의 공통점은 「고쳤다고 적은 것이 사실은 덜 고친 것」이다.** 앞의 판들은 「안 본 자리」가
문제였는데, 이번은 **고친 자리에 대해 내린 판정 자체가 틀렸다.**

| # | 무엇 | 심 | 10판이 왜 그렇게 판정했나 | 11판의 답 |
|---|---|---|---|---|
| 1 | **비밀값 비교가 런타임의 timing-safe API 를 안 쓴다**(위협 62) | **M** | 「`crypto.subtle.timingSafeEqual` 은 Workers 확장이고 Node 에 없다. 스위트가 Node 에서 도니 갈래가 둘로 갈린다」 — **테스트 환경의 결핍을 운영 구현의 근거로 썼다.** 게다가 T75-c 는 그 API 를 **쓰면 실패하도록** 되어 있었다(방어를 낮추는 쪽을 테스트가 강제했다) | 운영 코드는 `digest → crypto.subtle.timingSafeEqual()` **하나만** 부르고 **JS fallback 이 없다.** Node 쪽은 `scripts/_workers-shim.mjs` 가 채우고(어댑터는 `scripts/` 에만 살며 `test-dist` 가 비배포를 전수로 잰다), **진짜 workerd 프로세스**를 띄우는 `scripts/test-workerd.mjs` 가 운영 경로를 실제로 밟는다 |
| 2 | **제공자별 부분 시크릿으로 왕복이 시작된다**(위협 61) | **H** | 10판의 `signupPossible()` 은 **공통 키**만 봤고 제공자 자격은 `creds(env,p).id` **하나**로 봤다. 「쌍이 맞아야 제공자로 친다」는 규칙은 `readyProviders()`(= 화면)에만 있었다 | 규칙을 **`providerPossible(env, provider)` 하나**가 소유하고 `/health`·`/signup/start`·`/login/:provider`·`/cb`·`/exchange` 가 **같은 함수**를 본다. 카카오의 secret 선택 정책은 그 함수 안 한 줄로만 산다 |
| 3 | **돌연변이 검증이 재현 불가능했다**(위협 63) | **M** | 「34종 중 33종 사망」을 보고서에만 적었다 — 목록·실행 명령·죽인 테스트·로그 어느 것도 저장소에 없었다. **확인할 수도, 다시 돌릴 수도 없는 숫자는 검증이 아니라 주장이다** | `scripts/mutations.mjs`(목록) + `scripts/mutate.mjs`(실행기). 임시 복제본에서만 고치므로 작업 폴더가 안 더러워지고, **생존·앵커 실패가 하나라도 있으면 종료 코드가 0이 아니다** |

### 0-11-1. 고치기 전 실측 (2026-08-22 · HEAD `aceef3a` · 실제 라우트 호출)

| env | `/health` | `POST /signup/start` | Turnstile 호출 | `GET /login/:p` |
|---|---|---|---|---|
| `NAVER_ID` 만 | `providers: []` · `signupReady:false` | **200 · OAuth URL 발급** | **1회** | **302** |
| `GOOGLE_ID` 만 | `providers: []` · `signupReady:false` | **200 · OAuth URL 발급** | **1회** | **302** |
| `KAKAO_ID` 만(정책상 정상) | `providers: ["kakao"]` | 200 | 1회 | 302 |

⚠️ **화면은 맞게 답했는데 문이 열려 있었다.** 그래서 「`/health` 를 재는 테스트」로는 절대
안 잡힌다 — T77 이 **실제 라우트**를 부르는 이유다.

### 0-11-2. 돌연변이 결과 (2026-08-22 · `node scripts/mutate.mjs`)

**22종 · 사망 22 · 생존 0 · 앵커 실패 0.**
첫 실행에서는 **셋이 살아남았고 셋 다 실제 테스트 공백이었다** — 숨기지 않고 적는다:

| ID | 살아남은 이유 | 무엇을 더했나 |
|---|---|---|
| **M10** Turnstile `success` 검사 제거 | T70-b 가 넣던 실패 응답에 `action`·`hostname` 이 **없어서** 다른 두 검사가 대신 막았다 — `success` 검사가 그 둘에 **얹혀** 있었다 | T74-c2: **`success` 만 거짓**이고 나머지 둘은 우리 값인 응답 5종 |
| **M21** `markPending` fencing 제거 | T5 는 `markConfirmed` 의 fencing만 쟀다 | T78-a: 옛 epoch 의 lease 로는 **pending 도** 못 남긴다 |
| **M22** `confirmed_at IS NULL` 제거 | 처음 쓴 T78-b 가 「한참 뒤」(+10일)에 재확정을 시도해 **fencing 의 시간 조건이 먼저 걸렸다** — 다른 이유로 거짓이라 통과했다 | T78-b 를 **lease 가 살아 있는 시각**으로 고쳐 겨눈 곳을 정확히 했다 |

⚠️ **세 번째 줄이 이 판의 교훈이다.** 테스트가 **다른 이유로 통과**하면 그것은 통과가 아니다.
돌연변이는 그 차이를 드러내는 유일한 값싼 도구였다.

### 0-11-3. 10판에서 틀렸다고 확인한 서술

| 어디 | 10판 서술 | 실제 |
|---|---|---|
| `docs/HANDOFF.md` · `CLAUDE.md` | 「옛 배포의 `/api/book` 401 은 **DB 를 조회하고 나서** 나오는 답이다」 | **아니다.** 네 세대의 소스를 확인했다 — D1 세대의 `whoAmI()` 는 `if (!token) return null` 로 시작하고, KV 세대는 Bearer 토큰이 없으면 KV 를 안 만진다. **쿠키 없는 401 은 저장소를 안 건드린다.** 실제 위험은 **비어 있지 않은 위조 자격증명**이다(요청당 저장소 읽기 1회 · 요청 수 무제한) |
| §12-2 근처 | 「무엇을 붙일지는 그대로 사용자 결정이다」 | 그 결정은 **같은 날 §12-3-4 에서 A안으로 내려졌다.** 남은 것은 구성이지 결정이 아니다 |

---

## 0-12. 11판 이후 후속 재검증 (2026-08-23) — **설계 판은 그대로 11판이다**

⛔ **판 번호를 올리지 않았다.** 설계는 바뀌지 않았다 — 바뀐 것은 **화면 구현과 그것을 재는 검사**다.
그래서 설계서는 그대로 11판이고, 늘어난 것은 위협 **64** 와 테스트 **T80** 하나씩이다.

| # | 무엇 | 등급 |
|---|---|---|
| 1 | **화면이 서버의 `ready:false` 를 버린다**(위협 64) — `apiHealth()` 가 `ready` 를 안 실어 보내 판정하는 쪽이 언제나 `undefined` 를 읽었고, 계정 상태가 늘 `"ok"` 로 떨어졌다 | **M** |

### 0-12-1. 고치기 전 실측 (2026-08-23 · 새 검사가 red 인 상태)

`node scripts/test-client.mjs` → **78/85 통과 · 7개 실패 · exit 1**

| 검사 | 실패 이유 |
|---|---|
| readiness false(빈 store) | 「health 가 ready:false 인데 계정 기능을 연 상태로 판정했다」 |
| ready 누락 · 문자열 · 숫자 1 · 본문 `ok:false` | 「계약을 못 지킨 응답인데 계정 기능을 열었다」 |
| stale 표시 + readiness false(503 없이) | 「ready:false 인데 「카카오 계정」이라고 말한다」 |
| down → 401 | 「401 로 로그아웃됐는데 초대 링크 버튼이 보인다」 |

⚠️ **마지막 줄은 예상 밖의 두 번째 결함이었다.** `share-btn` 이 `accountDown()` 하나만 보고 있어서
**로그인한 적 없는 사람에게도** 보였고, 401 직후에도(그때 계정 계층은 `"ok"` 다 — 서버가 대답은 했다)
그대로 남았다. 같은 뿌리라 같이 고쳤다.

### 0-12-2. 왜 74개가 통과하고 있었나 — **검사 방법의 결함**

계정 서버 닫힘 검사들의 기본 store 가 `LOGGED_IN()` 이라, boot 뒤 `sync()` 가 `/book` 을 부르고
그 **503 응답**이 `request()` 안에서 `setAccountState("down")` 을 했다. 즉 그 검사들이 재고 있던 것은
**`ready` 계약이 아니라 뒤이은 503** 이었다. `ready` 를 통째로 버려도 초록불이 나온다.

**그래서 고친 것은 코드만이 아니다** — 판정 근거를 `/health` 하나로 좁힌 검사를 새로 만들었다:
계정 라우트를 **끊김**으로 두면 `request()` 가 accountState 를 안 건드리므로(의도된 동작),
남는 근거가 `/health` 뿐이다.

### 0-12-3. 돌연변이 결과 (2026-08-23 · `node scripts/mutate.mjs`)

동작 **M23~M25**(`apiHealth()` 가 `ready` 를 버림 · `d.ready !== false` 로 느슨하게 · 판정이 `/health` 를 안 봄)와
정적 **D14~D15**(운영 상태를 「아직 401」로 되돌림 · 돌연변이 총수를 낡은 22 로 되돌림)를 더했다.

⚠️ **M24 가 첫 실행에서 살아남았고, 그것도 실제 공백이었다.** 새로 쓴 fail-closed 검사가
**빈 store** 를 써서 `share-btn` 이 `!authToken()` 때문에 어차피 숨겨졌다 —
**이 검사가 닫으려던 결함과 똑같은 무늬**다. 검사를 로그인 상태로 바꿔 닫았다.

---

---

## 0-13. 위협 64 를 고친 뒤의 2차 재검증 (2026-08-23~24) — **판 번호는 그대로 11판이다**

⛔ **§0-12 의 수정이 만든 자리를 §0-12 자신은 못 봤다.** 판정을 다시 닫는 자리에서 독립 재감사를
돌렸더니, 위협 64 를 고친 코드 안에 **같은 무늬의 fail-open** 이 하나 남아 있었다.

| # | 무엇 | 등급 |
|---|---|---|
| 1 | **계약을 어긴 `/health` 응답의 `providers` 로 로그인 버튼을 그린다**(위협 65) — `ready` 를 `d.ready === true` 로 **정규화**하면 계약 위반 응답이 성공으로 통과하고, `ok:true` 라 `providers` 가 그대로 화면까지 간다 | **M** |

### 0-13-1. 고치기 전 실측 (2026-08-23 KST · 새 검사가 red 인 상태)

`node scripts/test-client.mjs` → **89/94 통과 · 5개 실패 · exit 1**

| 응답 | 실패 이유 |
|---|---|
| `{ok:true, providers:["kakao"]}` (`ready` 누락) | 「계약을 어긴 응답의 providers 로 로그인 버튼을 그렸다」 |
| `ready` 가 문자열 · 숫자 1 · `null` · 객체 | 같음 |

### 0-13-2. 왜 §0-12 의 94개가 통과하고 있었나

§0-12 의 검사들은 **계정 UI 가 닫히나** 하나만 봤다. 정규화된 `ready:false` 는 그 조건을 만족한다 —
계정 UI 는 실제로 닫힌다. 못 본 것은 **같은 응답의 `providers` 가 로그인 시작 경로로 흘러가는 길**이다.
「fail-closed 인가」를 **한 화면 요소**로만 재면 다른 요소가 열려 있는 것을 못 본다.

### 0-13-3. 부수 발견 — fixture 가 서버 계약을 안 지키고 있었다

계약을 엄격하게 만들자 **가입 화면 검사 하나가 깨졌다.** `/health` fixture 다섯 개가 `ready` 를
아예 안 싣고 있었다 — 서버는 언제나 싣는 값이다. 코드가 아니라 **fixture 가 틀린 것**이라
fixture 를 서버 계약에 맞췄다(`signupReady:false` 인데 `ready:true` 인 경우가 실제로 있다 —
`ready` 는 가입 전용 키를 보지 않는다).

### 0-13-4. 돌연변이 (실행 2026-08-24 KST)

동작 **M24 를 다시 쓰고**(느슨한 `!==false` → **타입 검사 제거**: 정규화가 부활하는 실제 무늬다)
**M26** 을 더했다(첫 화면 판정이 `ok` 만 보고 `ready` 를 안 봄).
⚠️ **옛 M24 를 그대로 두면 안 됐다** — 타입 검사가 생긴 뒤로는 `d.ready !== false` 가 `d.ready` 와
**같은 값**이라 그 변이는 죽을 수가 없다(동등 변이).
정적 **D16** 도 더했다 — 「배포 경계 범위의 위협 끝번호」를 낡은 값으로 되돌리는 변이다(검사 27).
⚠️ **검사 27 은 2026-08-24 에 한 번 더 고쳤다.** 처음엔 면제를 **줄 전체**에서 찾았고, 그래서
머리에 현재형 판정을 담고 꼬리에 「당시 …」를 단 긴 표 행이 통째로 빠져나갔다 — 배포 뒤에도
체크리스트 20번이 「배포하지 않았다」를 현재형으로 달고 통과했다. **검사 19-c 에서 이미 한 번
고친 무늬를 되풀이한 것**이라, 면제를 주장 바로 옆으로 좁히고 범위 주장의 정의도 넓혔다
(「미배포」와 「배포본에 들어 있다」는 같은 열린 구간을 가리킨다).

**마감 점검에서 하나가 더 나왔다(2026-08-24).** 이 절 바로 위 §13-6 의 합계 문장이
「**79건** 전부 실행 가능한 단언으로 연결됐다」로 남아 있었다 — T80·T81 을 더하면서 **표만 늘리고
합계를 안 고쳤다.** 검사 4 가 못 잡은 이유는 그 문형(`테스트 N건`)에 안 걸려서다: 개수를 말하는
방식이 하나 더 있었던 것이다. **검사 28** 이 §13-6 안의 「N건 전부 … 연결」을 표에서 뽑은 `maxT` 와
대조하고, **D17** 이 그 문장을 79 로 되돌리는 변이로 검사를 고정한다.
⚠️ 숫자를 검사 코드에 적지 않았다 — 적으면 T 를 더하는 사람이 검사까지 기억해야 한다.

`node scripts/mutate.mjs` → **총 43종 · 사망 43 · 생존 0 · 앵커 실패 0**
(동작 26 · 정적 17). ⚠️ **첫 실행에서 앵커 실패 3건**(D07·D08·D15)이 났다 — 이 회차의 문서 수정이
그 변이들의 `find` 문자열을 바꿔서다. 변이가 틀린 것이 아니라 **앵커가 낡은 것**이라 앵커를 고쳤다.

---

---

## 1. 지금 코드가 실제로 하는 일

문서가 아니라 `worker/index.js` 를 읽어 확인한 것이다. 줄 번호는 커밋 `8d8842d` 기준이다.

```
POST 없음.  가입이라는 개념이 코드에 존재하지 않는다.

GET  /api/login/:provider          :706-739
     ├ 설정 확인(id·STATE_KEY)  → 없으면 503
     ├ return 주소를 allowed() 로 검사
     ├ txn = mkToken();  Set-Cookie shh_t (HttpOnly·Secure·SameSite=Lax·Path=/api·600초)
     └ state = HMAC 서명([provider, back, now+600s, nonce, sha256(txn)])  → 제공자로 302

GET  /api/cb/:provider             :773-811   (카카오·구글)
GET  /api/exchange/:provider       :744-768   (네이버 — 콜백이 앱 도메인으로 온다)
     ├ limited(login)  → 10회/분
     ├ takeState()  서명·만료·provider 대조
     ├ bound()      shh_t 쿠키와 state 안 해시 대조   ← code 교환 **전에**
     └ exchange()   :465-494
        ├ 토큰 교환 → /me 호출 → provider_subject(who)
        ├ internalUid(env, name, who)   :511-526   ← ★ 여기가 곧 가입이다
        └ newSession(env, uid)          :244-263
```

| 확인한 사실 | 근거 |
|---|---|
| 소셜 로그인 성공이 곧 계정 생성이다. 분기가 없다 | `exchange()` 가 조건 없이 `internalUid()` 를 부른다 (`:492`) |
| `internalUid()` 는 **신규인지 기존인지 호출자에게 말하지 않는다** | `:511-526` — 두 경우 다 `id` 문자열 하나 |
| 동의·약관·연령을 적을 자리가 없다 | `users` 컬럼은 `id · provider · provider_subject · session_version · created_at` 뿐 (`worker/schema.sql:15-21`) |
| 신규 가입 경로는 `WRITE_ROUTES` 에 **자동으로 안 들어간다** | `/^\/(session\|book\|me\|friends)(\/\|$)/` (`:327`) — `/signup` 은 여기 없다 |
| 새 버킷 이름을 `RL_MAX` 에 안 넣으면 **조용히 120/분**이 된다 | `RL_MAX[bucket] ?? RL_MAX.write` (`:369`) |
| 상태 변경 요청의 Origin 검사는 **모든 non-GET** 에 이미 걸린다 | `:830-839` — 새 POST 라우트는 자동으로 보호된다 |
| 계정 삭제는 `DELETE FROM users` **한 문장** + CASCADE | `:948` |
| `users` 를 참조하는 **모든** 테이블은 CASCADE 여야 한다 — 테스트가 강제한다 | `scripts/test-migrations.mjs:96-103` |
| **GET 인데 DB 를 쓰는 라우트가 있다** — 메서드로 쓰기를 판정할 수 없다 | 아래 별도 표 |
| 정적 자산과 Functions 는 **보통** 같은 Pages 배포 단위로 나간다 | `wrangler.jsonc` 의 `pages_build_output_dir: "dist"` + 루트 `functions/` |
| 서비스워커는 `privacy.html` 을 **선캐시**한다 | `service-worker.js` ASSETS 목록 |

### 1-1. **GET 인데 DB 를 쓰는 자리** (2판에서 추가 — 유지보수 모드 설계의 근거)

| 라우트 | 무엇을 쓰나 | 근거 |
|---|---|---|
| `GET /api/cb/:provider` | `rate_limits`(limited) · `users`(internalUid) · `sessions`(newSession) · 청소 `DELETE`×2 | `:772`·`:492`·`:244-263` |
| `GET /api/exchange/:provider` | 위와 같음 | `:747`·`:492` |
| **`GET /api/friends`** | **`invite_codes` INSERT** — 코드가 없으면 그 자리에서 만든다 | `:963` → `myCode()` `:598-606` |
| (모든 `limited()` 호출) | `rate_limits` UPSERT | `:379-383` |

`GET /api/book`·`/api/me`·`/api/friends/:id/book`·`/api/ready`·`/api/health` 는 읽기 전용이다.
`GET /api/login/:provider` 는 DB 를 만지지 않는다(302 와 서명뿐).

> **이것이 §10-7 의 출발점이다.** 유지보수 모드를 `req.method !== "GET"` 으로 만들면
> 복원 중에 OAuth 콜백이 계정을 만들고 친구 화면이 초대 코드를 만든다.

마지막 두 줄이 §7(정책 문서 무결성)의 출발점이다. 서버 상수와 정적 파일은 보통 같은 배포에
실리지만 **"절대 갈릴 수 없다"고 말하지 않는다** — 별칭 전파 지연(`HANDOFF.md` §4-6: 프로덕션
별칭이 배포 직후 1분 정도 옛 응답을 준다)과 부분 롤백이 실재한다. 그리고 **서비스워커 캐시는
그 배포와 무관하게 옛 파일을 줄 수 있다.** 그래서 세대 일치를 **배포 구조에 기대지 않고
런타임에 해시로 대조한다**(§7-2 Q5·Q6).

---

## 2. 설계 목표와 불변식

| # | 불변식 | 어디서 강제되나 |
|---|---|---|
| I1 | 제공자 확인만으로 `users` 행이 생기지 않는다 | 서버 분기 (§7) |
| I2 | 기존 사용자는 가입 화면 없이 로그인된다 | 로그인 경로 (§5) |
| I3 | 신규 사용자는 가입 제출 전 세션이 없다 | 세션 발급이 계정 생성 뒤에만 |
| I4 | 계정과 필수 정책 이벤트가 갈라져 생기지 않는다 | `env.DB.batch()` 단일 트랜잭션 |
| I5 | 동시 가입에서도 `users` 행은 하나다 | `UNIQUE(provider, provider_subject)` + `ON CONFLICT DO NOTHING` |
| I6 | 진 쪽 요청은 이긴 계정 상태를 다시 조회해 안전하게 확인한다 | batch 뒤 `SELECT` |
| I7 | 로그인 API 와 가입 완료 API 를 혼동할 수 없다 | 경로 분리 + 서명된 `pv` 유무 |
| **I8** | **state 재생으로 「다른 사람의」 계정 세션을 얻을 수 없다.** 세션의 주인은 언제나 **그 왕복에서 제공자가 방금 인증한 계정**이다. ⚠️ **이 불변식은 세션 소유권만 말한다** — 정책 증거의 귀속은 **I17~I21** 이 따로 지킨다(4판). ⚠️ **로그인 state 의 동시 재사용으로 자기 세션이 하나 더 발급되는 것은 막지 않는다**(수용된 잔여 위험 — §4-A) | `bound()`(`:751`·`:781`) + 성공 응답의 `clearTxn()`(`:766`·`:809`) + `takeState()` 만료 10분. 검증: §13-5 **D 묶음**(특히 T30·T35) |
| I9 | 탈퇴 후 재가입은 **새 내부 UID** 다 | 행이 사라져 `SELECT` 가 비고 새 UUID |
| I10 | `UNIQUE(provider, provider_subject)` 유지 | `worker/schema.sql:23` |
| I11 | 사용자가 본 문서와 서버가 기록한 해시가 같다 | 양쪽 대조 (§7) |
| I12 | 확정(`confirmed`)된 삭제는 복원 뒤에도 유지된다 | 삭제 표식 + 재적용 (§10) |
| **I13** | **유지보수 모드에서는 DB 를 쓰는 라우트가 하나도 「진입」하지 못한다** — 메서드가 아니라 **라우트 목록**으로 막는다. ⚠️ **이것은 진입 차단이지 drain 이 아니다**(§10-9) | §10-7 전수 표 |
| **I14** | **삭제 표식 DB 는 과거로 복원하지 않는다.** 불가피하면 **현재 내보내기 → 복원 → 합집합 병합 → 대조**(**ledger 재작성 13단계**) 없이 재개하지 않는다. ⛔ **ledger 를 제자리 `restore` 하지 않는다** — 게이트·epoch·lease 가 함께 과거로 간다(§10-6-0) | §10-6 |
| **I15** | **reconciliation 이 표식을 지워서 「표식 없는 삭제」를 만드는 경로가 없다** — reconciliation 은 **승격만** 한다(promote-only). 추가로 활성 deletion lease 0건일 때만 실행한다 | §10-4 · §10-9 Q7 |
| **I16** | **검증 가능한 전역 write drain 이 구현·검증되기 전에는 주 D1 을 복원하지 않는다.** deletion lease 0건은 **삭제 saga 가 멈췄다는 증거일 뿐** 전역 쓰기 정지의 증거가 아니다 | §10-8-0 금지 gate · §10-9-6 전수 표 |

**가입 정책 증거 불변식** (4판 신설 — I8 과 **다른 것을 지킨다**. I8 은 세션 소유권, 아래는 증거 귀속)

| # | 불변식 | 어디서 강제되나 |
|---|---|---|
| **I17** | **하나의 가입 `state` 는 최대 하나의 계정 생성에만 쓰인다** | `consumed_signup_states` PRIMARY KEY (§5-3) |
| **I18** | **하나의 정책 행위는 여러 사용자 ID 에 귀속되지 않는다.** 「하나의 정책 행위」는 **하나의 가입 `state`** 로 식별한다 — 같은 `state_hash` 에서 비롯된 정책 이벤트는 **정확히 한 `user_id`** 에만 붙는다 | I17 의 결과 + `WHERE EXISTS`(§5-1) |
| **I18-b** | ⛔ **서로 무관한 가입이 같은 `pv`·`occurred_at` 을 갖는 것은 정상이다.** 이 조합에 **전역 `UNIQUE` 를 걸지 않는다** | §5-1 주석 · **T48** |
| **I19** | **소비 표식과 사용자·필수 정책 이벤트 기록이 갈라져 부분 성공하지 않는다** | 같은 `batch()` = 단일 트랜잭션 (R5) |
| **I20** | **replay 표에 raw state·`provider_subject`·사용자 ID·이메일·IP 를 저장하지 않는다** | R1·R2·R3 |
| **I21** | **중복 `state` 는 fail-closed 한다.** 통과시키는 fallback 이 없다 | R6·R8 → 409 `stateUsed` |

> ⚠️ **5판 정정 — I18 의 4판 표현은 틀렸다.** 4판은 「같은 `pv`·`occurred_at` 이 서로 다른
> `user_id` 에 붙지 않는다」고 **전역으로** 단정했다. **정상적인 서로 다른 가입 두 건이 그렇게 될 수 있다.**
>
> - `pv` 는 **정책 문서 번들의 해시**다. 문서가 안 바뀌면 **모든 사용자가 같은 값**을 갖는다 — 다른 것이 이상하다.
> - `occurred_at` 은 **밀리초 단위 서버 시각**이다. 두 사람이 같은 밀리초에 가입 버튼을 누르면 같은 값이 된다.
>
> 그래서 4판의 표현대로 구현하면 **정상 가입 두 건이 불변식 위반으로 잡히고**, 더 나쁘게는
> 구현자가 그 문장을 읽고 `UNIQUE(pv, occurred_at)` 또는 `UNIQUE(kind, pv, occurred_at)` 를
> 추가할 수 있다. 그러면 **같은 순간에 가입한 두 번째 사람의 가입이 통째로 실패한다** —
> 막으려던 것과 무관한 사람이 막힌다.
>
> 지켜야 하는 것은 **「하나의 정책 행위」의 식별자가 `state` 라는 것**이다. `pv`·`occurred_at` 은
> 그 행위의 **속성**이지 식별자가 아니다. 유일성은 `consumed_signup_states.state_hash` 의
> PRIMARY KEY 하나로 강제되고, 그것으로 충분하다(I17).

**삭제 실패 불변식** (4판 신설)

| # | 불변식 | 어디서 강제되나 |
|---|---|---|
| **I22** | **실패 응답을 받은 과거 삭제 요청만으로 운영자가 나중에 활동 중 계정을 삭제하지 않는다** | §10-5-1 S3·S5 |
| **I23** | **계정이 주 D1 에 살아 있으면 그 pending 은 「실패한 삭제」로 판정한다** — 「대기 중인 삭제 명령」이 아니다 | §10-5-1 S3 |
| **I24** | **새 삭제는 사용자의 새 요청으로만 시작한다.** 운영자가 saga 를 대신 실행하지 않는다 | §10-5-1 S4 |
| **I25** | **stale pending 의 수동 제거는** maintenance closed · 현재 epoch · 활성 deletion lease 0건 · **계정 존재 재확인** 뒤에만 한다 | §10-5-1 S6 · Q7 |

---

## 3. 회원가입 상태 머신

### 3-1. 정상 경로

```
anonymous
  └ 가입하기 → policy_screen
policy_screen            (약관 체크 · 만14세 체크 · 방침 요약+전문 링크 · 제공자 선택)
  └ 제출 → signup_started            POST /api/signup/start
oauth_started            (shh_t 쿠키 심음 · state 에 pv 서명 · 제공자로 이동)
  └ 제공자 동의 → provider_verified  (/cb 또는 /exchange 에서 code 교환 성공)
provider_verified
  ├ 기존 계정 있음 → existing_user → session_issued → active   (정책 이벤트 안 만듦)
  └ 없음          → account_and_policy_events_created (batch) → session_issued → active
```

로그인 경로:

```
anonymous → login_started (GET /api/login/:provider, pv 없음)
  → provider_verified
      ├ 기존 계정 → session_issued → active
      └ 없음      → signup_required   (계정 생성 안 함. 가입 화면으로 되돌림)
```

### 3-2. 상태별 표

`서버가 아는 값`은 그 시점에 서버가 요청으로부터 얻는 값이다. 서버는 **어떤 임시 상태도 저장하지
않는다**(§6 권고안 기준).

| 상태 | 서버가 아는 값 | 브라우저가 가진 값 | DB 기록 | 다음 상태 조건 | 실패 시 지울 임시값 | 사용자에게 보이는 것 | 재시도 |
|---|---|---|---|---|---|---|---|
| `anonymous` | 없음 | `shh-peek`(선택) | 없음 | 버튼 클릭 | — | 게이트 | ○ |
| `policy_screen` | 없음 | 표시한 **문서 해시 전부**(개수는 번들이 정한다) · 체크 상태 | 없음 | **필수 체크 전부**(개수는 `requiredPolicyKinds`, §8-0) + 제공자 선택 | 화면 상태(메모리) | 가입 화면 | ○ |
| `signup_started` | `provider`·`pv`·Origin | — | 없음 | `pv` == 현재 번들 | — | 이동 중 | ○ |
| `oauth_started` | 없음(서명해 들려보냄) | `shh_t` 쿠키 · `shh-nonce` | 없음 | 제공자 복귀 | `shh_t`(10분 만료) | 제공자 동의 화면 | ○ |
| `provider_verified` | `provider_subject` (메모리에만) | `shh_t` | 없음 | 계정 조회 결과 | `shh_t` 즉시 폐기 | — | ○ |
| `existing_user` | `uid` | — | 없음 | 세션 발급 | — | "이미 가입되어 있어 로그인했어요" | — |
| `signup_required` | 없음(subject 폐기) | — | **없음** | 사용자가 가입 화면으로 | `shh_t` | "아직 가입하지 않으셨어요" | ○ |
| `account_and_policy_events_created` | `uid`·`pv` | — | `users` 1행 + `policy_events` **N행**(N = `REQUIRED_POLICY_EVENTS`, §8-0) (**한 batch**) | batch 성공 | — | — | — |
| `session_issued` | `uid` | `shh_s` 쿠키 | `sessions` 1행 | — | — | "가입했어요" | — |
| `active` | `uid` | `shh_s`·`shh-via`·`shh-me` | — | — | — | 앱 | — |

### 3-3. 실패·중단 상태

| 상태 | 언제 | 서버 동작 | DB | 브라우저에서 지울 것 | 사용자 문구(안) | 재시도 |
|---|---|---|---|---|---|---|
| OAuth 거부 | 제공자 화면에서 취소 | `/cb` 가 `code` 없음 감지 | 없음 | `shh-nonce` | "로그인을 취소했어요" | ○ |
| state 만료 | 10분 초과 | 400 | 없음 | `shh_t` | "로그인 요청이 만료됐어요" | ○ |
| 브라우저 결속 실패 | `shh_t` 없음/불일치 | **code 교환 전** 400 + `clearTxn()` | 없음 | `shh_t` | "이 기기에서 시작한 로그인이 아니에요" | ○ |
| 가입 중단 | 정책 화면에서 뒤로 | 아무 일도 없음 | 없음 | 없음(서버 상태 자체가 없다) | — | ○ |
| 정책 문서 변경 | `pv` != 현재 번들 | `/signup/start` 409 `policyStale` · 콜백에서도 409 | 없음 | 화면 다시 읽기 | "방침이 바뀌었어요. 다시 확인해 주세요" | ○ |
| 가입 요청 만료 | state 10분 초과 | 위 state 만료와 같다 | 없음 | `shh_t` | 같음 | ○ |
| 중복 탭 | 두 탭이 가입 시작 | `shh_t` 는 한 칸 → 먼저 시작한 탭이 실패 | 없음 | `shh_t` | "다시 눌러 주세요" | ○ |
| 계정 생성 충돌 | 두 기기 동시 첫 가입 | `ON CONFLICT DO NOTHING` → 재조회 | `users` 1행 | — | 정상 가입 | — |
| 정책 이벤트 INSERT 실패 | batch 중 오류 | **batch 전체 롤백** → 계정도 안 생김 | 없음 | — | "가입하지 못했어요. 다시 시도해 주세요" | ○ |
| 세션 발급 실패 | batch 성공 후 `newSession` 실패 | 500 | 계정·이벤트 **있음**, 세션 없음 | — | "가입은 됐어요. 다시 로그인해 주세요" | ○ (다음 로그인이 기존 사용자 경로로 성공) |
| 기존 계정 발견 | 가입 경로인데 이미 계정 | 세션 발급, **이벤트 안 만듦** | 없음 | — | "이미 가입되어 있어 로그인했어요" | — |
| 탈퇴 직후 재가입 | 같은 제공자 계정 | 신규로 판정 → 새 UUID + 새 이벤트 | 새 행 | — | 정상 가입 | — |
| 오래된 앱 버전 요청 | 옛 PWA 가 `GET /login` 만 안다 | 신규면 `signup_required` 로 돌려보냄 | **없음** | — | "앱을 새로고침해 주세요" | ○ |

> **「세션 발급 실패」가 유일하게 중간 상태를 남긴다.** 계정과 이벤트는 있고 세션만 없다.
> 이것을 batch 에 넣지 않는 이유: `newSession` 은 만료 세션 청소까지 하는 별도 batch 이고,
> 하나로 묶으면 청소 실패가 가입 실패가 된다. 남는 상태가 **안전한 쪽**이다 — 다시 로그인하면
> 기존 사용자로 붙는다. 반대(세션은 있는데 계정이 없다)는 절대 생길 수 없다.

---

## 4. 가입 시점 대안 비교

세 안 모두 **현재 코드에 대입해서** 적었다.

### A안 — OAuth 전에 가입 화면

정책 수락을 **서명된 `state` 에 실어** OAuth 왕복을 건넌다. `makeState()` 는 이미
`[provider, back, exp, nonce, txn]` 를 HMAC 서명하고 있으므로 여섯 번째 칸 `pv` 를 더하면 된다
(`takeState()` 의 구조분해는 옛 state 에 대해 `undefined → ""` 로 안전하다).

- **임시 저장소가 필요 없다.** pending 테이블도, 암호화 쿠키도, 제공자 회원번호 보관도 없다.
- 문제: **기존 사용자도 가입 화면을 보게 된다.** 진입점이 하나면 피할 수 없다.

#### 4-A. **로그인 state** 의 재사용 — 4판 정정

> **문서 전역 규칙**: `state` 재사용 결론은 **§4-A(로그인)와 §4-B(가입) 둘**이다.
> **「state 는 1회용이다/아니다」라는 전역 단정 하나로 두 흐름을 뭉개지 않는다** — 4판이
> 고친 것이 정확히 그 뭉개기다. 다른 절에 낡은 단정이 있으면 이 두 절이 이긴다.

1판은 "재생해도 `code` 가 1회용이라 제공자가 거부한다"고 적었고, 2·3판은 반대로
"같은 브라우저면 10분 동안 계속 재사용된다"고 적었다. **둘 다 부정확하다.**
`worker/index.js` 를 다시 읽고 확인한 사실은 아래가 전부다.

| # | 코드 사실 | 근거 |
|---|---|---|
| 1 | `takeState()` 는 **서명과 만료만** 본다. **소비 기록이 없다** | `:180-191` — 반환 전에 아무것도 쓰지 않는다 |
| 2 | `bound()` 는 `shh_t` 쿠키 해시와 state 안 해시를 대조한다 | `:238` |
| 3 | **성공 응답이 `clearTxn()` 으로 `shh_t` 를 지운다** | `:766`(exchange) · `:809`(cb) — `Max-Age=0` |
| 4 | `bound()` 실패 응답도 `clearTxn()` 을 붙인다 | `:753` · `:784` |

3번 때문에 **정상 브라우저에서 첫 요청의 응답 처리가 끝난 뒤의 순차 재사용은 보통 막힌다** —
쿠키가 없어 `bound()` 가 거짓이 된다. 2·3판의 "10분 동안 순차적으로 계속 재사용된다"는
**과장이었다.**

그러나 **막히지 않는 경우가 남는다.** 1번(서버 소비 기록 없음) 때문이다.

| 경우 | 통과하나 | 왜 |
|---|---|---|
| 같은 `state` + **같은 `code`** | **아니오** | 제공자가 `code` 를 1회용으로 거부 |
| 완료 뒤 **순차** 재사용(정상 브라우저) + 새 `code` | **아니오** | `clearTxn()` 이 지운 쿠키 때문에 `bound()` 실패 |
| **동시 요청 2개** — 쿠키 삭제 응답이 도착하기 **전에** 둘 다 출발 + 각자 새 `code` | **예** | 둘 다 같은 `shh_t` 를 실어 보내므로 `bound()` 를 **둘 다 통과**한다 |
| **Set-Cookie 삭제를 따르지 않는 클라이언트**(스크립트·수동 `curl`·비표준 클라이언트)의 재전송 + 새 `code` | **예** | 쿠키가 그대로라 `bound()` 통과 |
| 다른 브라우저(`shh_t` 없음/불일치) | **아니오** | `bound()` 실패 — code 교환 **전에** 400 |
| 10분 초과 | **아니오** | `takeState()` 만료 |

**정확한 결론** — 이 문장이 문서 전역의 기준이다:

> 일반 브라우저의 완료 후 **순차** 재사용은 `clearTxn()` 으로 차단되지만, **서버 측 소비 기록이
> 없으므로 동시 요청 또는 쿠키 삭제를 무시한 재전송에서는 같은 state 가 여러 번 사용될 수 있다.**

##### 로그인 state 에서 이것이 뜻하는 것 (`pv` 없음)

| | 사실 |
|---|---|
| 세션 소유권 | 세션 주인은 언제나 **그 왕복에서 제공자가 방금 인증한 계정**이다. 남의 세션을 얻는 경로가 아니다 |
| 재사용이 만드는 것 | 같은 사람의 세션이 하나 더 생긴다. `sessions` 행이 늘 뿐 권한 경계가 넘어가지 않는다 |
| **판정** | **잔여 위험으로 감수한다.** 로그인 state 에는 `pv`·정책 행위 시각이 없어 **귀속시킬 증거가 없다** |

**왜 로그인은 감수해도 되나**: 얻는 것이 자기 세션뿐이고, 진짜 1회용을 만들려면 **인증 없는
자리에 서버 쓰기**가 필요하다. 그건 2026-08-11 에 일부러 없앴다(`worker/index.js:25-26`:
`curl` 반복만으로 무료 한도를 태울 수 있다). 로그인 경로에 그것을 되살리면 **DoS 표면이
커지는 대가로 얻는 것이 없다.**

#### 4-B. **가입 state** 의 재사용 — 4판 신설 · **여기서는 감수하지 않는다**

가입 state 에는 로그인 state 에 없는 것이 실린다.

```
pv (정책 번들 해시) · 이용약관 수락 · 만 14세 이상 진술 · occurred_at (행위 시각)
```

즉 **가입 state 는 「이 사람이 이 시각에 이 문서를 수락했다」는 증거를 나르는 봉투**다.
§4-A 의 통과 경로(동시 요청·쿠키 삭제 무시)가 그대로 여기에도 있으므로, **한 번의 수락이
여러 계정의 증거가 되는 상태**가 만들어진다.

##### 3판의 두 가지 잘못된 결론

| 잘못된 서술 | 왜 틀렸나 |
|---|---|
| 「가입 재시도는 `UNIQUE` 때문에 언제나 계정 하나로 수렴한다」 | `UNIQUE(provider, provider_subject)` 는 **같은 제공자 계정**일 때만 수렴시킨다. **다른 제공자 계정(A·B)이면 계정이 둘 생기고, 둘 다 같은 `pv`·`occurred_at` 을 갖는다.** 수렴은 일어나지 않는다 |
| 「T27: 다른 제공자 계정으로 재사용해도 **피해자 세션을 못 얻으니 성공**」 | **세션 소유권만 본 판정이다.** 세션은 안전하지만 **정책 증거가 오염된다.** 두 불변식은 별개이고, 3판은 하나로 다른 하나를 덮었다 |

##### 공격·실패 순서 (위협 34 — 심각도 **High**)

```
1  브라우저가 약관·연령을 확인하고 [가입하기] → 서버가 가입 state S 를 서명해 발급한다
     (S 안: pv · terms=accepted · age14=true · occurred_at)
2  같은 shh_t 를 쓰는 두 OAuth 왕복을 거의 동시에 시작한다
3  첫 번째는 제공자 계정 A 로, 두 번째는 제공자 계정 B 로 인증한다
4  두 요청 모두 서로 다른 유효 code 를 받는다 (제공자의 code 1회성으로는 못 막는다)
5  서버에는 state S 의 소비 여부를 기록하는 곳이 없다 (takeState 는 서명·만료만 본다)
6  clearTxn() 의 쿠키 삭제가 아직 도착하지 않아 둘 다 bound() 를 통과한다
7  두 요청이 모두 계정 생성까지 도달한다 — provider_subject 가 달라 UNIQUE 가 안 막는다
8  users 에 A·B 두 행, policy_events 에 같은 pv·occurred_at 이 2N 행
────
결과: 한 사람의 한 번의 수락·진술이 두 내부 계정의 정책 수락 증거가 된다.
      세션은 각자 자기 것이라 「세션 탈취」로는 잡히지 않는다.
```

**이것을 「자기 계정이니 안전」으로 분류하지 않는다.** 세션 소유권과 정책 증거 무결성은
**별도 불변식**이다(I8 vs I17~I21). 약관 수락·연령 진술은 **법적 증거로 쓰려고 남기는 기록**이고,
한 행위가 두 계정에 붙으면 그 기록은 어느 계정에 대해서도 정확하지 않다.

**심각도 High.** 계정 탈취는 아니지만 **기록 위조와 같은 급의 무결성 훼손**이고, 법률 검토가
「동의」로 결론 나면 **받지 않은 동의를 받았다고 적는 것**이 된다(`.claude/rules/privacy.md`).

##### 4판의 결론

> **가입 state 의 replay 는 잔여 위험으로 수용하지 않는다.** 서버 측 1회 소비 표식을
> 도입한다 — 설계는 **§5-3**.

| | 로그인 state | **가입 state** |
|---|---|---|
| `pv`·정책 행위 시각 | 없음 | **있음** |
| 재사용이 만드는 것 | 자기 세션 하나 더 | **정책 증거의 다중 귀속** |
| 순차 재사용 | `clearTxn()` 이 차단 | 같음 |
| 동시 요청·쿠키 삭제 무시 | **통과(감수)** | **통과 → 막는다(§5-3)** |
| 판정 | 잔여 위험 수용 | **수용하지 않음. fail-closed** |
| 불변식 | I8(세션 소유권) | **I17~I21**(증거 귀속) |

### B안 — OAuth 후 가입 화면

OAuth 로 `provider_subject` 를 얻은 뒤 계정을 만들지 않고 가입 화면을 띄운다. 그 사이
`provider_subject` 를 **어딘가 보관해야 한다** — 이것이 B안의 전부다(§6).

- 기존 사용자 경험이 가장 짧다.
- 대가: 임시 개인정보 · 탈취/재사용/만료/중복 제출 · 정리 작업 · 서버 상태 증가.
  pending 을 D1 에 두면 **Time Travel 이 그 pending 까지 복원**한다.

### C안 — 로그인과 회원가입 완전 분리

진입점을 둘로 나눈다. 가입 경로는 A안 그대로(정책 → OAuth → 생성), 로그인 경로는 계정을
만들지 않고 신규면 되돌려 보낸다.

- 신규 사용자가 로그인 버튼을 먼저 누르면 OAuth 왕복이 **한 번 헛돈다.**
- 그 대신 기존 사용자는 정책 화면을 다시 보지 않는다.

### 비교표

| 기준 | A | B | C (= A + 진입점 분리) |
|---|---|---|---|
| 보안(새 공격면) | 없음 | **가입 대기 토큰 하나가 통째로 새 공격면** | 없음 |
| 구현 복잡도 | 낮음 | 높음 | 낮음(+버튼 하나·분기 하나) |
| 사용자 이해도 | 낮음 — 기존 사용자가 매번 약관을 본다 | 높음 | 중간 — "가입/로그인"을 구분해야 한다 |
| OAuth 왕복(신규) | 1 | 1 | 1 (로그인 버튼을 먼저 누르면 2) |
| OAuth 왕복(기존) | 1 | 1 | 1 |
| 새 DB 테이블 | 0 | **1 (`pending_signups`)** 또는 0(쿠키안) | 0 |
| 개인정보 임시 저장 | **없음** | `provider_subject` 를 최소 수 분 보관 | **없음** |
| 재생 공격 | 순차 재사용은 `clearTxn()` 이 막는다. **동시 요청·쿠키 삭제 무시는 통과한다** — 로그인은 감수(§4-A), **가입은 소비 표식으로 막는다**(§4-B·§5-3) | 위와 같고, **pending 토큰 재사용을 별도로 막아야 한다**(새 공격면) | A 와 같다 — 같은 `state` 설계이고 **소비 표식도 같이 진다** |
| 동시 가입 | **같은** 제공자 계정이면 UNIQUE 로 수렴. **다른** 제공자 계정이면 수렴하지 않는다 → 소비 표식이 막는다(§5-3) | 같음 + pending 중복 제출 | A 와 같다 |
| 기존 사용자 호환 | 나쁨(전원 정책 화면) | 좋음 | 좋음 |
| 장애 복구 | 서버 상태 0이라 정리할 것이 없다 | pending 청소 필요 | 서버 상태 0 |
| 테스트 비용 | 낮음 | 높음(만료·재사용·중복·정리) | 낮음~중간 |
| 이 규모에 적합한가 | 부분적 | **과함** | **적합** |

### 권고 — **C안**

이유 셋.

1. **임시 개인정보가 0이다.** B안이 지불하는 값(제공자 회원번호 임시 보관, 토큰 탈취·재사용·만료,
   Time Travel 이 pending 까지 복원하는 문제)을 전부 안 낸다. 최소수집 원칙과도 같은 방향이다.
2. **이미 있는 자물쇠를 그대로 쓴다.** 정책 수락은 우리가 HMAC 서명한 `state` 를 타고 건너오고,
   그 서명은 지금 로그인 CSRF 를 막고 있는 바로 그 자물쇠다. 새 비밀값도, 새 테이블도 없다.
3. **기존 사용자를 괴롭히지 않는다.** A안의 유일한 약점이 진입점 하나뿐인 것인데, 버튼 하나와
   분기 하나로 해결된다.

**C안의 대가**는 명시한다: 신규 사용자가 「로그인」을 먼저 누르면 제공자 동의를 한 번 헛하게 된다.
현재 사용자 0명이고, 게이트에서 **「가입하기」를 주 버튼으로** 두면 실제 발생률은 낮다.

> **확정** — §17 결정 1(C안). 아래가 구현 대상이다.

---

## 5. C안 상세 흐름

```
① 게이트         [ 가입하기 ]  [ 로그인 ]  [ 로그인 없이 둘러보기 ]

② 가입 화면      GET /api/policies  →  { pv, docs:{terms:{path,hash}, privacy:{…}, age14:{…}} }
                 클라이언트가 각 path 를 fetch 해 **내용을 직접 해시**하고 서버 값과 대조
                 ├ 다르면 → 가입 버튼을 그리지 않는다("앱을 새로고침해 주세요")   ← fail-closed
                 └ 같으면 → 문서를 화면에 렌더하고 체크박스 N개(§8-0) + 제공자 3개(카카오·네이버·구글)

③ 제출           POST /api/signup/start   { provider, terms:true, age14:true, pv }
                 서버: Origin 검사(기존 :830-839) · signup 버킷 · pv == 현재 번들 · 두 값 모두 true
                 → occurredAt = Date.now()                        ← 행위 시각(§7-4)
                 → Set-Cookie shh_t
                 → state = sign([provider, back, exp, nonce, sha256(txn), pv, occurredAt])
                 → { url: "<제공자 authorize URL>" }   앱이 그 주소로 이동

④ 복귀           /api/cb/:provider  또는  /api/exchange/:provider   (지금 코드 그대로)
                 limited(login) → takeState → bound(shh_t)  ← 전부 code 교환 **전에**

⑤ 판정           verifyProvider() → { name, subject }        ← 계정을 만들지 않는다
                 findUser(name, subject)
                 ├ 있다  → newSession()                      (pv 는 무시. 이벤트 없음)
                 └ 없다
                    ├ state 에 pv 있음 → pv 재검사 → createAccountWithPolicy() → newSession()
                    └ pv 없음(로그인 경로) → signup_required (아무것도 안 만든다)
```

`POST /signup/start` 를 GET 링크(`/login/kakao?pv=…`)로 대신하지 않는 이유: 그러면
**남이 보낸 링크 하나가 약관 수락과 연령 진술을 만들어낸다.** 사용자는 가입 화면을 본 적이 없는데
기록에는 "수락함"이 남는다. POST 는 이미 있는 Origin 검사(`:830-839`)에 자동으로 걸린다.

### 5-1. 계정 생성 batch (동시 가입까지 포함)

```sql
-- ⓪ 가입 state 소비 표식. **이 문장이 먼저다** (4판 신설 — §5-3).
--    PRIMARY KEY 충돌이면 batch 전체가 롤백되므로 ①·②가 아예 실행되지 않는다.
--    저장하는 것은 HMAC(TOMBSTONE_KEY, state 전체) 하나뿐이다. raw state 를 넣지 않는다(§5-3-5).
INSERT INTO consumed_signup_states (state_hash, expires_at)
VALUES (?stateHash, ?stateExp);

-- ① 내 후보 id 로 넣어 본다. 진 쪽은 아무 행도 안 만든다.
INSERT INTO users (id, provider, provider_subject, session_version, created_at)
VALUES (?myId, ?provider, ?subject, 0, ?now)
ON CONFLICT (provider, provider_subject) DO NOTHING;

-- ② 이벤트는 **내가 실제로 이겼을 때만** 들어간다.
--    occurred_at 은 /signup/start 가 찍어 서명된 state 로 돌아온 서버 시각(§7-4),
--    recorded_at 은 지금 이 INSERT 의 서버 시각이다. 둘 다 서버가 만든다.
INSERT INTO policy_events (user_id, kind, action, document_version, occurred_at, recorded_at)
SELECT ?myId, 'terms', 'accepted', ?termsHash, ?occurredAt, ?now
 WHERE EXISTS (SELECT 1 FROM users WHERE id = ?myId);
-- 나머지 kind 도 같은 모양으로 반복한다. **개수는 `requiredPolicyKinds` 집합이 정한다**(§8-0) —
--    숫자를 하드코딩하지 않고 집합을 순회한다. 법률 결과에 따라 3 또는 4다.
```

셋(또는 넷)을 **한 `batch()`** 로 보낸다. D1 의 `batch` 는 하나의 트랜잭션이고 중간 실패는 전체
롤백이다(공식 문서: <https://developers.cloudflare.com/d1/worker-api/d1-database/>).

- 이긴 쪽: `consumed_signup_states` 1행 + `users` 1행 + `policy_events` **N행**(§8-0).
- **같은 제공자 계정**으로 진 쪽: ⓪이 충돌하거나(같은 state) ①에서 `DO NOTHING` 이 되고
  `WHERE EXISTS` 가 거짓이라 `policy_events` 0행. **중복 이벤트가 안 생긴다.**
- **다른 제공자 계정**으로 온 두 번째 요청: ①의 `UNIQUE` 가 **안 막는다**(subject 가 다르다).
  **⓪이 막는다** — 이것이 4판에서 ⓪을 넣은 이유다(§4-B).
- batch 뒤 `SELECT id FROM users WHERE provider=? AND provider_subject=?` 로 실제 id 를 얻어
  그 id 로 세션을 만든다(`internalUid()` 가 이미 쓰는 무늬 그대로).

`policy_events` 에 `UNIQUE` 를 걸지 않는다: 정책이 바뀌어 기존 사용자에게 재확인을 받는 날
같은 `kind` 가 여러 행 있어야 한다.

### 5-3. **가입 state 1회 소비 표식 (replay tombstone)** — 4판 신설

§4-B 의 위협 34 를 닫는 장치다. **로그인 경로에는 붙이지 않는다** — 거기엔 귀속시킬 증거가 없고,
붙이면 인증 없는 자리에 쓰기가 생겨 DoS 표면만 커진다(§4-A).

#### 5-3-1. 최소 스키마 (4단계 구현 요구사항 — **이번 단계에서 만들지 않는다**)

```sql
-- 주 D1. 이름은 기존 명명 규칙에 맞춰 4단계에서 확정할 수 있다.
CREATE TABLE consumed_signup_states (
  state_hash TEXT PRIMARY KEY,      -- HMAC-SHA256(TOMBSTONE_KEY, state 전체). 단순 해시가 아니다(§5-3-5)
  key_version INTEGER NOT NULL,     -- TOMBSTONE_KEY 회전용. 표식 수명이 10분이라 회전이 값싸다
  expires_at INTEGER NOT NULL       -- state 자신의 만료 시각(= now+600s)
);
CREATE INDEX consumed_signup_states_exp ON consumed_signup_states(expires_at);
```

#### 5-3-2. **바꾸면 안 되는 조건** (이름은 바꿔도 되지만 아래는 고정)

| # | 조건 | 왜 |
|---|---|---|
| **R1** | **raw state 를 저장하지 않는다** | state 안에는 `pv`·수락·연령 진술·`occurred_at` 이 들어 있다. 원문을 남기면 그 표가 곧 정책 기록의 사본이 된다 |
| **R2** | **`provider_subject`·사용자 ID·이메일·IP 를 저장하지 않는다** | 이 표는 **누가 가입했는지 몰라도 되는** 표다. 알면 그만큼 개인정보다 |
| **R3** | 저장값은 **전용 키를 쓴 `HMAC-SHA256(TOMBSTONE_KEY, state 전체)`** 다. **단순 SHA-256 을 쓰지 않는다** — 근거는 아래 §5-3-5 | 되돌릴 수 없어야 하고, **관찰된 state 로부터 표식을 계산할 수 없어야** 한다 |
| **R4** | **제공자 검증(`verifyProvider`)이 성공한 뒤에만** 소비 표식을 쓴다 | `/signup/start` 반복이나 위조 state 만으로 **DB 쓰기가 발생하지 않는다.** 2026-08-11 에 없앤 "인증 없는 자리의 쓰기"를 되살리지 않는다 |
| **R5** | 소비 표식 INSERT · 사용자 생성 · 필수 정책 이벤트 N행은 **반드시 하나의 주 D1 `batch()` 안에서 전부 성공하거나 전부 실패한다.** 나눌 수 없다면 **계정을 생성하지 않는다** | 부분 성공이 없어야 한다. §5-1 의 ⓪①② 가 그 배치다. 공식 문서: "Batched statements **are SQL transactions**" · "it **aborts or rolls back the entire sequence**"(§19). ⚠️ **「가능한 한」·「가능하면 원자적으로」·「실패해도 fallback 가능」 같은 표현을 쓰지 않는다** — 5판 정정. 4판까지 "가능한 한 같은 batch"라고 적혀 있었고, 그 한 단어가 **필수 원자성을 구현자의 재량으로 낮춘다** |
| **R6** | 같은 `state_hash` 가 이미 있으면 **가입 완료를 거부**한다 | PRIMARY KEY 충돌 → batch 전체 롤백 |
| **R7** | 중복 소비 실패 시 **사용자·정책 이벤트·세션을 새로 만들지 않는다** | 롤백이 사용자·이벤트를 되돌리고, 세션은 batch **성공 뒤에만** 만든다 |
| **R8** | `state_hash` 충돌·중복을 **fallback 으로 통과시키지 않는다** | fail-closed. `RL_MAX` fallback 과 같은 교훈(§12-1) |
| **R9** | **raw state 와 `state_hash` 전체를 로그에 남기지 않는다** | §12 로그 금지 목록에 둘 다 추가 |
| **R10** | 만료된 표식은 **서명 state 자신의 만료가 지난 뒤에만** 정리한다 | 만료 전에 지우면 그 순간 replay 창이 다시 열린다 |
| **R11** | 이 표는 **`pending_signups` 가 아니다** | provider 정보를 하나도 안 담는다. 결정 2 가 거부한 것은 **OAuth 전에 provider 정보를 보관하는 것**이고, 이 표는 **OAuth 검증 후의 중복 방지 표식**이다 |

#### 5-3-3. 응답과 정리

| 상황 | 응답 | DB |
|---|---|---|
| 정상 첫 소비 | 성공(세션 발급) | 표식 1 · `users` 1 · `policy_events` N |
| 같은 state 두 번째 | **409 `stateUsed`** — "이미 처리된 가입 요청이에요. 다시 시작해 주세요" | **무변화** |
| 만료 state | 400(기존 `takeState()` 만료) | 무변화. **표식을 보지도 않는다** |
| 정리 실행 시점 | 삭제 saga 정리(§10-5 ①)와 같은 무늬로 **가입 성공 경로 끝**에 `DELETE FROM consumed_signup_states WHERE expires_at < ?` 한 문장. **실패를 삼킨다** | 만료된 행만 |

#### 5-3-4. **Time Travel 로 이 표가 과거로 돌아가면**

| 사실 | 내용 |
|---|---|
| 위험 | 복원으로 소비 표식이 사라지면 그 state 를 **다시 쓸 수 있게 된다** |
| **그러나 무해하다** | 가입 state 는 **10분 만료**다(`makeState()` `:176`). 복원이 필요할 만한 사고 시간(분~시간)이면 **`takeState()` 가 먼저 거부**한다 — 표식이 없어도 통과하지 못한다 |
| **다만 단정하지 않는다** | 복원 시점이 발급 후 10분 이내인 극단적 경우는 원리적으로 남는다. **그때 뚫리는 것은 그 한 state 하나**이고, 유지보수 모드가 가입 진입을 이미 막고 있다(§10-7) |
| 기록 | 위협 34 의 「남는 위험」 칸 |

**이 표는 주 D1 에 둔다.** ledger(삭제 표식 DB)에 두지 않는다 — 계정 생성 batch 와 **같은
트랜잭션**에 들어가야 R5 가 성립하는데, 두 DB 사이에는 공통 트랜잭션이 없다(§10-3).

#### 5-3-5. **왜 단순 SHA-256 이 아니라 전용 키 HMAC 인가** (5판 신설 · 결정 A)

4판은 R3 를 「서명 state 전체의 SHA-256」이라고 적었다. **그 선택은 관찰자에게 대조를 허용한다.**

두 방식을 같은 공격자 앞에 세워 비교한다. 공격자는 **`state` 문자열을 관찰할 수 있는 사람**이다 —
제공자 접근 로그를 볼 수 있는 사람, 같은 기기의 브라우저 기록을 보는 사람, 조직 프록시 운영자,
그리고 **DB 표를 읽을 수 있게 된 사람**(유출·오설정).

| | 가) 단순 `SHA-256(state)` | 나) `HMAC(TOMBSTONE_KEY, state)` ← **채택** |
|---|---|---|
| 되돌릴 수 있나 | 아니오 | 아니오 |
| **관찰한 state 로 표식을 계산할 수 있나** | **예.** 해시 함수는 공개다. 로그에서 본 `state` 를 그대로 SHA-256 하면 DB 의 어느 행인지 **정확히 지목**된다 | **아니오.** 키가 없으면 계산 자체가 불가능하다 |
| 그래서 무엇이 새나 | 「내가 로그에서 본 그 가입 요청이 **실제로 계정 생성까지 갔다**」는 사실. 그리고 그 `state` 를 복호화하면(키가 샜다면) 그 행의 수락 시각·문서 해시까지 이어진다 | 대조가 성립하지 않는다 |
| 표만 유출됐을 때 | 값 자체로는 아무것도 아님(양쪽 같음) | 같음 |
| **state 로그 + 표가 함께 유출됐을 때** | **연결된다** | **연결되지 않는다**(키가 따로 유출되지 않는 한) |
| 비용 | 없음 | 시크릿 1개(`TOMBSTONE_KEY`) · 회전 절차 |

**이것은 레이트리밋 키가 2026-08-16 에 겪은 것과 같은 교훈이다.** 그때도 평문 `SHA-256(용도|IP|창)`
이었고, IPv4 는 경우의 수가 43억뿐이라 **실측 43회 대입으로 원본 IP 가 복원**됐다. 여기서는
전수 대입이 필요 없다 — **관찰한 값 하나만 있으면 바로 맞춰진다.** 더 쉬운 공격이다.

| 요구사항 | 값 |
|---|---|
| 키 이름 | **`TOMBSTONE_KEY`** — 전용. `SIGNUP_STATE_KEY`·`STATE_KEY`·`RL_KEY`·`DELETION_KEY`·OAuth secret **어느 것도 겸용하지 않는다** |
| 왜 `SIGNUP_STATE_KEY` 와도 나누나 | 겸용하면 **가입 state 복호화 키가 곧 표식 계산 키**가 된다. 하나가 새면 위 표의 「연결되지 않는다」가 그 자리에서 무너진다 |
| 키 없음 | **fail-closed.** 표식을 계산할 수 없으면 **가입을 처리하지 않는다.** 평문 해시로 되돌아가지 않는다(`RL_KEY` 와 같은 판단, `worker/index.js:335-338`) |
| 회전 | `key_version` 컬럼. 표식 수명이 **10분**뿐이라 옛 키는 10분 뒤 버려도 된다 — 삭제 표식(`DELETION_KEY`, 수십 일)보다 훨씬 값싸다 |
| `/api/ready` | `TOMBSTONE_KEY` 없으면 503 (`RL_KEY` 와 같은 자리) |

### 5-4. **가입 state 는 서명이 아니라 암호화한다 (AEAD)** — 5판 신설 · 결정 A

#### 5-4-1. 무엇이 문제였나

4판까지의 설계는 가입 정보를 **지금 `makeState()` 와 같은 방식**으로 나르게 되어 있었다.

```js
// worker/index.js:175-178 — 현재 코드
const makeState = async (env, provider, back, nonce, txn) => {
  const body = b64u(ENC.encode(JSON.stringify([provider, back, Date.now() + 600e3, nonce || "", txn || ""])));
  return body + "." + (await sign(env, body));   // ← 서명일 뿐, 암호화가 아니다
};
```

`b64u(...)` 는 **인코딩이지 암호화가 아니다.** Base64URL 은 누구나 되돌린다.
서명(`sign`)이 막는 것은 **변조**이지 **열람**이 아니다.

> **서명 = 봉투에 도장.** 내용을 바꾸면 들키지만 **누구나 읽는다.**
> **암호화 = 봉투를 잠금.** 열쇠가 없으면 못 읽는다.
> 4판까지의 설계는 **도장만 찍고** 약관 수락·연령 진술·행위 시각을 그 안에 넣으려 했다.

그리고 이 값은 `?state=…` 로 **제공자 인증 주소에 실려 나간다**(`:731-734`). 지나가는 자리:

| 자리 | 남는 것 |
|---|---|
| 카카오·네이버·구글 | **제공자 접근 로그** |
| 사용자 브라우저 | 주소창·방문 기록. 같은 기기를 쓰는 다른 사람이 본다 |
| 중간 프록시·보안 장비 | 조직 네트워크면 남을 수 있다 |
| 사용자가 링크를 복사하면 | 받은 사람이 본다 |
| 우리 서버 오류 로그 | 실수로 `state` 를 찍으면(§12 로그 금지 목록) |

**로그인 state 는 이 문제가 없다.** 담는 값이 제공자 이름·복귀 주소·만료·표 해시뿐이고,
넷 다 **읽혀도 되는 값**이다. 그래서 **로그인 state 는 그대로 두고 가입 state 만 바꾼다.**

#### 5-4-2. 두 state 를 명확히 가른다

| | 로그인 state | **가입 state** |
|---|---|---|
| 만드는 자리 | `GET /api/login/:provider` | `POST /api/signup/start` |
| 형식 | `b64u(json) + "." + HMAC` (**현행 유지**) | **`v1.<b64u(nonce)>.<b64u(ciphertext‖tag)>`** |
| 키 | `STATE_KEY` | **`SIGNUP_STATE_KEY`** (전용) |
| 담는 것 | provider · back · exp · nonce · txnHash | 위 전부 **+ `pv`(정책 문서 해시 묶음) · 수락 여부 · 연령 진술 · `occurred_at`** |
| 읽히면 | 문제 없음 | **문제 있음** — 그래서 암호화한다 |
| 소비 표식 | **없다**(§4-A 의 잔여 위험 수용) | **있다**(§5-3) |
| 서로 바꿔 쓸 수 있나 | **아니오** — `purpose` 필드가 다르고 복호화 키가 다르다 | 같음 |

#### 5-4-3. 명세 (4단계 구현 요구사항 — **이번 단계에서 코드를 만들지 않는다**)

| # | 항목 | 요구사항 |
|---|---|---|
| **S1** | 알고리즘 | **AES-256-GCM**(WebCrypto `AES-GCM`). 인증된 암호화라 **복호화가 곧 무결성 검증**이다 — 서명을 따로 붙이지 않는다(두 겹은 검증 순서 실수를 부른다) |
| **S2** | 키 | `SIGNUP_STATE_KEY`. 32바이트. **`STATE_KEY`·`TOMBSTONE_KEY`·`RL_KEY`·`DELETION_KEY`·OAuth secret 과 겸용 금지** |
| **S3** | **nonce** | 요청마다 `crypto.getRandomValues(new Uint8Array(12))`. ⚠️ **재사용 절대 금지** — GCM 은 같은 키로 nonce 를 두 번 쓰면 **평문이 복원되고 위조까지 가능해진다.** 카운터·시각·해시에서 유도하지 않는다. 오직 CSPRNG |
| **S4** | 버전 | 접두사 `v1.`. 형식을 바꿀 때 **옛 값을 조용히 받지 않기 위해서**다. 모르는 버전은 거부 |
| **S5** | **결속 — AAD** | GCM 의 **추가 인증 데이터(AAD)** 에 `purpose="signup"` · `provider` · `audience=APP_ORIGIN` · `exp` 를 넣는다. 암호문은 그대로여도 **다른 제공자·다른 목적·다른 오리진에서는 복호화가 실패**한다 |
| **S6** | 만료 | **10분**(현행과 같음). 평문 안에도 `exp` 를 두고 AAD 에도 둔다 — 안쪽이 진실, AAD 는 조기 거부용 |
| **S7** | **브라우저 결속** | `shh_t` 표는 **그대로 유지**한다(`bound()`). 암호화는 열람을 막을 뿐 **누가 들고 왔는지는 말하지 않는다.** 두 장치는 서로를 대체하지 않는다 |
| **S8** | 소비 표식 | §5-3 의 `consumed_signup_states`. **암호문 전체**를 `TOMBSTONE_KEY` 로 HMAC 한 값을 쓴다 |
| **S9** | **키 회전** | `SIGNUP_STATE_KEY_v<n>`. 버전을 접두사에 싣는다(`v1`→`v2`). **겹치는 기간은 10분이면 충분하다** — 그보다 오래 두 키를 살려 둘 이유가 없다 |
| **S10** | **복호화 실패** | **fail-closed.** 가입을 진행하지 않고 400 을 낸다. 실패 사유(만료·위조·버전 불일치)를 **응답으로 구분해 주지 않는다** — 구분해 주면 그 자체가 오라클이 된다 |
| **S11** | 키 없음 | `SIGNUP_STATE_KEY` 가 없으면 **가입 경로를 열지 않는다**(503). `/api/ready` 가 먼저 말한다 |
| **S12** | 최대 길이 | 암호문 포함 **2048자**. 넘으면 만들지도 받지도 않는다. URL 길이 한계와 로그 폭주를 함께 막는다 |
| **S13** | **로그 금지** | 평문 · 암호문 · nonce · `state_hash` **전부** 로그 금지(§12 목록에 추가). 오류 응답 본문에도 싣지 않는다 |
| **S14** | 평문은 서버 밖으로 안 나간다 | 복호화 결과를 응답·리다이렉트·해시 프래그먼트 어디에도 싣지 않는다 |

#### 5-4-4. 이 설계가 **하지 못하는 것** (단정하지 않는다)

| 사실 | 내용 |
|---|---|
| 제공자는 **암호문을 본다** | 값 자체·길이·시각이 제공자 로그에 남는다. 우리가 지울 수 없다 |
| 제공자는 **가입이 일어났다는 사실**을 안다 | OAuth 의 성질이다. 암호화로 바뀌지 않는다 |
| 암호화가 **법적 판단을 바꾸는지** | **모른다.** 법률 자료 **L15** 의 질문이다. 「암호화했으니 국외 이전이 아니다」라고 **이 문서는 주장하지 않는다** |
| 더 강한 대안 | 통과 자체를 없애는 것 — 로그인을 먼저 끝내고 **그 뒤 별도 화면**에서 수락을 받는다. C안 확정 흐름이 깨지므로 지금 고르지 않는다. **L15 의 답이 요구하면 그때 바꾼다**(구현이 없어 아직 값싸다) |

### 5-2. `internalUid()` 분해

지금 한 함수가 **조회와 생성**을 같이 한다(`:511-526`). 다음 여섯 역할로 나눈다.
계층을 새로 만들지 않는다 — 전부 `worker/index.js` 안의 함수다.

| 역할 | 새 이름(안) | 하는 일 | 부작용 |
|---|---|---|---|
| 제공자 확인 | `verifyProvider(env, name, code, state)` | 토큰 교환 → `/me` → `{name, subject}` | **로컬 DB 쓰기 없음. 다만 외부 OAuth 네트워크 호출 2회가 나가고 `code` 를 소비한다** — 되돌릴 수 없고 재시도하면 제공자가 거부한다 |
| 기존 사용자 조회 | `findUser(env, name, subject)` | `SELECT id` | 없음(읽기) |
| 계정+정책 생성 | `createAccountWithPolicy(env, name, subject, bundle)` | 위 batch → 재조회 → `id` | `users`·`policy_events` |
| 세션 발급 | `newSession(env, uid)` | 지금 그대로 | `sessions` |
| 정책 번들 조회 | `policyBundle(env)` | 서버 상수 반환 | 없음 |
| 삭제 표식 | §10·§11 | | 별도 저장소 |

`internalUid()` 는 **삭제한다.** 남겨 두면 "조회와 생성을 같이 하는 함수"가 계속 존재해서
누군가 다시 부른다. `scripts/test-friends.mjs` 가 이 함수를 직접 쓰므로(`signUp` 헬퍼)
테스트도 `createAccountWithPolicy` 로 옮겨야 한다 — §13 에 적었다.

---

## 6. 가입 대기 상태 — B안을 고를 경우에만

C안을 고르면 이 절 전체가 필요 없다. B안 검토용으로 남긴다.

### 방식 1 — 인증·암호화된 HttpOnly 쿠키

| 항목 | 요구 |
|---|---|
| 암호화 | **서명만으로는 안 된다.** 서명 쿠키는 내용이 평문이라 `provider_subject` 가 브라우저·프록시·로그에 그대로 보인다. AES-GCM 등 **인증된 암호화** 필요 |
| 키 | 전용 `SIGNUP_KEY`. `STATE_KEY`·`RL_KEY`·OAuth secret **재사용 금지** |
| 만료 | 10분 이하. 평문에 만료 시각을 넣고 **복호화 후 다시 검사** |
| nonce | 요청마다 무작위 12바이트. 재사용 금지 |
| 속성 | `HttpOnly; Secure; SameSite=Lax; Path=/api/signup` (지금보다 좁게) |
| 결속 | `shh_t` 와 묶는다 — 쿠키 안에 `sha256(shh_t)` 를 넣어 다른 브라우저로 옮기면 못 쓰게 |
| 1회용 | **쿠키 삭제만으로는 보장되지 않는다.** 삭제 지시는 브라우저에게 하는 부탁이고, 복사해 둔 값은 만료 전까지 다시 쓸 수 있다. 진짜 1회용을 원하면 서버 상태가 필요하다 → 방식 2 |

### 방식 2 — 서버측 `pending_signups`

| 항목 | 요구 |
|---|---|
| 토큰 | 원본 저장 금지. `SHA-256(token)` 만 (세션과 같은 규칙) |
| `provider_subject` | 평문 저장 금지 — 암호화하거나 HMAC. 그러면 조회 방식이 달라진다 |
| 만료·1회용 | `expires_at` + 소비 시 `DELETE … WHERE` 한 문장(읽고 나서 지우면 두 번 소비된다) |
| 청소 | 크론이 없다 → `newSession()` 이 하는 것처럼 다음 로그인에 붙인다 |
| 결속 | 계정 생성과 소비를 **같은 batch** 안에 |
| **Time Travel** | 복원하면 **소비된 pending 이 되살아난다.** 만료가 지났으면 무해하지만, 만료 전 복원이면 재사용 창이 열린다 |
| 개인정보 | 테이블 하나가 통째로 늘고, 그 안이 제공자 회원번호다 |

### 방식 3 — 임시 상태 자체 제거 (A/C안)

가능하다. §4·§5 가 그 설계다. **권고는 방식 3** 이다 — 가장 적은 임시 개인정보, 가장 적은 상태.
대가는 §4 에 적은 "신규 사용자가 로그인 버튼을 먼저 누르면 왕복 한 번 헛돔"뿐이다.

> **확정** — §17 결정 2(임시 상태 제거). 아래 방식 1·2 는 **구현하지 않는다**. 참고 기록이다.

---

## 7. 정책 문서와 `document_version`

### 7-1. 구조

```
policies/
  manifest.json                 ← 종류 · 파일 경로 · SHA-256 · 번들 정의
  terms-<hash12>.html           ← 이용약관 전문 (불변)
  privacy-<hash12>.html         ← 개인정보처리방침 전문 사본 (불변)
  age14-<hash12>.txt            ← 연령 진술 문구 (불변)
  summary-<hash12>.txt          ← 가입 화면 요약 (불변)
```

- **기존 버전 파일을 절대 덮어쓰지 않는다.** 고칠 때는 새 파일 + manifest 엔트리 추가.
- manifest 엔트리는 **불변**이다. 추가만 허용하고 수정·삭제는 테스트가 실패시킨다.
- `pv`(번들 id) = `SHA-256(정렬된 "kind:hash" 줄들)` 의 앞 12자.
- 서버는 현재 번들을 상수로 들고 있고, **manifest 에 등록된 해시만** 기록한다.
- `document_version` = 그 kind 의 **불변 파일 내용 해시**. `recorded_at` = 서버의 `Date.now()`.

### 7-2. 8개 질문에 대한 답

| # | 질문 | 답 |
|---|---|---|
| 1 | 전문과 요약이 다르면 무엇의 해시를 기록하나 | **전문의 해시.** 요약은 같은 번들에 묶인 별도 불변 파일이고, manifest 엔트리가 불변이라 `pv` 로 "그때 무슨 요약을 봤는지"가 재현된다. 요약만 고쳐도 새 파일·새 엔트리·새 `pv` 가 된다 |
| 2 | 전문을 안 열고 체크만 해도 수락으로 기록할 수 있나 | **법률 검토 필요**(L8 — `docs/PRIVACY_LEGAL_REVIEW_PACKET.md`). 설계는 두 경우를 다 지원한다 — 강제 열람이 필요하면 [전문] 클릭을 체크박스 활성 조건으로 두면 된다. Claude 가 판정하지 않는다 |
| 3 | 연령 진술 문구도 불변 파일인가 | **그렇다.** 화면에 뜨는 그 한 문장이 곧 진술 대상이라, 문장이 바뀌면 진술의 뜻이 바뀐다 |
| 4 | manifest 를 클라이언트가 위조할 수 없나 | **위조는 가능하지만 소용이 없다.** 기록되는 값은 언제나 서버 상수이고, 클라이언트가 보내는 `pv` 는 **일치할 때만 통과하는 문지기**로만 쓰인다. 클라이언트가 `document_version` 이나 `recorded_at` 을 지정하는 자리는 없다(2단계 §5 요구 5·6 유지) |
| 5 | 서버 코드와 정적 파일이 다른 배포 세대가 되면 | **가능성이 낮을 뿐 "될 수 없다"고 말하지 않는다**(2판 정정). 한 Pages 프로젝트라 보통 같은 배포 단위로 나가지만, 프로덕션 별칭 전파 지연이 실측됐고(`HANDOFF.md` §4-6) 옛 엣지 캐시가 며칠 살아남은 사례도 있다(§4-5). 그래서 **배포 구조에 기대지 않는다**: 배포 전에는 `test-policies` 가 서버 상수 == manifest == 파일 해시를 대조하고, **런타임에는 Q6 의 두 겹이 실제 바이트를 대조한다** |
| 6 | 서비스워커가 옛 정책을 캐시한 상태로 가입할 수 있나 | **두 겹으로 막는다.** ① 클라이언트가 자기가 렌더할 파일을 fetch 해 **직접 해시**하고 `GET /api/policies` 값과 다르면 가입 버튼을 안 그린다(fail-closed) ② 그래도 뚫리면 서버가 `pv` 불일치로 409. 화면이 옛 문서를 보여주고 서버가 새 해시를 기록하는 상태는 **어느 한쪽만 뚫려도 생기지 않는다** |
| 7 | 옛 버전 파일을 빌드 allowlist 에 어떻게 넣나 | `scripts/build.mjs` 의 `INCLUDE` 에 `"policies"` 폴더를 통째로 넣는다. 폴더 통째로는 원래 위험하지만 이 폴더는 **공개 목적의 불변 문서 전용**이다. 대신 `test-dist` 가 `policies/` 안에 `.html/.txt/.json` 외 확장자가 있으면 실패시킨다. 그리고 SW `ASSETS` 에는 **현재 번들 파일만** 넣는다(옛 버전까지 선캐시하면 캐시가 계속 자란다) |
| 8 | 문서가 바뀌었는데 서버 상수가 그대로면 어떤 테스트가 실패하나 | `scripts/test-policies.mjs` 의 "서버 상수 == manifest == 파일 해시" 대조. 하나라도 어긋나면 실패. 추가로 `test-dist` 가 SW 선캐시 목록과 현재 번들 파일이 같은지 본다 |

> **manifest 테스트가 증명하지 못하는 것**(2판 추가). 이 테스트는 **지금 저장소 안에서**
> 내용·파일명·manifest 항목이 서로 맞는지를 잰다. 그러나 **과거 항목을 파일과 manifest 에서
> 함께 지우는 의도적 삭제는 독립적으로 증명하지 못한다** — 둘 다 사라지면 남은 것끼리는
> 여전히 일관되기 때문이다. 그 삭제를 잡는 것은 테스트가 아니라 **Git 이력과 코드 리뷰**이고,
> 저장소 이력을 통제하는 사람에게는 그것도 방어가 아니다. 「불변」은 **자동으로 강제되는
> 성질이 아니라 운영 규칙**이며, 테스트는 실수를 잡을 뿐 고의를 막지 못한다.

### 7-3. `privacy.html` 과 `policies/privacy-<hash>.html` 의 관계

지금 `privacy.html` 은 내용이 바뀌는 **살아 있는 파일**이라 그대로는 `document_version` 의 대상이
될 수 없다. 규칙:

- 방침을 고칠 때마다 **고친 결과를 `policies/privacy-<hash12>.html` 로 복사**하고 manifest 에 추가한다.
- `privacy.html` 은 항상 **가장 최근 사본과 내용이 같아야 한다** — `test-policies` 가 대조한다.
- 두 파일 관리가 번거롭다는 것은 사실이다. 대안(하나로 두고 Git 이력에 기대기)은 **사용자가
  실제로 본 바이트를 재현할 수 없다** — Git 이 없는 사람에게 보여줄 수 없고, 우리 기록의
  검증 가능성이 저장소 이력에 묶인다. 두 벌을 유지하되 **테스트가 어긋남을 막는 쪽**을 택한다.

### 7-4. 정책 행위의 **시각** — 두 개를 구분한다 (2판 추가)

1판은 시각을 `recorded_at` 하나로 뭉갰다. 그런데 C안에서 두 시점은 **OAuth 왕복만큼 벌어진다**:

```
사용자가 체크하고 [가입하기] 를 누른 순간        ← 행위가 실제로 일어난 시각
  ↓ POST /signup/start
  ↓ 제공자 동의 화면 (사용자가 얼마나 머물지 우리가 모른다. state 만료 10분)
  ↓ /cb 또는 /exchange
계정과 정책 이벤트가 DB 에 들어간 순간            ← 기록 시각
```

정상 흐름에서도 **수 초~수 분**, 사용자가 제공자 화면에서 머뭇거리면 **최대 10분**이다.
그리고 **가입에 실패하면 기록 자체가 없다** — 즉 "체크했지만 기록되지 않은" 행위가 존재한다.

| 방식 | 무엇 | 위조 가능성 | 대가 |
|---|---|---|---|
| **A. `recorded_at` 하나** (1판) | DB 삽입 시각만 | 없음(서버 시계) | 사용자가 실제로 동의한 시각을 **모른다.** 최대 10분 늦은 시각이 기록된다 |
| **B. `occurred_at` + `recorded_at`** (권고) | `occurred_at` = `/signup/start` 가 요청을 받은 **서버 시각**을 **서명된 `state` 에 담아** 왕복시킨다. `recorded_at` = DB 삽입 시각 | **없음** — 둘 다 서버가 만들고, `occurred_at` 은 우리 HMAC 서명 안에 있어 클라이언트가 못 고친다 | `policy_events` 컬럼 하나 · `state` 에 정수 하나(약 13자) |

**권고 — B.** 이유:

- `occurred_at` 은 **클라이언트가 보낸 시각이 아니다.** 서버가 `/signup/start` 에서 `Date.now()`
  로 찍어 자기 서명 안에 넣고, 콜백에서 서명을 검증한 뒤 꺼낸다. 2단계 §5 요구 6
  (「`recorded_at` 은 서버 시각」)을 깨지 않는다 — **두 값 모두 서버 시각**이다.
- 두 값의 간격 자체가 진단 정보다. 10분을 넘는 값이 들어오면 state 만료 검사가 이미 막았어야
  하므로 **버그 신호**다.
- 비용이 컬럼 하나다. 나중에 붙이려면 `policy_events` 재생성 migration 이 필요하다
  (SQLite 의 `CHECK`·`NOT NULL` 제약 때문에 — 2단계 §4 와 같은 이유).

> **어느 시각을 법적 증거로 보아야 하는지는 Claude 가 판정하지 않는다.**
> 「동의·수락·진술이 있었던 시점」이 체크한 때인지 기록된 때인지, 가입이 중간에 실패해
> **기록이 남지 않은 체크**를 어떻게 보아야 하는지는 **L11** 로 법률 검토에 넘겼다
> (`docs/PRIVACY_LEGAL_REVIEW_PACKET.md`). 설계는 **두 값을 모두 남겨** 어느 쪽이 답으로
> 나오든 대응할 수 있게 한다 — 하나만 남기면 나중에 복원할 수 없다.

---

## 8. `policy_events` 법률 분기

**아직 migration 을 만들지 않았다**(결정 4에서 **생성 금지**로 확정됐다).
두 분기를 나란히 둔다. 두 분기 모두 `occurred_at`·`recorded_at` 두 컬럼을 갖는다(§7-4).

### 8-0. `requiredPolicyKinds` — **개수는 법률 결정이 정한다** (3판)

2판까지 문서 곳곳에 **`policy_events` 3행**이라 적혀 있었다. **그 3은 아직 확정되지 않은 값이다** —
국외 이전이 별도 동의 대상(제28조의8 제1항 제1호)이면 `xborder` 가 늘어 **4가 된다.** 고정 숫자를
문서에 박아 두면, 법률 결과가 나온 날 **테스트가 통과하면서 이벤트 하나를 빠뜨린다.**

```
requiredPolicyKinds   = 최종 법률 결정으로 확정되는 (kind, action) 집합
REQUIRED_POLICY_EVENTS = requiredPolicyKinds.length      ← 서버 상수
N                      = REQUIRED_POLICY_EVENTS          ← 이 문서에서 쓰는 표기
```

⚠️ **아래 표는 결정 전의 분기 비교다.** 2026-08-17 시점에는 어느 쪽인지 정해지지 않았다.
**2026-08-18 에 분기 ①(계약 이행)로 확정됐고**(§17 결정 3), ②·③ 행의 조합은 **지금 구현되어 있지 않다** —
스키마 `CHECK` 가 그 조합을 거부한다(`scripts/test-signup.mjs` T21 이 잰다). 기록으로 남긴다.

| 분기 | `requiredPolicyKinds` | N | 지금 |
|---|---|---|---|
| ① 계약 이행 | `terms/accepted` · `privacy/presented` · `age14/attested` | 3 | ✅ **채택** |
| ② 동의 | `terms/accepted` · `privacy/accepted` · `age14/attested` | 3 | ⚠️ 채택하지 않는다 |
| ② + 국외 이전 별도 동의 | 위 + `xborder/accepted` | **4** | ⚠️ 채택하지 않는다 |

**규칙**:

- 상태 머신 · 위협 모델 · 동시 가입 테스트 · state 재사용 테스트 · 기존 사용자 전환 참고 설계는
  **전부 이 정의 하나를 쓴다.** 어느 한 곳에 3을 적어 두지 않는다.
- 서버는 `requiredPolicyKinds` 를 **집합으로** 들고 있고, batch 는 그 집합을 순회해 INSERT 한다.
  숫자를 하드코딩하지 않는다.
- 테스트는 개수만 세지 않는다 — **최종 집합의 각 원소가 정확히 한 번씩** 기록되고
  **누락·중복·집합 밖 이벤트가 0** 임을 확인한다(§13).

### 분기 ①: 계약 이행(제15조 제1항 제4호)이 인정되는 경우

```sql
CHECK (
  (kind = 'terms'   AND action = 'accepted') OR
  (kind = 'privacy' AND action = 'presented') OR
  (kind = 'age14'   AND action = 'attested')
)
```

| 항목 | 내용 |
|---|---|
| UI 체크박스 | 2개 — 「(필수) 이용약관에 동의합니다」·「(필수) 만 14세 이상입니다」 |
| 방침 | 체크박스 없이 **요약 + 전문 링크**를 화면에 표시 |
| 거부 시 | 가입 불가(필수 두 개). 방침은 거부 대상이 아니다 |
| 철회 | 개인정보 동의가 아니므로 "동의 철회"라는 행위가 없다. 처리 중단은 **계정 삭제**다 |
| 계정 삭제 시 | 확인 필요(L5) |
| 보존기간 | 확인 필요(L5) |
| 기존 사용자 재확인 | 약관 개정 시에만 |
| 방침 문구 변경점 | `privacy.html:126-127` 의 "동의" 표현을 **처리 근거 = 계약의 이행**으로 고쳐야 한다 |

### 분기 ②: 개인정보 동의(제15조 제1항 제1호)가 필요한 경우

```sql
CHECK (
  (kind = 'terms'   AND action = 'accepted') OR
  (kind = 'privacy' AND action = 'accepted') OR
  (kind = 'age14'   AND action = 'attested') OR
  (kind = 'xborder' AND action = 'accepted')     -- 제28조의8 제1항 제1호일 때만
)
```

| 항목 | 내용 |
|---|---|
| UI 체크박스 | 3개 또는 4개. 「전체 동의」 단일 체크박스는 **두지 않는다**(제22조 제1항 구분 동의) |
| 거부 시 | 필수 항목 거부 → 가입 불가. **선택 항목 거부 → 서비스 거부 금지**(제22조 제5항) |
| 철회 | 철회 경로가 필요하다 → `withdrawn` action 을 `CHECK` 에 넣을지 확인 필요 |
| 계정 삭제 시 | 확인 필요(L5) |
| 보존기간 | 확인 필요(L5) |
| 기존 사용자 재확인 | 근거가 바뀌면 전원 재확인이 필요할 수 있다 — 확인 필요 |
| 방침 문구 변경점 | 동의 항목 구분·거부 시 불이익·철회 방법을 방침에 적어야 한다 |

### 두 분기 공통 — 반드시 알아야 할 기술적 사실

> **정책 이벤트를 계정 삭제 후에도 보존해야 한다는 결과가 나오면, `policy_events` 는
> `users` 를 외래키로 참조할 수 없다.**
>
> `scripts/test-migrations.mjs:96-103` 은 `users` 를 참조하는 **모든** 외래키가
> `ON DELETE CASCADE` 인지 전수 검사한다. 계정 삭제가 `DELETE FROM users` 한 문장인 것이
> 그 검사의 존재 이유다(2026-08-16 에 닫은 P0). 보존이 필요하면 선택지는 둘뿐이다:
>
> - (a) `policy_events` 에서 `users` 참조를 떼고 **가명 키**(§10 의 표식과 같은 성격)를 쓴다
> - (b) 정책 이벤트를 주 D1 밖(삭제 표식 저장소)으로 옮긴다
>
> 어느 쪽이든 **스키마가 통째로 달라진다.** 그래서 L5 의 답이 나오기 전에 migration 을 만들면
> 버리는 일이 된다. 2단계 §4 의 판단이 여기서 다시 확인된다.

---

## 9. 삭제 표식 저장소 비교

legacy KV 는 후보가 아니다(2단계 §11 에서 폐기하기로 한 자원이다).

| 기준 | 별도 D1 | 새 전용 KV | R2 최소 로그 | Durable Object | 저장소 없음(복원 금지) |
|---|---|---|---|---|---|
| 읽기 일관성 | **강함** | **읽기 전파가 eventual consistency** — `put` 이 성공해도 다른 위치의 읽기·`list()` 는 한동안 옛 값을 줄 수 있다 | 강함(read-after-write) | 강함 | — |
| 쓰기 결과 | SQL 결과(`changes`)로 즉시 확인 | `put` 자체의 성공·실패는 알 수 있다. **읽어서 확인하는 것이 늦다** | 확인 가능 | 확인 가능 | — |
| 목록·조회·정리 | **SQL 한 줄**(`WHERE`·`JOIN`·집계) | `list()` 훑기 + 키 설계로만 필터 | `list()` + 객체 읽기 | 코드로 직접 | — |
| 비용 | [?] 플랜별 DB 개수·쓰기 한도 **미확인** | [?] 미확인 | [?] 미확인 | [?] 미확인 | 0 |
| 운영 복잡도 | 낮음(`wrangler d1 execute`) | 중간 | 중간 | 높음 | **가장 낮음** |
| 주 D1 복원과 독립 | ○ | ○ | ○ | ○ | — |
| **표식 저장소 자체의 복원 위험** | **있다. 2판에서 심각도를 올렸다** — D1 이므로 이 DB 에도 Time Travel 이 켜져 있고, 과거로 되돌리면 **그 시점 이후의 표식이 통째로 사라진다**(확정된 삭제가 증발). §10-6 이 그래서 있다 | 없음(시점 복원 기능이 없다) | 없음 | — | — |
| 보유기간 삭제 | `DELETE … WHERE expires_at < ?` | TTL 로 자동 | 수동 | 코드 | — |
| 개인정보 최소화 | HMAC 값 하나 | 같음 | 같음 | 같음 | 최소 |
| 재식별 위험 | **있다**(복원된 DB 와 대조하면 특정 가능 — §10-8) | 같음 | 같음 | 같음 | — |
| 복원 시 전체 대조 비용 | `users` 를 페이지로 읽어 HMAC — 사용자 수에 비례 | 같음 | 같음 | 같음 | — |
| 현재 규모(0명)에 적합 | ○ | △ | △ | ✗ | ○ |

### 권고 — **별도 D1** (조건부. §10-6 의 restore 금지 규정과 **함께**)

**별도 D1 의 장점은 셋이다** (2판에서 정확하게 다시 적었다):

1. **강한 읽기 일관성** — 방금 적은 표식을 그 자리에서 다시 읽어 확인할 수 있다.
   saga 3단계(부재 확인)와 reconciliation 대조가 그 성질에 기대고 있다.
2. **SQL 조회** — `confirmed_at IS NULL`, `expires_at < ?`, 개수 세기, 집합 대조가 전부 한 줄이다.
   KV·R2 는 키를 훑어 코드로 걸러야 한다.
3. **운영 편의** — `wrangler d1 execute` 로 운영자가 직접 보고 고칠 수 있다. 복원 절차(§10-8)가
   사람 손으로 도는 절차라 이 점이 실질적이다.

**KV 를 고르지 않는 이유는 "쓰기 성공을 확인할 수 없어서"가 아니다**(1판의 과장 — 삭제했다).
`put` 의 성공·실패 자체는 알 수 있다. 정확한 한계는 **읽기 전파가 eventual consistency** 라는
것이다 — 방금 적은 표식을 다른 위치에서 읽거나 `list()` 로 훑으면 한동안 옛 상태가 보일 수 있고,
saga 3단계와 reconciliation 은 **적은 직후에 다시 읽어 판정**하므로 그 지연이 곧 오판이 된다.

- **[?] 비용과 플랜 조건은 확인하지 않았다.** 저장소를 실제로 만들기 **전에** Cloudflare 대시보드에서
  현재 플랜의 D1 개수 한도·쓰기 한도, Durable Objects 사용 조건을 확인한다. D1 플랜(Time Travel
  7일/30일)이 아직 미확인인 것과 같은 항목이라 **한 번에 같이 확인한다**(2단계 §12).
- "저장소 없음(복원 자체를 금지)"은 사용자 수가 적은 지금 **실효가 있다.** 다만 복원이 필요한 사고
  (데이터 손상)가 실제로 나면 **복원 없이는 못 고치고**, 그때 탈퇴자가 부활한다. 그래서 권고는
  둘을 겹치는 것이다 — 표식을 두되, 표식이 있어도 **복원은 여전히 승인 사항**으로 남긴다.

### 별도 D1 을 골랐을 때 반드시 다룰 함정

**주 D1 과 별도 D1 사이에는 공통 트랜잭션이 없다.** 그래서 계정 삭제는 원자적 트랜잭션이 아니라
**saga** 다. 아래 §10 이 그 전부다.

---

## 10. 삭제 saga 와 복원 절차

### 10-1. 표식 스키마(안)

```sql
CREATE TABLE deletions (
  mark            TEXT PRIMARY KEY,   -- HMAC(DELETION_KEY_v<n>, internal_uid)
  key_version     INTEGER NOT NULL,
  pending_at      INTEGER NOT NULL,
  confirmed_at    INTEGER,            -- NULL 이면 아직 삭제가 확정되지 않았다
  -- pending 이 이 시각을 넘도록 확정되지 않으면 **경보 대상**이다.
  -- ⚠️ 이 값이 지났다고 행을 지우지 않는다 — 지우면 "삭제는 됐는데 확정 기록만 실패한"
  --    표식이 사라져 복원 때 그 계정이 되살아난다. 정리는 reconciliation 판정 뒤에만.
  pending_alert_at INTEGER NOT NULL,  -- pending_at + PENDING_ALERT (24시간)
  -- 이 표식이 쓸모를 잃는 시각. **confirmed 가 되는 순간 다시 계산해 늘린다**(§10-5).
  expires_at      INTEGER NOT NULL
);
CREATE INDEX deletions_expires ON deletions(expires_at);
CREATE INDEX deletions_open    ON deletions(confirmed_at, pending_alert_at);
```

원본 제공자 ID·이메일·별명·단어장·세션을 **저장하지 않는다.** 저장하는 것은 HMAC 값 하나다.

> **`attempts` 컬럼은 삭제했다**(2판). 1판에 `attempts INTEGER NOT NULL DEFAULT 0` 이 있었는데
> **어디서도 갱신하지 않았다** — 값이 영원히 0인 컬럼이다. 재시도 횟수가 필요해지면
> 그때 갱신 자리와 함께 넣는다. 갱신되지 않는 컬럼은 나중에 읽는 사람에게 **거짓말을 한다**
> ("0회 재시도했다"로 읽힌다).

| 규칙 | 내용 |
|---|---|
| 키 | 전용 `DELETION_KEY`. `RL_KEY`·`STATE_KEY`·OAuth secret **재사용 금지** |
| 키 버전 | `key_version` 컬럼. 회전하면 새 표식만 새 키로 만든다 |
| 회전 중 조회 | **보유기간 동안 옛 키를 지우지 않는다.** 대조할 때 `key_version` 에 맞는 키를 쓴다 |
| 만료 | pending 과 confirmed 를 **다르게** 다룬다 — §10-5 |
| **복원** | **이 DB 는 과거로 복원하지 않는다** — §10-6 |
| 백업 | 표식 저장소보다 **오래된 주 D1 백업을 만들지 않는다**(2단계 §10) |

### 10-2. saga 순서

```
0. 유지보수 검사     MAINTENANCE=1 이면 여기서 503 (§10-7). 아래로 내려가지 않는다.
1. pending 기록      INSERT INTO deletions (mark, key_version, pending_at,
                                            pending_alert_at, expires_at)
                     VALUES (…, now, now + 24시간, now + 37일)
                     ON CONFLICT (mark) DO NOTHING          ← 중복 삭제 요청에 안전
2. 주 D1 삭제        DELETE FROM users WHERE id = ?          (한 문장 + CASCADE)
3. 부재 확인         SELECT 1 FROM users WHERE id = ?        → 없어야 한다
4. confirmed 기록    UPDATE deletions SET confirmed_at = ?1,
                            expires_at = ?1 + 37일          ← 확정 시점 기준으로 재계산(§10-5)
                      WHERE mark = ?
5. 만료 정리(부수)   DELETE FROM deletions
                      WHERE confirmed_at IS NOT NULL AND expires_at < now
                     ⚠️ try/catch 로 삼킨다 — 정리 실패가 삭제를 실패시키면 안 된다
```

1번의 `expires_at` 은 **임시값**이다. 4번이 성공하면 확정 시점 기준으로 다시 계산되고,
4번이 실패하면 reconciliation 이 승격하면서 계산한다(§10-4).

### 10-3. 실패 매트릭스

`—` 는 앞 단계가 실패해 그 단계에 도달하지 않았다는 뜻이다.

| pending 기록 | 주 D1 삭제 | 부재 확인 | confirmed 기록 | 사용자 응답 | 다음 조치 |
|---|---|---|---|---|---|
| **(유지보수 모드)** — | — | — | — | **503 "잠시 점검 중이에요"** | 아무것도 안 쓴다. 점검이 끝나면 재시도 (§10-7) |
| 실패 | — | — | — | **"지우지 못했어요"** (세션 유지) | 그 화면에서 재시도. 주 D1 은 손대지 않았다 |
| 성공 | 실패 | — | — | **"지우지 못했어요"** (세션 유지) | 재시도. pending 은 남되 **복원 시 삭제에 쓰지 않는다**(confirmed 만 쓴다) |
| 성공 | 성공(0행) | 부재 | 성공 | **"계정을 지웠어요"** | 다른 탭이 먼저 지웠다. 사용자가 원한 결과는 이뤄졌다 |
| 성공 | 성공 | **여전히 존재** | — | **"지우지 못했어요"** | 있을 수 없는 상태. 재시도 + 조사 대상 |
| 성공 | 성공 | 부재 | 실패 | **"계정을 지웠어요"** (계정은 실제로 없다) | reconciliation 이 pending → confirmed 로 승격 |
| 성공 | 성공 | 부재 | 성공 | **"계정을 지웠어요"** | 끝 |
| 성공 | 응답 전 Worker 중단 | — | — | 응답 없음 | 사용자는 다시 누른다. pending 은 `PRIMARY KEY` 라 중복이 안 생긴다. reconciliation 이 최종 상태를 정한다 |
| 성공(이미 있음) | — | — | — | 중복 삭제 요청 | `INSERT … ON CONFLICT DO NOTHING` → 같은 saga 를 이어서 진행 |
| — | — | — | — | 재가입자 | **새 UID → 새 HMAC.** 옛 표식과 절대 안 부딪힌다(§2 I9) |

### 10-4. reconciliation — **유지보수 모드 안에서만 실행한다**

**무엇을 하나**: `confirmed_at IS NULL` 인 표식마다, 주 D1 에 대응 계정이 아직 있는지 본다.

`mark` 는 HMAC 이라 역산할 수 없다. 그래서 반대로 간다 — **주 D1 의 `users.id` 를 페이지 단위로
읽어 각각 HMAC 하고**, 그 집합과 pending 표식을 대조한다.

#### 3판 재설계 — **reconciliation 은 승격만 한다(promote-only)**

2판은 "살아 있는 계정과 일치하는 pending 은 **삭제**한다"고 적었다. **그 삭제가 유일한
위험원이었다.** 3판에서 자동 삭제를 **없앤다.**

| pending 표식이 | 뜻 | **3판 조치** | 2판 조치(폐기) |
|---|---|---|---|
| 살아 있는 계정과 **일치** | 2단계(주 D1 삭제)가 실패했다 | **아무것도 하지 않는다.** 표식을 그대로 둔다 | ~~표식을 삭제~~ |
| 어느 계정과도 **불일치** | 삭제는 됐고 4단계만 실패했다 | `confirmed_at` 기록(**승격**) + `expires_at` 재계산(§10-5) | 같음 |

**왜 그대로 둬도 되나**: `pending` 은 **어떤 경우에도 복원 후 재삭제에 쓰이지 않는다**
(2단계 §9-7). 그러므로 살아 있는 계정에 붙은 pending 표식은 **아무 힘이 없다.** 남겨 두는
비용은 행 하나이고, 지우는 비용은 **표식 없는 삭제**다. 값이 비교가 안 된다.

**승격은 동시성에 안전하다** — 승격 조건이 "그 UID 의 계정이 **없다**"인데, 계정이 없다가
다시 생기는 유일한 길은 **재가입이고 재가입은 새 UUID** 다(I9). 따라서 스캔 도중에 무슨 일이
일어나도 **승격 판정이 뒤집히지 않는다.** 반대로 2판의 삭제 판정("있다")은 그 직후에
뒤집힐 수 있었고, 그것이 아래 경합의 전부다.

> **그래도 reconciliation 은 유지보수 모드에서만 돈다**(사용자 결정 6). promote-only 는
> **판정 자체를 안전하게** 만든 것이고, 유지보수 요구는 그 위에 얹는 두 번째 자물쇠다 —
> 복원 직전 실행이라 그 시점에는 어차피 쓰기가 멈춰 있어야 한다. **둘 중 하나만 믿지 않는다.**

**pending 표식을 지우는 자동 경로는 없다.** 지우려면 운영자의 수동 판정이 필요하고,
그 판정은 **활성 deletion lease 0건**(§10-9 Q5·Q7)이 확인된 상태에서만 한다. 그 조건은
「삭제 saga 가 돌고 있지 않다」는 뜻이며, **전역 쓰기가 멈췄다는 뜻이 아니다**(§10-9-6).
pending 판정에는 그것으로 충분하다 — `users` 행을 지우는 코드 경로는 삭제 saga **하나뿐**이기
때문이다(`worker/index.js:948`). 반대로 **주 D1 복원은 이 조건으로 부족하다**(§10-8-0).

#### ⚠️ 경합 — 2판에서 찾은 결함 (promote-only 로 닫혔다)

1판은 reconciliation 을 "복원 직전 + 수동"으로만 적고 **동시에 서비스가 돌고 있는 경우를
보지 않았다.** 실제로는 이 순서가 성립한다:

```
t0  reconciliation: users 를 훑는다 → 계정 X 가 살아 있다
t1  사용자 X 가 [계정 삭제] 를 누른다 → pending 은 이미 있다 → 주 D1 삭제 성공
t2  reconciliation: "X 는 살아 있었으니 pending 은 실패한 삭제다" → **표식을 지운다**
────
결과: 계정은 삭제됐는데 표식이 없다.  복원하면 그 사람이 되살아나고,
      아무도 그것을 알아챌 수 없다(표식이 없으므로 대조에 안 걸린다).
```

**이것이 「표식 없는 삭제」다.** 삭제 saga 전체가 막으려던 바로 그 상태를, reconciliation 이
만들어낸다. `t0`~`t2` 사이에 삭제가 하나만 성공하면 성립하므로 **확률이 낮은 사고가 아니다** —
reconciliation 은 사용자 전체를 훑느라 오래 걸린다.

**3판에서 이 경합은 `t2` 가 사라져 닫혔다** — promote-only 는 지울 것이 없다.
아래는 그래도 유지되는 운영 규정이다.

**규정** (사용자 결정 6에서 확정 · 3판에서 증거 요구 추가 · **4판에서 범위 확정**):

> **reconciliation 은 유지보수 모드(`maintenance` = `closed`, 현재 epoch)에서 신규 삭제 saga 의
> 진입이 막히고 활성 deletion lease 0건이 확인된 뒤에만 실행한다.** 서비스가 평시로 도는
> 동안에는 어떤 이유로도 실행하지 않는다.

##### 4판 — **필요한 quiescence 의 범위** (3판의 문서 내 충돌을 여기서 끝낸다)

3판은 이 자리에 "가입·삭제·세션 생성을 포함한 **모든 쓰기**가 중단되고"라고 적었고,
§10-9 Q7 은 "**활성 deletion lease 0건**이면 실행"이라고 적었다. **두 문장이 충돌했다.**

**promote-only reconciliation 이 실제로 판정하는 것은 둘뿐이다:**

1. pending 에 대응하는 `users.id` 가 **존재하는가**
2. 삭제 saga 가 **동시에 그 행을 지우고 있는가**

`users` 행을 지우는 코드 경로는 **삭제 saga 하나뿐**이므로(`worker/index.js:948`),
**2번은 deletion lease 0건으로 완전히 판정된다.** 단어장 저장·친구 관계·리미터 쓰기는
`users` 행의 존재 여부를 바꾸지 않으므로 **1번의 답을 뒤집지 못한다.**

| | **reconciliation** | **주 D1 restore** |
|---|---|---|
| 필요한 것 | **삭제 생명주기 quiescence** | **전역 write drain** |
| 조건 | ① `maintenance` = `closed` ② 현재 epoch ③ 신규 삭제 saga 진입 차단 ④ **활성 deletion lease 0건** | 위 전부 **+ §10-9-6 의 11개 쓰기 경로 전체**가 빠져나갔다는 **질의 가능한 증거** |
| 일반 쓰기(`PUT /book`·친구·리미터)의 drain | **필요조건이 아니다** — 판정 대상을 바꾸지 않는다 | **필요하다** — 복원본에 섞이면 데이터가 손상된다 |
| deletion lease 0건만으로 | **충분하다** | **부족하다**(§10-8-0) |
| 현재 상태 | 설계 완료. 구현은 4단계 | **금지.** ⚠️ **사유가 drain 이 아니다** — 전역 user-data drain 은 **결정 A′ 로 구현 완료**(2026-08-18, HTTP 요청·정리 크론). 남은 미충족은 §10-8-0 의 **⑤ 지금 임차증 0건 미확인 · ⑦ 옛 배포 차단 미증명 · ⑨ T8** 이다 |

> **단, 둘을 동시에 실행하지 않는다.** reconciliation 은 주 D1 복원 절차(§10-8)의 **6번 단계**로
> 들어가 있고, 그 절차 전체가 지금 금지 상태다. 평시 reconciliation(경보 대응)은 위 4개 조건에서
> 돌 수 있지만 **그때 restore 를 같이 시작하지 않는다.**

- 유지보수 모드가 무엇을 막는지는 **§10-7 의 전수 표**가 정한다. 메서드로 막지 않는다.
- ⚠️ **`/api/ready` 가 503 인 것만으로는 부족하다.** 그것은 「새 요청을 막기 시작했다」는 뜻일
  뿐 「이미 들어온 요청이 끝났다」는 뜻이 아니다. 증거 요건은 **§10-9** 가 정한다.
- **실행 조건**: ① 모든 복원 직전에 **필수** ② `pending_alert_at` 을 넘긴 표식이 있을 때
  ③ 운영자가 수동으로. 크론이 없으므로 자동 주기 실행은 **없다**(있는 척하지 않는다).
  **어느 경우든 §10-9 의 quiescence 증거가 없으면 실행을 거부한다.**
- 재가입자의 새 UID 는 HMAC 이 다르므로 위 대조에 **원리적으로 걸리지 않는다.**
- **`pending` 은 어떤 경우에도 복원 후 삭제에 쓰지 않는다**(2단계 §9-7).

### 10-5. 만료 기준과 **정리 실행 시점** (2판 재설계)

1판의 `expires_at = pending_at + 30일` 은 틀렸다. **pending 과 confirmed 는 성격이 다르다.**

| | pending (`confirmed_at IS NULL`) | confirmed |
|---|---|---|
| 뜻 | 삭제를 시도했다. 성공했는지 **모른다** | 삭제가 확정됐다. 복원 때 **반드시 다시 지워야 한다** |
| 시간이 지나면 | 아무것도 저절로 해결되지 않는다 | 복원 가능 기간이 지나면 쓸모가 없어진다 |
| **자동 삭제** | **금지.** 시간으로 지우면 "삭제는 됐는데 확정만 실패한" 표식이 사라진다 | 허용 |
| 기준 값 | `pending_alert_at = pending_at + 24시간` → **경보 임계값일 뿐** | `expires_at` |
| 정리 조건 | **자동 정리 없음.** reconciliation 은 **승격만** 하고 지우지 않는다(promote-only, §10-4). 승격되면 confirmed 규칙을 따른다. 승격되지 않는 pending 은 §10-5-1 의 운영 검토 대상 | `expires_at < now` |

**confirmed 의 보존 기한** — 확정되는 순간 다시 계산한다:

```
CONFIRMED_RETENTION = max(TIME_TRAVEL_WINDOW, BACKUP_MAX_AGE) + SAFETY_MARGIN
expires_at          = confirmed_at + CONFIRMED_RETENTION
```

> **5판 — 이 산식이 문서 전역의 유일한 기준이다.** 다른 곳에 `MAX_RESTORE_WINDOW + SAFETY_MARGIN`,
> `7일 + 30일`, `Time Travel 보유기간 + 30일`, "플랜이 무엇이든 자동으로 37일" 같은 서술이
> 있으면 **그것은 틀렸다.**
>
> **4판까지 무엇이 틀렸나: Time Travel 만 봤다.** 계정을 되살리는 경로는 **둘**이다(법률 패킷 §6-2 · 2단계 §10) —
> Time Travel 과 **수동 백업**. 4판 산식은 뒤엣것을 아예 세지 않았다. 그래서 표식보다 **오래된
> 백업이 하나라도 복원 가능한 상태로 남아 있으면**, 그 백업을 복원한 순간 표식은 이미 만료·삭제되어
> **되살아난 계정을 아무도 다시 지우지 못한다.** 그리고 아무도 알아채지 못한다 —
> 표식이 없다는 것은 「지울 것이 없다」와 구분되지 않기 때문이다.

| 항 | 값 | 무엇인가 | 확정 여부 |
|---|---|---|---|
| `TIME_TRAVEL_WINDOW` | **30일**(가정) | D1 이 복원해 주는 최대 과거 구간 | ❌ **플랜 미확인.** 공식 문서상 무료 **7일** / 유료 **30일**(§19) → 확인 전까지 **보수적으로 긴 쪽** |
| `BACKUP_MAX_AGE` | **7일**(규칙값) | **실제로 복원할 수 있도록 보관 중인** 수동 백업의 최대 나이 | ❌ **규칙은 있으나 준수 확인 수단이 없다.** 규칙은 「migration 직전 생성 · 24시간 뒤 삭제 · 7일 상한」(2단계 §10) |
| `SAFETY_MARGIN` | **7일** | 복원 결정과 실행 사이의 시간, 시계 오차, 운영자가 주말에 알아채는 경우 | 계획값 |
| `CONFIRMED_RETENTION` | **37일**(현재 계산) | `max(30, 7) + 7` | ⚠️ **미확인 항 둘을 보수적으로 가정한 계산값.** 확정 보유기간이 아니다 |

**함께 강제해야 하는 두 규칙** (산식만으로는 부족하다):

| # | 규칙 | 안 지키면 |
|---|---|---|
| **B1** | **표식보다 오래된 수동 백업은 복원할 수 없게 폐기한다.** 폐기할 수 없으면 **restore 금지 대상으로 명시 분류**하고 목록에 남긴다 | 그 백업 하나가 산식 전체를 무의미하게 만든다 |
| **B2** | 백업이 표식보다 오래 남는 운영이 **실제로 있다면**, 그것은 「표식 기간이 부족한 것」이다. 백업을 줄이거나 표식을 늘린다 — **둘 중 무엇을 택할지는 법률 자료 `L14`** | 산식이 현실과 어긋난 채 문서에만 남는다 |

**최종 숫자를 확정하려면 넷이 필요하다. 지금은 하나도 없다.**

| # | 필요한 것 | 상태 |
|---|---|---|
| 1 | Cloudflare 플랜 확인(Free/Paid) | ❌ 대시보드 미확인 |
| 2 | 실제 수동 백업 보유 실태 확인 | ❌ 미확인 |
| 3 | `SAFETY_MARGIN` 7일의 적정성 | ❌ 법률 검토 `L9-3b` |
| 4 | 위 셋을 합한 값의 법적 적정성 | ❌ 법률 검토 `L9-2`·`L9-3`·`L14` |

> ⛔ **그전까지 `privacy.html` 에 「14일 뒤 삭제」·「37일 뒤 반드시 삭제」 같은 단정 문장을
> 넣지 않는다.** 지킬 수단이 확정되지 않은 기간을 방침에 적으면 그 문장은 **적는 순간 거짓**이다.
> 이것은 §10-5 아래 「정리를 언제 실제로 실행하나」가 4판까지 「언젠가 치운다」였던 것과 같은 종류의
> 잘못이다 — 5판에서 둘 다 고쳤다.
- **`pending_at` 기준이 아니라 `confirmed_at` 기준인 이유**: 되살릴 수 있는 시점은 삭제가
  **실제로 일어난** 때부터 세어야 한다. pending 이 오래 걸렸다고 보존 기한이 먼저 닳으면
  정확히 그만큼 보호가 짧아진다.

### 10-5-2. **정리 전용 Scheduled Worker** — 5판 신설 · 결정 B

#### 왜 필요한가

4판까지 정리는 **다른 일이 일어날 때 곁다리로** 붙어 있었다.

| 지금 정리되는 것 | 언제 | 아무도 그 일을 안 하면 |
|---|---|---|
| 만료·폐기 세션 | **다음 로그인 때**(`worker/index.js:256-261`) | **영원히 남는다** |
| `rate_limits` 지난 행 | 같은 자리 | **영원히 남는다** |
| confirmed 삭제 표식 | 다음 삭제 saga 때 | **영원히 남는다** |
| `consumed_signup_states` | 다음 가입 성공 때 | **영원히 남는다** |

4판은 이것을 알면서 **「다만 ②·③으로 언젠가 치운다」**고 적었다. **「언젠가」는 보유기간이 아니다.**
방침에 「N일 뒤 삭제」라고 쓰면서 지우는 사람이 아무도 없으면, **그 문장은 적는 순간 거짓**이다.

> 🔎 **쉽게 말하면**: 지금 구조는 「손님이 올 때만 청소하는 가게」다. 손님이 한 달 안 오면
> 한 달치 쓰레기가 그대로 있다. 그런데 우리는 문 앞에 「매일 청소합니다」를 붙이려 하고 있었다.

#### 무엇으로 만드나

**Pages Functions 에 넣지 않는다.** Cron Trigger 는 Workers 의 `scheduled()` 핸들러에 붙는데
Pages Functions 에서 그것을 쓸 수 있다는 공식 근거를 찾지 못했다(§19). 억지로 끼워 넣는 대신
**정리 전용 Worker 를 별도 배포 대상으로 둔다.**

| 항목 | 값 | 근거 |
|---|---|---|
| 형태 | 독립 Worker. `scheduled()` 핸들러 하나 | |
| 바인딩 | 주 D1 `DB` · 삭제 표식 ledger D1 | 읽기·삭제만. **계정 생성·세션 발급 코드를 넣지 않는다** |
| Cron 한도 | **무료 계정당 5개** / 유료 250개. **계정 단위**다 | 공식 문서(§19). 4판의 「무료 플랜 지원 미확인」은 **틀렸고 5판에서 정정** |
| ⚠️ **CPU 시간** | **무료 10ms** / 유료 30초(<1시간 간격) | 공식 문서(§19). **설계 제약이다** — 아래 C7 참조 |
| 주기(안) | **1시간마다 1개**(`0 * * * *`). 5개 한도 중 1개만 쓴다 | 나머지 4개는 다른 용도로 남긴다 |
| 배포 | Pages 프로젝트와 **별개**. 배포 승인도 별개 | |

#### 무엇을 정리하나 (5가지 · C5 는 폐지)

| # | 대상 | 조건 | ⚠️ 주의 |
|---|---|---|---|
| **C1** | `consumed_signup_states` | `expires_at < now` | R10 — **만료 전에 지우면 replay 창이 다시 열린다** |
| **C2** | confirmed 삭제 표식 | `confirmed_at IS NOT NULL AND expires_at < now` | 위 산식대로. **B1·B2 를 함께 지켜야 의미가 있다** |
| **C3** | 만료·폐기 세션 | `expires_at < now OR revoked_at IS NOT NULL` | 지금 로그인 자리가 하는 것과 **같은 문장**. 옮기는 게 아니라 **더한다**(로그인 자리도 남긴다 — 둘 다 있어도 무해하다) |
| **C4** | `rate_limits` | `expires_at < now` | 같음 |
| ~~**C5**~~ | ~~만료 write lease~~ | — | ⛔ **2026-08-18 폐지.** 그때는 해제가 `released_at` 을 적는 UPDATE 라 「해제됐고 만료된」 행이 남았다. 결정 A′ 로 **해제가 행 삭제**가 되면서 표에 남은 행은 **전부** 「진행 중이거나 죽었다」가 됐다 — 지울 것이 하나도 없고, 시간으로 지우면 그 증거가 사라져 복원 금지가 저절로 풀린다 |
| **C6** | **unresolved pending 경보** | `confirmed_at IS NULL AND pending_alert_at < now` | ⛔ **지우지 않는다. 세어서 알린다.** 아래 참조. **구현(2026-08-18)**: 세는 곳은 `cleanup_runs.open_pending`, 알리는 곳은 `/api/ready` 의 **`cleanupAlert: true`**. ⚠️ 2026-08-18 이전에는 **「센다」만 있었다** — `/api/ready` 가 그 값을 읽고 버려서 D1 을 직접 조회하지 않는 한 어디에서도 보이지 않았다 |

#### ⛔ pending 삭제 표식은 **시간만으로 지우지 않는다**

이것이 이 Worker 에서 가장 중요한 규칙이다.

| | pending | confirmed |
|---|---|---|
| 뜻 | 삭제를 시도했다. **성공했는지 모른다** | 삭제가 확정됐다 |
| 시간이 지나면 | **아무것도 저절로 해결되지 않는다** | 복원 가능 기간이 지나 쓸모가 없어진다 |
| Worker 가 하는 일 | **개수를 세고 경보만.** `DELETE` 문을 아예 쓰지 않는다 | `expires_at < now` 면 삭제 |
| 왜 | 지우면 「삭제는 됐는데 확정 기록만 실패한」 표식이 사라져 **복원 때 그 사람이 되살아나고 아무도 모른다**. 반대로 **계정이 살아 있는데 지우면** 살아 있는 계정을 지우는 근거가 된다(I22~I25) | |
| 누가 판정하나 | **사람.** §10-5-1 의 S3~S7 · Q7b(계정 존재 재확인 포함 5개 조건) | 시간 |

**Worker 코드에 `deletions` 를 대상으로 하는 `DELETE` 는 C2 한 문장뿐이어야 하고, 그 `WHERE` 에는
반드시 `confirmed_at IS NOT NULL` 이 들어간다.** 이 조건이 빠진 문장이 있으면 테스트가 실패해야
한다(§13-5 **T44**).

#### 관측·안전 요구사항

| # | 요구사항 | 왜 |
|---|---|---|
| **C7** | **한 번의 실행이 무료 CPU 10ms 를 넘지 않게 설계한다.** 대상별 `LIMIT`(예: 1000행)을 두고 **여러 번에 걸쳐 나눠 지운다** | D1 질의는 대부분 I/O 라 CPU 는 적게 쓰지만, 10ms 는 **넉넉하지 않다**. 한 번에 다 지우려다 시간 초과로 **매번 아무것도 못 지우는** 상태가 가장 나쁘다 |
| **C8** | **재실행 안전(idempotent).** 같은 실행이 두 번 돌아도 결과가 같아야 한다 | 크론은 재시도될 수 있다. 전부 `WHERE 조건 AND 시각` 형태라 자연히 성립하지만, **명시적으로 요구사항에 둔다** |
| **C9** | **마지막 성공 시각**을 기록한다(`cleanup_runs` 1행 UPSERT) | 「돌고 있나」를 사람이 볼 수 있어야 한다. **기록이 없으면 안 돈 것과 구분되지 않는다** |
| **C10** | **삭제한 행 수**를 대상별로 기록한다 | 갑자기 0이 되거나 갑자기 폭증하는 것이 신호다 |
| **C11** | **연속 실패 횟수**를 기록하고 임계값(3회)에 닿으면 경보 | 조용히 멈춘 크론은 없는 크론보다 나쁘다(있다고 믿게 만든다). **구현(2026-08-18)**: `cleanup_runs.fail_streak` 기록 + `fail_streak >= 3` 이면 `/api/ready` 의 `cleanupAlert: true` + **실패한 회차는 `ctx.waitUntil()` 의 Promise 를 거부해** Cloudflare Cron Trigger 의 Past Events 에 실패로 남는다. ⚠️ 2026-08-18 이전에는 `tick()` 이 실패를 기록한 뒤 **예외를 삼켜서**, 3회 연속 실패해도 대시보드에는 성공 세 줄이 남았다. **기록에 실패해도 던진다** — 기록됨을 성공의 근거로 쓰지 않는다 |
| **C12** | **`/api/ready` 가 정리 상태를 드러낸다.** 마지막 성공이 `2 × 주기` 보다 오래됐으면 `cleanupStale: true`, 확정 안 된 표식이 있거나 연속 실패가 임계값에 닿으면 `cleanupAlert: true` | 배포 뒤 「무엇이 덜 됐나」를 한 곳에서 읽는다는 기존 원칙과 같다. **둘은 다른 질문이다** — stale = 정리가 제 시간에 **성공했나**, alert = **사람이 봐야 할 것**이 쌓여 있나. 크론이 매시 성공해도 확정 안 된 표식은 그대로 남으므로 **성공이 그 사실을 덮으면 안 된다**. ⛔ **개수·실패 횟수·오류 문자열은 싣지 않는다** — 이 응답은 인증 없이 열려 있고 그 값들은 운영 정보다(D1 직접 조회로 본다). ⚠️ **못 읽으면 `true` 다**(fail-closed): ledger 는 붙어 있는데 `cleanup_runs` 를 못 읽는 것을 「괜찮다」로 읽지 않는다 |
| **C13** | ⛔ **정리 실패는 기존 데이터를 자동 삭제하지 않는다(fail-closed).** 「오래 못 치웠으니 한꺼번에 지우자」류의 보상 동작을 넣지 않는다 | 정리 로직의 버그가 곧 데이터 손실이 된다. **못 지우는 것은 되돌릴 수 있고, 잘못 지우는 것은 되돌릴 수 없다** |
| **C14** | **유지보수 모드·restore_closed 중에는 돌지 않는다** | ledger 의 `maintenance` 행을 먼저 읽고, `open` 이 아니면 **아무것도 하지 않고 종료**한다. 복원 중 정리가 겹치면 §10-4 의 경합이 된다 |

#### **네 가지를 구분해 적는다** (4판이 뭉갠 것)

| 용어 | 뜻 | 값(안) |
|---|---|---|
| **유효기간** | 그 값이 쓸모를 잃는 시점 | `expires_at` |
| **삭제 가능 시점** | 지워도 되는 가장 이른 시각 | `expires_at < now` |
| **정리 작업 실행 주기** | Worker 가 실제로 도는 간격 | **1시간** |
| **실제 최대 저장기간** | 사용자에게 말할 수 있는 최대치 | **유효기간 + 실행 주기 + 실행 실패 허용 창** = 예: `37일 + 1시간 + α` |

> **방침에는 「실제 최대 저장기간」을 적는다.** 「유효기간」을 적으면 실제보다 짧게 말하는 것이 된다.
> 다만 **위 §10-5 의 넷이 확정되기 전까지 어느 숫자도 방침에 적지 않는다.**

**이번 단계에서는 이 Worker 를 만들지 않는다.** 4단계 구현 대상이다(§20).

#### 10-5-1. **살아 있는 계정에 붙은 pending 을 무기한 방치하지 않는다** (3판 · **4판에서 S3~S7 개정**)

promote-only(§10-4)는 pending 을 **자동으로 지우지 않는다.** 그래서 「주 D1 삭제가 실패했고
사용자도 재시도하지 않은」 표식은 **아무도 손대지 않으면 영원히 남는다.** 안전한 방향이긴 하지만
**개인정보를 무기한 보관하는 방향이기도 하다.** 둘을 함께 적는다.

| 규정 | 내용 |
|---|---|
| **S1** | `pending_alert_at`(= `pending_at + 24시간`)은 **경보 시점이지 보관 종료 시점이 아니다.** 지났다고 지우지 않는다 |
| **S2** | `pending_alert_at` 을 넘긴 표식이 하나라도 있으면 **24시간 안에 운영 검토를 시작한다.** 검토는 「그 UID 의 계정이 주 D1 에 있는가」를 보는 일이다 |
| **S3** | 계정이 **없다** → 승격(§10-4). 계정이 **있다** → **과거 삭제는 실패한 것으로 판정한다.** pending 은 「대기 중인 삭제 명령」이 **아니다** |
| **S4** | ⛔ **운영자는 옛 pending 만 보고 saga 를 재실행하지 않는다.** 새 삭제는 **사용자의 새 요청으로만** 시작한다(I24) |
| **S5** | 계정이 살아 있는데 사용자가 여전히 삭제를 원한다면, **앱의 계정 삭제를 다시 누르게 한다.** 그것이 새 saga 이고 새 pending 이다 |
| **S6** | 살아 있는 계정과 일치하는 **stale pending 의 제거**는 아래 다섯을 **전부** 만족할 때만 한다 — ① `maintenance` 가 `closed` ② **현재 epoch** ③ 활성 deletion lease **0건** ④ **그 UID 가 주 D1 에 여전히 존재함을 그 자리에서 재확인** ⑤ **운영 판정**(자동·시간 기반 아님) |
| **S7** | **unresolved pending 의 최대 보관기간은 이 문서가 정하지 않는다.** 기술적으로는 무기한 보관이 가장 안전하지만 그것이 법적으로 적정한지는 **외부 검토 사항**이다(법률 자료 L9-4·L9-5) |

#### ⚠️ S3·S4 가 4판에서 바뀐 이유 — **예고 없는 계정 삭제** (위협 35 · **High**)

3판의 S3 은 "운영자가 saga 를 재실행한다"를 선택지로 두었다. **안전하지 않다.**

```
1  사용자가 [계정 삭제] 를 누른다
2  주 D1 삭제가 실패한다 (결정 6 의 X — 보수적 응답)
3  사용자에게 "지우지 못했어요" 가 표시된다.  세션은 살아 있다
4  사용자는 삭제가 실패했다고 이해하고 계정을 그대로 계속 쓴다
5  새 단어·별명·친구 관계를 만든다.  며칠이 지난다
6  24시간 뒤(또는 그 뒤 언제든) 운영자가 경보를 보고 옛 pending 만 근거로 saga 를 재실행한다
7  사용자의 현재 계정과 5번의 데이터가 **별도 확인 없이** 삭제된다
────
사용자는 삭제가 실패했다고 들었고, 그 뒤 계속 썼고, 아무 예고 없이 전부 잃는다.
```

**pending 이 뜻하는 것은 「과거에 삭제 요청이 있었다」이지 「그 요청이 무기한 유효하다」가
아니다.** 실패 응답을 받은 뒤 사용자가 계속 사용했다면, 그것은 **삭제 의사가 유지되고 있다는
증거가 아니다** — 우리가 "못 지웠다"고 말했기 때문이다.

> **규정**: 계정이 주 D1 에 **살아 있으면** 그 pending 은 **실패 기록**이다.
> **되살려 실행하지 않는다.** 삭제는 언제나 **사용자의 새 요청**에서 시작한다.

**법률 검토 종속 항목**: 검토에서 **삭제 요청 기록 자체의 보존이 필요하다**고 판단되면,
stale pending 을 지우는 대신 **`aborted` 같은 별도 상태**로 남겨야 할 수 있다. 그 경우
「pending / confirmed」 두 상태가 아니라 셋이 되고 보유기간도 따로 정해야 한다.
**지금 만들지 않는다** — 법률 자료 L9-4 에 질문으로 남겼다.

> **왜 여기에 숫자를 안 쓰나**: "90일 뒤 자동 삭제" 같은 규칙을 지금 적으면, 그것은 곧
> **시간 기반 자동 삭제**이고 위협 31 이 막으려던 바로 그 동작이다. 기간을 정하려면 먼저
> 「그 표식이 개인정보인가」에 대한 답이 있어야 한다.

### 10-6. **삭제 표식 DB 자체는 과거로 복원하지 않는다** (2판 신설)

1판의 가장 큰 구멍이다. 표식 DB 도 D1 이므로 **여기에도 Time Travel 이 항상 켜져 있다.**
그런데 이 DB 의 복원은 주 D1 의 복원과 **위험의 방향이 반대**다.

| 복원 대상 | 무슨 일이 나나 |
|---|---|
| 주 D1 | 삭제된 **계정이 되살아난다** → 표식으로 다시 지운다(§10-8) |
| **표식 DB** | ① 그 시점 이후에 확정된 **표식이 통째로 사라진다** — 그 사람들은 복원 때 **영영 되살아난다.** 대조에 안 걸리니 **아무도 알아챌 수 없다** ② 그 시점에 pending 이었다가 이후 지워진 표식이 **부활**해, 살아 있는 계정을 지울 후보로 돌아온다 |

②는 `pending` 을 재삭제에 쓰지 않는 규칙이 막는다. **①은 아무것도 막지 못한다** — 없는 표식은
없다는 사실조차 남기지 않는다.

**규정:**

> **삭제 표식 DB 는 원칙적으로 `restore` 금지다.** 주 D1 을 복원하는 절차(§10-8)는 표식 DB 를
> 건드리지 않는다. 표식 DB 의 손상은 **복원이 아니라 재작성**으로 다룬다.

불가피하게 복원해야 한다면(예: 표식 DB 스키마가 깨졌다) **아래를 전부 마치기 전에는
서비스를 재개하지 않는다.** 하나라도 건너뛰면 확정된 삭제가 사라진다.

#### ⛔ 10-6-0. **ledger 를 제자리 복원하면 게이트가 같이 과거로 간다** (5판 신설)

4판의 「ledger 복원 9단계」에는 **자기참조 결함**이 있었다. 이 DB 에는 표식만 있는 게 아니다.

| 테이블 | 무엇 | 복원되면 |
|---|---|---|
| `deletions` | 삭제 표식 | 그 시점 이후 확정분이 **사라진다**(위 ①) |
| **`maintenance`** | **게이트 자체**(`open`/`maintenance`/`restore_closed`) | 1번에서 방금 `closed` 로 바꾼 것이 **`open` 으로 되돌아간다** |
| **`epoch`** | 세대 번호 | **되돌아간다** → 옛 세대의 요청이 다시 유효해진다 |
| **`write_leases`** | 진행 중 작업 표시 | **되돌아간다** → 이미 끝난 lease 가 살아 있는 것으로 보인다 |

즉 4판 절차의 **1번(유지보수 진입)을 4번(복원 실행)이 취소한다.** 문을 잠그고 나서
**문 잠근 기록까지 과거로 되돌리는** 절차였다.

**규정 (5판):**

> **ledger 는 `restore` 하지 않는다.** 제자리 복원 자체를 절차에서 **없앤다.**
> 표식 DB 의 손상은 **복원이 아니라 「새 DB 로 재작성」**으로 다룬다.
> 그리고 **게이트는 복원 대상 DB 안에서 판정되어서는 안 된다** — 아래 G1~G3.

#### 게이트가 자기 자신을 잃지 않게 하는 세 규칙

| # | 규칙 | 왜 |
|---|---|---|
| **G1** | **ledger 를 읽지 못하면 `restore_closed` 로 판정한다**(fail-closed) | ledger 를 교체하는 동안 앱은 **저절로 닫힌다.** 별도 신호가 필요 없다 |
| **G2** | ledger 의 `maintenance` 행이 **없거나 읽을 수 없거나 형식이 다르면** → `restore_closed` | 「모르겠으면 연다」가 아니라 「모르겠으면 닫는다」 |
| **G3** | **coordination 테이블(`maintenance`·`epoch`·`write_leases`)은 어떤 병합에서도 과거 값을 받지 않는다** | 과거 값을 받는 순간 G1·G2 가 무의미해진다 |

> **이름**: 아래를 **「ledger 재작성 13단계」**라 부른다(4판의 「복원 9단계」를 대체한다).
> §10-8 의 **「주 D1 조건부 복원 11단계」**와 **다른 절차이고, 같은 작업 창에서 실행하지 않는다.**
> 두 절차를 한 창에서 돌리면 대조의 기준이 되는 쪽이 함께 흔들려 **검증 자체가 무의미해진다.**

| # | 단계 | 확인 | 되돌릴 수 있나 |
|---|---|---|---|
| 1 | **`restore_closed` 진입**(§10-7) | `/api/ready` → `mode:"restore_closed"`·503 | 예 |
| 2 | **현재 ledger 를 통째로 내보낸다**(`wrangler d1 export`) — 잃을 것이 여기 다 있다 | 행 수 `N_now` · confirmed 수 `C_now` 기록 | 읽기만 |
| 3 | 내보낸 파일의 **해시·크기**를 기록한다 | 저장소 밖·암호화 볼륨 | 읽기만 |
| 4 | **export 를 실제로 되읽어 본다** — 파일이 존재하고 파싱되고 `N_now` 행이 나오는지 | **여기서 실패하면 5번으로 가지 않는다** | 읽기만 |
| 5 | ⚠️ **새 ledger D1 을 만든다**(`shhh-deletions-v2` 등). **기존 DB 를 복원하지 않는다** | **별도 최종 승인 필요.** 무료 플랜 D1 한도 10개(§19) 안인지 확인 | 삭제 가능 |
| 6 | 새 DB 에 스키마 생성(§10-1) | 테이블·인덱스 존재 확인 | 예 |
| 7 | 과거 데이터가 **필요한 경우에만**: **임시 DB** 를 하나 더 만들어 거기에 복원하고 `deletions` **만** 추출한다 | ⚠️ **운영 ledger 에 restore 를 실행하지 않는다** | 임시 DB 폐기 가능 |
| 8 | `deletions` **합집합 병합** — 규칙은 §10-6-1 이 **전부 결정적으로** 정한다. 결과를 새 DB 에 적재 | — | 새 DB 재생성 |
| 9 | ⛔ **coordination 테이블은 병합하지 않는다.** 새로 초기화한다 — `maintenance = 'restore_closed'` · `epoch = max(관찰된 모든 epoch) + 1` · `write_leases` **비움** | **G3.** 과거 값이 하나라도 들어가면 중단 | — |
| 10 | **대조 4종** — ① 행 수 ≥ `N_now` ② 3번 파일의 **모든 `mark` 가 새 DB 에 있는지 전수** ③ confirmed 수 ≥ `C_now` ④ 같은 `mark` 의 `key_version` 불일치 0건 | **하나라도 미달이면 중단.** 바인딩을 바꾸지 않는다 | 예 |
| 11 | **바인딩 전환** — Pages 프로젝트의 ledger 바인딩을 새 DB 로. **G1 덕분에 전환 중에는 앱이 자동으로 닫혀 있다** | 전환 후 `/api/ready` 가 여전히 `restore_closed` 인지 | 되돌릴 수 있음(옛 DB 를 아직 안 지웠다) |
| 12 | **전환 후 재검증** — 새 바인딩으로 10번의 4종을 **다시** 돌린다 | 배포 전파 지연 때문에 반드시 다시 한다(`HANDOFF.md` §4-6) | 예 |
| 13 | **`open` 으로 전환하고 서비스 재개.** 7번의 임시 DB 를 폐기한다. **옛 ledger 는 최소 보유기간이 지날 때까지 남긴다** | 폐기 목록을 기록 | — |

**중단하면 어디에 머무나**: 어느 단계에서 멈추든 **`restore_closed` 에 머문다.**
「일단 열고 나중에 고치자」는 경로를 만들지 않는다.

#### 10-6-1. 병합 규칙 — 모든 경우가 결정된다 (3판 완성)

2판은 「`confirmed_at` 은 NULL 이 아닌 쪽」이라고만 적어 **둘 다 non-NULL 인 경우가 미정**이었다.
같은 `mark` 의 두 판본(내보낸 현재 = `cur`, 복원된 과거 = `old`)에 대해 아래가 전부다.

| 컬럼 | 규칙 | 왜 |
|---|---|---|
| `confirmed_at` | 한쪽만 non-NULL → **그 값** · 둘 다 non-NULL → **더 늦은(큰) 값** · 둘 다 NULL → NULL | 확정을 pending 으로 되돌리면 그 삭제가 **무효**가 된다. 늦은 쪽을 남기면 `expires_at` 재계산(§10-5)이 **더 긴 보존**으로 이어진다 |
| `expires_at` | **더 큰 값** | 보존을 짧게 하는 방향은 언제나 위험한 쪽 |
| `pending_at` | **더 이른(작은) 값** | 최초 시도 시각이 사실이다 |
| `pending_alert_at` | `pending_at` 을 따라 재계산 | 파생값이라 따로 병합하지 않는다 |
| `key_version` | **같아야 한다.** 다르면 **손상으로 간주하고 자동 병합하지 않는다** | 같은 `mark` 가 다른 키로 나왔다는 것은 HMAC 충돌이거나 데이터 오염이다. 어느 쪽이든 **사람이 봐야 한다** |

**중단 규칙 — fallback 을 만들지 않는다:**

| 상황 | 조치 |
|---|---|
| 2·4번의 현재 ledger export·되읽기가 **실패**했다 | **재작성 중단.** 서비스는 **`restore_closed`** 로 **남긴다** |
| 10·12번의 대조 4종을 **완료할 수 없다** | **바인딩 전환 금지 / 재개 금지.** `restore_closed` 유지 |
| 같은 `mark` 에서 `key_version` 불일치 | **자동 병합 중단.** 사람이 판정하기 전까지 재개하지 않는다 |
| coordination 테이블에 **과거 값이 하나라도 들어갔다** | **중단.** 새 DB 를 버리고 5번부터 다시 한다(G3) |
| 위 어느 경우든 | **「어떻게든 복원하고 서비스 재개」라는 fallback 은 만들지 않는다.** 표식을 잃은 채 여는 것보다 닫아 두는 것이 낫다 |

#### 10-6-2. 키 회전과 **중복 UID** (3판 추가)

키를 회전하면 **한 UID 가 여러 key version 의 표식과 일치할 수 있다.** 회전 전에 pending 이
생기고 회전 후 재시도로 새 표식이 생기면 같은 사람에게 표식이 둘이다.

| # | 규칙 |
|---|---|
| K1 | 복원 후 대조는 **보유기간 안의 모든 key version 으로** 각각 HMAC 해 시도한다(§10-1 의 「보유기간 중 옛 키 삭제 금지」가 그래서 있다) |
| K2 | **재삭제 대상은 표식 개수가 아니라 고유 UID 로 중복 제거한다.** 표식 3개가 같은 UID 를 가리켜도 대상은 1명이다 |
| K3 | 검증식은 **`삭제된 users 행 수 == 고유 UID 수`** 다. 표식 수와 비교하면 **정상 상황이 실패로 판정된다** |
| K4 | 같은 UID 에 대한 `DELETE` 는 **한 번만** 실행한다. 두 번 실행하면 두 번째가 0행이라 K3 의 검증이 거짓 실패를 낸다 |
| K5 | 여러 표식 중 **하나라도 confirmed** 면 그 UID 는 재삭제 대상이다. 나머지가 pending 이어도 **pending 때문에 대상에서 빼지 않는다**(confirmed 가 이긴다) |
| K6 | 반대로 **pending 만 있는 UID 는 대상이 아니다**(2단계 §9-7) |

### 10-7. 유지보수 모드 — **상태가 셋이다** (2판 신설 · **5판 전면 개정**)

#### ⚠️ 4판까지 무엇이 틀렸나

4판의 표는 상태가 **둘**(열림/유지보수)이었고, 유지보수 중에도 아래 셋을 **허용**했다.

- `GET /api/book` · `GET /api/me`
- `GET /api/friends/:id/book`

근거는 "DB 를 안 **쓴다**"였다. **쓰기만 본 것이 잘못이다.**

> **주 D1 을 과거로 복원하면 탈퇴한 사람의 `users`·`sessions`·`books`·`friendships` 가 전부
> 되살아난다.** 재삭제가 끝나기 전까지 그 사람은 **살아 있는 계정**이다. 그러면 —
> ① 되살아난 `sessions` 행으로 **로그인 상태가 부활**하고(`whoAmI` 가 `JOIN users` 라 행이 돌아오면 통과한다)
> ② `GET /api/book` 이 **지웠던 단어장을 그대로 돌려주고**
> ③ 친구의 `GET /api/friends/:id/book` 이 **탈퇴한 사람의 단어장과 별명을 다시 보여준다.**
>
> `privacy.html:73` 의 「지우시면 그 자리에서 사라집니다」가 그 순간 거짓이 된다.
> **쓰기를 막는 것만으로는 이것을 하나도 막지 못한다.**

#### 세 상태

| 상태 | 언제 | 무엇을 막나 |
|---|---|---|
| **`open`** | 평상시 | 아무것도 안 막는다 |
| **`maintenance`** | 일반 점검 · **reconciliation** | **DB 를 쓰는 라우트 전부.** 읽기는 허용 |
| **`restore_closed`** | **주 D1 복원 전후** — 재삭제·CASCADE 잔여 검사·세션 검사가 **전부 끝날 때까지** | **사용자 데이터에 닿는 모든 것.** 읽기도, 인증도 막는다 |

#### 라우트 전수 표

| 라우트 | 메서드 | DB | `maintenance` | **`restore_closed`** | 왜 |
|---|---|---|---|---|---|
| `/api/health` | GET | 없음 | **허용** | **허용** | DB 를 안 만진다. 사용자 데이터도 없다 |
| `/api/ready` | GET | SELECT | **허용 + `maintenance`** | **허용 + `restore_closed`** | 상태를 알려야 한다. **사용자 데이터를 안 읽는다**(COUNT 뿐) |
| 정적 정책 파일 (`/policies/*.html`) | GET | 없음 | 허용 | **허용** | 불변 문서. 사용자 데이터가 아니다 |
| `/api/policies` | GET | 없음 | 허용 | **허용** | 서버 상수 |
| `/api/login/:provider` | GET | 없음 | **차단** | **차단** | 통과시키면 제공자까지 갔다가 콜백에서 막힌다. 앞에서 끊는다 |
| ⚠️ `/api/cb/:provider` | **GET** | `rate_limits`·`users`·`sessions`+청소 | **차단** | **차단** | **여기가 계정을 만든다**(`:492`) |
| ⚠️ `/api/exchange/:provider` | **GET** | 위와 같음 | **차단** | **차단** | 같음(`:747`) |
| `/api/signup/start` | POST | `rate_limits` | 차단 | **차단** | |
| **`/api/book`** | **GET** | 읽기 | 허용 | ⛔ **차단** | **되살아난 탈퇴자의 단어장을 돌려준다** |
| **`/api/me`** | **GET** | 읽기 | 허용 | ⛔ **차단** | 같음 + 계정 존재 자체를 확인해 준다 |
| **`/api/friends`** | **GET** | ⚠️ `invite_codes` **INSERT**(`myCode()`) | **차단** | ⛔ **차단** | 초대 코드가 없으면 그 자리에서 만든다(`:963`·`:598-606`) |
| **`/api/friends/:id/book`** | **GET** | 읽기 | 허용 | ⛔ **차단** | **친구에게 탈퇴자의 단어장·별명이 다시 보인다** |
| **세션 인증 자체**(`whoAmI`) | — | 읽기 | 통과 | ⛔ **수행하지 않는다** | 되살아난 `sessions` 행으로 **로그인 상태가 부활**한다. 인증을 시도조차 하지 않는다 |
| `/api/book` | PUT | `books` | 차단 | **차단** | |
| `/api/me` | DELETE | `users` + ledger | 차단 | **차단** | **복원 중 삭제가 곧 §10-4 의 경합이다** |
| `/api/session` | DELETE | `users`·`sessions` | 차단 | **차단** | |
| `/api/friends` | POST | `rate_limits`·`friendships` | 차단 | **차단** | |
| `/api/friends/code` | POST | `rate_limits`·`invite_codes` | 차단 | **차단** | |
| `/api/friends/:id` | PUT · DELETE | `friendships` | 차단 | **차단** | |
| **그 밖의 모든 `/api/*`** | 전부 | — | 허용 목록 밖이면 차단 | **허용 목록 밖이면 차단** | 기본값이 「차단」이다 |

**`restore_closed` 에서 허용되는 것은 네 줄뿐이다** — `/api/health` · `/api/ready` ·
`/api/policies` · 정적 정책 파일. **나머지는 전부 503 이다.**

#### `restore_closed` 를 언제 푸나

| # | 조건 | 확인 방법 |
|---|---|---|
| 1 | 확정된 삭제 표식 **전부** 재적용 완료 | 재삭제한 `users` 행 수 == 대상 고유 UID 수 |
| 2 | CASCADE 잔여 0건 | `sessions`·`books`·`friendships`·`invite_codes` 에 없는 `user_id` 참조 0건 |
| 3 | **되살아난 세션 전부 무효** | 재삭제 대상 UID 의 `sessions` 행 0건 |
| 4 | pending 표식 판정 완료 | §10-5-1 S3~S7 |
| 5 | 위 넷의 **결과를 기록** | 운영자가 나중에 대조할 수 있어야 한다 |

**하나라도 미달이면 읽기를 다시 열지 않는다.** 「쓰기만 먼저 열자」도 하지 않는다 —
막으려는 것이 **읽기 노출**이기 때문이다.

#### 왜 `MAINTENANCE=1` 환경변수가 아니라 ledger 행인가

환경변수는 **배포 세대마다 다르게 전파**된다. 이 저장소는 프로덕션 별칭이 배포 직후 약 1분간
옛 응답을 주는 것을 **실측**했다(`HANDOFF.md` §4-6). 게이트의 진실 원본은 **ledger 의
`maintenance` 행**이고, 모든 세대가 매 요청마다 그 행을 읽는다(§10-9 Q1·Q4).

⚠️ **그러나 그것도 「지금 배포된 세대」에만 통한다.** 게이트 로직 자체가 없는 **옛 배포**는
그 행을 읽지도 않는다 — 그것이 §10-8-1 이 다루는 문제다.

**구현 요구사항** (구현 단계에서 지킬 것):

1. 검사는 **`limited()` 보다 먼저** 온다. 뒤에 두면 리미터가 `rate_limits` 에 쓴다.
2. 판정은 **허용 목록**으로 한다 — 위 표의 「허용」만 통과시키고 나머지는 전부 503.
   차단 목록으로 만들면 **새 라우트의 기본값이 「통과」**가 되어 다음 사고가 예약된다
   (`scripts/build.mjs` 의 allowlist 와 같은 판단).
3. 응답은 503 + `Retry-After` + 사람이 읽을 한 줄. 오류 내용·복원 사실을 밖으로 말하지 않는다.
4. **`restore_closed` 에서는 세션 인증(`whoAmI`)을 아예 수행하지 않는다.** 라우트 판정이
   인증보다 **먼저** 온다. 순서가 바뀌면 되살아난 세션 행을 한 번 조회하게 되고,
   그 조회 결과가 로그·오류 메시지·타이밍으로 새어 나갈 수 있다.
5. 상태는 **`open` | `maintenance` | `restore_closed`** 세 값이다. 불리언이 아니다 —
   불리언으로 만들면 「읽기는 열린 유지보수」와 「전면 차단」을 구분할 수 없다.
6. `/api/ready` 는 상태를 **그대로** 돌려준다:

```json
{ "ok": true, "mode": "restore_closed", "configReady": true, "db": true,
  "providers": ["kakao","naver","google"], "ready": false,
  "cleanupStale": false, "cleanupAlert": false }
```

⛔ **정리 상태는 boolean 두 개가 전부다.** `open_pending`·`fail_streak`·`last_error`·`last_counts`
는 이 응답에 **싣지 않는다** — 이 응답은 인증 없이 열려 있다. 숫자와 사유는
`wrangler d1 execute shhh-ledger --command "SELECT * FROM cleanup_runs"` 와 observability 로 본다.
**외부 알림(Slack·이메일·Sentry)은 4단계 로컬 구현 범위가 아니다** — 운영 반영 단계의 별도 결정이다.
`cleanupStale`·`cleanupAlert` 는 **`ready` 를 내리지 않는다**: 정리가 밀린 것은 보유기간 문제이지
사용자가 앱을 못 쓰는 상태가 아니다(리미터를 fail-open 으로 둔 것과 같은 판단).

| `mode` | HTTP | `ready` |
|---|---|---|
| `open` | 200 (설정이 갖춰졌으면) | `configReady && db` |
| `maintenance` | **503** | `false` |
| `restore_closed` | **503** | `false` |

`ready` 가 `false` 인 이유가 **설정 미비인지 · 유지보수인지 · 복원 중인지** 구분되어야 한다 —
2026-08-16 에 `configReady`/`db` 를 가른 것과 같은 이유다(한 값이 여러 가지를 말하면
진짜 장애를 아무도 못 알아챈다).

⚠️ **`mode` 는 밖으로 나가는 값이다.** 「복원 중」이라는 사실 자체는 숨기지 않는다 —
숨기면 사용자가 언제 돌아와야 할지 모른다. 다만 **무엇을 복원하는지·왜인지는 말하지 않는다.**

### 10-8-1. **옛 배포 세대 차단** — 5판 신설 · 결정 C

#### 왜 필요한가 — 게이트가 닿지 않는 곳이 있다

§10-7 의 게이트는 **그 코드가 실행될 때만** 작동한다. 그런데 Cloudflare Pages 는 배포마다
**영구 주소**를 준다.

> 공식 문서: hash 기반 주소는 "**atomic and may always be visited in the future**"
> — 새 배포가 나가도 **옛 주소는 계속 살아 있다**(§19).

그리고 **바인딩은 프로젝트 단위**다. 옛 배포에도 **같은 D1 이 붙어 있다.** 그래서 —

```
사용자/공격자 → 774015e9.shhh-app.pages.dev  (게이트 없는 옛 코드)
                        ↓  같은 DB 바인딩
                   주 D1  ← 복원 중인데 쓰기가 들어온다
```

**복원 중에 이 경로로 들어온 쓰기는 복원본에 섞인다.** 그리고 복원 중이 아니어도, 옛 배포는
`restore_closed` 를 모르므로 **되살아난 탈퇴자의 단어장을 그대로 돌려준다.**

#### 4판의 T8 이 잘못됐다

4판 T8 의 합격 조건은 **「거부되거나 최소한 **탐지**되는지」**였다.

⛔ **탐지는 복원 허용 근거가 아니다.** 「나중에 로그를 보면 알 수 있다」는 것은
**이미 탈퇴자의 데이터가 나간 뒤**라는 뜻이다. 5판에서 합격 조건을 바꾼다.

| | 4판 | **5판** |
|---|---|---|
| T8 합격 | 거부됨 **또는** 탐지됨 | ⛔ **거부됨만.** 탐지됨은 **불합격** |
| 탐지만 가능하면 | 복원 허용 | **주 D1 restore 금지 유지**(§10-8-0) |

#### 확인된 사실 (2026-08-17 공식 문서)

| 사실 | 내용 | 그래서 |
|---|---|---|
| hash URL 영속성 | "may always be visited in the future" | 옛 배포는 **저절로 사라지지 않는다** |
| **배포 삭제** | `npx wrangler pages deployment delete` — 다만 "**The latest deployment for a branch cannot be deleted.**" | **대부분은 실제로 지울 수 있다.** 「영구 금지」가 유일한 답은 아니다 |
| **Access 보호 범위** | "this will **only protect your preview deployments** … does **not** protect your `*.pages.dev` domain or custom domain" | hash 주소는 덮을 수 있고, **canonical 은 못 덮는다** |

> ⚠️ **다음 문장을 사실로 확정하지 않는다**: 「삭제할 수 없는 브랜치별 최신 배포는 곧 게이트가
> 들어 있는 코드다.」 **브랜치별 최신 배포가 오래된 게이트 미탑재 코드일 수 있다** —
> 몇 달 전에 만들고 방치한 브랜치가 있으면 그 브랜치의 최신 배포는 옛 코드이고,
> **지울 수도 없다.** 목록과 source commit 을 **직접 확인해야만** 알 수 있다.

#### restore 금지가 풀리려면 — **12개 전부 증명**

| # | 조건 | 증명 방법 |
|---|---|---|
| **D1** | 전체 Pages deployment 와 alias 목록을 확보한다 | `wrangler pages deployment list` — 목록 자체를 기록으로 남긴다 |
| **D2** | production · preview · branch alias · 고유 hash URL 을 **모두 분류**한다 | 주소 종류별로 표를 만든다. 하나라도 분류 안 된 것이 있으면 미달 |
| **D3** | **삭제할 수 없는 각 브랜치 최신 배포가 게이트 탑재 버전인지** 확인한다 | 각 배포의 **source commit** 을 확인. 게이트 도입 커밋 이후인지 대조 |
| **D4** | 게이트 미탑재 최신 브랜치는 **새 안전 버전을 배포**하거나 해당 preview 접근을 **Access 로 차단**한다 | 둘 중 하나. 「나중에 하자」는 미달 |
| **D5** | 삭제 가능한 게이트 미탑재 배포는 **별도 최종 승인 후** 삭제한다 | ⚠️ **되돌릴 수 없다.** 승인 없이 실행하지 않는다 |
| **D6** | 롤백용으로 남길 1~2개 배포도 **반드시 게이트 탑재본**이어야 한다 | 롤백 대상이 게이트 미탑재면 **롤백이 곧 방어 해제**다 |
| **D7** | Preview Access 적용 여부를 **실제 비인증 요청**으로 검증한다 | 설정 화면이 아니라 **응답**으로 본다 |
| **D8** | canonical 은 Access 보호 대상이 아니므로 **최신 세대 전파와 `restore_closed` 동작을 별도로 검증**한다 | canonical 에 요청 → `mode: restore_closed` · 503 |
| **D9** | **남아 있는 모든 공개 URL** 에서 `restore_closed` 중 사용자 데이터 **읽기와 쓰기가 모두 거부**되어야 한다 | 각 URL × (읽기 1개, 쓰기 1개) 전수 |
| **D10** | 삭제된 URL 은 **실제 404/접근 거부**를 확인한다 | 대시보드 표시가 아니라 HTTP 응답 |
| **D11** | T8 합격 조건은 **「거부됨」만** 허용한다 | 「탐지됨」은 불합격 |
| **D12** | **하나라도 증명하지 못하면 주 D1 restore 금지는 유지한다** | 부분 충족은 충족이 아니다 |

**이번 3단계에서는 설계만 확정한다.** 실제 Pages deployment 삭제는 **하지 않는다** —
되돌릴 수 없으므로 별도 최종 승인을 다시 받는다.

#### 이것으로도 **막지 못하는 것**

| 사실 | 내용 |
|---|---|
| 옛 배포는 **정의상 새 규칙을 모른다** | D1~D12 는 「옛 배포를 없애거나 닿지 못하게」 하는 것이지 「옛 배포가 규칙을 지키게」 하는 것이 아니다 |
| 삭제·Access 를 되돌리면 | 그 자리에서 다시 열린다. **복원 작업 창마다 재확인**해야 한다 |
| 커스텀 도메인이 붙으면 | 주소가 하나 더 는다. **D1~D12 를 다시 돈다** |
| 비용·도메인이 필요한 조치 | **임의로 구매·적용하지 않는다.** 설계 조건으로만 둔다 |

### 10-8-0. ⛔ **주 D1 restore 는 지금 운영상 금지다** (3판 신설)

아래 §10-8 은 **조건부 절차**다. **아직 활성화되지 않았다.**

> **아래 ①~⑨ 가 구현되고 실측으로 검증되기 전까지, 주 D1 의 `time-travel restore` 와
> 수동 백업 복원을 실행하지 않는다.**
>
> **2026-08-18 갱신 — 조건이 8개에서 9개가 됐다.** ①②④는 결정 A′(§10-9-8)로 구현됐고,
> 그 대신 **⑤ `noActiveLeases`(지금 이 순간 활성·stale 임차증 0건)** 가 새로 들어왔다.
> 「구조가 있다」와 「지금 0이다」는 다른 말이고, 뒤엣것은 **질의해야만** 알 수 있다.

**왜 금지인가 (1) — 쓰기**: §10-9-6 이 세어 보니 주 D1 에 쓰는 경로는 **11개**인데, 제안된
deletion lease 는 그중 **하나**(삭제 saga)만 추적한다. 나머지 10개는 **유지보수 플래그가 진입을
막기 전에 이미 들어온 요청**으로 계속 쓸 수 있고, 그 쓰기가 복원과 겹치면 복원 결과가 오염된다.

**왜 금지인가 (2) — 읽기 (5판 추가)**: 4판까지 이 절은 **쓰기만** 말했다. **그것으로는 부족하다.**
복원 직후 되살아난 탈퇴자의 계정은 재삭제가 끝나기 전까지 **살아 있는 계정**이고, 그동안
`GET /api/book` · `GET /api/me` · `GET /api/friends/:id/book` 이 **지웠던 단어장과 별명을 그대로
돌려준다.** 되살아난 `sessions` 행으로 **로그인 상태까지 부활**한다(§10-7). 쓰기 drain 을 아무리
잘 만들어도 이것은 하나도 막지 못한다 — **읽기를 막아야 막힌다.**

| # | 금지가 풀리는 조건 | 확인 방법 | 2026-08-18 |
|---|---|---|---|
| ① | §10-9-6 의 **모든 쓰기 경로**를 포괄하는 전역 drain 이 구현됐다 | 요청당 임차증 1개(A′) | ✅ |
| ② | 그 drain 이 **질의 가능한 0/비0 값**을 낸다 | `drainState()` → `{open, stale, live, drained}` | ✅ |
| ③ | **`restore_closed` 상태가 구현됐다** — 읽기·세션 인증까지 막는다(§10-7) | 라우트 전수 표의 `restore_closed` 열 | ✅ |
| ④ | **사용자 데이터 read/write 전체의 in-flight drain** 이 0임을 질의할 수 있다 | 읽기 경로도 같은 임차증을 든다(T6b) | ✅ |
| ⑤ | **지금 이 순간 활성·stale 임차증이 0건이다** | `drainReport()` 의 질의 결과. **손으로 적지 않는다** | ⛔ 그때그때 |
| ⑥ | 재삭제·CASCADE 잔여 검사·세션 검사가 끝나기 전에는 **읽기를 다시 열 수 없다** | §10-7 의 해제 조건 1~5 | ✅ |
| ⑦ | **옛 배포 세대가 게이트를 무시할 수 없음이 증명됐다** | §10-8-1 의 **D1~D12 전부**. **T8-b 가 「거부됨」** | ⛔ **미충족** |
| ⑧ | **ledger 가 자기 자신을 복원하지 않는 구조다**(G1~G3) | §10-6-0 | ✅ |
| ⑨ | 회귀 테스트 **T1~T8 · T40~T47**(§13-5)이 통과한다 | `npm test`. **T8-b 가 아직 「재현」이라 미달** | ⛔ **미충족** |

⚠️ **⑤는 나머지와 성질이 다르다.** 나머지는 「만들었나」이고 ⑤는 「지금 0인가」다.
구조가 다 갖춰져도 요청이 하나 돌고 있으면 복원하면 안 된다. 그래서 `RESTORE_STATE` 의
기본값이 `false` 이고, 참으로 만드는 길은 **`drainReport()` 의 질의 결과** 하나뿐이다.

⚠️ **④를 「write drain」이라고 줄여 부르지 않는다.** 이름이 범위를 정한다 — 4판이 정확히
그 이름 때문에 읽기를 빠뜨렸다. **`user-data drain`** 이라 부른다.

#### ⛔ 6판 정정 — 이 gate 는 **복원을 막지 못한다** (2026-08-19)

`worker/ops.js` 의 머리말은 「문서가 아니라 실행되는 검사다」라고만 적었고, 그 문장은
**이 코드가 복원을 막는다**고 읽힌다. **막지 못한다.**

- `wrangler d1 time-travel restore` 는 **계정 권한으로 실행되고 앱 코드를 지나지 않는다.**
  대시보드에서도 같은 일을 할 수 있다.
- 그러므로 `restoreGate()`·`restorePreflight()`·`beginRestore()` 는 **사전점검**이다.
  반환값은 언제나 **`restoreAllowed: false`** 이고, 그 이유를 함께 돌려준다.
- 실제 통제는 코드 밖에 있다 — **계정 권한 · 승인 절차 · 옛 배포 제거**(§10-8-1).

그래도 조건을 코드에 두는 것은 옳다. 사람이 기억해야 도는 규칙은 언젠가 잊히고, 여기 있는
조건은 **질의 가능한 증거**로 바뀌기 때문이다. 바뀐 것은 **주장의 크기**뿐이다.

**6판이 닫은 우회로**(위협 42): `restoreGate(state)` 가 **임의의 객체**를 그대로 믿어, 아홉을
전부 `true` 로 적은 객체 하나면 통과했다 — 그리고 **테스트가 정확히 그것을 「gate 가 열린다」로
확인하고 있었다.** 지금은 **코드가 소유한 조건을 인자로 바꿀 수 없고**, 밖에서 오는 것은
`drainReport()` 의 질의 결과(⑤) 하나뿐이다. 재개방 판정(`reopenReport`)도 대상 목록을 인자로
받지 않고 **주 D1 과 ledger 를 직접 읽어 스스로 만든다** — 예전에는 `targets: []` 하나면
「잔여 0 · 살아 있는 계정 0」이라 `canReopen: true` 였다. `setMode(env, "open")` 도 보고서를
받지 않고 **그 자리에서 다시 판정한다.**

**금지 상태에서 복원이 필요해지면**(예: 운영 사고로 데이터가 손상됐다) **임의로 실행하지 않고
사고 대응으로 승격한다** — 작업을 멈추고, 상태를 기록하고, **사용자 승인과 별도 설계 검토를
받는다.** "이번만 예외"는 없다. 예외를 한 번 만들면 그 예외가 절차가 된다.

> 이 금지는 `CLAUDE.md` §9 와 `docs/HANDOFF.md` §4-3 의 「지금은 복원 금지」와 **같은 사실**이다.
> 2판까지는 "절차를 지키면 복원할 수 있다"로 읽힐 여지가 있었고, 그것이 3판에서 고친 것이다.

### 10-8. 주 D1 **조건부** 복원 11단계 (Time Travel · 수동 백업 공통) — **§10-8-0 충족 후에만**

**절차는 1~11 의 11단계다**(0번은 절차가 아니라 **전제 조건 확인**이다). 하나라도 건너뛰면
재개하지 않는다. **§10-8-0 의 ①~④ 가 충족되기 전에는 1번부터 시작하지 않는다.**

| # | 단계 | 확인 |
|---|---|---|
| 0 | **금지 gate 확인**(§10-8-0) — 전역 drain 이 구현·검증됐는가 | 아니면 **여기서 끝난다.** 사고 대응으로 승격 |
| 1 | ⛔ **`restore_closed` 진입**(§10-7) + ledger `mode='restore_closed'`, `epoch+1`(§10-9 Q4). **읽기·세션 인증까지 막는다** | `/api/ready` → `mode:"restore_closed"`·`ready:false`·**503** |
| 2 | **배포 세대 확인** — 지금 도는 코드가 lease·drain 로직을 아는 세대인가. 배포 고유 주소와 canonical 을 **둘 다** 본다 | 세대가 다르면 중단 |
| 3 | 쓰기가 **진입**하지 못하는지 확인 — `/api/cb`·`/api/exchange`·`GET /api/friends`·`DELETE /api/me` 를 직접 찔러 503 을 본다 | **표를 믿지 말고 찔러 본다.** 이것은 진입 차단 확인이지 drain 확인이 아니다 |
| 4 | **drain — 이미 들어온 요청이 빠졌는지 확인.** ① 삭제 saga: 활성 lease 0건(§10-9 Q5) ② **나머지 10개 경로: 전역 drain 지표 0**(§10-8-0 ①) 을 **둘 다** 본다. 0이 된 시각을 `drained_at` 에 적는다 | **lease 0건만으로는 통과하지 못한다.** 고정 sleep 은 증거가 아니다 |
| 5 | 복원 **전** bookmark 기록(`wrangler d1 time-travel info`) 또는 현재 상태 백업 | 되돌릴 수단 확보 |
| 6 | **reconciliation 실행**(§10-4, promote-only) — `drained_at` 이 현재 epoch 에 있어야 실행된다 | 증거 없으면 **거부된다** |
| 7 | 복원 실행 — **주 D1 만.** 표식 DB 는 건드리지 않는다(§10-6) | — |
| 8 | 복원된 `users.id` 를 **페이지 단위**(예: 500행)로 읽어 HMAC → **confirmed 표식과 대조**. 여러 key version 을 **전부** 시도하고 결과를 **UID 로 중복 제거**(§10-6-2) | **고유 UID 목록** 확보 |
| 9 | 일치 계정 `DELETE FROM users WHERE id IN (…)` → CASCADE | **지운 행 수 == 고유 UID 수** |
| 10 | 잔여 검사 — `books`·`friendships`·`invite_codes`·`sessions` 에 그 uid 가 0건 | 0이 아니면 재개 금지 |
| 11 | 유지보수 해제(`mode='open'`) 후 `/api/ready` 가 200 인지 확인 | — |

**감사 로그에 남기는 것**: 시각 · 복원 대상 시점 · 대조한 계정 수 · 일치 수 · 삭제 행 수 ·
키 버전 · 실행자. **남기지 않는 것**: 실제 `uid` · `provider_subject` · `mark` 원문 · 별명 · 단어.

> **표식을 「익명정보」라고 부르지 않는다.** 복원된 DB 와 대조하면 특정 개인을 가릴 수 있으므로
> **가명·최소 식별값**으로 취급한다. 법률 검토 자료에 이 성질을 그대로 적었다(L9).

### 10-9. **quiescence — 「막기 시작했다」와 「빠져나갔다」는 다른 말이다** (3판 신설)

2판은 `MAINTENANCE=1` 과 `/api/ready` 503 확인만으로 "모든 쓰기가 멈췄다"고 간주했다.
**틀렸다.** 유지보수 검사를 **이미 통과한** 요청은 그 뒤에도 DB 를 쓴다.

#### 10-9-1. 공격·실패 순서 (신규 Critical — 위협 32)

```
1  사용자가 [계정 삭제] 를 누른다. 요청이 유지보수 검사를 **통과**한다(그때는 아직 open).
2  saga 1단계 pending 기록 성공 → 2단계(주 D1 삭제) **직전에 요청이 멈춘다**
   (콜드 스타트·D1 지연·재시도 백오프·엣지 스케줄링 — 이유는 여럿이다)
3  운영자가 유지보수를 켠다
4  /api/ready 가 maintenance:true · 503 을 답한다        ← 여기서 "멈췄다"고 믿었다
5  reconciliation 이 돈다. 계정 X 는 아직 살아 있다 → (2판) pending 을 **지운다**
6  1번 요청이 재개되어 주 D1 의 계정 X 를 **삭제한다**
7  saga 4단계 confirmed UPDATE → 표식이 없으므로 **0행**
────
8  계정도 없고 표식도 없다.  복원하면 X 가 되살아나고 대조에 걸리지 않는다.
```

**5번이 promote-only(§10-4)로 사라졌으므로 이 경로의 Critical 결과는 3판에서 이미 닫혔다.**
7번의 UPDATE 도 표식이 남아 있어 성공한다. 그러나 **quiescence 문제 자체는 남는다** —
6번의 쓰기가 **복원이 끝난 뒤에 시작되면** 복원 결과가 오염되고, 그 오염은 삭제 불변식과
무관하게 위험하다. 그래서 절차 요건을 따로 세운다.

##### ⚠️ 4판 정정 — **D1 이 취소하는 것과 취소하지 못하는 것**

3판은 "쓰기가 **복원 도중에** 착지한다"고 적었다. **부정확하다.** 공식 문서는 이렇게 말한다
(<https://developers.cloudflare.com/d1/reference/time-travel/>, 2026-08-17 확인):

> "Queries in flight will be cancelled, and an error returned to the client."
> "Restoring a database to a specific point-in-time is a _destructive_ operation, and
> overwrites the database in place."

즉 **이미 D1 에 들어간 query·transaction 은 D1 이 취소하고 클라이언트에 오류를 준다.**
**Worker 요청 전체가 자동으로 사라진다는 뜻이 아니다.** 취소되는 것은 D1 쪽 query 이고,
그 요청을 들고 있는 Worker 는 계속 살아 있다. 그래서 진짜 위험은 아래 다섯이다.

| # | 실제 위험 | 왜 D1 의 취소가 못 막나 |
|---|---|---|
| 1 | 유지보수 검사를 통과했지만 **아직 D1 에 query 를 보내지 않은** Worker 요청 | 보낸 게 없으니 취소할 것도 없다. 복원이 끝난 뒤 **처음** 보낸다 |
| 2 | **외부 OAuth 호출을 기다리는** 요청(`verifyProvider` 의 토큰 교환·`/me`) | 그 대기는 D1 밖이다. 응답이 오면 그때 `users` 를 쓴다 — 복원 **후**다 |
| 3 | 복원으로 **취소된 요청을 클라이언트가 재시도**하는 경우 | 취소는 오류를 줄 뿐 재시도를 막지 않는다. 앱의 자동 저장·동기화가 그렇게 만들어져 있다 |
| 4 | **옛 배포 세대**가 복원 후 새 쓰기를 시작하는 경우 | 게이트 자체를 모른다(위협 33) |
| 5 | 복원 **전 검증을 통과한 뒤**, 복원 완료 후 늦게 시작되는 쓰기 | 검증은 그 시점의 사진이다 |

**전역 drain 이 여전히 필요한 이유가 정확히 이 다섯이다.** D1 의 취소는 **복원 순간에 DB 안에
있던 것**만 정리한다. 우리가 막아야 하는 것은 **복원 순간에 아직 DB 밖에 있던 요청**이고,
그건 Worker 쪽에서 세지 않으면 셀 수 없다.

#### 10-9-2. 단정하지 않는 것

| 단정 | 왜 틀리나 |
|---|---|
| 「`MAINTENANCE=1` 을 켰으니 쓰기가 없다」 | 플래그는 **새 요청의 진입**만 막는다. in-flight 요청은 그대로 진행한다 |
| 「`/api/ready` 가 503 이니 안전하다」 | 그 응답은 **한 인스턴스가 새 요청을 막는다**는 뜻이다. 다른 인스턴스의 진행 중 요청을 말하지 않는다 |
| 「환경 변수를 바꿨으니 전 엣지에 즉시 반영된다」 | 설정 전파와 배포 별칭 전파는 **즉시가 아니다.** 이 저장소는 프로덕션 별칭이 배포 직후 약 1분간 옛 응답을 주는 것을 **실측**했다(`HANDOFF.md` §4-6) |
| 「배포를 한 번 했으니 모든 요청이 같은 코드 세대다」 | 롤백·부분 전파·설치형 PWA 의 옛 클라이언트가 실재한다. **코드 세대는 요청마다 다를 수 있다** |
| 「N초 자면 충분하다」 | 고정 sleep 은 **가정이지 증거가 아니다.** 느린 D1 응답·재시도 하나면 넘어간다 |

#### 10-9-3. 대안 비교

| | **대안 1 — ledger epoch + 삭제 saga 한정 lease** | 대안 2 — 실행시간 상한 + 세대 확인 + drain + 재검증(운영 절차) |
|---|---|---|
| 무엇 | ledger D1 에 `maintenance(epoch, mode)` 와 `write_leases` 를 둔다. 삭제 saga 는 **한 문장으로** "mode 가 open 일 때만 lease 를 딴다". 유지보수 전환은 `mode='closed', epoch+1` → **활성 lease 0건**을 SQL 로 확인 | 모든 write 경로의 최대 실행시간을 명시하고, 배포 세대를 확인한 뒤 그 상한보다 충분히 긴 시간을 기다리고, 다시 찔러 확인 |
| 증거 | **기계가 판정한다** — `SELECT COUNT(*) FROM write_leases WHERE released_at IS NULL AND expires_at > now` == 0 | **사람이 증언한다** — 체크리스트·타임스탬프 |
| 설정 전파 의존 | **없다.** 게이트가 env 가 아니라 **ledger 행**이라 모든 인스턴스가 같은 값을 읽는다 | **있다.** env·배포 전파를 신뢰해야 한다 |
| 비용 | 삭제 1건당 ledger 쓰기 2회(획득·해제). **`PUT /book` 같은 뜨거운 경로에는 안 붙인다** | 0줄 |
| 새 실패 모드 | ledger 장애 시 삭제가 **fail-closed**(못 지운다). 그것이 옳은 방향이다 | 없음 |
| 남는 구멍 | **lease 로직이 없는 옛 코드 세대**는 lease 를 따지 않는다 → 그 세대가 도는 동안에는 증거가 불완전하다 | 위 전부 |
| 이 규모에 | 적합 — 삭제는 드문 사건이라 비용이 사실상 0 | 적합하나 **증거가 약하다** |

#### 10-9-4. 권고 — **대안 1**(단, 대안 2의 절차를 버리지 않는다)

**대안 1을 권고한다.** 이유 셋.

1. **reconciliation 의 거부 조건을 기계가 판정할 수 있다.** "증거 없이는 거부"가 성립하려면
   증거가 **질의 가능**해야 한다. 대안 2의 증거는 사람의 증언이라 거부 로직을 만들 수 없다.
2. **설정 전파를 신뢰하지 않아도 된다.** 게이트가 ledger 행이므로 어느 인스턴스가 읽어도 같다.
   `MAINTENANCE` 환경변수는 **빠른 1차 차단**으로 남기되, 정확성이 걸린 판정(삭제 saga 진입·
   reconciliation 실행)은 **ledger 행이 진실의 원본**이다.
3. **비용이 삭제 경로에만 붙는다.** 계정 삭제는 사람 일생에 몇 번이다. 뜨거운 경로
   (`PUT /book` 0.8초 자동저장)에는 붙이지 않으므로 D1 쓰기 증폭이 없다.

**그러나 대안 2의 운영 절차를 버리지 않는다.** 대안 1에는 위 표의 「남는 구멍」이 있다 —
**lease 로직이 배포되기 전 코드 세대, 그리고 롤백된 세대는 lease 를 따지 않는다.** 그래서
복원 절차(§10-8)는 배포 세대 확인과 실제 찔러보기를 **계속 요구한다.** 두 대안은 배타적이지
않고, 대안 1이 증거를 만들고 대안 2가 그 증거의 사각을 덮는다.

#### 10-9-5. 설계 요구사항 — **3판 당시의 안(역사 기록)**

> ⚠️ **아래 SQL 과 Q1~Q12 는 「삭제 saga 에만 임차증을 붙이는」 3판 안이다.** 결정 A′(§10-9-8)와
> 2026-08-18 의 정리 크론 수정으로 **여러 곳이 실제 코드와 다르다.** 남겨 두는 것은 그때 무엇을
> 왜 골랐는지가 §10-8-0 의 근거이기 때문이고, **현재 규칙으로 읽으면 안 된다.**
>
> | 여기 적힌 것 | 현재 코드 |
> |---|---|
> | `mode IN ('open','closed')` | **3상태** `open`·`maintenance`·`restore_closed`(§10-7) |
> | `released_at` 컬럼 + `write_leases_active` 인덱스 | **둘 다 없다.** 해제는 행 DELETE 이고(A′), 남은 행은 전부 「안 끝났다」 |
> | Q1 `WHERE m.mode='open'` 고정 | 허용 모드를 **인자로 받는다** — `LEASE_MODES_REQUEST`(open·maintenance) / `LEASE_MODES_CLEANUP`(open) |
> | Q2·Q5 의 `released_at IS NULL` | 조건 자체가 없다. `drainState()` 는 **만료로 거르지도 않는다**(만료 미해제 = `stale`) |
> | Q5 의 `expires_at > ?` | **뺐다.** 만료를 자동 해제로 치면 「모르는 것」이 0으로 세어진다 |
> | Q9 「해제는 `released_at` 을 적는다」 | **행을 지운다** |
> | Q11 「다른 10개 경로에는 증거가 없다」 | 온라인 workload **전부**가 임차증을 든다(§10-9-6 A-1·A-2) |
> | 「임차증은 삭제 saga 만 딴다」 | HTTP 요청 하나에 하나 · 정리 크론 실행 하나에 하나 |
>
> 현재 스키마의 원본은 `worker/ledger-schema.sql`, 현재 규칙의 원본은 §10-9-8 의 A′ 표다.

```sql
-- ledger D1 (주 D1 아님)
CREATE TABLE maintenance (
  id     INTEGER PRIMARY KEY CHECK (id = 1),   -- 행 하나
  mode   TEXT NOT NULL CHECK (mode IN ('open','closed')),
  epoch  INTEGER NOT NULL,
  closed_at INTEGER, drained_at INTEGER        -- drained_at 이 곧 증거다
);
CREATE TABLE write_leases (
  lease_id    TEXT PRIMARY KEY,
  epoch       INTEGER NOT NULL,
  started_at  INTEGER NOT NULL,
  expires_at  INTEGER NOT NULL,   -- started_at + LEASE_TTL
  released_at INTEGER
);
CREATE INDEX write_leases_active ON write_leases(released_at, expires_at);
```

| # | 요구사항 |
|---|---|
| Q1 | 삭제 saga 는 **진입 시 lease 를 딴다.** 획득은 **한 문장**이다: `INSERT INTO write_leases … SELECT ?,m.epoch,?,? FROM maintenance m WHERE m.mode='open'`. 행이 안 생기면(=closed) **503**. 읽고 나서 쓰지 않는다 |
| Q2 | saga 의 **모든 ledger 쓰기**는 자기 lease 가 살아 있을 때만 통과한다(fencing): `… AND EXISTS (SELECT 1 FROM write_leases WHERE lease_id=? AND released_at IS NULL AND expires_at > ?)` |
| Q3 | saga 는 **주 D1 삭제 직전에 fencing 을 한 번 더 확인**한다. 실패하면 삭제하지 않고 503 으로 끝낸다(pending 은 남는다 — 남는 것이 안전한 쪽) |
| Q4 | 유지보수 전환: `UPDATE maintenance SET mode='closed', epoch=epoch+1, closed_at=?` → 이 순간부터 **새 lease 를 딸 수 없다** |
| Q5 | drain: `SELECT COUNT(*) FROM write_leases WHERE released_at IS NULL AND expires_at > ?` 가 **0** 이 될 때까지 기다린다. 0이 된 시각을 `drained_at` 에 적는다 |
| Q6 | `LEASE_TTL` 은 **Worker 최대 실행시간보다 길게** 잡는다. 짧으면 살아 있는 요청의 lease 가 먼저 만료돼 drain 이 거짓으로 0이 된다. 값은 구현 단계에서 실측해 정한다 — **여기 숫자를 적어 두고 맞다고 하지 않는다** |
| Q7 | **reconciliation 은 `mode='closed'` · **현재 epoch** · 활성 lease **0건**일 때만 실행한다.** 아니면 **거부**한다. 이것이 "증거 없이는 거부"의 구현이다. ⚠️ **이 조건은 reconciliation 에 충분하고, 주 D1 복원에는 부족하다** — 근거는 §10-4 의 범위 표. `users` 행을 지우는 경로가 삭제 saga 하나뿐이라 reconciliation 의 판정은 lease 로 완전히 덮이지만, 복원은 **11개 경로 전부**의 drain 을 요구한다(§10-8-0) |
| **Q7b** | **stale pending 의 수동 제거**(§10-5-1 S6)는 Q7 의 세 조건에 더해 **④ 그 UID 가 주 D1 에 여전히 존재함을 그 자리에서 재확인** 해야 한다. 재확인 없이 지우면 **그 사이 삭제된 계정의 표식을 지우는 것**이 되어 위협 27 이 되살아난다 |
| Q8 | `epoch` 불일치 요청은 **fail-closed** — 옛 epoch 의 lease 를 든 요청은 Q2 의 fencing 을 통과하지 못한다 |
| Q9 | 해제: saga 가 끝나면 `released_at` 을 적는다. 실패해도 `expires_at` 이 결국 만료시킨다 |
| Q10 | 만료된 lease 행 정리는 §10-5 ①과 같은 자리(삭제 saga 끝)에 붙이고 **실패를 삼킨다** |
| Q11 | **고정 sleep 을 증거로 쓰지 않는다.** 삭제 saga 에 대한 증거는 Q5 의 카운트다. sleep 은 폴링 간격일 뿐이다. **다른 10개 경로에 대해서는 이 설계에 증거가 없다**(§10-9-6) |
| Q12 | 복원 절차는 **여기에 더해** 배포 세대 확인과 실제 찔러보기를 요구한다(§10-8 2번) — Q1~Q11 은 lease 를 아는 코드 세대에서만 성립한다 |

> **이 설계는 삭제 불변식만 다룬다.** Q3 의 확인과 실제 `DELETE FROM users` 사이에는 여전히
> 틈이 있고, 주 D1 과 ledger 사이에 **공통 트랜잭션이 없기 때문에** 그 틈을 코드로 없앨 수
> 없다. 그래서 **promote-only(§10-4)가 1차 방어이고 lease 는 2차**다 — 틈으로 새어 나간
> 요청이 있어도 표식이 지워지지 않으므로 「표식 없는 삭제」가 되지 않는다.
> **어느 하나만으로 안전하다고 적지 않는다.** 그리고 **이 lease 로는 복원 안전성을 말할 수
> 없다** — 이유는 바로 아래 §10-9-6 이다.

#### 10-9-6. **주 D1 접근 경로 전수 분류** (3판 신설 · **2026-08-18 결정 A′ 와 정리 크론 반영으로 다시 씀**)

> **3판·4판에서 이 절의 제목은 「deletion lease 가 포괄하지 못하는 쓰기 경로 전수」였다.**
> 그때는 임차증이 삭제 saga 하나에만 붙어 있어서, 아래 A-1 표의 ○ 가 **하나뿐**이라는 사실이
> 곧 §10-8-0 의 근거였다. 결정 A′ 로 그 사실이 바뀌었다.
>
> ⚠️ **그런데 표를 `worker/index.js` 만 세는 채로 두었더니 「HTTP 11개 = 전체 workload」로
> 읽혔다.** 실제로 그렇게 읽어서 **정리 크론이 분류 밖에 남았고**, 크론이 주 D1 을 지우는
> 도중에 `drainState()` 가 `open:0 · drained:true` 를 답했다(2026-08-18 재현 · **T47b**).
> 그래서 이 절은 **`worker/` 디렉터리 전체**를 세 부류로 나눈다. 새 Worker 가 생기면
> 이 표에 행을 더한다 — `scripts/test-docs.mjs` 가 주 D1 을 만지는 파일이 여기 이름을
> 올렸는지 대조한다(정규식이라 **완전한 증명은 아니다**. 빠뜨림을 줄이는 장치다).

| 부류 | 무엇 | 임차증 | 무엇이 지켜주나 |
|---|---|---|---|
| **A. 온라인 workload** | 사람의 요청이나 cron 이 부르면 도는 것 — HTTP Worker(A-1) · Scheduled Cleanup(A-2) | **필수** | `acquireLease()` → 주 D1 첫 접근 **전** 획득 → 가장 바깥 `finally` 에서 DELETE 해제 |
| **B. 복원·운영 명령** | `worker/ops.js`. **HTTP 라우트가 아니다** — 사람이 실행하고 사람이 판정한다 | **씌우지 않는다** | 함수 자체의 선행조건. 일반 요청 임차증을 무조건 씌우면 `restore_closed` 에서 복원 작업 자체가 자기 게이트에 막힌다 |
| **C. 공개 상태 확인 예외** | `/health` · `/ready` · `/policies` (`LEASE_FREE`) | 없음 | 사용자 **행 내용을 응답하지 않는다**. 복원 중에도 답해야 하는 유일한 창구다 |

##### A-1. HTTP Worker (`worker/index.js`) — 주 D1 쓰기 **10개** (8판: 리미터가 빠졌다)

4단계 구현으로 **1·2 의 내용이 바뀌었다** — `internalUid()` 는 없어졌고(조회와 생성을 같이 해서
로그인만으로 계정이 생겼다), 그 자리에 `createAccountWithPolicy()` 가 들어와 **한 batch 에서
세 표를 쓴다.** 읽기 경로도 같은 임차증을 든다(아래 「읽기 전용」).

| # | 라우트 · 호출 경로 | 쓰는 테이블 | 유지보수 **진입** 차단 | **요청 임차증(A′)** | 이미 진입한 요청을 drain 할 수단 | 복원과 경쟁하면 |
|---|---|---|---|---|---|---|
| 1 | `GET /api/cb/:provider` **가입 갈래** → `createAccountWithPolicy()` `:670` | **`users`** · `policy_events` · `consumed_signup_states` (한 batch) | ○(§10-7) | **○** | **있다** — `drainState()` | **외부 OAuth 응답을 기다리다가 복원 후에 계정을 만든다**(§10-9-1 의 위험 2). 복원본에는 없는 행이라 이후 상태가 갈린다 |
| 2 | `GET /api/exchange/:provider` → 같음 · **로그인 갈래**는 `consumeSignupState()` `:697` | 위와 같음 · `consumed_signup_states` | ○ | **○** | **있다** — `drainState()` | 위와 같음. 소비 표식이 사라지면 **같은 state 가 다시 쓰일 수 있다** |
| 3 | 1·2 → `newSession()` `:372` | `sessions` | ○ | **○** | **있다** — `drainState()` | 복원으로 사라진 세션 토큰을 사용자가 들고 있게 된다(로그인 상태가 깨진다) |
| 4 | 1·2 → `newSession()` 의 청소 batch | `sessions` | ○ | **○** | **있다** — `drainState()` | 복원본의 행을 지운다 |
| ~~5~~ | ~~**모든 `limited()` 호출**~~ | ~~`rate_limits`~~ | — | — | — | ⛔ **8판에서 주 D1 을 떠났다**(위협 49 · §0-8-1). 카운터는 ledger `rate_limits` 이고 게이트와 한 문장으로 묶여 있다 — 그래서 이 행이 「임차증 밖 주 D1 쓰기」의 **마지막 예외**였다 |
| 6 | **`GET /api/friends`** → `myCode()` `:780` | **`invite_codes`** | ○ | **○** | **있다** — `drainState()` | 복원 후 없는 코드가 링크로 돌아다닌다 |
| 7 | `POST /api/friends/code` → `rotateCode()` `:790` | `invite_codes` | ○ | **○** | **있다** — `drainState()` | 위 + 폐기 행이 되살아난다 |
| 8 | `PUT /api/book` `:1269` | `books` | ○ | **○** | **있다** — `drainState()` | **사용자 단어장이 소실되거나 옛 버전으로 되돌아간다** |
| 9 | `POST /api/friends` `:1418` · `PUT /api/friends/:id` `:1484` · `DELETE /api/friends/:id` `:1507` | `friendships` | ○ | **○** | **있다** — `drainState()` | 관계가 한쪽만 남는다 |
| 10 | `DELETE /api/session` → `killSessions()` `:411` | `users`(session_version)·`sessions` | ○ | **○** | **있다** — `drainState()` | 로그아웃이 무효가 된다 — **탈취된 세션이 되살아난다** |
| 11 | **`DELETE /api/me`** → 삭제 saga `:1331` | `users`(+CASCADE) · ledger | ○ | **○** | **있다** — `drainState()` + saga fencing(§10-9 Q5) | §10-9-1 의 그 경로 |

**읽기 전용**: `/api/book` · `/api/me` · `/api/friends/:id/book` ·
`GET /api/login/:provider`(302 와 서명뿐) — **전부 임차증을 든다**(T6b). 5판이 잡은 것이
이것이다: 4판 설계는 「DB 를 안 쓴다」는 이유로 읽기를 놓아 주었고, 그러면 복원으로 되살아난
탈퇴자의 단어장이 그대로 읽힌다(위협 36).
**임차증 없는 것은 `/api/health` · `/api/ready` · `/api/policies` 셋뿐이다**(아래 C).

##### A-2. Scheduled Cleanup (`worker/cleanup/index.js`) — 주 D1 쓰기 **2개** (8판: C4 가 ledger 로 갔다)

**별도 배포 Worker 이고 cron 으로만 돈다.** HTTP 핸들러가 없다고 해서 분류에서 빠지지 않는다 —
지우는 대상이 사용자 데이터이기 때문이다.

| # | 작업 | 만지는 테이블 | 유지보수 **진입** 차단 | **임차증** | 복원과 경쟁하면 |
|---|---|---|---|---|---|
| C1 | 만료된 소비 표식 정리 | **주 D1** `consumed_signup_states` | ○ — 획득 실패면 그 자리에서 끝 | **○** | 만료 전 표식이 사라지면 **replay 창이 다시 열린다** |
| C3 | 만료·폐기 세션 정리 | **주 D1** `sessions` | ○ | **○** | 복원본의 세션 행을 지운다 — 로그인 상태가 깨진다 |
| C4 | 만료 리미터 행 정리 | **ledger** `rate_limits`(8판 이전 → 주 D1 이었다) | ○ | **○** | 한도 카운터가 뒤섞인다 |
| C2 | 확정된 표식 정리 | ledger `deletions` | ○ | **○** | 표식이 사라지면 재삭제 대상을 못 고른다 |
| — | 실행 기록 | ledger `cleanup_runs` | — | — | 운영 기록. 사용자 데이터가 아니다 |

> 임차증은 **실행 하나에 하나**다(문장마다가 아니다). 획득은 `LEASE_MODES_CLEANUP = ['open']`
> 한 문장이고, 실패하면 **주 D1 을 한 줄도 읽지 않고** 끝난다. 이미 시작한 실행은 중간에
> 끊지 않는다 — HTTP 요청과 같다. 대신 **끝날 때까지 `drained` 가 참이 되지 않는다.**
>
> ⚠️ `LEASE_MODES_REQUEST` 는 `open`·`maintenance` 인데 크론은 `open` 만이다. `maintenance` 는
> **읽기를 허용하는 상태**라 요청은 계속 추적돼야 하지만(안 그러면 허용된 읽기가 추적 밖에서
> 돈다), 크론은 급하지 않아 **다음 시간에 돌면 된다.** 두 상수를 바꿔 넘기면 T47d 가 잡는다.
>
> ⛔ **`write_leases` 를 지우는 정리 대상은 없다.** 해제가 행 삭제라 표에 남은 행은 전부
> 「아직 안 끝났다」의 증거이고, 시간으로 지우면 그 증거가 사라져 복원 금지가 저절로 풀린다.

##### B. 복원·운영 명령 (`worker/ops.js`) — 주 D1 읽기 **4곳**

**요청 임차증을 씌우지 않는다.** 이 명령들은 `restore_closed` 이후에 운영자가 **의도적으로**
실행하는 것이라, 요청과 같은 규칙을 씌우면 복원 작업이 자기 게이트에 막힌다. 대신 각 함수가
**선행조건을 스스로 검사**하고, 미달이면 `ok:false` 를 돌려준다(강제 진행 플래그가 없다).

| 함수 | 주 D1 접근 | 선행조건 |
|---|---|---|
| `reconcile()` | `SELECT id FROM users` 페이지 스캔 | `mode ≠ open` **그리고** 활성 임차증 0건. 승격만 하고 **지우지 않는다**(§10-4) |
| `removeStalePending()` | `SELECT id FROM users` | 위 둘 **+ 운영자 판정(`confirmedByOperator`) + 계정 존재 재확인 수단**. 다섯 조건 전부 |
| `reopenReport()` | `sessions`·`books`·`invite_codes`·`friendships`·`users` **COUNT** | 읽기 전용. 재개 가능 판정을 위한 잔여 검사(§10-8 9~11번) |
| `scanUserMarks()` | `SELECT id FROM users` 페이지 스캔 | 복원 **후** 재삭제 대상 산출. `restoreTargets()` 는 순수 함수다 |

##### C. 공개 상태 확인 예외 (`LEASE_FREE`)

`/health`(DB 를 아예 안 본다) · `/ready`(`COUNT(*)` 집계만. **행 내용을 읽지 않는다**) ·
`/policies`(빌드에 박은 상수). 여기서 임차증을 따면 `restore_closed` 에서 획득이 거부돼
**운영자가 상태를 볼 수단이 사라진다.**

#### 10-9-7. **3단계에서 확정하는 범위 결정 8건**

| # | 결정 |
|---|---|
| 1 | **deletion lease 는 삭제 saga 와 삭제 표식 불변식만 보호한다.** 다른 용도로 확대 해석하지 않는다 |
| 2 | **deletion lease 0건은 전체 DB 쓰기 quiescence 를 증명하지 않는다.** 증명하는 것은 「삭제 saga 가 진행 중이 아니다」뿐이다 |
| 3 | **`/api/ready` 의 503 · `MAINTENANCE` 플래그 · 고정 대기 시간 중 무엇도, 셋을 합쳐도 전역 drain 의 증거가 아니다.** 전부 「새 요청의 진입을 막기 시작했다」만 말한다 |
| 4 | **모든 DB 쓰기 경로(§10-9-6 의 11개)를 포괄하는 검증 가능한 전역 drain 이 4단계에서 구현·검증되기 전까지 주 D1 의 `time-travel restore` 와 백업 복원을 운영상 금지한다**(§10-8-0) |
| 5 | 금지 상태에서 복원이 필요하면 **임의 실행하지 않고 사고 대응 절차로 승격**해 별도 승인·설계 검토를 받는다 |
| 6 | **「11단계를 따르면 지금도 안전하게 복원할 수 있다」는 표현을 문서에서 제거한다.** 3판에서 제거했다 |
| 7 | §10-8 의 11단계는 **「전역 drain 구현 후 활성화할 조건부 절차」**로 표시한다 |
| 8 | **ledger 재작성 13단계(§10-6)와 주 D1 조건부 복원 11단계(§10-8)는 다른 절차다.** 이름을 붙여 구분하고 같이 실행하지 않는다. ⛔ **ledger 는 제자리 `restore` 자체를 하지 않는다**(§10-6-0) |

#### 10-9-8. 전역 drain 의 **후보** — 4단계에서 비교한다 (여기서 확정하지 않는다)

셋 다 이 저장소에서 실측한 적이 없다. **지금 고르지 않는다.**

| 후보 | 얼개 | 걸리는 것 |
|---|---|---|
| **A. 전역 write lease** | §10-9-5 의 lease 를 **11개 경로 전부**에 건다 | 뜨거운 경로(`PUT /book` 0.8초 자동저장)에 ledger 쓰기 2회가 붙는다 → **D1 쓰기 증폭**. 2026-08-16 에 초대 링크 회전으로 겪은 문제와 같은 종류 |
| **B. Durable Object 카운터** | in-flight 요청 수를 DO 하나가 센다 | 새 리소스·새 과금·새 장애 지점. **비용 [?] 미확인** |
| **C. 요청 카운터(원자적 증감)** | 진입 시 +1, 종료 시 −1 을 한 행에 | 예외로 종료하면 **누수**되어 영원히 0이 안 된다. TTL 보정이 필요하다 |

각 후보의 D1 쓰기 한도(하루 10만)·비용·지연 영향은 **4단계에서 실측**한다.

##### ✅ 2026-08-18 사용자 결정 — **A′ (A안 보완)**

D안(문장 안의 게이트)을 함께 제시했으나 **단독 채택하지 않았다.** 사용자가 든 이유 넷:

| # | 이유 |
|---|---|
| 1 | 주 D1 안에 둔 게이트는 Time Travel 복원 때 **과거의 `open` 으로 함께 되돌아간다** |
| 2 | 복원 직후 다시 닫기 전까지의 **틈**과, 아직 D1 query 를 시작하지 않은 **지연 Worker 요청**을 못 막는다 |
| 3 | `restoreGate` 가 요구하는 **질의 가능한 drain** 과 read/write in-flight 0 **증거**를 주지 못한다 |
| 4 | D1 비용은 SQL 문장 수가 아니라 **`rows_written` 와 인덱스 쓰기**로 재야 하므로, 문장 수 기반 비용표는 확정 근거가 아니다 |

**A′ 의 규정** (구현: `worker/ledger.js` · `worker/index.js` · `worker/ops.js`)

| 규정 | 어디에 |
|---|---|
| 주 D1 의 사용자 데이터를 읽거나 쓰는 **작업 하나에 임차증 하나.** SQL 문장마다가 아니다 | HTTP: `export default.fetch` · 크론: `runCleanup()` |
| **세션 인증을 포함해** 주 D1 사용자 데이터에 처음 닿기 전에 획득 | 게이트 검사 **직후**, `whoAmI` **앞** |
| 모든 DB 작업이 끝난 뒤 **가장 바깥 `finally`** 에서 해제 | 같은 곳. 라우트 안의 `return` 수십 개를 믿지 않는다 |
| **「작업」에는 Scheduled Cleanup 이 포함된다**(2026-08-18 추가) | `worker/cleanup/index.js` 의 `runCleanup()` — 획득 실패면 주 D1 을 **한 줄도** 안 읽고 끝낸다 |
| 허용 모드를 **호출부가 인자로 말한다.** 요청과 크론은 같은 규칙이 아니다 | `LEASE_MODES_REQUEST`(open·maintenance) / `LEASE_MODES_CLEANUP`(open). 값은 신뢰된 상수만, SQL 에는 placeholder 로만 들어간다 |
| `restore_closed` 전환 이후 신규 획득을 **원자적으로** 거부 | `acquireLease()` — 어느 목록에도 `restore_closed` 가 없다 |
| 활성 임차증이 0이 아니면 drain·restore **거부** | `drainReport()` → `RESTORE_STATE.noActiveLeases` → `beginRestore()` |
| **TTL 만료를 자동 해제로 치지 않는다.** 미해제는 `stale` 로 세고 복원을 계속 차단 | `drainState()` — `expires_at` 으로 거르지 않는다 |
| 제외 목록: `/health` · `/ready` · `/policies` | `LEASE_FREE`. 근거는 그 상수의 주석(사용자 데이터 미접근 · 복원 중에도 답해야 함 · 읽기 전용) |

**해제는 행을 지운다**(UPDATE 가 아니다). 요청마다 행이 쌓이면 정리 크론(시간당 200행)보다
빨리 자라기 때문이다. 미해제 행은 **지우지 않는다** — 그것이 `stale` 의 유일한 증거다.

**비용** (`node scripts/measure-lease-cost.mjs`, `PUT /book` 200회) — **세 숫자를 구분한다.**

| | ① SQL 문장/작업 | ② 기본 행 변경/작업 | ③ 인덱스 포함 **추정** |
|---|---|---|---|
| 임차증 없음 | 0 | 0 | 0 |
| **A′** | **2**(획득·해제) | **2** | **4** |

| 숫자 | 어떻게 나왔나 | 무엇이 아닌가 |
|---|---|---|
| ① 문장 수 | 셰임이 **실제로 센다** | — |
| ② 기본 행 변경 | 셰임이 `meta.changes` 로 **실제로 센다** — INSERT 1행 · DELETE 1행 | D1 의 회계 단위가 아니다 |
| ③ 인덱스 포함 | **현재 스키마에서 계산한 추정치.** `write_leases` 의 인덱스는 `lease_id` PK 하나뿐이라 문장당 +1 | ⛔ **실측이 아니다** |

> **Cloudflare 공식 문서는 인덱스 갱신이 추가 `rows_written` 으로 계산된다고 명시한다**
> (D1 pricing — `developers.cloudflare.com/d1/platform/pricing/`, 2026-08-18 확인).
> 그래서 ③을 세는 것 자체는 맞다. 다만 **행 수를 세는 방식이 우리 계산과 같은지는 확인하지
> 않았다** — 확정 숫자는 원격에서 `meta.rows_written` 을 받아야 나오고, 원격 작업은 별도 승인
> 대상이다. 실제 지연시간도 마찬가지다(셰임은 in-memory sqlite 라 D1 왕복이 없다).
>
> 2026-08-18 에 `write_leases_active(released_at, expires_at)` 인덱스를 **없앴다.** 해제가 행
> 삭제라 `released_at` 이 항상 NULL 이었고, 조회는 `COUNT(*)` 와 PK 조회뿐이라 인덱스가 고를
> 것이 없었다. 순수한 쓰기 비용이었고 ③이 6 에서 4 로 내려갔다.

주 D1 쪽 쓰기는 늘지 않는다(임차증은 ledger D1 에만 쓴다).

⚠️ **규모 한계 — 상한을 보수적으로 잡는다.** D1 무료 한도는 하루 10만 행 쓰기다.
**기본 행(②) 기준이면 하루 5만 작업, 인덱스 포함 추정(③) 기준이면 하루 2만 5천 작업**에서
임차증만으로 한도를 태운다. **어느 쪽도 확정 사실이 아니다** — 실측 전에는 **더 보수적인 쪽
(2만 5천)** 을 기준으로 본다. 지금은 사용자 0명이라 여유가 있지만 **일 활성 수백 명 규모에서
다시 계산해야 한다** — 그때의 선택지는 유료 플랜 또는 제외 목록 재검토다.
숫자를 재지 않고 「괜찮다」고 적지 않는다.

⚠️ 여기에는 **정리 크론의 임차증 2문장**이 안 들어 있다. 크론은 시간당 1회라 하루 24회 ×
2문장이고, 위 상한에 견주면 무시할 수 있다.

**D안은 폐기가 아니라 보류다.** 필요하면 A′ **위의 보조 방어**로 다시 검토한다.
**D안을 근거로 복원 금지를 해제하지 않는다.**

---

## 11. 보안 위협 모델

심각도: **C**ritical(계정 탈취·타인 데이터 노출) / **H**igh(기록 위조·삭제 무력화) / **M**edium.

| # | 위협 | 심 | 공격·실패 순서 | 막는 설계 | 필요한 회귀 테스트 | 남는 위험 |
|---|---|---|---|---|---|---|
| 1 | 공격자가 만든 OAuth callback 링크 | C | 공격자가 자기 `code&state` 링크를 피해자에게 보냄 | `bound()` — `shh_t` 쿠키 대조를 **code 교환 전에**(`:751`·`:781`). **이미 있다** | `test-friends` 76~84 유지 | 없음 |
| 2 | **OAuth state 재사용 — 2판 정정** | M | ⓐ 같은 state + **같은 code** → 제공자가 거부 ⓑ 같은 state + **새 code**(authorize URL 재실행) → **10분 안에는 통과한다** | ⓐ `code` 1회용 ⓑ **막지 않는다** — 브라우저 결속(`shh_t`) + 10분 만료로 **감수**(§4-A). 결과는 같은 계정 하나(`UNIQUE`) | `test-signup` **9a**(같은 code → 실패) · **9b**(새 code → 통과함을 확인) · **9c**(다른 브라우저 → 400) — 셋을 **구분해서** 잰다. **T24~T29**(§13-5)가 같은 사실을 6방향에서 고정한다 | **ⓑ 는 남는 위험이다.** 같은 `pv` 로 10분 안에 여러 번 시도 가능. 없애려면 인증 없는 자리에 서버 쓰기가 필요하다(2026-08-11 에 일부러 없앤 것) |
| 3 | 브라우저 결속 쿠키 탈취 | C | `shh_t` 를 훔쳐 다른 브라우저에서 사용 | `HttpOnly`·`Secure`·10분 만료·`Path=/api` | 기존 | 기기 전체가 털린 경우는 못 막는다 |
| 4 | 가입 대기 토큰 탈취/재사용/만료 | C | — | **C안에는 그런 토큰이 없다** | — | B안을 고르면 §6 의 항목이 전부 살아난다 |
| 5 | 두 탭 동시 가입 | H | 두 탭이 같은 제공자 계정으로 동시 제출 | `UNIQUE(provider,provider_subject)` + `ON CONFLICT DO NOTHING` + `WHERE EXISTS` (§5-1) | `test-signup`: 동시 2건 → `users` 1행, `policy_events` **정확히 N행**(§8-0)이고 집합의 각 원소가 한 번씩 | 없음 |
| 6 | 신규·기존 분기 경합 | H | 조회 직후 남이 먼저 만든다 | 판정을 **INSERT 문장 안**에 두고 batch 뒤 재조회 | 위와 같음 | 없음 |
| 7 | 계정만 생기고 정책 이벤트가 없음 | H | batch 중간 실패 | `batch()` = 단일 트랜잭션 → 전체 롤백 | `test-signup`: 이벤트 INSERT 강제 실패 → `users` 0행 | 없음 |
| 8 | 정책 이벤트만 있고 계정이 없음 | H | 위와 반대 | 같은 batch + `WHERE EXISTS` | 같음 | 없음 |
| 9 | 클라이언트가 `document_version` 위조 | H | 위조한 해시를 제출 | 서버는 **자기 상수만** 기록. 클라이언트 값은 문지기로만 | `test-signup`: 위조 `pv` → 409, DB 무변화 | 없음 |
| 10 | 클라이언트가 `recorded_at` 위조 | M | 시각을 실어 보냄 | 서버 `Date.now()` 만 사용. 본문의 시각 필드를 읽지 않는다 | `test-signup`: 시각 필드 무시 확인 | 없음 |
| 11 | 서비스워커가 옛 정책 문서 제공 | H | 옛 캐시의 방침을 보고 가입 | ① 클라이언트가 렌더할 파일을 해시해 서버 값과 대조(fail-closed) ② 서버 `pv` 불일치 409 | `test-policies` + `test-client`: 해시 불일치면 버튼 안 그림 | 사용자가 캐시를 못 지우면 가입을 못 한다(**안전한 실패**) |
| 12 | 가입 API CSRF | C | 남의 사이트가 `POST /signup/start` | 기존 Origin 검사(`:830-839`)가 모든 non-GET 에 적용 | `test-friends` 82·83 무늬로 `/signup/start` 추가 | 없음 |
| 13 | Origin 없는 가입 요청 | C | `curl` 로 Origin 없이 | 같은 검사가 **없는 것도 막는다** | 위와 같음 | 없음 |
| 14 | 외부 Origin 가입 요청 | C | 다른 도메인에서 | `allowed()` | 위와 같음 | 없음 |
| 15 | 가입 endpoint 레이트리밋 누락 | H | 가입 시작을 무한 반복 | **전용 `signup` 버킷.** `WRITE_ROUTES` 에 넣지 않는다(이중 계수 재발 방지) | `test-friends` 무늬로 한도 초과 429 | 공유 IP 는 한도를 나눠 쓴다 |
| 16 | 가입 취소 후 임시값 잔존 | M | 정책 화면에서 이탈 | **서버 상태가 없다.** `shh_t` 는 10분 만료 | `test-client`: 이탈 후 localStorage 잔존 0 | 없음 |
| 17 | 닉네임·정책 문구 XSS | C | `<script>` 를 별명에 | 별명은 `textContent` 로만 렌더(현재 규칙). 정책 문서는 **우리가 쓴 정적 파일** | `test-client`: `innerHTML` 사용 자리 검사 | 정책 파일에 스크립트를 우리가 넣으면 막을 것이 없다 → CSP 가 2차 방어 |
| 18 | 제공자 회원번호 로그 노출 | H | 오류 로그에 subject | 지금도 안 찍는다(`:481` `why()` 80자·`pathTemplate`) | `test-friends` 로그 검사 무늬 유지 | 없음 |
| 19 | 삭제 표식으로 계정 삭제 DoS | H | 남의 `mark` 를 위조해 삽입 | `mark` = HMAC(전용키, uid). 키도 uid 도 모른다. ledger 쓰기는 worker 만 | `test-deletion-ledger`: 임의 mark 는 대조에 안 걸림 | ledger 키가 새면 성립 → 키 회전 절차 |
| 20 | pending 을 confirmed 로 위조 | H | ledger 직접 조작 | ledger 접근 권한 = 운영자. 주 D1 조건부 복원 11단계의 **9번이 삭제 행 수를 검증**(§10-8) | `test-deletion-ledger`: pending 은 재삭제에 안 쓰임 | 운영자 권한 탈취는 못 막는다 |
| 21 | 복원 후 삭제 계정 부활 | C | 복원 실행 | **1차: §10-8-0 의 복원 금지 gate**(전역 drain 전에는 복원 자체를 안 한다) · 2차: 금지가 풀린 뒤의 §10-8 조건부 11단계(유지보수 → 진입 차단 확인 → **drain 확인** → reconciliation → 복원 → 대조 → 재삭제 → 잔여 검사) | `test-deletion-ledger` 7: 복원 시뮬레이션 → CASCADE 확인 | **절차를 안 지키면 그대로 성립한다**(도구가 아니라 절차다). 그래서 현재는 **복원 자체를 금지**한다 |
| 22 | 키 교체 후 과거 표식 무효화 | H | 옛 키를 지움 | `key_version` + **보유기간 중 옛 키 삭제 금지** | `test-deletion-ledger`: 두 버전 혼재 대조 | 키를 실수로 잃으면 그 표식은 영구 무효 |
| 23 | 오래된 수동 백업 복원 | H | 30일보다 오래된 백업 | 2단계 §10 — 표식 보유기간보다 오래된 백업을 **만들지 않는다** | 자동 검사 불가(절차) | 절차 위반 시 성립 |
| 24 | 기존 앱 버전이 새 가입 API 를 잘못 호출 | M | 옛 PWA 가 `GET /login` 만 안다 | 신규면 `signup_required` 로 되돌림. **계정을 만들지 않는다** | `test-signup`: `pv` 없는 신규 → 계정 0행 | 옛 앱 사용자는 새로고침 전까지 가입 불가(안전한 실패) |
| 25 | OAuth 비활성 상태에서 신규 경로가 열림 | H | 시크릿 없이 배포 | `/login` 이 `id`·`STATE_KEY` 없으면 503(`:712`). `/signup/start` 도 같은 검사 | `test-signup`: 제공자 미설정 → 503 | 없음 |
| **26** | **삭제 표식 DB 를 과거로 복원** | **C** | 표식 DB 가 손상 → 운영자가 `restore` → **그 시점 이후의 confirmed 표식이 전부 사라진다** → 나중에 주 D1 을 복원하면 **그 사람들이 영영 되살아나고, 대조에 안 걸리므로 아무도 모른다** | **원칙적으로 restore 금지**(§10-6). ⛔ **제자리 restore 를 절차에서 없앴다**(5판, §10-6-0) — 되돌리면 `deletions` 뿐 아니라 **게이트(`maintenance`)·`epoch`·`write_leases` 까지 과거로 간다.** 불가피하면 **ledger 재작성 13단계** — 현재 내보내기 → **새 DB** → `deletions` 만 합집합 병합(규칙 전부는 **§10-6-1**) → **coordination 은 새로 초기화**(G3) → 대조 4종 → 바인딩 전환 뒤에만 재개. **주 D1 복원(§10-8)과 같이 실행하지 않는다** | `test-deletion-ledger` **11**(복원 시뮬레이션 → 병합 후 confirmed 개수가 줄지 않음) · **12**(병합이 `confirmed_at` 을 pending 으로 되돌리지 않음) | **절차다.** 운영자가 표식 DB 를 그냥 되돌리면 그대로 성립한다 |
| **27** | **reconciliation 경합 — 표식 없는 삭제** | **C** | reconciliation 이 "계정 X 살아 있음"을 읽는다 → 그 사이 X 가 삭제된다 → reconciliation 이 "실패한 삭제"라 보고 **표식을 지운다** → 계정은 없는데 표식도 없다 | **1차: promote-only**(§10-4) — reconciliation 이 **지우는 동작을 아예 갖지 않는다.** 지울 것이 없으므로 `t0`~`t2` 창이 성립하지 않는다 · 2차: **활성 deletion lease 0건일 때만 실행**(§10-9 Q7) | `test-deletion-ledger` **13**(활성 lease 가 있는 중 reconciliation → **거부**) · **14**(유지보수 중 삭제 요청 503) · **T2·T3**(§13-5) | 운영자가 **수동으로** pending 을 지우면 그대로 성립한다(§10-5-1 S3·S4 가 그것을 금지한다) |
| **28** | **유지보수 모드를 메서드로 판정** | **C** | `req.method !== "GET"` 으로 막는다 → 복원 중에 `GET /cb` 가 **계정을 만들고**, `GET /friends` 가 **초대 코드를 INSERT** 한다 | **허용 목록 기반 라우트 표**(§10-7). 검사는 `limited()` 보다 **앞**에 둔다 | `test-friends` 신규 블록: 유지보수 중 `GET /cb`·`GET /exchange`·`GET /friends` → **503 이고 DB 행 수 무변화** | 새 라우트를 표에 안 넣으면? → **허용 목록이라 기본값이 차단**이다 |
| **29** | **`RL_MAX` fallback 재발** | H | 새 버킷 이름을 오타내거나 등록을 잊는다 → `?? RL_MAX.write` 로 **조용히 120/분** | **fallback 자체를 제거**하고 미등록 버킷은 던지게 한다(§12) | `test-friends`: 미등록 버킷으로 `limited()` 호출 → **테스트 실패** | 없음 |
| **30** | **정책 행위 시각 왜곡** | M | OAuth 왕복이 최대 10분이라 기록 시각이 실제 행위보다 늦다 | `occurred_at`(서명된 state 안의 서버 시각) + `recorded_at`(삽입 시각) **둘 다 기록**(§7-4) | `test-signup` **19**: `occurred_at ≤ recorded_at` 이고 간격이 state 만료(10분) 이내 | **어느 쪽이 법적 증거인지는 미확정**(L11) |
| **31** | **`pending` 을 시간으로 자동 삭제** | **C** | `expires_at` 기반 정리가 pending 까지 지운다 → "삭제는 됐고 확정만 실패한" 표식이 사라져 **복원 때 부활** | pending 은 **reconciliation 판정 뒤에만** 삭제. 시간 기준은 `pending_alert_at`(경보)뿐(§10-5) | `test-deletion-ledger` **9**: `pending_alert_at` 이 지난 pending 은 **여전히 남아 있어야 한다** | 없음 |
| **32** | **deletion lease 0건을 「전역 쓰기 drain」으로 오인해 복원을 시작** | **C** | ① 운영자가 유지보수를 켠다 ② `/api/ready` 503 · 활성 lease 0건을 확인하고 "모든 쓰기가 멈췄다"고 판단한다 ③ 그러나 **이미 유지보수 검사를 통과한** `PUT /book`·`GET /cb`(외부 OAuth 응답 대기 중)·`DELETE /session`(로그아웃)·`POST /friends` 가 아직 살아 있다(§10-9-6 의 10개 경로 — **lease 를 따지 않아 카운트에 안 잡힌다**) ④ 복원 **전 검증**을 통과한다 ⑤ 복원이 실행된다 — D1 은 **그 순간 DB 안에 있던 query 만** 취소한다(공식 문서) ⑥ ③의 요청 중 **아직 D1 에 query 를 보내지 않았던 것**과 **취소 오류를 받고 재시도하는 것**이 **복원 완료 후에 쓰기를 시작한다**(§10-9-1 의 위험 1~5) ⑦ 복원본에 없는 계정·세션·단어장 버전이 섞이거나, 복원본의 행이 덮인다 ⑧ **로그아웃이 무효가 되어 폐기했어야 할 세션이 되살아나고**(경로 10), 단어장이 옛 버전으로 되돌아간다(경로 8) | **① 복원 금지 gate**(§10-8-0) — 전역 drain 이 구현·검증되기 전에는 절차를 **시작하지 않는다** ② 구현 후에는 §10-8 4번이 lease 0건 **과** 전역 drain 지표 0 을 **둘 다** 요구한다 ③ 문서에서 「lease 0건 = 안전」 표현을 제거했다(§10-9-7) | **T6·T6b·T6c**(요청이 도는 동안 활성 1 · 읽기도 추적 · TTL 만료 ≠ 해제) · **T47b·T47d**(정리 크론도 같은 임차증) · **T7**(미충족이 남으면 복원 절차 시작 **거부**) · **T1**·**T3**(§13-5) | **위 ①~⑧ 의 공격 흐름은 2026-08-17 까지의 사실이고 그대로 보존한다.** **현재(2026-08-18): 결정 A′ 로 전역 user-data drain 이 구현됐다** — ③의 「lease 를 따지 않아 카운트에 안 잡힌다」가 더 이상 성립하지 않는다(HTTP 요청·정리 크론 모두 임차증을 든다 · §10-9-6). ⚠️ **그래도 복원 금지는 유지된다** — 남은 사유는 drain 이 아니라 **T8(옛 배포 세대는 게이트를 읽지 않는다) · 옛 배포 차단 D1~D12 미증명 · 지금 이 순간 임차증 0건 미확인**이다(§10-8-0 의 ⑤⑦⑨) |
| **33** | **옛 배포 세대가 유지보수·lease 를 모른 채 쓰기를 계속** | **H** | 롤백·부분 전파·별칭 지연으로 **게이트 로직이 없는 세대**가 일부 요청을 처리한다 → 유지보수 중에도 그 세대가 `users`·`books` 를 쓴다. 실측된 사실이다(`HANDOFF.md` §4-6: 별칭이 배포 직후 약 1분 옛 응답) | ① 게이트의 진실 원본을 **env 가 아니라 ledger 행**으로 둔다(§10-9 Q1·Q4) — 옛 세대는 lease 자체를 안 따므로 **새 세대의 삭제 saga 는 여전히 fencing 된다** ② §10-8 2번의 **배포 세대 확인** ③ §10-8 3번의 **실제 찔러보기** | **T8**(§13-5) — ⛔ **5판: 합격 조건은 「거부됨」만. 「탐지됨」은 불합격**(§10-8-1 D11) | **코드로는 못 막는다.** 옛 세대는 정의상 새 규칙을 모른다. **막는 길은 §10-8-1 의 D1~D12** — 옛 배포를 지우거나 Access 로 닿지 못하게 하는 것이다. 12개를 전부 증명하기 전까지 §10-8-0 의 금지가 유지된다 |
| **34** | **가입 `state` 재사용으로 하나의 정책 행위가 여러 계정에 귀속** | **H** | ① 브라우저가 약관·연령을 확인하고 가입 state S 를 받는다(S 안: `pv`·수락·진술·`occurred_at`) ② 같은 `shh_t` 를 쓰는 두 OAuth 왕복을 거의 동시에 시작한다 ③ 첫째는 제공자 계정 A, 둘째는 계정 B 로 인증한다 ④ 둘 다 **서로 다른 유효 code** 를 받는다 — 제공자의 code 1회성으로는 못 막는다 ⑤ 서버에 **state S 의 소비 여부를 기록하는 곳이 없다**(`takeState()` `:180-191` 은 서명·만료만 본다) ⑥ `clearTxn()`(`:766`·`:809`)의 쿠키 삭제가 아직 도착하지 않아 **둘 다 `bound()` 를 통과**한다 ⑦ `provider_subject` 가 달라 `UNIQUE` 가 **안 막는다** → 계정 두 개가 생긴다 ⑧ **같은 `pv`·`occurred_at` 이 두 `user_id` 에 기록된다** | **주 D1 의 1회 소비 표식**(§5-3) — `consumed_signup_states` INSERT 를 계정 생성 batch 의 **첫 문장**으로 두어 PRIMARY KEY 충돌 시 **batch 전체가 롤백**된다. 둘째 요청은 **409 `stateUsed`** 로 끝나고 사용자·이벤트·세션을 만들지 않는다. **fallback 없음**(R8) | §13-5 **D-2·D-3** — **T26**(같은 제공자 계정 동시 경쟁) · **T27**(다른 제공자 계정 동시 경쟁 → 계정 정확히 1개) · **T28**(트랜잭션 롤백) · **T29**(재소비 거부) | ⚠️ **소비 표식이 Time Travel 로 사라지면** 그 state 를 다시 쓸 수 있다. 다만 가입 state 는 **10분 만료**라 복원 시점이 발급 후 10분 이내인 극단적 경우만 남는다(§5-3-4). ⚠️ **로그인 state 의 재사용은 여전히 감수한다** — 거기엔 귀속시킬 증거가 없다(§4-A) |
| **35** | **실패한 삭제 요청의 지연 재실행으로 활동 중 계정이 예고 없이 삭제** | **H** | ① 사용자가 계정 삭제를 누른다 ② 주 D1 삭제가 실패한다 ③ 사용자에게 **"지우지 못했어요"** 가 표시되고 세션이 살아 있다(결정 6 의 X) ④ 사용자는 삭제가 실패했다고 이해하고 **계정을 계속 쓴다** ⑤ 새 단어·별명·친구 관계를 만든다 ⑥ 24시간 뒤 운영자가 경보를 보고 **옛 pending 만 근거로** saga 를 재실행한다 ⑦ 현재 계정과 ⑤의 데이터가 **별도 확인 없이** 삭제된다 | **운영자 재실행 금지**(§10-5-1 S3·S4·I22~I24) — 계정이 주 D1 에 살아 있으면 그 pending 은 **실패 기록**이지 대기 중인 명령이 아니다. 새 삭제는 **사용자의 새 요청**으로만 시작한다. stale pending 의 수동 제거도 **계정 존재 재확인**을 포함한 5개 조건에서만(S6·Q7b) | §13-5 **E 묶음** — **T37**(살아 있는 계정의 pending 은 승격도 삭제도 되지 않는다) · **T38**(운영자 재실행 경로가 **존재하지 않는다**) · **T39**(계정 재확인 없는 stale 제거 거부) | **절차다.** 운영자가 ledger 와 주 D1 에 직접 접근할 수 있으므로 수동 실행은 기술로 못 막는다. ⚠️ 법률 검토가 **삭제 요청 기록의 보존**을 요구하면 `aborted` 상태가 필요해진다(L9-4) |
| **36** | **복원 직후 되살아난 탈퇴자의 데이터가 읽힌다** | **C** | ① 주 D1 을 복원한다 ② 탈퇴한 X 의 `users`·`sessions`·`books`·`friendships` 가 **전부 되살아난다** ③ 재삭제가 끝나기 전에 X 의 옛 세션 쿠키로 `GET /api/book` 이 들어온다 — `whoAmI` 는 `JOIN users` 라 행이 돌아온 순간 **통과한다** ④ **지웠던 단어장이 그대로 응답된다** ⑤ 친구 Y 의 `GET /api/friends/:X/book` 도 X 의 단어장·별명을 다시 보여준다 | ⛔ **`restore_closed` 상태**(§10-7) — 읽기와 **세션 인증 자체**를 막는다. 허용은 `/api/health`·`/api/ready`·`/api/policies`·정적 정책 파일 **넷뿐**. 해제는 재삭제·CASCADE 잔여·세션 검사가 **전부** 끝난 뒤 | §13-5 **F 묶음** — **T40**(자기 단어장) · **T41**(친구가 보는 단어장·목록) · **T42**(`/api/me`) · **T43**(허용 목록이 정확히 넷) | ⚠️ **4판 설계로는 이 셋이 전부 200 이었다** — "DB 를 안 쓴다"를 근거로 허용했다. **쓰기만 본 것이 잘못이었다.** 옛 배포 세대에는 이 게이트가 없으므로 위협 33·§10-8-1 이 함께 필요하다 |
| **37** | **ledger 를 제자리 복원해 게이트가 함께 과거로 간다** | **C** | ① ledger 손상으로 운영자가 `restore` 를 실행한다 ② 되돌아가는 것은 `deletions` 만이 아니다 — **`maintenance`(게이트)·`epoch`·`write_leases` 가 같이 과거로 간다** ③ 방금 `closed` 로 바꾼 게이트가 **`open` 으로 되돌아간다** ④ epoch 이 되돌아가 **옛 세대의 요청이 다시 유효**해진다 ⑤ 이미 끝난 lease 가 살아 있는 것으로 보여 drain 판정이 거짓이 된다 | ⛔ **제자리 restore 를 절차에서 없앤다**(§10-6-0). **ledger 재작성 13단계** — 새 DB 생성 → `deletions` 만 병합 → **coordination 은 새로 초기화**(G3). 그리고 **ledger 를 못 읽으면 `restore_closed`**(G1·G2) 라 교체 중에는 앱이 저절로 닫힌다 | `test-deletion-ledger` **13**(coordination 이 과거 값을 받지 않는다) · **14**(ledger 조회 실패 시 fail-closed) | **절차다.** 운영자가 대시보드에서 직접 restore 를 누를 수 있다. G1 이 그 뒤의 피해는 줄이지만 표식 손실 자체는 막지 못한다 |
| **38** | **보유기간을 약속했는데 지우는 사람이 없다** | **M** | ① 방침에 「N일 뒤 삭제」를 적는다 ② 실제 정리는 **다른 일이 일어날 때만** 돈다 — 세션·`rate_limits` 는 **다음 로그인**, 삭제 표식은 **다음 삭제**, 소비 표식은 **다음 가입** ③ 사용자가 0명이거나 뜸하면 **아무 일도 일어나지 않는다** ④ 만료된 값이 **무기한 남는다** ⑤ 방침 문장이 **적는 순간 거짓**이 된다 | **정리 전용 Scheduled Worker**(§10-5-2) — 1시간 주기. **삭제 대상 4개**(`consumed_signup_states` · 세션 · `rate_limits` · **확정된** 삭제 표식) **+ pending 경보 집계 1개**(세기만 한다). ⛔ **`write_leases` 는 대상이 아니다** — 해제가 행 DELETE 라 남은 행은 전부 진행 중이거나 stale 이다. **마지막 성공 시각·삭제 행 수·연속 실패 경보**를 남기고 `/api/ready` 가 `cleanupStale`(제 시간에 도나)과 `cleanupAlert`(봐야 할 것이 쌓였나)로 드러낸다. 실패한 회차는 `ctx.waitUntil()` Promise 를 **거부**한다. ⛔ **pending 표식은 시간만으로 지우지 않는다**(세어서 경보만) | §13-5 **G 묶음** — **T44**(pending 은 안 지운다) · **T45**(만료 전 삭제 금지) · **T46**(재실행 안전) · **T47**(유지보수 중 미실행) | ⚠️ 무료 플랜 **CPU 10ms** 제약이 있다(§19) — 한 번에 다 지우려다 매번 시간 초과로 **아무것도 못 지우는** 상태가 될 수 있다. `LIMIT` 로 나눠 지운다(C7). ⚠️ **정리 수단이 생겨도 §10-5 의 네 미확정 항이 확정되기 전에는 방침에 숫자를 적지 않는다** |
| **39** | **`LEDGER` 바인딩이 없으면 게이트도 임차증도 없이 사용자 데이터가 오간다** | **H** | ① `LEDGER` 를 빠뜨리고(또는 잘못된 id 로) 배포한다 ② `readMode()` 가 바인딩이 없을 때 **`open`** 을 돌려준다 ③ 임차증 획득이 `if (env.LEDGER && …)` 로 감싸여 있어 **통째로 건너뛴다** ④ `GET /book`·`PUT /book`·로그인 콜백이 **전부 200** 이다(실측) ⑤ 그 배포에서 일어난 쓰기는 **어느 drain 에도 안 잡히고**, 계정 삭제는 표식 없이… 는 아니다(삭제만은 503 이었다) — **삭제만 막고 나머지는 다 열려 있었다** | ⛔ **`unbound` 모드**(2026-08-19) — `readMode()` 가 `open` 대신 `unbound` 를 돌려주고, `maintenanceAllows()` 는 **아는 모드만** 열고 나머지를 전부 막는다. 임차증 획득도 `rt.lease` 기준으로 바뀌어, 바인딩이 없으면 그 라우트는 게이트에서 이미 503 이다(두 번째 자물쇠는 lease 직전 검사) | **T8-a**(9개 라우트가 전부 503 · 데이터 무변화) · **T8-a1**(`readMode`·`maintenanceAllows` 계약을 **계층별로** 따로) · **T8-a2**(질의 실패·`maintenance` 행 없음도 같은 결론) | `/health`·`/ready`·`/policies` 는 그대로 열린다 — 무엇이 고장 났는지 볼 수단이 그것뿐이다. `/health` 는 `providers: []`·`ledgerBound:false` 로 답해 **버튼을 그리지 않게** 한다 |
| **40** | **인증 없는 임의 경로가 주 D1·ledger 쓰기를 유발한다(쓰기 증폭)** | **H** | ① 임차증을 **라우트 판정보다 먼저** 딴다 ② `POST /없는주소` 하나가 ledger 에 **INSERT + DELETE** 를 남긴다 — 인증도, 존재하는 라우트도 필요 없다 ③ `GET /login/kakao` 도 주 D1 을 한 줄도 안 만지면서 같은 쓰기를 낸다 ④ 공격자가 아무 주소나 반복해 **D1 무료 한도(하루 10만 쓰기)** 를 태운다 ⑤ 그게 바닥나면 **정상 사용자의 단어장 저장이 먼저 죽는다** | **라우트 분류를 맨 앞으로**(`routeFor()`) — 모르는 경로·허용되지 않은 method 는 **게이트·임차증·리미터 어느 것도 지나지 않고** 404. 임차증은 `lease:true` 인 라우트만, 버킷은 **라우트 하나에 하나**(예전에는 `POST /friends` 가 `write`+`friends` 로 두 번 셌다) | **T50**(없는 주소·잘못된 method 100회 → 두 DB 쓰기 0) · **T51**(로그인 시작 → 쓰기 0) · **T52**(정상 읽기·쓰기 = 임차증 정확히 하나) · **T53**(429 반복 시 상한) · **T55**(분류표 자체) | ⚠️ **실재하는 라우트는 차단된 429 요청에도 임차증을 든다** — 임차증을 리미터보다 먼저 따야 세션 조회까지 추적되기 때문이다. 요청당 ledger 쓰기 2회가 상한이고, 그 이상의 방어는 **엣지(WAF)** 의 몫이다(`docs/OPS_RUNBOOK.md` §13) |
| **41** | **등록되지 않은 레이트리밋 버킷이 조용히 가장 넉넉한 한도를 물려받는다** | **M** | ① `RL_MAX[bucket] ?? RL_MAX.write` ② 새 버킷을 만들고 한도 등록을 잊거나 이름을 오타 낸다 ③ 그 자리는 **한도 120** 으로 돈다 — 좁으라고 만든 버킷이 가장 넓어진다 ④ 아무도 못 알아챈다(막히지 않으니 증상이 없다). 실제로 `auth` 버킷이 그랬고 **한 번도 먼저 막은 적이 없었다** ⑤ `[]` 인덱싱이라 `__proto__`·`constructor` 도 값을 내놓고, 그 값은 숫자가 아니라 비교가 늘 거짓 = **무제한** | `rlMax()` 가 **던진다**(fail-fast) + `Object.hasOwn` 으로 **자기 속성만** 본다. 라우트 표가 쓰는 버킷 전부가 등록돼 있는지도 테스트가 대조한다 | **T54**(모르는 이름·프로토타입 속성·비문자열 전부 예외 · 라우트 표의 버킷은 전부 등록) | 없음. 이 실패는 배포 전 테스트에서 잡힌다 |
| **42** | **복원 사전점검을 인자로 우회한다** | **H** | ① `restoreGate(state)` 가 **임의의 객체**를 그대로 믿는다 ② 아홉 조건을 전부 `true` 로 적은 객체 하나면 `allowed:true` ③ **테스트가 정확히 그것을 「gate 가 열린다」로 확인하고 있었다** — 검사가 아니라 인자를 재고 있었다 ④ `reopenReport(env, { targets: [] })` 는 빈 목록이라 잔여 0·살아 있는 계정 0 → **`canReopen:true`** ⑤ 즉 **아무것도 확인하지 않고** 읽기를 다시 여는 길이 있었다 | 코드가 소유한 조건은 **인자로 못 바꾼다**(밖에서 오는 것은 질의 결과 하나뿐) · 재개방 판정은 **대상 집합을 스스로 만든다**(주 D1 을 훑고 ledger 의 confirmed 와 대조) · `setMode(env,"open")` 은 보고서를 받지 않고 **그 자리에서 다시 판정한다** · 반환값은 언제나 `restoreAllowed:false` | **T7**(9조건) · **T7-b**(손으로 만든 상태 객체로 통과 불가) · **H1~H1-e**(모드·임차증·미확정 표식·빈 증거·재개방 성공) | ⚠️ **앱 코드는 여전히 복원을 막지 못한다.** 이것은 실수로 열리는 것을 막는 장치이고, 실제 통제는 계정 권한·승인 절차·옛 배포 제거다 |
| **43** | **`restore_closed` 에서 `maintenance` 로 걸어 나가 되살아난 탈퇴자의 단어장이 읽힌다** | **C** | ① 주 D1 을 복원한다 — 탈퇴자의 `users`·`sessions`·`books` 가 되살아난다 ② 운영자가 `restore_closed` 로 닫는다(여기까지는 맞다) ③ 재삭제·잔여 확인 **전에** `setMode(env, "maintenance")` 를 부른다 ④ **통과한다** — 자물쇠가 `restore_closed → open` 에만 걸려 있었다 ⑤ 그런데 `maintenance` 는 `GET /book`·`GET /me`·`GET /friends/:id/book` 을 **허용하는 상태**다(§10-7 `MAINT_READS`) ⑥ 되살아난 세션 쿠키로 **지웠던 단어장이 200 으로 그대로 읽힌다** — 위협 36 과 같은 결과가 자물쇠를 부수지 않고 재현된다 | 판정 기준을 **「목적지가 사용자 데이터를 읽게 하는가」**로 바꾼다(`READS_USER_DATA = {open, maintenance}`) · `restore_closed` 에서 나가는 길은 **검증된 `open` 하나**뿐이다(`maintenance` 로 가려면 `open` 을 거친다 — 길이 하나면 빠뜨릴 자리가 없다) · 전환은 **CAS**(판정에 쓴 `mode`·`epoch` 을 조건에 넣는다) — 판정 직후 다른 운영 전환이 끼어들면 **닫는 쪽으로** 실패한다 | **T56**(3×3 전환 전수 · 거부 후 `/book` 이 여전히 503 · CAS 경합) | 재현 R1. 6판까지 「자물쇠가 있다」고 적은 것은 **한 전환에 대해서만** 참이었다 |
| **44** | **호출자가 넘긴 mark 함수가 재삭제 대상을 0 으로 만든다** | **C** | ① `reopenReport(env, { markFns })` 가 **임의의 실행 함수**를 받는다 ② `() => "언제나같은값"` 을 넘긴다 ③ 그 함수로 만든 사용자 표식이 ledger 의 confirmed 집합과 **하나도 안 맞는다** ④ `restoreTargets()` 의 교집합이 비어 **`targets: 0`** ⑤ 대상이 0 이니 잔여 조회도 0, 살아 있는 계정도 0 ⑥ **`canReopen: true`** ⑦ `setMode(env, "open", { markFns })` 가 그대로 통과해 읽기가 열린다 — 되살아난 탈퇴자가 살아 있는 채로 | **인자를 없앤다.** 키 재료는 **레지스트리**(`DELETION_MARKERS`)가 소유하고 `reopenReport`·`reconcile`·`removeStalePending` 은 거기서 만든다 · ledger 에 **모르는 `key_version`** 이 하나라도 있으면 자동 판정을 **거부**한다(지금 지원 버전은 하나뿐이고, 회전을 미리 만들지 않는다) | **T57**(상수·다른 키·버전 뒤바뀜·빈 목록·함수 아님 — 전부 `targets` 불변 · 모르는 `key_version` 거부) | 재현 R2. ⚠️ 6판 주석은 「틀린 함수를 주면 대상이 **늘어나지 줄지 않는다**」였다 — **정확히 반대**였고, 그 문장이 검토를 통과시켰다 |
| **45** | **경보 시각 전의 미확정 삭제를 안 세고 「미확정 0건」으로 읽는다** | **H** | ① 삭제 saga 가 주 D1 삭제 직전에 죽는다 → `deletions` 에 pending 한 줄(경보 시각은 **24시간 뒤**) ② 그 직후 복원이 필요해진다 ③ `openPendingCount()` 는 `confirmed_at IS NULL AND pending_alert_at < now` 라 **0** 을 답한다 ④ `reopenReport()` 의 ④번 조건과 `restorePreflight().openPending` 이 둘 다 그 값을 쓴다 ⑤ **「미확정 삭제 표식 0건」이 근거로 기록되고** 재개방·복원 사전점검이 통과한다 ⑥ 그 표식은 「누구를 다시 지워야 하는지 아직 모른다」였다 | **두 질문을 갈라 함수를 나눈다.** `pendingTotalCount()` = 확정 안 된 것 **전부**(시각을 안 본다 — 시간이 지난다고 해결되지 않는다) → **안전 조건**이 쓴다. `pendingAlertCount()` = 경보 시각을 넘긴 것(엄격한 `<`) → **경보**(정리 크론 · `/api/ready` 의 `cleanupAlert`)가 쓴다 | **T58**(전체/경보 분리 · 경계 시각 `<`·`=`·`>` 각각 · 안전 조건은 전체를 쓴다) | 재현 R3. 한 값이 두 질문에 답하면 둘 중 하나는 반드시 틀린다 |
| **46** | **잘못된 `DELETION_KEY` 로 살아 있는 계정의 표식이 confirmed 로 승격된다** | **C** | ① 어떤 이유로 `DELETION_KEY` 가 바뀌는데 `DELETION_KEY_VERSION` 은 그대로다(교체 실수 · 환경 혼동 · 시크릿 복원) ② `reconcile()` 이 주 D1 의 `users.id` 를 **지금 키로** HMAC 해 살아 있는 표식 집합을 만든다 ③ 그 집합이 옛 키로 만든 ledger 의 pending 과 **하나도 안 맞는다** ④ 계정이 멀쩡히 있는 pending 이 「계정이 없다 → 삭제는 됐고 확정 기록만 실패했다」로 읽힌다 ⑤ **confirmed 로 승격된다** ⑥ 승격은 되돌릴 수 없다 — 그 뒤로 그 사람은 「지워진 사람」이고, 복원 절차(§10-8)는 그를 **재삭제 대상**으로 삼는다 ⑦ HMAC 은 역산할 수 없으므로 **표식만으로는 키를 검증할 수 없다** | 표식을 **처음 남길 때**(`markPending`) 키 검사값을 함께 기록한다 — `deletion_keys(key_version, key_check)` · `key_check = HMAC(DELETION_KEY, "shhh!/deletion-key-check/v<버전>")` (고정 문자열 하나의 HMAC 이라 uid 도 표식도 담지 않는다) · 이미 기록이 있는데 다르면 **던진다**(어긋나는 키로 표식을 더하면 두 벌의 증거가 영원히 대조되지 않는다) · `deletionEvidenceUsable()` 이 `reconcile`·`reopenReport`·`removeStalePending` **앞에서** 대조하고 안 맞으면 **아무것도 하지 않는다** | **T59**(호출자 함수로 승격 불가 · 키가 틀리면 reconcile·stale 제거 거부 · 올바른 키에서는 계정이 살아 있으면 승격 안 하고 사라지면 승격한다) | 재현 R4. ledger migration **`0002_deletion_key_check.sql`** 이 표를 더한다 |
| **47** | **인증 없는 요청이 지속 저장소 쓰기 할당량을 무제한으로 태운다** | **H** | ① 임차증을 **라우트 판정 뒤·인증 앞·리미터 앞**에서 딴다(인증 조회까지 추적하려는 올바른 이유였다) ② 그런데 인증이 필요한 읽기 라우트 넷은 **레이트리밋 버킷이 아예 없었다**(`bucket: null`) ③ 쿠키 없이 `GET /book` 을 두드린다 — 401 이 나가기 전에 임차증 INSERT+DELETE 로 **ledger 쓰기 2회** ④ 매번 다른 위조 쿠키를 써도 같다(버킷이 없으니 셀 것도 없다) ⑤ 버킷이 있는 라우트(`/cb`)도 **리미터가 임차증보다 뒤라** 429 를 받으면서 쓰기 2회를 냈다 ⑥ **요청 수에 상한이 없으니 쓰기에도 상한이 없다** — 실측 익명 200회 → ledger 400 쓰기, 5만 회면 D1 무료 한도(하루 10만)가 바닥난다 ⑦ 바닥나면 **정상 사용자의 단어장 저장이 먼저 죽는다** | 리미터를 **임차증보다 앞으로** 옮긴다(막힌 요청은 어느 DB 도 만지지 않는다) · 임차증을 드는 라우트는 **전부 버킷을 갖는다**(`read` 신설 · 분당 120) · 리미터가 인증 앞이므로 세는 신원은 **IP** 다 | **T60**(총량 불변식 — 요청 수를 2배로 늘려도 쓰기가 같다 · 429 이후 쓰기 0 · `restore_closed` 에서는 리미터도 주 D1 을 안 만진다 · 정상 사용자는 그대로) | 재현 R5. ⛔ **옛 T53 은 「요청당 정확히 둘」을 확인하고 그것을 「유한한 상한」이라 적었다** — 요청 수가 무제한이면 요청당 상수는 상한이 아니다. 그 검사를 총량 불변식으로 교체했다. ⚠️ **여러 IP 로 나눠 오는 분산 요청은 앱 코드로 못 막는다**(엣지의 일 · §12-1 ①). ⚠️ 대가: 공유 IP(CGNAT)는 같은 버킷을 나눠 쓴다 |

| **48** | **stale pending 제거와 진행 중인 계정 삭제가 겹쳐 「증거 없는 삭제」가 만들어진다** | **C** | ① 실패한 삭제가 남긴 pending 표식 하나가 있다(계정은 살아 있다) ② 운영자가 `maintenance` 에서 `removeStalePending()` 을 실행한다 — 모드·임차증·키·**계정 존재**를 확인하고 「지워도 되는 찌꺼기」로 판정한다 ③ 그 사이 유지보수가 풀리고 사용자가 **진짜로 `DELETE /me`** 를 누른다 ④ 주 D1 의 `users` 행이 사라진다(표식은 아직 pending) ⑤ 운영자 쪽 `DELETE FROM deletions` 가 **아무것도 다시 확인하지 않고** 실행된다 ⑥ 사용자 쪽 `markConfirmed` 는 지울 행이 없어 0행 — 코드가 그 오류를 삼킨다 ⑦ 결과는 **`users` 0행 · `deletions` 0행**: 지워졌는데 지운 증거가 없다 ⑧ 복원하면 그 계정이 되살아나고 **재삭제 대상 목록에 안 나온다** | 제거를 **조건부 한 문장**으로 만든다 — `DELETE ... WHERE mark=? AND confirmed_at IS NULL AND EXISTS(maintenance 가 판정 때의 mode·epoch 그대로) AND NOT EXISTS(write_leases 에 행이 있다)`. 판정과 삭제 사이에 모드가 바뀌었거나 작업이 하나라도 시작됐으면 **0행이 지워진다.** 확인은 「그때 그랬다」가 아니라 **「지금 그렇다」**여야 한다 | **T61**(a 정상 제거는 그대로 · b 삭제와의 경합에서 `users=0 && deletions=0` 불가 · c 모드만 바뀐 경우 · d epoch 만 오른 경우 · e 임차증만 생긴 경우) | ⚠️ **운영자는 D1 콘솔로 직접 지울 수 있다** — 이 방어는 앱의 명령 경로에만 걸린다. §10-5-1 의 절차가 여전히 필요하다 |
| **49** | **`drained:true` 라고 답한 뒤에 주 D1 쓰기가 일어난다** | **H** | ① 요청이 게이트를 통과한다(그때는 `open`) ② 리미터의 `rate_limits` UPSERT **직전**에서 멈춘다(D1 왕복·스케줄링) ③ 운영자가 `restore_closed` 로 전환한다 ④ 그 요청은 **아직 임차증을 안 땄다** — 리미터가 임차증보다 앞이기 때문이다(위협 47 을 닫으면서 그렇게 됐다) ⑤ `drainState()` 가 **`open:0 · drained:true`** 를 답한다 ⑥ 운영자가 그 답을 근거로 복원을 시작한다 ⑦ 멈춰 있던 요청이 깨어나 **주 D1 에 쓴다** — 복원본에 섞이거나 복원 직후 값을 덮는다 | **리미터 카운터를 ledger D1 로 옮긴다**(§0-8-1). 그러면 ① 주 D1 은 **예외 없이 임차증 안에서만** 만져지고 ② 카운터 UPSERT 를 게이트와 **한 문장**으로 묶을 수 있다(`WHERE EXISTS(maintenance.mode IN ('open','maintenance'))`) — 전환 뒤에 깨어난 요청은 0행을 쓰고 429 로 끝난다 | **T62**(게이트 통과 후 UPSERT 직전 정지 → `restore_closed` → drain 확인 → 재개: 주 D1 **질의 0건·쓰기 0건**) | ⚠️ 주 D1 의 `rate_limits` 표는 **남겨 둔다** — 파괴적 원격 migration 은 별도 승인 대상이고 안 쓰는 표는 해가 없다. ⚠️ **ledger 쓰기 총량은 줄지 않는다** — 그건 위협 50 이 다룬다 |
| **50** | **엣지 남용 방어 없이 계정 경로를 열면 익명 요청이 D1 쓰기 할당량을 태운다** | **H** | ① 리미터가 세는 신원은 **IP** 다(인증보다 앞이라 그 자리에서는 누구인지 모른다 · 위협 47) ② 한도는 **IP·분당**이라 서로 다른 IP 로 나눠 오면 그대로 곱해진다 ③ 익명으로 닿는 버킷이 여섯이다(login·signup·read·write·friends·rotate) ④ 실측: IP 하나가 한 창에 두 저장소 합 **861 쓰기**(T63-b 가 코드에서 직접 잰다 — 숫자를 문서에 손으로 적지 않는다) ⑤ 하루치로 환산하면 **IP 한 개**로도 D1 무료 한도(하루 10만 쓰기)를 넘긴다 ⑥ 바닥나면 **계정 전체의 D1 질의가 실패**하고 정상 사용자의 저장이 먼저 죽는다 | **앱 코드로는 못 막는다** — 분산 요청을 끊는 것은 엣지(WAF 레이트리밋 · Turnstile)의 일이고, 둘 다 커스텀 도메인 또는 Workers 전환이 필요하다(§12-2). 그래서 코드가 강제하는 것은 하나다: **남용 방어 바인딩이 없으면 계정 라우트를 열지 않는다.** `/health`·`/ready`·`/policies` 만 답하고 나머지는 **어느 DB 도 만지기 전에** 503, `/health` 는 `providers: []` 로 버튼도 안 그리게 한다 | **T63**(a 방어 없음 → 6버킷 × 5 IP 전부 503 · 두 DB **질의 0건** · b 세는 설정의 실측 비용과 `/health.ready === false` · c 엣지 바인딩이 붙으면 열린다) | ⛔ **이것은 「막았다」가 아니라 「열지 않았다」다.** 공개 계정 기능은 엣지 방어가 실제로 붙기 전까지 **No-Go** 이고, 무엇을 붙일지는 도메인·요금제가 걸린 **사용자 결정**이다(§12-2) |
| **51** | **삭제 증거 키가 어긋난 배포가 `/ready` 200 으로 통과한다** | **H** | ① `DELETION_KEY` 를 잘못 넣고(또는 예전 값으로) 배포한다 — 버전은 그대로다 ② `deletionEvidenceUsable()` 은 그것을 **정확히 잡아낸다**(위협 46 에서 만든 검사값) ③ 그런데 `/ready` 가 그 함수를 **부르지 않는다** — 주 D1 스키마와 ledger 표 존재만 본다 ④ 배포 후 smoke test 가 **200 · `ready:true`** 로 통과한다 ⑤ 그 배포는 reconciliation 을 돌리면 **살아 있는 계정을 승격**시킬 수 있는 상태다(위협 46 의 조건이 그대로 선다) ⑥ 처음 알게 되는 것은 **첫 계정 삭제**에서다 | `/ready` 가 `deletionEvidence` 를 **실제로 판정**하고 거짓이면 **503 · `ready:false`**. 표식이 하나도 없어도 **키 기록이 있으면 대조한다** — 빈 ledger 로 시작한 첫 배포만 통과한다 | **T64**(a 빈 ledger 통과 · b 어긋난 키 → 503 · 응답에 값·검사값·표 이름·오류 문자열 **0건** · c 모르는 `key_version` · d 스키마 부재와 **구분되어** 나간다) | ⛔ 응답은 **boolean 하나**다. 개수·키 이름·검사값은 안 나간다 — 인증 없이 열린 자리다 |
| **52** | **바인딩이 아닌 값·고장 난 바인딩이 「남용 방어 있음」으로 판정된다** | **C** | ① `abuseGuard()` 가 `env.RL` 의 **truthy 여부만** 본다 ② 대시보드 오타·잘못된 타입·빈 객체가 그대로 `"edge"` 다 ③ `limited()` 의 엣지 갈래는 `try { … } catch { return false }` 라 **호출이 던지면 통과**다 ④ `/ready` 는 `abuseReady:true` · **200** 이라 운영자에게는 정상으로 보인다 ⑤ 실측: 가짜 값 다섯 가지 전부 `/ready` 200 이고 익명 요청이 진행돼 **ledger 쓰기 2회**를 냈다 ⑥ 반대로 반환값이 이상하면(`{nope:1}`) **전부 429** 가 되어 고장이 곧 서비스 거부인데 그때도 `/ready` 는 200 이다 | ① 모드를 **운영자가 선언**한다(`EDGE_GUARD` = `waf` 또는 `ratelimit`) — 선언이 없으면 `none` ② `ratelimit` 은 **`typeof env.RL?.limit === "function"`** 일 때만 인정한다 ③ 호출이 던지거나 `success` 가 boolean 이 아니면 **`broken`** 이고 계정 경로는 **503** 이다(429 아님 — 「한도 초과」와 「방어 고장」은 다른 말이다) ④ `/ready` 는 바인딩을 **실제로 한 번 호출해** 확인하고 실패하면 `abuseReady:false` · **503** ⑤ 응답에 바인딩 이름·예외 문자열을 싣지 않는다 | **T65** | ⚠️ `waf` 모드는 **런타임이 증명할 수 없다**(§12-3-2). 그 경우의 보장은 호스트 잠금과 운영 점검이지 코드가 아니다 |
| **53** | **문서의 버킷별 한도 6개가 엣지 갈래에서 하나도 적용되지 않는다** | **H** | ① Rate Limiting 바인딩의 한도는 **설정의 `simple.limit`·`period`** 에 고정된다 — `limit()` 인자는 `key` 뿐이다(공식 문서 · wrangler 4.123 스키마 확인) ② 그래서 바인딩 **하나**로는 5·10·20·120 을 낼 수 없다 ③ 코드는 `rlMax(bucket)` 을 계산해 놓고 엣지 갈래에서 **쓰지 않는다** ④ 실측: 한도 100 짜리 mock 에서 `rotate`(문서상 5/분)가 **20회 연속 통과** ⑤ 즉 「엣지가 센다」는 칸이 **문서의 숫자와 무관한 다른 한도**를 가리키고 있었다 | **두 층을 분리하고 이름을 다르게 부른다.** ① **버킷별 한도(`RL_MAX`)의 집행자는 우리 ledger 카운터 하나**다 — 전역 일관이고 `rlMax()` 가 실제로 쓰인다. 엣지 바인딩이 있어도 **끄지 않는다** ② 엣지 바인딩은 **볼류메트릭 사전 거름**이고 그 한도는 설정에 산다 ③ `scripts/test-config.mjs` 가 설정의 `ratelimits[].simple.limit` 이 `max(RL_MAX)` **이상**인지 본다 — 사전 거름이 우리 버킷보다 좁으면 문서의 숫자가 거짓이 된다 | **T66** | ⚠️ 대가: 허용되는 요청마다 ledger 쓰기 **1건**이 는다(카운터). 그 대신 숫자가 진짜가 된다. ⚠️ 엣지 카운터는 **colo 별·eventual consistent** 라 어떤 구성에서도 「전역 N/분」을 보장하지 않는다 |
| **54** | **여러 IP 가 한 번씩만 요청해도 지속 저장소 쓰기가 늘어난다** | **H** | ① IP 별 리미터는 **같은 IP 의 반복**만 센다 ② 실측: 서로 다른 IP 100개 × 익명 `GET /book` 1회 → 429 **0건** · 401 **100건** · **ledger 쓰기 200건**(임차증 INSERT+DELETE) ③ 9판에서 카운터가 항상 돌면 요청당 **3건**이 된다 ④ 엣지 바인딩을 붙여도 colo 별이라 저속 분산에는 거의 안 걸린다 ⑤ WAF 무료 등급은 **규칙 1개 · IP · 10초 · 경로 필드**뿐이라 이것도 볼류메트릭 방어다 | **완전히 막을 수 없다. 그래서 범위를 수치로 적고 승인을 받는다**(§12-3-3). 코드가 실제로 줄일 수 있는 것은 하나 — **인증이 필요한 라우트에서 우리가 발급하지 않은 쿠키를 DB 없이 걸러내는 것**(서명 세션 envelope). 그러면 쿠키 없음·위조 쿠키의 비용이 요청당 3 → **0** 이 된다. ⚠️ **세션 구조 변경이라 사용자 승인 대상이다**(`CLAUDE.md` §4) | **T67** | 남는 것: **유효한 세션을 가진** 분산 요청(계정이 실제로 있어야 한다) · 인증이 필요 없는 라우트(`/cb`·`/signup/start`). 전자는 가입 경로가, 후자는 엣지가 다룬다 |
| **55** | **운영자가 고른 방어와 코드가 문을 여는 조건이 다르다** | **H** | ① runbook 은 A(WAF) · B(Workers 바인딩) · C(Turnstile) · D(닫아 둠)를 제시했다 ② 그런데 코드는 **`env.RL` 하나만** 알았다 ③ WAF 규칙을 걸어도 `env.RL` 은 안 생긴다 → 계정 경로는 **여전히 503** ④ Turnstile 도 마찬가지다 ⑤ 즉 **B 만이 실제로 문을 여는 선택지**였는데 문서는 넷을 동등하게 적었다 ⑥ 게다가 A 를 골라도 **`*.pages.dev` 로 오는 요청은 WAF 를 지나지 않는다** — 우회 경로가 그대로 열려 있다 | ① `EDGE_GUARD` 로 **어느 모드인지 선언**한다 ② `waf` 는 **호스트 잠금**과 짝이다 — `APP_ORIGIN` 의 호스트가 `*.pages.dev` 면 **선언해도 `none`** 이고, 계정 경로는 그 호스트로 온 요청만 받는다 ③ `ratelimit` 은 바인딩 검증(위협 52) ④ **Turnstile 은 단독 모드가 아니다** — 가입·로그인만 덮으므로 `waf`·`ratelimit` 위에 얹는 보조다 ⑤ `waf` 의 실재는 코드가 증명하지 못하므로 **runbook 의 외부 점검 항목**으로 남긴다 | **T68** | ⚠️ 호스트 잠금은 `*.pages.dev` **접근 자체**를 막지 못한다(정적 화면은 그대로 뜬다). 막는 것은 그 호스트로 오는 **계정 API** 다 |
| **56** | **공개 `/ready` 가 인증 없이 두 DB 를 무제한으로 조회한다** | **H** | ① `/ready` 는 `ALWAYS_OPEN` 이라 남용 방어·게이트·리미터를 전부 지나간다 ② 한 번 부를 때마다 주 D1 **1 질의**(7개 표 COUNT) · ledger **5 질의**(스키마·삭제 키·정리 상태·모드) ③ 실측 10회 → 주 D1 10 · ledger 50 ④ 인증이 없으니 **요청 수에 상한이 없다** ⑤ 그래서 8판의 「방어가 없으면 D1 이 탈 일이 없다」는 **계정 경로에만** 참이었다 ⑥ 게다가 응답이 `signupReady`·`ledgerBound`·`providers`·`cleanupAlert` 로 **구성 상태를 익명에게** 알려준다 | **진단과 생존 확인을 가른다.** ① 전체 진단은 **`READY_KEY` 헤더**(`X-Ready-Key`)를 요구한다 — 상수 시간 비교, DB 접근 없음 ② 키 없는 `/ready` 는 **어느 DB 도 만지지 않고** `{ok, ready:false, diagnostics:false}` · 503 ③ `/health` 는 지금처럼 DB 를 안 본다(화면이 버튼을 그리는 근거) ④ 키가 등록되지 않은 배포에서는 진단이 **불가**다 — 「키가 없으면 전부 공개」로 되돌아가지 않는다 | **T69** | ⚠️ 배포 후 smoke test 와 출시 체크리스트가 **키를 들고** 부르도록 절차를 고쳐야 한다(`docs/OPS_RUNBOOK.md`). 값은 명령 출력·로그·커밋에 남기지 않는다 |

| **57** | **시크릿을 일부만 넣은 배포에서 가입이 끝까지 진행돼 되돌릴 수 없는 사용자 데이터가 생긴다** | **C** | ① `signupReady` 는 `ledgerBound`·`SIGNUP_STATE_KEY`·`TOMBSTONE_KEY`·`TURNSTILE_*` 넷만 봤다 ② `SESSION_ENVELOPE_KEY`·`DELETION_KEY` 가 빠져도 **참**이었고 `/signup/start` 가 200 이었다 ③ 콜백은 `TOMBSTONE_KEY` 하나만 보고 계정·정책 기록·세션을 만들었다(실측: users 1 · policy_events 3 · sessions 1 · 제공자 호출 2) ④ 그렇게 만든 세션은 다음 요청부터 **전부 503** 이라 그 계정은 쓸 수 없다 ⑤ `DELETION_KEY` 가 없으면 **지울 수도 없다** ⑥ 시크릿 12개를 손으로 넣는 절차에서 하나 빠지는 것은 **정상 범위의 사고**다 | **조건을 함수 둘이 소유한다.** `loginPossible(env)`(DB·LEDGER·APP_ORIGIN·STATE_KEY·RL_KEY·SESSION_ENVELOPE_KEY) · `signupPossible(env)`(거기에 SIGNUP_STATE_KEY·TOMBSTONE_KEY·DELETION_KEY·TURNSTILE_SECRET·TURNSTILE_SITE_KEY). **화면(`/health`) · 가입 시작 · 로그인 시작 · 콜백** 넷이 같은 함수를 본다. 콜백 검사는 **제공자 호출 앞**이라 실패가 되돌릴 수 있는 실패로 남는다 | **T73** | ⚠️ 대가: `DELETION_KEY` 가 없으면 **가입이 안 열린다**(로그인은 열린다). 지울 수 없는 계정을 새로 만들지 않겠다는 뜻이다 |
| **58** | **Turnstile 검증이 `success` 만 보고 `action`·`hostname` 을 안 본다** | **H** | ① 공식 문서는 「Validate the action and hostname when specified」라고 적는다 ② 코드는 `r.success === true` 하나였다 ③ 그래서 **다른 자리에 붙인 위젯**(문의 폼 등)의 토큰을 가입에 그대로 쓸 수 있다 ④ 같은 코드가 여러 주소로 뜨므로(프로덕션 별칭 · `<해시>.pages.dev` · preview alias) **다른 별칭에서 푼 토큰**도 통과한다 ⑤ 화면은 한 번 쓴 토큰을 **버리지 않아서** 사용자가 같은 값으로 계속 눌러 `timeout-or-duplicate` 로 무한히 막혔다 | ① 서버가 `action === "signup"` 과 `hostname === APP_ORIGIN 의 호스트` 를 함께 본다 — **두 필드가 없는 응답도 거부**다(「지정했을 때만」을 「없으면 통과」로 읽지 않는다) ② 기준은 **`APP_ORIGIN`** 이고 요청이 도착한 주소가 아니다 ③ 화면이 위젯에 같은 `action` 을 싣고, 보낸 뒤에는 토큰을 버리고 `turnstile.reset(widgetId)` 로 되돌린다 | **T74** | ⚠️ preview 별칭에서는 가입이 **실패한다** — 그 호스트에서 푼 토큰의 `hostname` 이 `APP_ORIGIN` 과 다르기 때문이다. 호스트 잠금과 같은 방향의 대가다 |
| **59** | **비밀값 비교가 원문 문자열을 인덱싱한다** | **M** | ① 옛 `sameSecret` 은 `a.length !== b.length` 로 먼저 끊고 `charCodeAt` 을 하나씩 XOR 했다 ② 끝까지 도는 성질은 맞지만, 원문 문자열 인덱싱의 시간은 엔진의 표현(latin1/UTF-16·rope)에 좌우되고 **그 성질은 우리가 정하지 못한다** ③ 이 함수는 state 서명 · 세션 envelope 서명 · 로그인 왕복 표 · 운영자 키 넷을 판정한다 | 두 값의 **SHA-256 요약 32바이트**를 비교한다 — 길이가 달라도 하는 일이 같고, 요약 바이트가 몇 개 맞았는지로 원문을 좁힐 수 없다. **비동기가 됐으므로 부르는 자리는 전부 `await`** 다(빠뜨리면 Promise 가 늘 참이라 검사가 통째로 무력해진다 — 소스에서 전수로 잰다) | **T75** | ⚠️ `crypto.subtle.timingSafeEqual` 은 **Workers 런타임 확장**이라 Node 에 없다. 이 저장소의 스위트는 `worker/index.js` 를 Node 에서 그대로 돌리므로 그것을 쓰면 갈래가 둘로 갈리고 하나는 영원히 테스트를 안 지난다 |
| **60** | **방어 코드의 실패 갈래가 어느 테스트에도 안 걸려 있다** | **H** | ① `countVerdict()` 의 `catch { return BROKEN }` 을 `return OK` 로 바꾸는 돌연변이가 **스위트 25개를 전부 통과**했다(실측 exit 0) ② 그 배포는 ledger 가 답을 안 하는 동안 계정 경로를 **세지 않고 통과**시킨다 — 위협 52 가 닫았다고 적은 바로 그 상태다 ③ `RL_KEY` 부재는 재고 있었지만(test-friends 120) **저장소가 던지는 경우**는 아무도 안 쟀다 ④ 같은 무늬로 `test-docs` 의 순서 검사가 주석에는 셋(엣지→게이트→리미터)을 적고 **실제로는 둘만** 쟀다 | ① **T76** 이 버킷 6개 전부에 대해 카운터 문장만 던지게 만들고 **503 · 주 D1 쓰기 0 · 응답 무누설**을 잰다. 반대쪽(멀쩡하면 지나간다)도 함께 잰다 ② 순서 검사에 `gate > rl` 를 더했다 ③ **판정 근거를 통과 개수에서 돌연변이 생존율로 옮긴다** | **T76** | ⚠️ 이것은 코드 결함이 아니라 **테스트 공백**이다. 그래서 고친 것도 코드가 아니라 검사다 — 그리고 그 사실이 「스위트가 다 통과한다」가 왜 완료의 근거가 못 되는지의 다섯 번째 증거다 |

| **61** | **제공자 ID 만 있고 secret 이 없어도 왕복이 시작된다** | **H** | ① 「쌍이 맞아야 제공자로 친다」는 규칙이 `readyProviders()`(= `/health` 목록)에만 있었다 ② 시작 경로 셋(`/signup/start` · `/login/:provider` · 콜백)은 `creds(env, p).id` **하나만** 봤다 ③ 실측(`NAVER_ID` 만 있는 env): `/health` 는 `providers: []`·`signupReady:false` 로 **맞게** 답하는데 `POST /signup/start` 는 **200** 이고 **Turnstile 검증을 1회 호출**한 뒤 네이버 OAuth 주소를 발급했다. `GET /login/naver` 도 **302** ④ 구글도 같다 ⑤ 사용자는 제공자 화면까지 갔다가 토큰 교환에서 실패하고, 우리는 그 왕복만큼 외부 호출·리미터·로그를 태운다 | **규칙을 함수 하나가 소유한다** — `providerPossible(env, provider)`: 카카오는 ID 필수·secret 선택(콘솔 정책), 네이버·구글은 **둘 다 필수**, 모르는 이름은 거짓. `/health`·`/signup/start`·`/login/:provider`·`/cb`·`/exchange` **다섯 자리가 같은 함수**를 본다. 검사는 **Turnstile 호출·state 발급·외부 호출·DB 쓰기·세션 발급 전부보다 앞**이다 | **T77** | ⚠️ 카카오의 secret 선택 정책은 그대로다 — 그 예외는 이 함수 안 **한 줄로만** 산다 |
| **62** | **비밀값 비교가 런타임의 timing-safe API 를 쓰지 않는다** | **M** | ① 10판은 두 값의 SHA-256 요약을 만든 뒤 **JS 반복문 XOR** 로 비교했다 ② 그 근거로 적힌 것은 「`crypto.subtle.timingSafeEqual` 은 Workers 확장이고 Node 에 없어서 스위트가 못 돈다」였다 — **테스트 환경의 결핍을 운영 구현의 보안 수준을 낮출 이유로 썼다** ③ 게다가 `T75-c` 는 그 API 를 **쓰면 실패하도록** 되어 있어, 검사가 약한 쪽을 **강제**하고 있었다 ④ Cloudflare 공식 권고는 「digest → `timingSafeEqual()`」이다 | ① 운영 코드는 `crypto.subtle.timingSafeEqual()` **하나만** 부르고 **fallback 이 없다** ② Node 스위트는 `scripts/_workers-shim.mjs` 가 Node 의 `crypto.timingSafeEqual` 을 **workerd 와 같은 실패 모양**(길이 불일치는 예외)으로 끼운다 ③ 어댑터는 `scripts/` 에만 살고 `dist/`·`functions/`·`worker/` 어디에도 없다(`test-dist` 가 전수로 잰다) ④ **진짜 workerd 프로세스**를 띄우는 스위트가 운영 경로를 실제로 밟는다 | **T75 · T79** | ⚠️ 요약을 먼저 만드는 이유는 그대로다 — `timingSafeEqual` 은 **길이가 다르면 던진다**(workerd 실측). 원문을 넣으면 길이가 관측 가능한 신호가 된다 |
| **63** | **돌연변이 검증이 저장소에서 재현되지 않는다** | **M** | ① 10판은 「돌연변이 34종 중 33종 사망」을 완료 판정의 근거로 적었다 ② 그런데 저장소에는 **목록도 실행 명령도 죽인 테스트도 로그도** 없었다 ③ 확인할 수도 다시 돌릴 수도 없는 숫자는 **검증이 아니라 주장**이다 ④ 그리고 그 주장은 판정 근거로 쓰이고 있었다 | `scripts/mutations.mjs` 가 목록의 원본이고(각 항목이 **깨뜨리는 불변식**과 **죽여야 할 스위트**를 함께 적는다) `node scripts/mutate.mjs` 가 실행한다. **임시 복제본에서만** 고치므로 작업 폴더가 안 더러워지고, 기준선이 빨가면 먼저 멈추며, **생존·앵커 실패가 하나라도 있으면 종료 코드가 0이 아니다** | **T78** 외 | ⚠️ **목표 숫자를 정해 두고 맞추지 않는다.** 첫 실행에서 셋이 살아남았고 셋 다 실제 공백이었다(§0-11-2) |
| **64** | **화면이 서버의 `ready:false` 를 버린다** | **M** | ① `js/authApi.js` 의 `apiHealth()` 가 응답에서 `providers`·`signupReady`·`turnstileSiteKey` 만 꺼내고 **`ready` 를 버렸다** ② 판정하는 쪽(`js/auth.js` 의 boot)은 `h.ready === false` 를 읽는데, 없는 필드라 **언제나 `undefined`** 고 `undefined === false` 는 거짓이라 **계정 상태가 늘 `"ok"`** 였다 ③ 라이브 `/api/health` 가 정확히 `{"ok":true,"ready":false,"providers":[]}` 를 준다 — 즉 **지금 배포에서 재현된다** ④ `test-client` 74개가 전부 통과하고 있었다: 그 검사들의 기본 store 가 로그인 상태라 **뒤이은 `/book` 503** 이 우연히 상태를 down 으로 바꿔 줬다. 재던 것은 `ready` 계약이 아니라 **뒤이은 503** 이었다 | ① `apiHealth()` 의 **모든 반환 모양**에 `ready` 가 boolean 으로 들어간다(실패는 얼린 `DOWN_HEALTH` 하나) ② 여는 조건을 `h.ok === true && h.ready === true` 로 **명시**한다 — 모르면 닫는다 ③ 응답 본문의 `ok` 도 함께 본다(200 으로 오는 오류 페이지) ④ 초대 링크 버튼은 `!authToken() || accountDown()` — 로그인한 적 없는 사람에게도 보이고 있었다 | **T80** | ⚠️ **서버의 503 fail-closed 를 우회하는 결함이 아니다** — 데이터는 새지 않았다. 깨진 것은 **화면의 fail-closed 와 그것을 재는 검사**이고, 그래서 4단계 로컬 완료 판정의 근거가 무너졌다. ⚠️ 돌연변이 M24 가 **새로 쓴 검사에서 같은 무늬**(빈 store 때문에 우연히 통과)를 다시 잡았다 |
| **65** | **계약을 어긴 `/health` 응답의 `providers` 로 로그인 버튼을 그린다** | **M** | ① 위협 64 를 닫으면서 `apiHealth()` 를 `ready: d.ready === true` 로 고쳤다 — **정규화**다 ② 그래서 `ready` 가 누락·문자열·숫자·`null`·객체인 응답이 **성공 응답으로 통과**하고 `ready:false` 로 조용히 바뀐다 ③ 계정 UI 는 닫히지만 `ok:true` 라 **`providers` 는 그대로 실려 나가** 화면이 「카카오로 로그인」을 그린다 — `ready` 를 계약대로 못 지킨 서버가 준 목록이라 **그 버튼이 되는지 아무도 모른다** ④ 위협 64 를 닫은 `test-client` 94개 중 **어느 것도 이 갈래를 재지 않았다** — 그 검사들은 「계정 UI 가 닫히나」만 봤다 | `apiHealth()` 의 최소 계약에 **`typeof d.ready === "boolean"`** 을 넣는다. 계약을 어긴 응답은 정규화하지 않고 **`DOWN_HEALTH`** 로 보낸다 — 「서버에 못 물어봤다」와 같은 갈래다(버튼을 안 그리고 이유를 말한다) | **T81** | ⚠️ **위협 64 의 수정이 만든 자리다.** 「모르면 닫는다」를 값 하나(`ready`)에만 적용하고 **응답 전체**에는 적용하지 않으면, 닫는 곳과 여는 곳이 갈린다 |

> ⛔ **위협 50 의 「완료」 판정은 9판에서 철회됐다(2026-08-22).** 닫힌 것은 **「`EDGE_GUARD` 가 없을 때
> 계정 경로가 DB 를 만지기 전에 닫힌다」 하나**다. 「방어가 있다」고 판정되는 조건, 열린 뒤의
> 버킷별 한도, 분산 IP, 공개 `/ready` 의 비용은 **위협 52~56 이 이어받는다**(§0-9).

---

## 12. 레이트리밋 · CSRF · 입력 검증

| 경로 | 메서드 | 인증 | Origin 검사 | CSRF | 버킷 | 기준 | 권고 한도 | 본문 | 허용 필드 |
|---|---|---|---|---|---|---|---|---|---|
| `/api/policies` | GET | 불필요 | — | 해당 없음 | 없음(안 센다) | — | — | — | — |
| `/api/signup/start` | POST | 불필요 | **필수** | Origin + 쿠키 없음 | `signup` | IP | **10/분** | ≤ 8KB(`MAX_BODY`) | `provider`·`terms`·`age14`·`pv` |
| `/api/login/:provider` | GET | 불필요 | — | state 서명 | 없음(현행 유지) | — | — | — | — |
| `/api/cb/:provider` | GET | 불필요 | — | state + `shh_t` | `login` | IP | 10/분(현행) | — | — |
| `/api/exchange/:provider` | GET | 불필요 | — | state + `shh_t` | `login` | IP | 10/분(현행) | — | — |
| 가입 취소 | — | — | — | — | — | — | — | — | **API 없음**(서버 상태가 없다) |

- **`/signup` 을 `WRITE_ROUTES` 에 넣지 않는다.** 넣으면 `write`(120) 와 `signup` 이 같은 요청을
  두 번 센다 — 2026-08-16 에 `auth`/`login` 이중 계수로 이미 겪은 실수다(`:700-703`).
- 응답: 형식 오류 400 · 정책 구식 409 `policyStale` · 한도 초과 429 · 제공자 미설정 503 ·
  유지보수 중 503(§10-7).

### 12-2. **남용 방어 계약 — 없으면 계정 경로를 열지 않는다** (8판 신설 · 위협 50)

> ⛔ **이 절은 8판의 계약이고 9판이 대체했다(2026-08-22).** 아래 표의 「`RL` 바인딩 있음 →
> 열린다」는 **truthy 검사 하나**였고, 문자열·빈 객체·던지는 `limit()` 이 전부 통과했다
> (위협 52). 「엣지가 센다」도 **문서의 버킷별 한도와 다른 숫자**를 가리켰다(위협 53).
> **현재 계약은 §12-3 이다.** 이 절은 왜 그렇게 시작했는지의 기록으로 남긴다.


리미터가 세는 신원은 **IP** 다. IP·분당 한도는 **IP 를 나누면 그대로 곱해진다** — 앱 코드가
할 수 있는 것이 없다. 그래서 8판은 막는 대신 **열지 않기로** 했다.

| `env` 상태 | `abuseGuard()` | 계정 라우트 | 카운터 | `/health.ready` |
|---|---|---|---|---|
| `RL` 바인딩 있음 | `"edge"` | 열린다 | ⛔ **엣지가 센다**(우리 DB 를 안 쓴다) — **9판에서 틀렸다고 정정**: 엣지 한도는 설정에 고정이라 문서의 버킷별 숫자와 무관하다(위협 53) | 될 수 있다 |
| `DEV_RATE_LIMIT` 만 있음 | `"dev"` | 열린다 | ledger `rate_limits` | **언제나 false** |
| 둘 다 없음 | `"none"` | **DB 를 만지기 전에 503** | 없음 | false |

- **`DEV_RATE_LIMIT` 은 로컬 전용이다.** 배포 가능한 설정 파일에 들어가면
  `scripts/test-config.mjs` 가 실패시킨다. 이 값으로는 `ready` 가 **절대** 참이 되지 않는다 —
  「테스트가 통과하니 배포해도 된다」로 읽히는 길을 막는 자리다.
- **계정 라우트 = `/health`·`/ready`·`/policies` 를 뺀 전부**다. `GET /login/:provider` 도
  포함한다 — 열어 두면 제공자까지 갔다가 콜백에서 죽는다.
- **확인된 사실(2026-08 공식 문서)**: Pages Functions 의 지원 바인딩 목록에 **ratelimits 가
  없다**(Workers 전용). Turnstile 도 바인딩이 아니다. WAF 레이트리밋 규칙은 **우리 존**이
  필요한데 `*.pages.dev` 는 Cloudflare 소유다. 즉 **지금 이 프로젝트에는 `"edge"` 가 없다** —
  그래서 계정 경로는 fail-closed 이고, 그 상태가 정확히 「공개 출시 No-Go」의 기술적 내용이다.
- 사용자 결정이 필요한 항목(커스텀 도메인 · Workers 전환 · Turnstile · 유료 플랜)은
  `docs/OPS_RUNBOOK.md` §13 에 대안·비용·대가와 함께 적는다. **Claude 가 고르지 않는다.**

### 12-3. **남용 방어 계약 v2 — 선언 · 검증 · 두 층 분리** (9판 신설 · 위협 52~56)

#### 12-3-1. 모드는 운영자가 선언하고, 코드가 실재를 검증한다

`abuseGuard(env)` 는 더 이상 바인딩의 **존재**를 방어의 증거로 쓰지 않는다.

| `env` | `abuseGuard()` | 계정 라우트 | `abuseReady` | 근거 |
|---|---|---|---|---|
| `EDGE_GUARD="ratelimit"` + `typeof RL.limit === "function"` | `"ratelimit"` | 열린다 | **바인딩을 실제로 호출해 본 결과** | 위협 52 |
| `EDGE_GUARD="ratelimit"` + `RL` 이 없거나 모양이 다름 | `"none"` | **DB 전에 503** | false | 위협 52 |
| `EDGE_GUARD="waf"` + `APP_ORIGIN` 이 커스텀 도메인 | `"waf"` | **그 호스트로 온 것만** | true(선언 기반) | 위협 55 |
| `EDGE_GUARD="waf"` + `APP_ORIGIN` 이 `*.pages.dev` | `"none"` | **DB 전에 503** | false | 위협 55 |
| 모르는 `EDGE_GUARD` 값 | `"none"` | **DB 전에 503** | false | 기본값이 안전한 쪽 |
| `DEV_RATE_LIMIT` 만 있음(로컬) | `"dev"` | 열린다 | **언제나 false** | 8판과 같다 |
| 아무것도 없음 | `"none"` | **DB 전에 503** | false | 8판과 같다 |

- **`broken` 은 `over` 와 다른 응답이다.** 한도 초과는 **429 + `Retry-After: 60`**,
  방어 고장은 **503 + `Retry-After: 60`** 이다. 8판에서는 고장이 조용한 통과였고,
  반환값이 이상할 때는 **전부 429**(= 전체 서비스 거부)였다 — 둘 다 원인을 감춘다.
- **엣지 사전 거름은 게이트보다 앞이다.** 우리 DB 를 하나도 안 만지므로 그 자리가 가장 싸고,
  그래서 「고장 난 바인딩」의 503 은 **두 DB 질의 0건**으로 나간다(T65).

#### 12-3-2. `waf` 모드 — 코드가 증명할 수 없는 것과 그 짝

**애플리케이션 런타임은 WAF 규칙의 존재를 알 수 없다.** 요청이 규칙을 지나왔는지 알려주는
헤더도 바인딩도 없다. 그래서 `waf` 는 **선언**이고, 코드가 대신 강제하는 것은 둘이다.

1. **호스트 잠금.** 계정 라우트는 `new URL(env.APP_ORIGIN).host` 로 온 요청만 받는다.
   `*.pages.dev` 로 온 계정 API 는 **403** 이다 — WAF 규칙은 우리 존에만 걸리므로
   pages.dev 경유는 규칙을 통째로 건너뛴다.
2. **`*.pages.dev` 를 `APP_ORIGIN` 으로 둔 채 `waf` 를 선언하면 `none` 이다.**
   그 조합은 「WAF 를 붙였다」가 성립할 수 없는 조합이다.

**남는 것은 운영 점검이다** — WAF 규칙이 실제로 있는지는 `docs/OPS_RUNBOOK.md` §13-2 의
외부 확인 항목이 답한다. `/ready` 는 그것을 **참이라고 말하지 않는다**: `abuseReady` 는
「앱 쪽 조건이 갖춰졌나」이고, 외부 검증은 **사람이 체크리스트에 기록**한다.

#### 12-3-3. 무엇을 어디까지 막나 — 수치로

| 공격 | 9판 이후 | 무엇이 막나 |
|---|---|---|
| 같은 IP 의 반복 | **버킷별 한도에서 멈춘다**(login 10 · signup 10 · friends 20 · rotate 5 · read 120 · write 120, 분당) | 우리 ledger 카운터(전역 일관) |
| 방어가 선언되지 않음 | 계정 경로 **전부 503 · 두 DB 질의 0건** | `abuseGuard` |
| 바인딩이 고장 남 | 계정 경로 **503 · 두 DB 질의 0건** | 엣지 사전 거름의 `broken` |
| 공개 `/ready` 반복 | **두 DB 질의 0건** | `READY_KEY` |
| **여러 IP × 인증 없는 요청** | 요청당 ledger 쓰기 **3건**(카운터 1 + 임차증 2) — **상한 없음** | ⛔ **못 막는다.** 서명 세션 envelope 를 도입하면 **0건**(사용자 결정 · §12-3-4) |
| **여러 IP × 유효한 세션** | 계정 수만큼 곱해진다 | ⛔ 코드로 못 막는다 — 가입 경로(제공자 인증)가 상한이다 |
| 볼류메트릭(초당 수천) | — | ⛔ 엣지의 일. WAF 무료 등급은 **규칙 1 · IP · 10초 · 경로 필드** |

**환산**: D1 무료 한도는 하루 **쓰기 10만 행**이다. 요청당 3건이면 **약 3.3만 요청**으로
하루치가 소진된다. 서명 envelope 를 도입하면 그 3.3만은 **인증에 성공한 요청**에만 적용된다.

#### 12-3-4. 사용자 결정 — **확정됨 (2026-08-22)**

| # | 결정 | 그래서 코드가 이렇게 된다 |
|---|---|---|
| **0** | **공개 배포 안전 동기화를 먼저 한다.** 계정 기능을 여는 배포가 아니라 **현재 공개 환경을 닫힌 상태로 맞추는** 배포다 | `READY_KEY` 등록 · `EDGE_GUARD` 는 **아직 안 켠다** · 계정 API 는 D1 접근 전 503 · 비인증 `/ready` 는 진단 비공개. 배포 후 실제 공개 주소에서 재검증 |
| **1** | **엣지 방어는 A(커스텀 도메인 + WAF)** | `EDGE_GUARD="waf"` + 호스트 잠금. API 는 `worker/index.js` 가 403, 정적 화면은 `functions/_middleware.js` 가 302 로 정식 호스트에 보낸다. **WAF 작동은 설정값이 아니라 Security Events 와 실제 제한 테스트로 확인한다** |
| **2** | **B(Workers 전환)는 보류** | 지금 구조를 바꾸지 않는다. WAF 운영 결과로 부족하다고 판단되면 **같은 호스트의 `/api/*` Worker Route** 를 먼저 검토한다 |
| **3** | **Turnstile 은 공개 회원가입에만** | `POST /signup/start` 가 토큰을 요구하고 **서버가 siteverify 로 검증**한다. 결과는 AEAD 가입 state 의 `hv` 로 콜백까지 이어진다. 로그인·인증된 읽기·쓰기에는 **초기 출시에 적용하지 않는다** — 공격 지표가 늘면 로그인 시작점까지 넓힌다 |
| **4** | **서명 세션 envelope 를 도입한다** | 전용 `SESSION_ENVELOPE_KEY` · 판과 만료를 담은 HMAC. 쿠키 부재·변조·만료·모르는 판은 **D1·ledger 접근 전에** 401. 통과해도 **D1 이 최종 판정**한다. 키 교체 = 전원 로그아웃(운영 문서에 명시) |
| **5** | **D1 비용 수치는 실측 전까지 추정치다** | 「요청당 3건」은 SQLite 의 **논리적 변경 수**다. D1 은 인덱스 변경도 쓰기 행으로 셀 수 있으므로 **「약 3.3만 요청이면 하루 한도 소진」을 확정 수치로 쓰지 않는다.** 배포 후 `meta.rows_written` 으로 확정한다(§12-3-6) |
| **6** | **잔여 위험을 인정한다** | 저속 분산 · 정상 계정을 만든 공격자 · 탈취된 세션 · WAF 탐지 지연 · Worker 요청량 소진 · 운영 설정 오류. **「완전히 제거됨」으로 기록하지 않는다** |
| **7** | **단계 판정과 출시 조건** | 결정 확정은 3단계, envelope 구현·테스트는 4단계 로컬, 도메인·WAF·D1·시크릿·OAuth·배포는 4단계 원격. 폐쇄 베타 진입 조건 9가지는 §21-0 |

**처리 순서를 고정한다**(결정 4):

```
요청
 → 라우트 판정(없으면 404 · 어느 DB 도 안 만진다)
 → 남용 방어 모드 · 호스트 잠금 · 엣지 사전 거름     ← CPU/엣지만
 → **세션 envelope 검증(CPU)** · 경로별 인증 요구
 → 유지보수 게이트(ledger 읽기)
 → 내부 제한기(ledger 쓰기) → 임차증
 → D1 세션·사용자 확인(whoAmI)
 → 실제 요청 처리
```

⚠️ **envelope 통과는 인증이 아니다.** 그 구분이 무너지면 로그아웃·탈퇴·세대 무효화가 전부
무력해진다 — `test-friends` **T71-c·T71-d** 가 그 자리를 고정한다.

#### 12-3-6. D1 비용 — **아직 추정치다** (결정 5)

지금 테스트가 세는 것은 `sqlite3_total_changes()`, 즉 **논리적 행 변경 수**다. D1 이 청구하는
`rows_written` 은 **기본키·보조 인덱스 갱신도 함께** 셀 수 있어 더 클 수 있다.

| 무엇 | 지금 값 | 성격 |
|---|---|---|
| 인증된 요청 1건의 ledger 변경 | **3**(카운터 1 + 임차증 2) | 실측(논리적 변경 수) |
| 인증 없는 요청 1건 | **0** | 실측(결정 4 이후) |
| 하루 10만 쓰기를 태우는 요청 수 | ~3.3만 | ⚠️ **추정치** — 확정 수치로 쓰지 않는다 |

**배포 후 확정할 것**: 요청 유형별 실제 읽기·쓰기 행(`meta.rows_written`) · 익명 요청의 실제
비용 · 일일 한도 소진 예상치 · **경보와 계정 기능 자동 폐쇄 기준**. 절차는
`docs/OPS_RUNBOOK.md` §15.

#### 12-3-7. 잔여 위험 — 인정하고 관찰한다 (결정 6)

- 서로 다른 IP 에서 오는 **저속 분산 요청**
- **정상적으로 계정을 만든** 공격자의 요청
- **탈취된 유효 세션**을 쓰는 요청
- Cloudflare WAF 의 **탐지·차단 지연**
- **Worker 요청량 자체**의 소진(D1 과 별개다)
- **WAF 또는 시크릿의 운영 설정 오류**

⛔ 이 여섯을 「완전히 제거됨」으로 적지 않는다. **폐쇄 베타에서 사용량과 차단 이벤트를 관찰한
뒤** 공개 출시 여부를 다시 결정한다.

#### 12-3-5. 공개 readiness 설계

| 주소 | 인증 | DB | 무엇을 답하나 |
|---|---|---|---|
| `/api/health` | 없음 | **안 만진다** | 프로세스가 살아 있나 · 어느 제공자 버튼을 그릴 수 있나 |
| `/api/ready` (키 없음) | — | **안 만진다** | `{ok:true, ready:false, diagnostics:false}` · **503** |
| `/api/ready` (`X-Ready-Key`) | `READY_KEY` | 주 D1 1 · ledger 5 | 8판까지의 전체 진단 |

- 비교는 **상수 시간**(`sameSecret`)이고, 틀린 키는 DB 를 만지지 않는다.
- `READY_KEY` 가 등록되지 않은 배포에서는 **진단이 불가**다. 「키가 없으면 전부 공개」로
  되돌아가지 않는다 — 그것이 지금 고치는 결함이다.
- 응답에 표 이름·SQL·오류 문자열·개수를 싣지 않는 규칙은 그대로다.

### 12-1. `RL_MAX` fallback 제거 — 구현 단계 필수 요구사항 (2판)

1판은 "`RL_MAX` 에 `signup: 10` 을 추가한다"로 끝냈다. **그것으로는 재발을 못 막는다.**

```js
const max = RL_MAX[bucket] ?? RL_MAX.write;   // worker/index.js:369 — 현재 코드
```

이 한 줄 때문에 **버킷 이름을 오타 내거나 등록을 잊으면 조용히 120/분**이 된다. 그리고 그것은
정확히 2026-08-16 에 이미 일어난 사고다 — `auth` 버킷이 `RL_MAX` 에 없어서 write 기본값 120 이
적용됐고, 한도 10 인 `login` 보다 훨씬 느슨해 **한 번도 먼저 막은 적이 없었다**(`:700-703`).
같은 코드가 그대로 남아 있으므로 **다음 버킷에서 같은 일이 난다.**

| # | 구현 단계에서 반드시 할 것 |
|---|---|
| 1 | `?? RL_MAX.write` **fallback 을 제거한다** |
| 2 | 등록되지 않은 버킷 이름으로 `limited()` 를 부르면 **던진다**(조용히 통과시키지 않는다) |
| 3 | 던진 것을 라우트가 삼키지 않는지 확인한다 — 전역 예외 그물(`:657-667`)이 500 으로 바꾸는데, **개발·테스트에서 즉시 드러나면 목적은 달성**된다. 운영에서 500 이 나는 것보다 조용히 120 인 것이 나쁘다 |
| 4 | `test-friends` 에 **미등록 버킷 호출 → 실패**를 박는다. 이 검사가 곧 "새 버킷을 등록하게 만드는 장치"다 |
| 5 | `RL_MAX` 에 `signup: 10` 추가 |

**왜 fallback 을 없애는 것이 더 안전한가**: fallback 이 있으면 실수가 **동작하는 코드**가 되어
아무도 못 알아챈다. 없애면 실수가 **실패하는 테스트**가 된다. 잊을 수 있는 자리를 없애는 것이
잊지 말자고 적는 것보다 싸다 — 서비스워커 캐시 이름을 빌드가 박게 만든 것과 같은 판단이다.
- **로그에 남길 수 있는 값**: 제공자 이름 · 실패 종류 · 경로 템플릿(`pathTemplate`).
- **로그 금지**: 가입 대기 토큰(있다면) · `provider_subject` · `pv` 나 문서 해시 **전체** ·
  `state` · `code` · `mark` · 별명 · IP 원문.

---

## 13. 테스트 명세 (이번 단계에서는 파일을 만들지 않는다)

### `scripts/test-signup.mjs` (신규)

`test-friends.mjs` 와 같은 방식 — `worker/index.js` 를 그대로 불러 진짜 sqlite(`_d1.mjs`) 위에서 돈다.

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | 신규 OAuth 성공(로그인 경로, `pv` 없음) | `users` **0행**, 응답이 `signup_required` |
| 2 | 기존 사용자 로그인 | 세션 발급, `policy_events` 증가 0 |
| 3 | 신규 + 유효 `pv` | `users` 1행 + `policy_events` **N행**(§8-0) + 세션. **`requiredPolicyKinds` 의 각 원소가 정확히 한 번**, 누락·중복·집합 밖 이벤트 0 |
| 4 | 약관 미체크 | `/signup/start` 400, DB 무변화 |
| 5 | 연령 미체크 | 400, DB 무변화 |
| 6 | 방침 `presented` 기록 | 분기 ① 기준 `action='presented'` 로 기록 |
| 7 | 잘못된 조합(`age14/accepted`) | `CHECK` 위반으로 INSERT 실패 → batch 롤백 |
| 8 | `state` 만료 | 400, DB 무변화 |
| **9a** | `state` 재사용 — **같은 `code`** | 두 번째는 제공자가 `code` 를 거부 → 실패 |
| **9b** | `state` 재사용 — **새 `code`**(authorize URL 재실행) | **통과한다.** 이것이 현재 설계의 감수 사항임을 테스트가 **명시적으로 고정**한다(§4-A). 결과 계정은 **하나**여야 하고 `policy_events` 도 **N행 그대로**(§8-0)여야 한다 — 재생이 이벤트를 **불리지 않는다** |
| **9c** | `state` 재사용 — **다른 브라우저**(`shh_t` 없음/불일치) + 새 `code` | **400**, code 교환 **전에** 거부. `users`·`sessions` 무변화. **I8 의 「다른 사람의 세션을 얻을 수 없다」가 여기서 검증된다** |
| 10 | 두 탭 동시 가입 | `withLatency` 로 경합 재현 → `users` 1행 · `policy_events` **정확히 N행**(§8-0) |
| 11 | 세션 고정 | `shh_t` 없는 콜백 → code 교환 **전** 400 |
| 12 | 가입 완료 후 새 세션 | 가입 전 쿠키로는 아무것도 안 된다 |
| 13 | 계정·이벤트 원자성 | 이벤트 INSERT 실패 주입 → `users` 0행 |
| 14 | Origin 없음/외부/허용 | 403·403·통과 |
| 15 | 가입 rate limit | 11번째 429 |
| 16 | 제공자 ID 비노출 | 응답 본문·로그에 `provider_subject` 0회 |
| 17 | 위조 `pv` | 409, DB 무변화 |
| 18 | 탈퇴 후 재가입 | 새 `users.id` != 옛 id |
| **19** | **행위 시각** | `occurred_at ≤ recorded_at`, 간격이 state 만료(10분) 이내. 클라이언트가 보낸 시각 필드는 **무시**된다 |
| **20** | **유지보수 중 가입** | `MAINTENANCE=1` 에서 `POST /signup/start`·`GET /cb` → **503, `users`·`rate_limits` 행 수 무변화** |

### `scripts/test-policies.mjs` (신규)

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | 파일 해시 == manifest | 일치 |
| 2 | 파일을 고치고 manifest 를 그대로 | **실패** |
| 3 | 기존 manifest 엔트리 수정·삭제 | **실패**(엔트리 불변) |
| 4 | 서버 상수 == manifest 현재 번들 | 일치 |
| 5 | `privacy.html` == 최신 `policies/privacy-*.html` | 일치 |
| 6 | SW 선캐시 목록 == 현재 번들 파일 | 일치 |
| 7 | 미등록 `document_version` 제출 | 서버가 거부 |
| 8 | `pv` 계산이 파일 내용에만 의존 | 파일 한 글자 변경 → `pv` 변경 |

> **이 테스트가 증명하지 못하는 것**(2판): 과거 항목을 **파일과 manifest 에서 함께** 지우는
> 의도적 삭제는 잡지 못한다 — 둘 다 사라지면 남은 것끼리는 여전히 일관되기 때문이다.
> 「불변」은 테스트가 아니라 **Git 이력과 리뷰**로 지켜지는 운영 규칙이다(§7-2 Q8).
> 테스트는 **실수를 잡을 뿐 고의를 막지 못한다.** 문서에 그렇게 적는다.

### `scripts/test-deletion-ledger.mjs` (신규)

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | pending 기록 실패 | 사용자 "지우지 못했어요", 주 D1 무변화 |
| 2 | 주 D1 삭제 실패 | 실패 응답, 세션 유지, pending 남음 |
| 3 | confirmed 기록 실패 | **성공 응답**(계정은 없다), pending 남음 |
| 4 | 중복 삭제 요청 | 표식 1개, saga 이어서 진행 |
| 5 | 재가입 | 새 UID 의 HMAC 이 옛 표식과 불일치 |
| 6 | pending 오삭제 방지 | 복원 후 재삭제에 pending 이 **안 쓰임** |
| 7 | confirmed 재삭제 | 복원 후 일치 계정 삭제 + CASCADE 0건 잔여 |
| 8 | 키 버전 | v1·v2 혼재 상태에서 둘 다 대조됨 |
| **9** | **만료 정리 — pending 과 confirmed 분리** | `pending_alert_at` 이 지난 **pending 은 여전히 남아 있다**(자동 삭제 금지). `expires_at` 이 지난 **confirmed 만** 제거된다 |
| 10 | 실제 UID 로그 비노출 | 로그 문자열에 uid 0회 |
| **11** | **ledger 복원 + 합집합 병합** | 현재 ledger 내보내기 → 과거 스냅샷으로 교체 → 병합. **병합 후 confirmed 개수가 복원 전보다 줄지 않는다.** 내보낸 모든 `mark` 가 결과에 있다 |
| **12** | **병합이 확정을 되돌리지 않는다** | 같은 `mark` 가 옛 스냅샷에서 pending, 현재에서 confirmed → 병합 결과는 **confirmed**. `expires_at` 은 **큰 쪽** |
| **13** | **reconciliation 경합 거부** | `MAINTENANCE` 가 아닌 상태에서 reconciliation 호출 → **거부**(실행되지 않는다). 이것이 위협 27 의 유일한 방어다 |
| **14** | **유지보수 중 삭제 차단** | `MAINTENANCE=1` 에서 `DELETE /me` → 503, `users`·`deletions` 행 수 **무변화** |
| **15** | **확정 시 `expires_at` 재계산** | `confirmed_at` 이 기록되는 순간 `expires_at == confirmed_at + 37일`. pending 시절의 임시값이 그대로 남아 있지 않다 |
| **16** | **정리가 삭제를 실패시키지 않는다** | 5번 정리 문장에 실패를 주입해도 `DELETE /me` 는 **성공 응답** |

**중간 상태를 실제로 만들 수 있는가**: 1·2·3·16 은 ledger 핸들을 **주입 가능한 인자**로 두면
실패를 실제로 일으킬 수 있다. 7·11·12 는 "복원"을 sqlite 두 인스턴스(스냅샷 / 현재)로 재현한다 —
진짜 Time Travel 은 못 부르지만 **재적용·병합 로직 자체는 실제로 돈다**.
13 은 `MAINTENANCE` 플래그를 끈 채 부르면 되므로 그대로 잴 수 있다.
**1·2·3·13 을 정규식 소스 검사로 대신하지 않는다.**

> **못 재는 것도 적어 둔다**: 진짜 `wrangler d1 time-travel restore` 는 테스트에서 부를 수 없다.
> 11·12 가 재는 것은 **병합 규칙**이지 Cloudflare 의 복원 동작이 아니다. 복원 자체는
> §10-6·§10-8 의 **절차**가 담당하고, 절차는 자동 검사가 없다.

### `scripts/test-friends.mjs` 에 추가할 블록 (유지보수 모드)

워커 라우트 검사는 이 파일에 산다. 새 파일을 만들지 않는다.

| # | 시나리오 | 기대 |
|---|---|---|
| M1 | `MAINTENANCE=1` · `GET /api/cb/:provider` | **503**, `users`·`sessions`·`rate_limits` 행 수 무변화 |
| M2 | `MAINTENANCE=1` · `GET /api/exchange/:provider` | 같음 |
| M3 | `MAINTENANCE=1` · **`GET /api/friends`** | **503**, `invite_codes` 행 수 **무변화**(myCode 가 INSERT 하지 못한다) |
| M4 | `MAINTENANCE=1` · `PUT /book` · `POST /friends` · `DELETE /session` · `DELETE /me` | 전부 503, 행 수 무변화 |
| M5 | `MAINTENANCE=1` · `GET /api/health` · `GET /api/book` · `GET /api/friends/:id/book` | **통과**(읽기는 열려 있다) |
| M6 | `mode='maintenance'` · `GET /api/ready` | **503** + `mode:"maintenance"` · `ready:false` |
| M7 | 유지보수 해제 후 같은 요청들 | 정상 동작(회귀 없음) |
| M8 | **미등록 rate limit 버킷** | `limited()` 가 **던진다**(120 으로 조용히 통과하지 않는다) — §12-1 |

### 기존 테스트 중 손댈 것

| 파일 | 무엇 |
|---|---|
| `test-friends.mjs` | `signUp()` 헬퍼가 `internalUid()` 를 직접 쓴다 → `createAccountWithPolicy()` 로 교체. 156개 단언의 **의도는 그대로** 둔다. **+ 유지보수 모드 블록 M1~M8**(위) |
| `test-client.mjs` | 게이트에 가입/로그인 두 버튼 · 정책 해시 불일치 시 버튼 미표시 · `signup_required` 응답 처리 |
| `test-migrations.mjs` | `policy_events` 의 CASCADE(§8 의 분기에 따라 달라진다) · 새 `CHECK` 조합 검사 |
| `test-sw.mjs` | 정책 파일이 선캐시 목록에 있는지 |
| `test-dist.mjs` | `policies/` 가 `dist` 에 나가는지 · 그 안에 허용 확장자만 있는지 |
| `_d1.mjs` | ledger 용 **두 번째 DB 핸들** 지원(현재는 `makeD1()` 하나). 두 DB 사이에 **공통 트랜잭션이 없다는 사실**이 셰임에서도 성립해야 한다 — 하나로 합치면 saga 가 트랜잭션처럼 보여 §10-3 의 실패를 아예 못 잰다 |

### 13-5. **4단계에서 구현할 테스트 명세 81건** (3판 신설 · 4판 개정 · **5판 확장** · **T49 는 4단계 구현 중 추가** · **T50~T55 는 6판 재감사에서 추가** · **T56~T60 은 7판 독립 재감사에서 · T61~T64 는 8판 마감 감사에서 · T65~T69 는 9판 남용 방어 재감사에서 · T73~T76 은 10판 전체 재검증에서 · T77~T79 는 11판 독립 검토에서 · **T80·T81 은 2026-08-23 후속 재검증에서** 추가** — 5판 시점에는 파일을 만들지 않았다)

위 표들이 "무엇을 재나"였다면 여기는 **"어떻게 재나"**다. 각 항목에 사전조건·입력 순서·
예상 상태·예상 응답·**실패하면 잡아내는 불변식**을 적는다. 구현 파일은 4단계에서 만든다.

#### A. Quiescence 및 유지보수 (`test-deletion-ledger.mjs` · `test-friends.mjs`)

| # | 사전조건 | 입력 · 동시 실행 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T1** | `mode='open'`, 계정 X 존재. saga 를 2단계(주 D1 삭제) **직전에 멈추는 주입점**을 둔다 | ① `DELETE /me`(X) 진입 → lease 획득 → pending 기록 → **정지** ② `mode='closed'`, `epoch+1` ③ drain 폴링 | pending 표식 1행 **남는다**. `users` 에 X **아직 있다**. 활성 lease **1건** | ①은 아직 응답 없음. ②③은 운영 명령(HTTP 아님) | **유지보수 전환은 in-flight 요청을 끝내지 못한다.** drain 카운트가 0이 되면 **실패**(테스트가 lease 를 세고 있으므로 1이어야 한다) |
| **T2** | T1 상태(활성 lease 1건) | reconciliation 실행 | ledger·`users` **무변화**. pending 그대로 | reconciliation **거부** (운영 명령이면 비영 종료 코드) | **I15** — 증거 없이는 실행하지 않는다. 실행되면 위협 27 이 되살아난다 |
| **T3** | T1 의 saga 를 재개시켜 정상 종료(`released_at` 기록) | ① saga 재개 → 주 D1 삭제 성공 → confirmed ② drain 폴링 → 0 ③ reconciliation | 활성 lease **0건**. 표식은 **confirmed**. `users` 에 X **없음** | reconciliation **허용**. 승격 대상 0건(이미 confirmed) | **lease 해제 후에만 허용**된다. T2 와 쌍으로 봐야 「거부가 늘 거부」가 아님이 증명된다 |
| **T4** | `mode='closed'`(유지보수 활성) | 신규 `DELETE /me` · `GET /cb` · `GET /exchange` · `GET /friends` 각각 1회 | `users`·`sessions`·`invite_codes`·`rate_limits`·`deletions` **행 수 전부 무변화** | 전부 **503** + `Retry-After` | **I13** — 진입 차단은 라우트 허용 목록으로 한다. `GET` 이라 통과하면 위협 28 |
| **T5** | `mode='closed'`, `epoch = E+1`. **옛 epoch `E` 의 lease** 를 든 요청을 만든다 | 그 lease 로 ledger 쓰기(confirmed UPDATE) 시도 | ledger **무변화** | **503** (fail-closed) | §10-9 Q2·Q8 — epoch 불일치 요청은 통과하지 못한다. 통과하면 유지보수 경계가 무의미 |
| **T6** | `mode='closed'`, 활성 lease **0건**. 동시에 `PUT /book` 요청 하나를 **유지보수 검사 통과 후** 지점에서 멈춰 둔다 | ① drain 지표(lease 기반)를 읽는다 ② 멈춘 `PUT /book` 을 재개시킨다 | lease 카운트는 **0** 인데 `books.version` 이 **증가한다** | 재개된 `PUT` 은 **200** | **핵심 회귀** — 「lease 0건 = 모든 쓰기 정지」가 **거짓**임을 테스트가 고정한다. 이 테스트가 통과하지 않으면(=쓰기가 안 일어나면) 오히려 전제가 바뀐 것이므로 설계를 다시 본다 |
| **T7** | §10-8-0 의 **9조건 중 하나라도 미충족**인 상태(5판 당시 명세는 「전역 drain 미구현(①~④ 미충족)」이었다 — 조건이 9개로 늘고 drain 이 구현되면서 대상이 바뀌었다) | 주 D1 복원 절차 진입 명령 | 아무 변경 없음 | 절차 **시작 거부**, 사유에 **미충족 조건이 열거된다**. 실제 구현은 9조건 각각을 따로 잰다(§13-6 T7) | **I16** — 금지 gate 가 문서가 아니라 실행 가능한 검사로 존재한다. 사람이 기억해야 도는 규칙은 언젠가 잊힌다 |
| **T8** | 게이트 로직이 **없는** 옛 세대 핸들러를 준비한다(현재 `worker/index.js` 를 그대로 쓰면 된다) | `mode='restore_closed'` 상태에서 옛 세대로 `PUT /book` **과** `GET /book` | 옛 세대는 **쓰고 읽는다**(막히지 않는다) | 200 | **위협 33 을 사실로 고정한다.** ⛔ **5판 — 이 테스트가 「200」인 동안 주 D1 restore 금지는 유지된다.** 합격 조건은 **「거부됨」만**이고 「탐지됨」은 **불합격**이다(§10-8-1 D11). 거부로 바꾸는 길은 코드가 아니라 **§10-8-1 의 D1~D12**(옛 배포를 없애거나 닿지 못하게 하는 것)다 |

> ⚠️ **위 T6 은 5판 당시의 명세이고, 현재 구현으로 대체됐다.** 그날의 기대값은 「lease 카운트가
> 0인데 쓰기가 일어난다」였다 — 그때는 임차증을 삭제 saga 만 땄기 때문이다. **2026-08-18 결정 A′
> 구현 이후 T6 은 실패 재현이 아니라 통과 검사**이고, 실제 기대값은 「도는 동안 활성 1 · 도는 중
> 복원 거부 · 끝난 뒤에만 0」이다. 무엇을 실제로 재는지는 **§13-6 의 T6·T6b·T6c 행**이 원본이다.
> 이 명세를 지우지 않는 이유는 **왜 그 시험이 필요했는지가 여기에만 남기 때문이다.**
>
> **지금 「실패를 재현하는 테스트」는 T8 하나다.** 통과가 곧 안전이 아니라 **우리가 아직 못 막는
> 것을 잊지 않기 위한 고정 장치**이고, 거부로 바꾸는 길은 코드가 아니라 §10-8-1 의 D1~D12 다.

#### B. Ledger 병합 (`test-deletion-ledger.mjs`)

사전조건 공통: 두 sqlite 인스턴스 `cur`(현재 내보내기) · `old`(복원된 과거)를 만들고
§10-6-1 의 병합 함수를 **순수 함수로** 호출한다. 진짜 `time-travel restore` 는 부르지 않는다.

| # | 사전조건 | 입력 | 예상 결과 | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T9** | 같은 `mark` 가 양쪽에 있고 `confirmed_at` 이 **둘 다 non-NULL**(`cur`=T2, `old`=T1, T1<T2) | 병합 | `confirmed_at = T2`(**더 늦은 값**) | — | §10-6-1 — 2판의 미정 케이스. 이른 값을 고르면 `expires_at` 이 짧아져 보호가 준다 |
| **T10** | `expires_at` 이 `cur`=E2, `old`=E1 (E1<E2) | 병합 | `expires_at = E2`(**더 큰 값**) | — | 보존을 짧게 하는 방향은 언제나 위험한 쪽 |
| **T11** | `pending_at` 이 `cur`=P2, `old`=P1 (P1<P2) | 병합 | `pending_at = P1`(**더 이른 값**) | — | 최초 시도 시각이 사실이다. 늦은 값을 쓰면 `pending_alert_at` 이 밀려 경보가 늦는다 |
| **T12** | 같은 `mark` 인데 `key_version` 이 `cur`=v2, `old`=v1 | 병합 | **병합하지 않고 중단.** 결과 DB 를 쓰지 않는다 | 절차 중단(비영 종료) | §10-6-1 — 자동 판단 금지. 같은 mark 가 다른 키에서 나온 것은 충돌이거나 오염이다 |
| **T13** | 2번의 export 를 실패시키거나 해시가 불일치하게 만든다 | 복원 절차 실행 | 복원 **실행되지 않음**. ledger 무변화 | 절차 중단 | §10-6-1 중단 규칙 — 검증 불가 = 복원 금지 |
| **T14** | T12·T13 의 중단 상태 | 「어떻게든 진행」 경로를 찾는다 | **그런 경로가 없다** — 강제 진행 플래그·기본값 병합이 코드에 존재하지 않는다 | — | **fallback 없음.** 소스에 우회로가 있으면 실패해야 한다(`RL_MAX` fallback 과 같은 교훈, §12-1) |
| **T15** | 같은 UID 에 대해 v1 표식(pending)과 v2 표식(confirmed)이 각각 존재 | 복원 후 대조 → 재삭제 대상 산출 | 대상 **1명**(표식 수 2 ≠ 대상 수 1) | — | K2·K3 — 표식 개수로 세면 **정상 상황이 실패로 판정**된다 |
| **T16** | 같은 UID 에 confirmed 1건 + pending 1건 | 대상 판정 | **재삭제 대상에 포함**(confirmed 우선) | — | K5 — pending 이 confirmed 를 무효화하지 못한다 |
| **T17** | pending **만** 있는 UID 를 섞는다 | 대상 판정 + `삭제된 users 행 수 == 고유 UID 수` 검증 | pending-only UID 는 **대상 아님**. 검증식이 **참** | — | K6 · 2단계 §9-7 — pending 을 재삭제에 쓰면 살아 있는 계정을 지운다 |

#### C. Policy events (`test-signup.mjs` · `test-policies.mjs`)

`N = REQUIRED_POLICY_EVENTS = requiredPolicyKinds.length`(§8-0). **테스트는 3을 하드코딩하지 않고
서버 상수를 읽는다** — 법률 결정으로 N 이 바뀌어도 테스트를 고치지 않아야 한다.

| # | 사전조건 | 입력 | 예상 DB | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T18** | 신규 제공자 계정, 유효 `pv` | `POST /signup/start` → 콜백 | `users` 1행 + `policy_events` **N행**. `requiredPolicyKinds` 의 **각 원소가 정확히 한 번** | 302/200 성공 | I4 — 계정과 필수 이벤트가 갈라지지 않는다 |
| **T19** | 같음. 이벤트 INSERT 중 **하나**를 강제 실패시킨다 | 같은 흐름 | `users` **0행**, `policy_events` **0행**(batch 전체 롤백) | 실패 응답 | I4 · 위협 7 — `batch()` 가 단일 트랜잭션이라는 전제를 실제로 잰다 |
| **T20** | 같은 `kind` 를 두 번 담아 보낸다 | 가입 요청 | `users` 0행, 이벤트 0행 | **400** | 중복 이벤트가 「N행」 검사를 우회하지 못한다 |
| **T21** | 허용되지 않은 조합(예: `age14`/`accepted`) | 가입 요청 | `CHECK` 위반 → batch 롤백, `users` 0행 | 400 또는 500 (구현 시 400 으로 정규화) | 스키마 `CHECK` 가 실제로 강제된다 — 문서에만 있는 제약이 아니다 |
| **T22** | `policies/` 파일을 한 글자 고치고 manifest 는 그대로 둔다 | `test-policies` 실행 | — | — | I11 — 파일·manifest·서버 상수 세 값이 갈리면 **빌드/테스트가 실패**한다 |
| **T23** | 유효한 state 를 받은 뒤 **정책 번들을 새 버전으로 교체** | 옛 `pv` 가 든 state 로 가입 완료 시도 | `users` 0행, 이벤트 0행 | **409** `policyStale` | 사용자가 본 문서와 기록될 해시가 다르면 기록하지 않는다 |

#### D. **state 재사용 — 로그인과 가입을 나눠서 잰다** (`test-signup.mjs`) — 4판 전면 개정

**전제 사실**(§4-A·§4-B):

- `takeState()` 는 서명·만료만 본다 — **서버 소비 기록이 없다**(`:180-191`).
- 성공 응답의 `clearTxn()` 이 `shh_t` 를 지운다(`:766`·`:809`) → **순차 재사용은 보통 막힌다.**
- 그러나 **동시 요청**과 **쿠키 삭제를 무시하는 클라이언트**는 통과한다.
- **로그인 state** 는 그 통과를 감수한다. **가입 state** 는 소비 표식으로 막는다(§5-3).

3판의 T26·T27 은 **다른 제공자 계정으로 새 계정과 정책 이벤트가 생기는 것을 「성공」으로
기대**하고 있었다. 그것이 위협 34 다. 아래가 그 정정이다.

**D-1. 정상 · 순차 재사용**

| # | 사전조건 | 입력 · 순서 | 예상 DB | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T24** | 신규 제공자 계정, 유효 가입 state | 정상 가입 왕복 **1회** | `consumed_signup_states` 1행 · `users` 1행 · `policy_events` **N행** · 세션 1행 | 성공 | 대조군. 소비 표식이 **정상 가입을 막지 않는다** |
| **T25** | T24 직후. **`clearTxn()` 이 적용된 정상 브라우저**(쿠키 없음) | 같은 state **순차** 재사용 + 새 `code` | **전부 무변화** — 표식 1행 그대로, `users`·`policy_events`·`sessions` 증가 0 | **400**, **code 교환 전** | `bound()` 가 code 교환 앞에 있다(`:751`·`:781`). 순차 재사용은 **쿠키 삭제로** 막힌다 — 소비 표식이 없어도 여기까지는 막혔다 |

**D-2. 동시 재사용 — 4판의 핵심**

두 요청을 `withLatency` 로 **겹치게** 띄운다. 둘 다 같은 `shh_t` 를 싣고, **서로 다른 새 `code`** 를 쓴다.

| # | 사전조건 | 입력 · 동시 실행 순서 | 예상 DB | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T26** | 가입 state S 1개, `shh_t` 유효 | 요청 ①② 동시 · **같은 제공자 계정**(같은 `provider_subject`) · 새 code 두 개 | **정확히 한 요청만 성공**: `users` **1행** · `policy_events` **정확히 N행** · `consumed_signup_states` **1행** | 하나는 성공, 다른 하나는 **409 `stateUsed`** | **I17** — 하나의 가입 state 는 최대 하나의 계정 생성에만 쓰인다. `UNIQUE` 가 아니라 **소비 표식**이 이겼는지 보려면 `policy_events` 가 N행인지도 함께 본다 |
| **T27** | 같음 | 요청 ①② 동시 · **서로 다른 제공자 회원번호**(A·B) · 새 code 두 개 | **정확히 하나의 계정만 생성.** 다른 쪽은 `users`·`policy_events`·`sessions` **0행** | 하나는 성공, 다른 하나는 **409 `stateUsed`** | **I18** — 동일 정책 행위를 두 계정에 귀속하지 않는다. ⚠️ **`UNIQUE(provider, provider_subject)` 는 여기서 아무것도 막지 못한다**(subject 가 다르다). 이 테스트가 실패하면 위협 34 가 살아 있는 것이다 |

**D-3. 트랜잭션 원자성과 재소비**

| # | 사전조건 | 입력 | 예상 DB | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T28** | 정상 가입 흐름. **소비 표식 INSERT 뒤** 정책 이벤트 하나를 강제 실패시킨다 | 가입 완료 | **batch 전체 롤백** — `users` **0행** · `policy_events` **0행** · `consumed_signup_states` **0행** | 실패 응답 | **I19** — 표식만 남고 계정이 없으면 **사용자가 영영 그 state 로 가입 못 한다.** 부분 성공이 없어야 한다 |
| **T29** | 이미 소비된 state(T24 의 것) | 같은 state 로 가입 완료 재시도(쿠키를 수동으로 되돌려 `bound()` 를 통과시킨다) | **무변화** | **409 `stateUsed`** | **I21** — fail-closed. 「어떻게든 통과」 경로가 소스에 없다(R8) |

**D-4. 기타 · 경계**

| # | 사전조건 | 입력 | 예상 DB | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T30** | 다른 브라우저(`shh_t` 없음/불일치) | 가입 state + 새 code | 무변화. **소비 표식도 안 생긴다** | **400**, code 교환 **전** | I8 + **R4** — 인증 전에는 DB 를 쓰지 않는다 |
| **T31** | state 발급 후 시각을 10분+1초 진행 | 교환 시도 | 무변화. **소비 표식 조회도 하지 않는다** | **400** | `takeState()` 만료가 **먼저** 걸린다. 만료 state 는 표식이 없어도 통과하지 못한다(§5-3-4) |
| **T32** | 위 전 경로 | 로그 문자열 전수 검사 | — | — | **R9·I20** — raw `state` 와 `state_hash` **전체**가 로그에 0회. `pv`·`provider_subject` 도 0회 |
| **T33** | 소비 표식 1행, `expires_at` 이 **아직 미래** | 정리 문장 실행 | **표식이 남아 있다** | — | **R10** — 만료 전에 지우면 그 순간 replay 창이 다시 열린다 |
| **T34** | 소비 표식 1행, `expires_at` 이 **과거** | 정리 실행 → 그 뒤 **새 가입** | 만료 행만 사라지고, 새 가입은 **정상 성공** | 성공 | 정리가 정상 가입을 방해하지 않는다. 정리 실패는 **삼킨다**(§5-3-3) |
| **T35** | **로그인** state(`pv` 없음), 같은 `shh_t`, 같은 제공자 계정 | 두 요청 동시 + 새 code 두 개 | `users` **1행 그대로** · `policy_events` **증가 0** · 세션은 **둘 다 발급될 수 있다** | 둘 다 성공 가능 | **감수한 잔여 위험을 명시적으로 고정**(§4-A). 로그인에는 소비 표식을 **안 붙인다** — 붙이면 인증 없는 자리의 쓰기가 되살아난다. 이 테스트가 「실패」로 바뀌면 그건 **설계 변경**이지 버그 수정이 아니다 |

> **T27 과 T35 를 나란히 두는 것이 이 묶음의 요점이다.** 같은 「동시 재사용」인데 한쪽은
> **막고**(가입 — 증거가 실려 있다) 한쪽은 **감수한다**(로그인 — 실린 것이 없다).
> 3판은 이 둘을 하나로 뭉개서 **가입 쪽을 감수해 버렸다.**

#### E. **삭제 pending 판정과 운영자 재실행 금지** (`test-deletion-ledger.mjs`) — 4판 신설

| # | 사전조건 | 입력 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T36** | pending 표식 1행. 그 UID 의 `users` 행이 **없다** | reconciliation(Q7 조건 충족) | 표식이 **confirmed 로 승격**. `expires_at` 이 `confirmed_at + CONFIRMED_RETENTION`(§10-5 산식)으로 재계산. **숫자를 하드코딩하지 않는다** | — | promote-only 의 정상 경로(§10-4) |
| **T37** | pending 표식 1행. 그 UID 의 `users` 행이 **살아 있다**(사용자가 실패 응답 후 계속 사용) | reconciliation(같은 조건) | **아무 변화도 없다** — 승격 안 함, 표식 삭제 안 함, **`users`·`books`·`friendships` 무변화** | — | **I22·I23** — 살아 있는 계정의 pending 은 **실패 기록**이지 대기 중인 삭제 명령이 아니다. 계정이 지워지면 위협 35 |
| **T38** | T37 상태 | **옛 pending 으로 삭제 saga 를 다시 시작하는 경로를 찾는다** | **그런 경로가 없다** — reconciliation 도, 정리 문장도, 운영 스크립트도 `DELETE FROM users` 를 부르지 않는다 | — | **I24** — 새 삭제는 **사용자의 새 요청**으로만 시작한다. 소스에 우회로가 있으면 실패해야 한다(T14 와 같은 무늬) |
| **T39** | T37 상태에서 **stale pending 수동 제거**를 호출한다. 단 **계정 존재 재확인을 생략**한다 | 제거 시도 | **거부.** 표식 무변화 | 절차 중단(비영 종료) | **I25 · Q7b** — 재확인 없이 지우면 그 사이 삭제된 계정의 표식을 지우게 되어 **위협 27 이 되살아난다** |

#### F 묶음 — **`restore_closed` 읽기 차단** (T40~T43 · 5판 신설 · 결정 D)

| # | 준비 | 실행 | 주 D1 기대 | HTTP 기대 | 무엇을 고정하나 |
|---|---|---|---|---|---|
| **T40** | 계정 X 를 삭제해 confirmed 표식을 만든 뒤, **주 D1 을 X 가 살아 있던 시점으로 되돌린 상태**를 셰임으로 만든다. `mode='restore_closed'` | X 의 (되살아난) 세션 쿠키로 `GET /api/book` | 무변화 | ⛔ **503.** 단어장 본문이 **응답에 없다** | **핵심 회귀.** 4판 설계로는 **200 + 지웠던 단어** 였다 |
| **T41** | 같음. Y 는 X 의 친구였다 | Y 의 세션으로 `GET /api/friends/:X/book` · `GET /api/friends` | 무변화 | ⛔ **둘 다 503** | 탈퇴자의 단어장·별명이 **친구에게 다시 보이는** 경로 |
| **T42** | 같음 | X 의 세션으로 `GET /api/me` | 무변화 | ⛔ **503.** 계정 존재 여부도 알려주지 않는다 | 되살아난 세션으로 **로그인 상태가 부활**하는 경로 |
| **T43** | `mode='restore_closed'` | `/api/health` · `/api/ready` · `/api/policies` · 정적 정책 파일 | 무변화 | **허용**(ready 는 503 + `mode`) | 허용 목록이 **정확히 넷**임을 고정. 다섯 번째가 늘면 실패 |

#### G 묶음 — **정리 Worker** (T44~T47 · 5판 신설 · 결정 B)

| # | 준비 | 실행 | 기대 | 무엇을 고정하나 |
|---|---|---|---|---|
| **T44** | pending 표식 1행(`pending_alert_at` 지남) · confirmed 표식 1행(`expires_at` 지남) | 정리 Worker 1회 | **confirmed 만 삭제.** pending **그대로**. 경보 카운트 1 | ⛔ **가장 중요.** `deletions` 대상 `DELETE` 의 `WHERE` 에 `confirmed_at IS NOT NULL` 이 없으면 실패 |
| **T45** | 만료된 `consumed_signup_states` 1행 · **아직 안 만료된** 1행 | 정리 Worker 1회 | 만료된 것만 삭제 | **R10** — 만료 전에 지우면 replay 창이 다시 열린다 |
| **T46** | 위와 같은 상태 | 정리 Worker **2회 연속** | 2회차 결과가 1회차와 같다. 오류 없음 | **C8** 재실행 안전성 |
| **T47** | `mode='maintenance'` 또는 `'restore_closed'` | 정리 Worker 1회 | **아무것도 지우지 않는다.** 즉시 종료 | **C14** — 복원 중 정리가 겹치면 §10-4 의 경합 |

| **T48** | ⛔ **서로 무관한 정상 가입 두 건**을 만든다 — 같은 `pv`, **같은 `occurred_at`(밀리초까지 동일하게 주입)**, **서로 다른 `state`**, 서로 다른 제공자 계정 | 순차로 가입 완료 | **둘 다 성공.** `users` 2행 · `policy_events` 2N행 · 세션 2개 | 둘 다 200 | ⛔ **I18-b.** 이 테스트가 실패하면 누군가 `UNIQUE(pv, occurred_at)` 류의 제약을 넣었다는 뜻이고, 그 순간 **같은 순간에 가입한 두 번째 사람이 막힌다.** 스키마에 그런 인덱스가 없는지도 함께 검사한다(`test-migrations` 무늬) |

| **T49** | 정리 크론을 **실패시킨다**(ledger 가 `cleanup_runs` 외 질의에 실패). 그리고 확정 안 된 pending 표식 1행이 있는 상태 | `scheduled()` 를 1·2·3회, 그 뒤 정상 1회 | ① 실패한 회차의 `ctx.waitUntil()` Promise 가 **rejected** ② `fail_streak` 1→2→3 ③ **3회에서** `/api/ready.cleanupAlert:true`(2회에서는 `false`) ④ pending 이 있으면 **성공 회차 뒤에도** `cleanupAlert:true` ⑤ `cleanup_runs` 를 못 읽으면 `cleanupAlert:true` ⑥ 응답에 개수·`fail_streak`·`last_error` 가 없다 | **C6·C11·C12.** 4판까지는 실패를 기록하고 **삼켜서** Cron Trigger 에 성공으로 남았고, `/api/ready` 는 센 값을 **읽고 버렸다** — 「센다」만 있고 「알린다」가 없었다. ⚠️ 이 T 는 **5판 이후에 추가**됐다(T48 까지가 5판 명세) |

#### H. 쓰기 증폭 · 라우트 분류 · 버킷 등록 (6판 추가 · `test-friends.mjs`)

**결정 A′ 의 대가를 재는 자리다.** 요청 하나가 임차증 하나를 든다는 것은, 요청이 하나 늘 때마다
ledger 쓰기가 둘(INSERT+DELETE) 는다는 뜻이기도 하다 — 그래서 **「요청」이 무엇인지가 곧 비용**이고,
인증 없는 아무 주소가 요청으로 세어지면 그게 공격 도구가 된다(위협 40).

| # | 사전조건 | 입력 · 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T50** | 정상 상태(`mode='open'`) | 없는 주소 50회 + 허용되지 않은 method 50회 | **주 D1 `rate_limits` 무변화 · ledger 총 변경 수 무변화**(둘 다 0) | 404 | **위협 40.** 라우트 판정이 게이트·임차증·리미터보다 **앞**에 있다. 하나라도 뒤로 가면 인증 없는 요청이 우리 D1 쓰기 할당량을 태운다 |
| **T51** | 같음 | `GET /login/kakao` 20회 | 두 DB 모두 **무변화** | 302 | 로그인 시작은 302 하나와 서명 하나다 — 주 D1 을 안 만지므로 임차증도, 계수도 없다 |
| **T52** | 같음, 로그인된 세션 | `GET /book` 1회 · `PUT /book` 1회 | ledger 변경 **정확히 2**(INSERT+DELETE) · `rate_limits` 는 쓰기 요청에만 **1행** | 200 | **요청 하나에 임차증 하나.** 2 보다 크면 한 요청이 여러 개를 들고 있다는 뜻이고, 그중 하나만 해제돼도 drain 이 거짓으로 0 이 된다 |
| **T53** | 같음 | `GET /cb/kakao` 60회(같은 IP) → 429 확인 후 10회 더 | `rate_limits` 합계가 **한도+1 에서 멈춘다** · 차단된 10회의 ledger 쓰기는 **정확히 20**(요청당 2) | 429 | **리미터가 증폭기가 되지 않는다.** 그리고 차단 이후에도 ledger 쓰기가 **요청당 상수**로 유지된다 — 상한이 있다는 사실 자체를 고정한다 |
| **T54** | — | `rlMax()` 에 모르는 이름 · `__proto__`·`constructor`·`toString` · 비문자열 | — | 전부 **예외** | **위협 41.** 등록되지 않은 버킷이 `write`(120)로 조용히 축소되지 않는다. 라우트 표가 쓰는 버킷이 전부 등록돼 있는지도 함께 잰다 |
| **T55** | — | `routeFor(method, path)` 전수 | — | 순수 함수(HTTP 없음) | 표가 **판정의 원본**이다. 실재하지 않는 주소·method 는 `null`, 주 D1 을 만지는 라우트는 `lease:true`, 버킷은 **라우트 하나에 하나**, `HEAD` 는 `GET` 과 같은 라우트 |
| **T56** | 되살아난 탈퇴 계정(계정·단어장 있음 + **확정 표식** 있음) · `restore_closed` | `setMode()` 를 **3×3 전수**로 부른다 · 각 전환 뒤 `GET /book` | `restore_closed` 에서 나가는 전환은 **검증된 `open` 하나만** 성공. 거부된 전환 뒤 모드는 **그대로**이고 `/book` 은 **503** | 읽기가 열린 모드에서는 되살아난 대상이 **살아 있지 않아야** 통과 | **위협 43.** 자물쇠를 「한 전환」이 아니라 **「읽기가 열리는 모든 목적지」**에 건다. CAS 도 함께 잰다 — 판정 뒤 다른 전환이 끼어들면 **거부** |
| **T57** | 같은 상태 · `reopenReport()`·`setMode("open")` 에 **호출자 함수**를 넘긴다(상수 · 다른 키 · 버전 뒤바뀜 · 빈 목록 · 함수 아님) | 다섯 가지를 각각 넘긴다 | **무엇을 넘겨도 `targets` 가 안 변하고 `canReopen:false`** · `setMode("open")` 도 거부 | — | **위협 44.** 「인자가 무시된다」를 재는 것이지 「인자를 검증한다」가 아니다 — 검증 규칙을 얹으면 결국 인자를 믿는 함수다. 모르는 `key_version` 이 있으면 판정 자체를 거부하는 것도 여기서 잰다 |
| **T58** | 확정 안 된 표식 1건 · `pending_alert_at` 이 **아직 안 됐다** | `pendingTotalCount` / `pendingAlertCount` · 경계 시각 `<`·`=`·`>` · `reopenReport`·`restorePreflight` | 전체 **1** · 경보 **0**(`=` 도 아직 경보가 아니다) · 안전 조건은 **전체**를 써서 재개방·사전점검 **거부** | — | **위협 45.** 전체 개수는 **시각에 따라 변하지 않는다** — 그것도 함께 잰다 |
| **T59** | 살아 있는 계정 1명 + 그 계정의 **실제 pending** | ① 호출자 함수(상수·다른 키·빈 문자열)로 `reconcile()` ② `env.DELETION_KEY` 가 틀린 상태로 `reconcile()`·`removeStalePending()` ③ 올바른 키 | ①② **승격 0건**(②는 `ok:false` + 사유가 키) ③ 계정이 살아 있으면 승격 0, **계정을 지우면 승격 1** | 사용자 행이 있는 동안에는 **어떤 키 오류에서도** 승격 없음 | **위협 46.** ③이 없으면 「늘 거부」라는 회귀를 못 잡는다 |
| **T60** | 인증 없는 요청 — 쿠키 없음 · **매번 다른** 위조 쿠키 · 틀린 OAuth 콜백 · 없는 주소 | 각 경우를 **200회와 400회** 반복하고 두 저장소의 `total_changes()` 를 잰다 · 429 이후 150회 추가 · `restore_closed` 에서 50회 | **요청 수를 2배로 늘려도 쓰기가 같다**(총량 불변식) · 429 이후 **두 저장소 쓰기 0** · `restore_closed` 에서 **두 저장소 쓰기 0** · 정상 사용자의 읽기·저장은 200 | 측정마다 **다른 IP** — 같은 IP 면 앞 측정이 버킷을 소진해 뒤 측정이 거짓으로 통과한다 | **위협 47.** ⛔ 옛 T53 은 「요청당 정확히 둘」을 재고 그것을 「유한한 상한」이라 적었다. **요청 수가 무제한이면 요청당 상수는 상한이 아니다** — 그래서 N 을 바꿔 가며 잰다 |

#### I. 마감 감사 — 경합 · drain 경계 · 남용 방어 · readiness (8판 신설 · `test-stage34-closeout.mjs`)

**여기서 재는 것은 「코드가 이렇게 생겼나」가 아니라 「두 작업이 겹칠 때 무엇이 남나」다.**
넷 다 **스위트 23개가 전부 통과하는 상태에서** 재현됐다. 도구는 하나 — 특정 SQL 이 실행되기
**직전**에 그 호출을 세우고, 그 창에 다른 작업을 밀어 넣는다. ⛔ **공개 디버그 라우트를 만들지
않는다**(재현을 위해 프로덕션에 뚫은 구멍이 곧 다음 위협이다). 테스트는 셰임의 `prepare` 만 감싼다.

| # | 사전조건 | 입력 · 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T61** | 계정 1명 + 그 계정의 **pending 표식** 1건 · `maintenance` | **a** 그냥 제거 · **b** 「계정 존재 확인」 뒤 정지 → `open` 전환 → 진짜 `DELETE /me` 를 주 D1 삭제 뒤·확정 전에 정지 → 둘 다 재개 · **c** 모드만 바꿈(epoch 고정) · **d** epoch 만 올림 · **e** 임차증만 하나 생김 | **a** 제거 1건 · **b~e** 제거 **0건**, 표식 그대로 · **b** 에서 `users=0 && deletions=0` 은 **불가능** | b 의 `DELETE /me` 는 200 | **위협 48.** 확인은 「그때 그랬다」가 아니라 「지금 그렇다」여야 한다. **a 가 없으면 「늘 거부」라는 회귀를 못 잡는다** |
| **T62** | 로그인된 세션 · `open` | 리미터 카운터 INSERT **직전**에 정지 → `restore_closed` 전환 → `drainState()` 확인 → 재개. 그 시점 이후 주 D1 에 던져진 SQL 을 **전부 기록**한다 | `drained:false` 이거나, `drained:true` 이면 재개 후 주 D1 **질의 0건 · 쓰기 0건** | 429 또는 503 | **위협 49.** 요청은 어느 쪽이든 하나여야 한다 — **세어지든가, 아예 안 만지든가.** 「샌 쓰기가 한 줄뿐」은 답이 아니다 |
| **T63** | — | **a** 남용 방어 없는 `env` 로 6버킷 × 5 IP · `GET /login/:provider` 포함 · **b** 세는 설정에서 버킷별 실측 후 무료 한도와 비교 · **c** 엣지 바인딩 있는 `env` | **a** 두 DB **질의 0건·쓰기 0건** · **b** 측정값에서 IP 수를 **계산한다**(하드코딩 금지) · **c** 정상 200 이고 카운터 쓰기 0 | **a** 전부 503 · **c** 200 | **위협 50.** a 의 대상 목록은 `routeBuckets()` 와 **대조해서** 만든다 — 새 버킷이 측정 밖에 남으면 실패한다. b 는 「이 설정은 ready 가 아니다」를 고정한다 |
| **T64** | 완전한 설정(`/ready` 200) | **a** 빈 ledger · **b** 표식+키 기록을 남긴 뒤 `DELETION_KEY` 교체 · **c** 모르는 `key_version` 삽입 · **d** `deletion_keys` 표 DROP | **a** `deletionEvidence:true` · **b·c** `false` · **d** `ledger:false` 로 **구분** | **a** 200 · **b·c·d** 503 | **위협 51.** 응답 본문에 키 값·검사값·표 이름·SQL·오류 문자열이 **하나도** 없어야 한다 |

#### J. 남용 방어 계약 v2 — 선언 · 검증 · 두 층 (9판 신설 · `test-abuse-guard.mjs`)

⛔ **T63 의 판정을 먼저 정정한다.** 8판은 T63 을 「위협 50 을 닫았다」의 근거로 썼다. 실제로 잰
범위는 그보다 훨씬 좁다 — **아래가 T63 이 지금부터 인정받는 범위다.**

| 옛 판정 | **정정 (9판)** |
|---|---|
| T63-a: 「남용 방어가 없으면 계정 경로가 안 열린다」 | 그대로 유효하다. 단 **`EDGE_GUARD` 미선언 상태의 fail-closed 증거일 뿐**이고, 방어가 **있을 때**를 아무것도 말하지 않는다 |
| T63-b: 「IP 하나가 하루 한도를 태운다」 | **내부 D1 카운터의 비용 측정**으로만 인정한다. 방어 설계의 검증이 아니다 |
| T63-c: 「엣지 바인딩이 있으면 200 이고 카운터 쓰기 0」 | ⛔ **방어 완료의 증거가 아니다.** 이것은 「**동작하는 mock** 을 넣으면 문이 열린다」를 확인한 것이고, 실제로 재현된 것은 **가짜·고장 난 값도 똑같이 문을 연다**는 사실이었다(위협 52). 9판에서 카운터는 **항상** 돌므로 「카운터 쓰기 0」 단언도 함께 폐기한다 |

| # | 사전조건 | 입력 · 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T65** | `EDGE_GUARD="ratelimit"` | `RL` 자리에 **일곱 가지**를 넣는다 — 문자열 · `{}` · `limit` 없음 · 동기 예외 · reject · `{nope:1}` · `{success:"yes"}` | **주 D1·ledger 질의 0건 · 쓰기 0건** | `/ready` **503**(`abuseReady:false`) · 계정 경로 **503** | **위협 52.** 「있다」가 아니라 「**부를 수 있고 답이 boolean 이다**」가 조건이다. 응답에 바인딩 이름·예외 문자열·`EDGE_GUARD` 값이 **하나도** 없어야 한다 |
| **T66** | 열린 설정(`dev` 또는 검증된 `ratelimit`) | 버킷 여섯 각각: 한도까지 → 한도+1. 엣지 mock 은 **전부 허용**으로 둔다 | 카운터가 실제로 센다 | 한도까지 **429 아님** · 한도+1 **429** | **위협 53.** ① 엣지가 늘 허용해도 **우리 카운터가 막는다**(가짜 mock 으로 통과 불가) ② `routeBuckets()` ⊆ `RL_MAX` 전수 ③ 라우트 하나에 버킷 하나 ④ 설정에 `ratelimits` 가 있으면 `simple.limit` ≥ `max(RL_MAX)`(`test-config`) |
| **T67** | 열린 설정 | **1 IP × 반복** · **100 IP × 1회** · **100 IP × 2회** · 버킷 여섯 전부 | 주 D1·ledger **쓰기 수를 센다** | 429 · 401 · 503 수를 **전부 기록** | **위협 54.** 측정값이 **§12-3-3 의 표와 같아야** 한다. 「막았다」고 적은 칸은 0 이어야 하고, **「못 막는다」고 적은 칸은 실제로 늘어나야 한다** — 늘지 않으면 문서가 과장이 아니라 **거짓**이다 |
| **T68** | 모드별 | **waf**: 커스텀 호스트 200 · `*.pages.dev` 호스트 **403** · `APP_ORIGIN` 이 pages.dev 면 선언해도 `none` · **ratelimit**: 바인딩 검증·fail-closed · **turnstile**: 보조로만(단독 선언은 `none`) | 닫히는 경우 두 DB 질의 0건 | 위 표대로 | **위협 55.** 운영자가 고른 모드가 **코드에서 실제로 문을 여는가**. 여기서 A 를 골랐는데 계속 503 이면 그것이 실패다 |
| **T69** | `guard=none` 과 `guard` 있음 양쪽 | 키 없이 `/ready` × N · 틀린 키 · 맞는 키 · `/health` | 키 없음·틀린 키 → **두 DB 질의 0건** · 맞는 키 → 진단 수행 | 키 없음 **503**(`diagnostics:false`) · 맞는 키 200/503 | **위협 56.** 공개 응답에 `signupReady`·`providers`·`cleanupAlert`·표 이름·오류 문자열이 **없어야** 한다. `/health` 는 여전히 DB 를 안 만진다 |

> ⚠️ **T8 은 6판에서 둘로 갈렸다.** 5판 T8 은 「지금 코드에 `LEDGER: undefined` 를 넣어 200 이
> 나오는 것」을 옛 세대라고 불렀는데, 그건 옛 세대가 아니라 **지금 코드의 fail-open** 이었다
> (위협 39). 이제 **T8-a** 가 현재 코드의 fail-closed 를 재고, **T8-b** 가 고정 fixture
> (`scripts/fixtures/legacy-worker.mjs`)로 옛 세대를 재현한다. **아직 못 막는 것으로 남은
> 실패 재현은 T8-b 하나다.**

#### K. 사람 확인 · 세션 envelope (9판 · 사용자 결정 3·4 · `test-signup.mjs` · `test-friends.mjs` · `test-client.mjs`)

| # | 사전조건 | 입력 · 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T70** | 가입 설정 완료 | **a** 토큰 없음·모양 틀림 · **b** 검증 서버가 거절(위조·재사용·만료) · **c** 검증 서버 장애 · **d** `TURNSTILE_SECRET` 부재 · **e** 실제로 보낸 본문 확인 · **f** `hv` 없는 state 로 콜백 · **g** 정상 왕복 · **h** 로그인 시작 | f 에서 **계정 0명 · 제공자 호출 0회** | **a·b·c** 400(`humanCheck`) · **d** 503 · **f** 302 아님 · **g** 302 | **결정 3.** ① 서버가 결과를 **실제로 본다**(모양만 흉내내면 e 가 잡는다) ② 콜백이 `hv` 를 대조한다 — 없으면 Turnstile 은 화면 장식이다 ③ 검증 장애가 통과가 되지 않는다 ④ **로그인·읽기·쓰기는 덮지 않는다**(h) |
| **T71** | 로그인된 세션 | **a** 정상 · **b** 쿠키 없음·칸 부족·서명 변조·만료 늘리기·이미 만료·모르는 판 등 11가지 · **c** 서명은 맞는데 DB 에 없음 · **d** 로그아웃 뒤 같은 쿠키 · **e** 다른 시크릿으로 서명 · **f** 키 교체 · **g** 키 부재 · **h** 저장 형태 · **i** `auth` 라우트 전수 | **b·i** 에서 주 D1 질의 **0건** · ledger 쓰기 **0건** | **a** 200 · **b·c·d·e·f·i** 401 · **g** 503 | **결정 4.** ① 값싼 문이 **DB 앞**에 있다 ② **envelope 통과 ≠ 인증**(c·d) ③ 키는 전용이고(e) 교체는 전원 로그아웃(f) ④ 거절 사유를 말하지 않는다 ⑤ 표가 `auth:true` 라 적은 자리가 **전부** 이 문 뒤에 있다 |
| **T72** | 가입 화면 | 위젯 스크립트 실패 · site key 부재 · 토큰 도착 · 토큰 만료 · 서버의 `humanCheck` 400 | — | — | **결정 3의 화면 쪽.** ① 토큰 전에는 버튼이 **잠겨 있다** ② 만료되면 **다시 잠긴다**(1회용 토큰을 재사용하지 않는다) ③ 가입 시작 본문에 **그 토큰**이 실린다 ④ 위젯을 못 그리면 **이유를 말한다** ⑤ site key 가 없으면 외부 요청을 **아예 안 만든다** |

#### L. 부분 시크릿 · Turnstile 필드 · 비밀값 비교 · 실패 갈래 (10판 · `test-signup.mjs` · `test-friends.mjs` · `test-abuse-guard.mjs` · `test-client.mjs`)

| # | 사전조건 | 입력 · 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T73** | 시크릿 8개 중 **하나씩** 뺀 배포 | **a** `/health` · **b** `POST /signup/start` · **c** 온전한 env 로 만든 state 를 **시크릿 빠진 배포의 콜백**에 넣는다 · **d** 로그인 왕복도 같게 · **e** `newSession` 직접 호출 | **c·d** 에서 users · policy_events · sessions · consumed_signup_states **전부 0** · 제공자 호출 **0회** | **a** `signupReady:false`(전부 있으면 참) · **b** 503 · **c·d** `#login=ok` 아님 | ① 조건이 **한 함수**에 있다 — 화면·시작·콜백이 갈라지지 않는다 ② 거부가 **외부 호출 앞**이다(`code` 는 되돌릴 수 없다) ③ 부분 구성에서 **쓸 수 없는 계정**이 생기지 않는다 |
| **T74** | 가입 설정 완료 | **a** `action` 이 다르거나 없음 · **b** `hostname` 이 다르거나 없음 · **c** 요청이 **다른 호스트**로 도착 · **d** 정상 · **e** `APP_ORIGIN` 이 주소가 아님 · **f** 화면 상수 대조 | **e** 에서 검증 서버 호출 **0회** | **a·b·c** 400 · **d** 200 | ① `success` 만으로는 부족하다 ② 기준은 **`APP_ORIGIN`** 이고 도착 주소가 아니다 ③ 화면과 서버가 **같은 `action` 문자열**을 쓴다 |
| **T75** | 운영자 키 등록 | **a** 맞는 키 / 틀린 키 7종(길이·대소문자·앞뒤 자름) · **b** 소스에서 `sameSecret` 호출 전수 · **c** 구현 형태 · **d** envelope 서명 변조 | — | **a** 맞는 키만 진단 · 나머지 전부 503 **같은 모양** | ① 틀린 값의 응답이 **한 가지 모양**이다(길이가 맞았는지가 안 샌다) ② `await` 를 빠뜨린 호출이 **하나도 없다** ③ 원문 인덱싱으로 되돌아가지 않는다 |
| **T76** | 엣지 모드 구성됨 | 버킷 **6개 전부** — 카운터 문장(`INSERT INTO rate_limits`)만 던지게 한 ledger · 그리고 멀쩡한 ledger | 주 D1 쓰기 **0** | **503**(429 가 아니다) · 멀쩡하면 503 아님 | ① 「셀 수 없다」와 「한도 초과」는 다른 답이다 ② 셀 수 없는 상태로 사용자 데이터를 만지지 않는다 ③ **대표 라우트 표가 실제 버킷 목록과 같다**(새 버킷이 검사 밖에 남지 않는다) |

#### M. 제공자별 설정 · 표식 fencing · 진짜 workerd (11판 · `test-signup.mjs` · `test-deletion-ledger.mjs` · `test-workerd.mjs`)

| # | 사전조건 | 입력 · 순서 | 예상 DB / ledger | 예상 HTTP | 잡아내는 불변식 |
|---|---|---|---|---|---|
| **T77** | 제공자 자격 9경우(각 제공자의 ID 만 · secret 만 · 둘 다) | **a** `/health` 목록 · **b** `POST /signup/start` · **c** `GET /login/:provider` · **d** 막힌 경우의 표 4종 · **e** 온전한 설정으로 만든 state 를 **secret 이 사라진 배포의 콜백**에 · **f** 정상 왕복 · **g** 모르는 제공자 이름 | 막힌 경우 users·sessions·policy_events·consumed_signup_states **전부 0** · 제공자 호출 **0회** · **Turnstile 호출 0회** | 막힘 **503** · 정상 **200/302** · 모르는 이름 **404** | ① 규칙이 **한 함수**에 있다(화면과 문이 갈라지지 않는다) ② 거부가 **Turnstile·외부 호출 앞**이다 ③ **카카오의 secret 선택 정책은 그대로다** |
| **T78** | 삭제 표식 ledger | **a** 옛 epoch 의 lease 로 `markPending` · **b** 살아 있는 lease 로 **두 번째** `markConfirmed` · **c** 정상 통과 | **a** `deletions` 행 수 불변 · **b** `confirmed_at`·`expires_at` 불변 | — | ① fencing 은 pending 에도 걸린다 ② **확정은 한 번뿐이다** — 반복하면 보유기간이 영원히 뒤로 밀린다 |
| **T79** | 실제 workerd 프로세스 | 배포되는 것과 **같은 파일**을 모듈로 넣고 HTTP 로 부른다. 맞는 운영자 키 · **길이가 다른** 틀린 키 6종 · 헤더 없음 | — | 맞는 키만 진단 · 나머지 전부 **503 같은 모양** | ① 운영 경로가 **Node 가 아닌 런타임**에서 실제로 돈다 ② 길이가 다른 입력에서 **예외가 안 난다**(원문을 넣는 구현이면 500) ③ 이 프로세스에는 **어댑터가 없다**(오염 확인) |
| **T80** | 화면의 계정 readiness | `/health` 가 `ready:false`·`ready` 누락·`ready` 가 문자열/숫자/`null`·본문 `ok:false`·본문이 배열·`providers` 가 배열이 아님·500·네트워크 실패·시간 초과·JSON 아님. **계정 라우트는 끊김**(`request()` 가 accountState 를 안 건드리는 갈래)이라 판정 근거가 `/health` 하나뿐이다 | 로그인 표시가 **있는** store | 계정 동작 전부 숨김 · 「카카오 계정」 단정 0건 · 점검 안내 · `shh-via` **보존**(401 이 아니다) · `ready:true` 면 복구 | ① 빈 store 로 재면 `!authToken()` 때문에 **우연히 통과**한다(M24 가 그렇게 살아남았다) ② 뒤이은 503 에 기대면 재는 것이 `ready` 계약이 아니다 |
| **T81** | 계약을 어긴 `/health` 응답의 제공자 목록 | `ready` 누락 · 문자열 · 숫자 · `null` · 객체이면서 **`providers` 는 비어 있지 않다**(`{ok:true, ready:"true", providers:["kakao"]}`) | 빈 store | 로그인 버튼 0개 · 문구는 **「연결할 수 없어」**(「준비 중」이 아니다 — 원인이 다르면 할 일이 다르다) | ① 계정 UI 만 재면 통과한다 — 정규화된 `ready:false` 로 계정 UI 는 실제로 닫힌다 ② 재야 하는 것은 **같은 응답의 `providers` 가 로그인 시작 경로로 흘러가는가** 다 |

**합계 81건.** T1~T48 이 5판 명세이고, **T49 는 2026-08-18 구현 중에, T50~T55 는 2026-08-19
자체 재감사에서, T56~T60 은 같은 날 독립 재감사에서, T61~T64 는 2026-08-20 마감 감사에서 더해졌다** —
설계가 요구한 「세어서 알린다」(C6)·「연속 실패 경보」(C11)가 코드에 반쪽만 있었기 때문이고,
T50~T55 는 **구현이 설계보다 느슨했던 자리**(위협 39~42)를 고정하기 위해서다.
**T65~T69 는 2026-08-22 남용 방어 재감사에서** 더해졌고, 그 다섯은 T63 이 「방어가 있을 때」를
한 번도 재지 않았다는 사실에서 나왔다(위협 52~56 · §0-9).
**T70~T72 는 같은 날 사용자 결정 3·4**(Turnstile · 세션 envelope)로 더해졌고,
**T73~T76 은 그 뒤 공식 4단계 전체 재검증**(위협 57~60 · §0-10)에서,
**T77~T79 는 독립 검토**(위협 61~63 · §0-11)에서, **T80 은 11판 이후 후속 재검증**(위협 64 · §0-12)에서, **T81 은 2차 재검증**(위협 65 · §0-13)에서 더해졌다.
**T56~T60(`scripts/test-reaudit.mjs`)은 그 T50~T55 를 포함한 스위트 전부가 통과하는 상태에서
재현된 우회**(위협 43~47)를 고정한다. ⚠️ **번호가 늘어난 것 자체는 완료의 근거가 아니다** —
이 저장소는 같은 이유로 세 판 연속 「완료」를 잘못 선언했다.
**아래 문장은 5판 당시의 사실이다: 이 중 지금 존재하는 파일은 하나도 없다.** `scripts/test-signup.mjs` ·
`scripts/test-policies.mjs` · `scripts/test-deletion-ledger.mjs` · `scripts/test-cleanup.mjs` 는
**4단계에서 만든다.** 그것이 3단계의 실패가 아니라 **3단계의 산출물이 설계와 명세라는 뜻**이다.

---

### 13-6. **T1~T81 구현 매핑** (명세 → 실제 단언 · 2026-08-18 · **6판·7판 갱신 2026-08-19 · 10판 갱신 2026-08-22**)

§13-5 가 「어떻게 재나」였다면 여기는 **「어디서 실제로 재나」**다.
「T1~T60 통과」라고만 적으면 어느 단언이 무엇을 지키는지 확인할 수 없어서, 각 T 를 파일과
단언 메시지로 잇는다.

⚠️ **지금 「실패를 재현하는 테스트」는 T8-b 하나다** — 옛 배포 세대는 게이트를 읽지 않으므로
막지 못한다. 통과가 곧 안전이 아니라 **아직 못 막는 것을 잊지 않기 위한 고정 장치**이고,
이 표에서 그 한 줄만 결과 칸이 다르다.
**T6 은 2026-08-18 에 성격이 바뀌었다** — 결정 A′ 구현으로 「lease 0건인데 쓰기가 돈다」를
고정하던 시험이 **「도는 동안 활성 1이고 복원이 거부된다」를 검증하는 통과 검사**가 됐다.
5판 당시의 옛 기대값은 §13-5 에 그대로 두되 **대체됐다고 표시했다.**

| T | 파일 | 단언(메시지 일부) | 실제로 검증한 불변식 | 결과 |
|---|---|---|---|---|
| T1 | `test-deletion-ledger.mjs` | 「전환 직후 활성 lease 가 0이다」 | 유지보수 전환은 in-flight 요청을 끝내지 못한다 | 통과 |
| T2 | `test-deletion-ledger.mjs` | 「활성 lease 가 있는데 reconciliation 이 실행됐다」 | 증거 없이는 실행하지 않는다(I15) | 통과 |
| T3 | `test-deletion-ledger.mjs` | 「lease 0건인데 reconciliation 이 거부됐다」 | 해제 후에는 실제로 허용된다(T2 의 짝) | 통과 |
| T4 | `test-friends.mjs` | 「유지보수 중인데 `<메서드> <경로>` 가 통과했다」 12경로 | 진입 차단은 허용 목록으로 한다. GET 이라 통과하면 위협 28 | 통과 |
| T5 | `test-deletion-ledger.mjs` | 「옛 epoch 의 lease 로 확정이 기록됐다」 | epoch 불일치는 fencing 을 통과하지 못한다 | 통과 |
| **T6** | `test-friends.mjs` | 「지연된 쓰기가 진행 중인데 활성 임차증이 1이 아니다」 · 「요청이 아직 도는데 drained 라고 말한다」 · 「활성 임차증이 있는데 복원 gate 가 통과시킨다」 · 「restore_closed 인데 새 임차증이 발급됐다」 · 「요청이 끝났는데 임차증이 안 풀렸다」 | **결정 A′ 의 전역 user-data drain** — 요청 하나에 임차증 하나, 도는 중 `drained:false`, 끝난 뒤에만 0 | 통과 |
| **T6b** | `test-friends.mjs` | 「지연된 읽기가 추적되지 않는다」 · 「읽기가 끝났는데 임차증이 안 풀렸다」 | **읽기도 추적된다**(「write drain」이라는 이름이 4판에서 읽기를 빠뜨렸다) | 통과 |
| **T6c** | `test-friends.mjs` | 「만료된 미해제 임차증을 0으로 셌다」 · 「stale 임차증이 있는데 복원을 허용한다」 | **TTL 만료 ≠ 해제.** 시간으로 0 이 되지 않는다 | 통과 |
| **T47b·T47d** | `test-cleanup.mjs` | 「★ 정리 크론이 주 D1 을 지우는 중인데 활성 임차증이 0이다」 · 「maintenance 인데 정리 크론이 임차증을 땄다」 | **정리 크론도 같은 임차증 체계 안에 있다**(허용 모드만 다르다) | 통과 |
| T7 | `test-deletion-ledger.mjs` | 미충족이 남았는데 허용되나 · **9조건 각각** · `noActiveLeases` 가 질의로만 참이 되나 | 금지 gate 가 실행되는 검사로 존재한다(I16) | 통과 |
| **T8-a** | `test-friends.mjs` | 「LEDGER 없이 `<메서드> <경로>` 가 통과했다 — fail-open 이다」 9라우트 · 「503 에 데이터가 실렸다」 · 「눌러도 503 인 제공자 버튼을 그리라고 말한다」 | **위협 39** — 바인딩이 없으면 사용자 데이터 API 가 전부 닫힌다. `/health`·`/ready`·`/policies` 만 열린다 | 통과(**6판 신설**) |
| **T8-a1** | `test-friends.mjs` | 「바인딩이 없는데 readMode 가 open 이라고 답한다」 · 「모드 `<x>` 에서 `<메서드> <경로>` 가 허용됐다」 | **두 자물쇠를 계층별로 따로 잰다.** T8-a 만으로는 하나가 무너져도 통과한다(실측: 돌연변이 M1·M2) | 통과(**6판 신설**) |
| **T8-a2** | `test-friends.mjs` | 「ledger 질의 실패인데 읽기가 통과했다」 · 「maintenance 행이 없는데 통과했다」 | 「모른다」를 「열려 있다」로 읽지 않는다 | 통과(**6판 신설**) |
| **T8-b** | `test-friends.mjs` + `scripts/fixtures/legacy-worker.mjs` | 「전제가 바뀌었다 — 옛 세대가 읽기에서 막혔다」 · 「옛 세대가 임차증을 든다 — fixture 가 옛 세대가 아니다」 | **옛 배포는 게이트를 읽지도 않는다**(위협 33). ⚠️ 옛 세대를 **고정 fixture** 로 재현한다 — 지금 코드에 바인딩 하나를 빼서 「옛 세대」라 부르면 그건 fail-open 을 전제로 굳히는 것이다(위협 39) | ⚠️ **실패 재현(고정) — 지금 유일하다** |
| T9 | `test-deletion-ledger.mjs` | 「confirmed_at 이 더 이른 값으로 병합됐다」 | 보호가 짧아지는 방향을 고르지 않는다 | 통과 |
| T10 | `test-deletion-ledger.mjs` | 「expires_at 이 더 작은 값으로 병합됐다」 | 같음 | 통과 |
| T11 | `test-deletion-ledger.mjs` | 「pending_at 이 더 늦은 값으로 병합됐다」 | 최초 시도 시각이 사실이다(경보가 밀리지 않는다) | 통과 |
| T12 | `test-deletion-ledger.mjs` | 「key_version 이 다른데 병합했다」 | 자동 판단 금지 · 결과 DB 를 쓰지 않는다 | 통과 |
| T13 | `test-deletion-ledger.mjs` | 「검증 안 된 입력으로 병합했다」 4경우 | 검증 불가 = 복원 금지 | 통과 |
| T14 | `test-deletion-ledger.mjs` | 「ops.js 에 강제 진행 경로 `<패턴>` 이 있다」 | fallback 없음. 세 번째 인자로도 안 통한다 | 통과 |
| T15 | `test-deletion-ledger.mjs` | 「키 회전으로 표식이 둘인 사람이 대상에서 빠졌다」 | 표식 수 ≠ 대상 수. 세는 단위는 고유 UID | 통과 |
| T16 | `test-deletion-ledger.mjs` | 「pending 이 confirmed 를 무효화했다」 | confirmed 우선 | 통과 |
| T17 | `test-deletion-ledger.mjs` | 「pending 만 있는 사람이 재삭제 대상이 됐다」 | pending 을 재삭제에 쓰면 살아 있는 계정을 지운다 | 통과 |
| T18 | `test-signup.mjs` | 「정책 기록이 N행이 아니다」 · 「`<kind>` 가 정확히 한 번이 아니다」 | 계정과 필수 이벤트가 갈라지지 않는다(I4) | 통과 |
| T19 | `test-signup.mjs` | 「이벤트가 실패했는데 계정이 남았다」 | `batch()` 가 단일 트랜잭션임을 실제로 잰다 | 통과 |
| T20 | `test-signup.mjs` | 「본문에 끼워 넣은 항목이 기록에 반영됐다」 | **적응**: 클라이언트가 kind 를 보내는 자리가 없음 + 서버 집합에 중복 없음 | 통과 |
| T21 | `test-signup.mjs` | 「`<kind>/<action>` 이 들어갔다 — CHECK 가 안 걸렸다」 5조합 | 스키마 `CHECK` 가 실제로 강제된다 | 통과 |
| T22 | `test-policies.mjs` | 「서버 상수의 `<kind>` 해시가 실제 파일 내용과 다르다」 | 파일·manifest·서버 상수 세 값이 갈리면 실패(I11) | 통과 |
| T23 | `test-signup.mjs` | 「옛 pv 인데 제공자를 불렀다」 · 「옛 pv 로 계정이 생겼다」 | 본 문서와 기록될 해시가 다르면 기록하지 않는다 | 통과 |
| T24 | `test-signup.mjs` | 「소비 표식이 1행이 아니다」 · 「표 컬럼이 늘었다」 | 대조군 — 표식이 정상 가입을 막지 않는다 | 통과 |
| T25 | `test-signup.mjs` | 「표 없는 재사용이 통과했다」 · 「제공자를 불렀다」 | 순차 재사용은 쿠키 삭제로 막힌다(code 교환 전) | 통과 |
| T26 | `test-signup.mjs` | 「성공이 정확히 하나가 아니다」(같은 subject) | 하나의 가입 state → 최대 하나의 계정 생성(I17) | 통과 |
| T27 | `test-signup.mjs` | 「성공이 정확히 하나가 아니다」(**다른 subject**) | UNIQUE 가 못 막는 것을 소비 표식이 막는다(I18 · 위협 34) | 통과 |
| T28 | `test-signup.mjs` | 「계정은 없는데 소비 표식만 남았다」 | 부분 성공이 없다(I19) | 통과 |
| T29 | `test-signup.mjs` | 「이미 쓴 state 가 다시 통했다」 | fail-closed · 우회 경로 없음(R8 · I21) | 통과 |
| T30 | `test-signup.mjs` | 「인증 전에 소비 표식이 생겼다」 | 인증 전에는 DB 를 쓰지 않는다(I8 · R4) | 통과 |
| T31 | `test-signup.mjs` | 「만료된 state 가 표식을 만들었다」 | 만료가 먼저 걸린다 | 통과 |
| T32 | `test-signup.mjs` | 「로그에 `<무엇>` 가 남았다」 5종 + 소스 패턴 4종 | raw state·state_hash·pv·subject 가 로그에 0회(R9 · I20) | 통과 |
| T33 | `test-signup.mjs` | 「아직 안 만료된 표식을 지웠다」 | 만료 전 삭제는 replay 창을 다시 연다(R10) | 통과 |
| T34 | `test-signup.mjs` | 「만료된 표식이 안 지워졌다」 · 「정리가 정상 가입을 방해했다」 | 정리가 가입을 막지 않는다 | 통과 |
| T35 | `test-signup.mjs` | 「로그인이 계정을 만들었다」 · 「정책 기록을 만들었다」 | **감수한 잔여 위험을 명시적으로 고정**(§4-A) | 통과 |
| T36 | `test-deletion-ledger.mjs` | 「계정이 없는 pending 이 승격되지 않았다」 · 「expires_at 산식이 다르다」 | promote-only 의 정상 경로 | 통과 |
| T37 | `test-deletion-ledger.mjs` | 「살아 있는 계정의 pending 을 보고 무언가를 바꿨다」 | 살아 있는 계정의 pending 은 실패 기록이다(I22·I23 · 위협 35) | 통과 |
| T38 | `test-deletion-ledger.mjs` | 「`<파일>` 에 users 를 지우는 문장이 있다」 | 새 삭제는 사용자의 새 요청으로만 시작한다(I24) | 통과 |
| T39 | `test-deletion-ledger.mjs` | 「조건이 빠졌는데 제거가 실행됐다」 · 「계정이 없는 pending 을 지웠다」 | 재확인 없는 제거 금지(I25 · Q7b) | 통과 |
| T40 | `test-friends.mjs` | 「503 인데 응답에 단어가 실려 나왔다」 · 「맞는 쿠키와 틀린 쿠키의 응답이 다르다」 | 되살아난 단어장이 안 나가고 **인증을 수행조차 안 한다** | 통과 |
| T41 | `test-friends.mjs` | 「친구에게 탈퇴자의 단어장이 보인다」 | 친구 경로도 막힌다 | 통과 |
| T42 | `test-friends.mjs` | 「503 인데 응답에 계정 id 가 실렸다」 | 계정 존재 여부도 알려주지 않는다 | 통과 |
| T43 | `test-friends.mjs` | 「restore_closed 에서 `<경로>` 가 허용 목록에 들었다」 9경로 | 허용 목록이 정확히 넷이다(기본값이 차단) | 통과 |
| T44 | `test-cleanup.mjs` | 「만료된 pending 표식을 지웠다」 + 소스 검사 | `deletions` DELETE 는 `confirmed_at IS NOT NULL` 하나뿐 | 통과 |
| T45 | `test-cleanup.mjs` | 「아직 안 만료된 소비 표식을 지웠다」 | 만료 전 삭제 금지(R10) | 통과 |
| T46 | `test-cleanup.mjs` | 「두 번째 실행이 상태를 또 바꿨다」 | 재실행 안전(C8) | 통과 |
| T47 | `test-cleanup.mjs` | 「`<mode>` 인데 건너뛰지 않았다」 2상태 | 복원 중 정리가 겹치면 §10-4 의 경합(C14) | 통과 |
| T48 | `test-signup.mjs` | 「같은 순간의 두 번째 가입이 막힌다」 + 인덱스 검사 | `UNIQUE(pv, occurred_at)` 류가 없다(I18-b) | 통과 |
| T49 | `test-cleanup.mjs` | 「★ `<k>`회째 실패인데 Cron Promise 가 성공으로 끝났다」 · 「연속 실패 `<k>`회의 경보가 틀렸다」 · 「★ 확정 안 된 표식이 있는데 경보가 없다」 · 「정리 상태를 못 읽는데 경보가 꺼져 있다」 · 「/ready 응답에 `<값>` 가 새어 나왔다」 | 실패가 실패로 끝나고, 경보가 boolean 하나로 나가고, 숫자는 안 나간다(C6·C11·C12) | 통과 |

| **T50** | `test-friends.mjs` | 「없는 주소가 주 D1 에 rate_limits 행을 만들었다」 · 「없는 주소가 ledger 에 임차증을 남겼다」 | **위협 40** — 라우트 판정이 게이트·임차증·리미터보다 앞이다 | 통과(**6판 신설**) |
| **T51** | `test-friends.mjs` | 「로그인 시작이 임차증을 땄다 — 주 D1 을 안 만지는데 추적한다」 | 주 D1 을 안 만지는 요청은 세지도 추적하지도 않는다 | 통과(**6판 신설**) |
| **T52** | `test-friends.mjs` | 「읽기 하나에 임차증이 하나가 아니다」 · 「쓰기 하나가 rate_limits 버킷 하나를 넘게 만들었다」 | 요청 하나 = 임차증 하나 = ledger 변경 둘 | 통과(**6판 신설**) |
| **T53** | `test-friends.mjs` | 「429 를 받으면서 카운터가 계속 올랐다」 · 「차단된 요청의 ledger 쓰기가 요청당 둘을 넘는다」 | 리미터가 증폭기가 되지 않고, 차단 뒤에도 쓰기가 상수로 유지된다 | 통과(**6판 신설**) |
| **T54** | `test-friends.mjs` | 「등록 안 된 버킷 `<x>` 이 통과했다」 · 「프로토타입 속성 `<x>` 이 버킷으로 통과했다」 · 「라우트가 쓰는 버킷 `<b>` 이 등록돼 있지 않다」 | **위협 41** — 미등록 버킷은 fail-fast | 통과(**6판 신설**) |
| **T55** | `test-friends.mjs` | 「`<메서드> <경로>` 가 라우트로 잡힌다」 · 「`<메서드> <경로>` 가 주 D1 을 만지는데 임차증을 안 든다」 · 「회전이 자기 버킷을 안 쓴다」 | 라우트 표가 판정의 원본이다(순수 함수) | 통과(**6판 신설**) |
| **T56** | `test-reaudit.mjs` (R1) | 「`<from>` → `<to>` 가 검증 없이 통과했다」 · 「거부인데 모드가 바뀌었다」 · 「maintenance 로 새어 나가 되살아난 단어장이 읽힌다」 · 「판정 뒤에 게이트가 바뀌었는데 그대로 덮어썼다」 | 읽기가 열리는 목적지로 가는 **모든** 전환에 같은 검증 · CAS | 통과(**7판 신설**) |
| **T57** | `test-reaudit.mjs` (R2) | 「`<상수/다른 키/…>` 함수로 재삭제 대상이 0 이 됐다」 · 「… 로 `setMode("open")` 이 통과했다」 · 「모르는 key_version 이 있는데 재개방이 통과했다」 | 삭제 증거의 키 재료는 **코드가 소유한다** | 통과(**7판 신설**) |
| **T58** | `test-reaudit.mjs` (R3) | 「경보 시각 전인데 경보 대상으로 셌다」 · 「경보 시각과 같은 순간에 이미 경보다」 · 「재개방 판정이 갓 만들어진 미확정 표식을 못 봤다」 | 안전 조건과 경보는 다른 질문이다 | 통과(**7판 신설**) |
| **T59** | `test-reaudit.mjs` (R4) | 「`<…>` 함수로 살아 있는 계정의 표식이 승격됐다」 · 「키가 틀린데 reconciliation 이 실행됐다」 · 「계정이 없는데 승격하지 않았다」 | 사용자 행이 있는 동안에는 어떤 키 오류에서도 승격하지 않는다 | 통과(**7판 신설**) |
| **T60** | `test-reaudit.mjs` (R5) | 「요청을 2배로 늘리니 `<주 D1/ledger>` 쓰기가 `<a>` → `<b>` 로 늘었다」 · 「429 이후에도 … 쓰기가 났다」 · 「restore_closed 인데 리미터가 주 D1 에 썼다」 | 인증 없는 반복 요청이 지속 저장소 쓰기를 무제한으로 늘리지 못한다 | 통과(**7판 신설**) |

| **T61** | `test-stage34-closeout.mjs` | 「계정이 사라졌는데 삭제 표식이 0건이다」 · 「삭제가 진행 중인데 운영자의 제거가 표식을 `<n>`건 지웠다」 · 「판정 뒤 `<모드/epoch/임차증>` 이 바뀌었는데 표식을 지웠다」 | 증거 삭제는 **판정 순간이 아니라 삭제 순간**의 상태로 결정된다 | 통과(**8판 신설**) |
| **T62** | `test-stage34-closeout.mjs` | 「`drained:<v>` 라고 답한 뒤 주 D1 에 쓰기 `<n>`건 · 질의 `<n>`건이 났다」 | 주 D1 은 **예외 없이 임차증 안에서만** 만져진다 | 통과(**8판 신설**) |
| **T63** | `test-stage34-closeout.mjs` | 「남용 방어가 없는데 `<버킷>` 경로가 `<코드>` 로 열렸다」 · 「닫혀 있다면서 `<DB>` 에 질의를 `<n>`건 던졌다」 · 「IP `<n>`개로 D1 한도를 태울 수 있는 설정이 ready 라고 답한다」 | 막지 못하는 것을 **열지 않는다** | 통과(**8판 신설**) |
| **T64** | `test-stage34-closeout.mjs` | 「`DELETION_KEY` 가 어긋났는데 `/ready` 가 200 이다」 · 「/ready 응답에 `<문자열>` 가 새어 나왔다」 | 삭제 증거의 **의미적** 사용 가능성이 readiness 에 들어간다 | 통과(**8판 신설**) |
| **T65** | `test-abuse-guard.mjs` | 「닫혔다면서 `<DB>` 에 질의 `<n>`건」 · 「abuseReady 가 참이다 — 부를 수 없는 바인딩이다」 · 「눌러도 503 인 버튼을 그리라고 한다」 · 「응답에 `<문자열>` 가 새어 나왔다」 | 방어는 **있다**가 아니라 **부를 수 있고 답이 boolean 이다** | 통과(**9판 신설**) |
| **T66** | `test-abuse-guard.mjs` | 「한도+1 인데 `<코드>` — 엣지 mock 이 전부 허용했고 우리 카운터는 안 셌다」 · 「요청 하나가 카운터 행 `<n>`개를 만들었다」 · 「엣지가 막은 요청이 임차증을 땄다」 | 문서의 버킷별 한도는 **우리 카운터가 집행한다**. 엣지는 사전 거름이고 층이 다르다 | 통과(**9판 신설**) |
| **T67** | `test-abuse-guard.mjs` | 「분산 요청이 막혔다 — 문서가 「못 막는다」고 적었다」 · 「요청 `<n>`건에 ledger 쓰기 `<n>`건」 · 「닫힌 상태에서 DB 를 만졌다」 | **막는 칸은 0 · 못 막는 칸은 실제로 는다.** 과장과 거짓을 함께 잡는다 | 통과(**9판 신설**) |
| **T68** | `test-abuse-guard.mjs` | 「WAF 모드인데 계정 경로가 `<코드>` 다 — 골라도 문이 안 열린다」 · 「pages.dev 우회가 `<코드>` 로 통과했다」 · 「Turnstile 단독이 모드로 인정됐다」 | 운영자가 고른 모드가 **코드에서 실제로 문을 연다.** 모르는 선언은 `none` | 통과(**9판 신설**) |
| **T69** | `test-abuse-guard.mjs` | 「키 없는 /ready `<n>`회가 `<DB>` 에 질의 `<n>`건」 · 「인증 없는 응답이 `<필드>` 를 말한다」 · 「운영자 키인데 아무것도 안 물어봤다」 | 진단은 **운영자만**, 공개 응답은 **DB 를 안 만진다** | 통과(**9판 신설**) |
| **T70** | `test-signup.mjs` | 「turnstile=`<값>` 로 가입이 시작됐다」 · 「검증 서버가 죽었는데 가입이 시작됐다 — fail-open 이다」 · 「사람 확인을 안 지난 state 로 가입이 끝났다」 · 「검증 서버를 `<n>`번 불렀다」 | 서버가 사람 확인 결과를 **실제로 보고**, 콜백이 그것을 대조한다 | 통과(**9판 신설 · 결정 3**) |
| **T71** | `test-friends.mjs` | 「`<사유>`: `<코드>` 로 지나갔다」 · 「주 D1 에 질의 `<n>`건 — 값싼 문이 아니다」 · 「서명만 맞으면 통과했다 — envelope 이 인증을 대신하고 있다」 · 「키를 바꿨는데 옛 쿠키가 살아 있다」 | 값싼 문이 DB 앞에 있고, **그 통과가 인증이 아니다** | 통과(**9판 신설 · 결정 4**) |
| **T72** | `test-client.mjs` | 「사람 확인 전인데 가입 버튼이 열렸다」 · 「사람 확인이 만료됐는데 버튼이 열려 있다」 · 「가입 시작이 사람 확인 토큰을 안 보냈다」 · 「위젯 실패를 화면이 말하지 않는다」 | 화면이 토큰 없이 진행하지 않고, **왜 못 누르는지 말한다** | 통과(**9판 신설 · 결정 3**) |

| **T73** | `test-signup.mjs` | 「`<키>` 가 없는데 가입 준비됨이라고 말한다」 · 「`<키>` 없이 가입 시작이 `<코드>` 로 열렸다」 · 「`<키>` 없이 계정이 만들어졌다」 · 「`<키>` 가 없는데 제공자를 `<n>`번 불렀다」 | 조건이 **한 함수**에 있고, 거부가 **외부 호출 앞**이다 | 통과(**10판 신설**) |
| **T74** | `test-signup.mjs` | 「action=`<값>` 인데 가입이 시작됐다」 · 「요청이 도착한 주소를 기준으로 삼아 다른 별칭에서 푼 토큰이 통과했다」 · 「화면 action 과 서버 action 이 다르다」 | `success` 만으로는 부족하고, 기준은 **`APP_ORIGIN`** 이다 | 통과(**10판 신설**) |
| **T75** | `test-friends.mjs` | 「틀린 키(`<값>`)로 진단이 나왔다」 · 「틀린 키의 응답 모양이 다르다」 · 「await 없는 sameSecret 호출이 있다」 · 「원문 길이로 먼저 끊는다」 | 비교가 **원문을 훑지 않고**, 부르는 자리가 전부 `await` 다 | 통과(**10판 신설**) |
| **T76** | `test-abuse-guard.mjs` | 「카운터가 던지는데 `<코드>` 다」 · 「셀 수 없는데 주 D1 에 쓰기 `<n>`건」 · 「대표 라우트 표가 실제 버킷 목록과 다르다」 | 방어의 **실패 갈래**가 fail-closed 이고, 그 사실이 **관측된다** | 통과(**10판 신설**) |

| **T77** | `test-signup.mjs` | 「`<경우>`: 가입 시작이 `<코드>` 로 열렸다」 · 「설정이 덜 됐는데 검증 서버를 `<n>`번 불렀다」 · 「/login/`<p>` 가 `<코드>` 다」 · 「secret 이 사라졌는데 콜백이 성공했다」 | 제공자 자격 규칙이 **화면과 문에서 같다**. 거부가 외부 호출 앞이다 | 통과(**11판 신설**) |
| **T78** | `test-deletion-ledger.mjs` | 「옛 epoch 의 lease 로 pending 표식이 남았다」 · 「이미 확정된 표식이 다시 확정됐다」 · 「보유기간이 뒤로 밀렸다」 | fencing 은 pending 에도 걸리고, **확정은 한 번뿐이다** | 통과(**11판 신설**) |
| **T79** | `test-workerd.mjs` | 「맞는 키인데 진단이 안 나왔다 — 안전 비교가 안 돈다」 · 「거절 응답의 모양이 `<n>`가지다」 · 「workerd 로그에 예외가 있다」 · 「이 Node 프로세스에 어댑터가 실려 있다」 | 운영 경로가 **진짜 Workers 런타임**에서 돈다 | 통과(**11판 신설**) |
| **T80** | `test-client.mjs` | 「계약을 못 지킨 응답인데 초대 링크 버튼이 보인다」 · 「ready:false 인데 「카카오 계정」이라고 단정한다」 · 「점검 안내가 없다」 · 「readiness false 가 로그인 표시를 지웠다 — 401 이 아니다」 · 「ready:true 인데 계정 기능이 닫힌 채로 남았다」 | 화면이 서버의 readiness 를 **버리지 않고** 그대로 fail-closed 로 반영한다 | 통과(**2026-08-23 신설**) |
| **T81** | `test-client.mjs` | 「계약을 어긴 응답의 providers 로 로그인 버튼을 그렸다」 · 「「서버에 못 물어봤다」와 같은 갈래로 다루지 않는다」 | 계약을 어긴 응답은 **`providers` 도 못 믿는다** — 정규화하지 않고 `DOWN_HEALTH` 로 보낸다 | 통과(**2026-08-23 신설**) |

**81건 전부 실행 가능한 단언으로 연결됐다.**
⚠️ **「연결됐다」는 「실패 갈래까지 쟀다」가 아니다**(2026-08-22 정정). 위협 60 이 그 증거다 —
`countVerdict` 의 `catch` 를 통과로 바꾸는 돌연변이가 **스위트 25개를 전부 지나갔다.**
개수와 연결은 **필요조건일 뿐**이고, 판정 근거는 **돌연변이 생존율**이다. 그중 **「거부됨」이 아니라 「아직 통과함」을
고정하는 것은 T8-b 하나**이고, §10-8-0 의 복원 금지를 유지시키는 테스트 근거도 그 하나다.
**T6 은 결정 A′ 의 정상 통과 검사다** — 「도는 동안 활성 1 · 그동안 복원 거부 · 끝난 뒤에만 0」을
검증한다(T6b 읽기 추적 · T6c TTL 만료 ≠ 해제 · T47b·T47d 정리 크론도 같은 임차증).
⚠️ **5판 당시 여기에는 「T6·T8 두 건」이라고 적혀 있었다.** 그날은 임차증을 삭제 saga 만 땄기
때문이고, 2026-08-18 결정 A′ 구현으로 T6 의 성격이 바뀌었다.

#### 명세와 구현이 달라진 곳 (숨기지 않는다)

| # | 명세 | 구현 | 왜 |
|---|---|---|---|
| **T20** | 「같은 `kind` 를 두 번 담아 보낸다 → 400」 | 클라이언트가 kind 를 보내는 **자리가 없다**. 서버가 자기 집합을 순회한다. 대신 ① 집합에 중복이 없고 ② 본문에 `kinds`·`events`·`policy_events` 를 끼워 넣어도 기록이 안 늘어남을 잰다 | 명세가 「클라이언트가 이벤트 목록을 보낸다」를 전제했는데 그 설계를 택하지 않았다. 재는 불변식(중복 이벤트가 N행 검사를 우회하지 못한다)은 그대로다 |
| **§5-4 S5·S6** | AAD 에 `exp` 를 넣되 `exp` 는 암호문 안에만 | **모순이었다** — AES-GCM 은 복호화 **전에** AAD 를 재구성해야 하는데, 암호문 안의 값은 복호화 전에 읽을 수 없다. 형식을 `v1.<exp>.<nonce>.<ct>` 로 바꿔 `exp` 를 헤더에 두고, **AAD 와 평문 양쪽에 넣어 대조**한다 | 헤더만 고치면 AAD 불일치로 복호화가 실패하고, 안팎이 다르면 우리가 거부한다. `test-signup.mjs` A1·A7 이 이 형식을 고정한다 |
| **§10-1 `expires_at`** | 「37일」 | 상수 이름 `CONFIRMED_RETENTION` 으로 두고 **테스트가 숫자를 하드코딩하지 않는다** | 37일은 `max(가정 30일, 백업 7일) + 여유 7일` 의 **기술적 보수 계산값**이고 법정 보유기간이 아니다. 요금제가 확정되면 이 상수만 바꾼다 |


## 14. 기존 사용자와 구버전 호환

**현재 0명이다. 구현 직전에 반드시 다시 조회한다**(2026-08-17 값은 그날의 사실일 뿐이다).

### 14-0. **권고 — release gate 하나로 끝낸다** (2판)

1판은 "0명이면 A, 1명 이상이면 B" 라고 두 설계를 나란히 두었다. 그건 **아직 존재하지 않는
사용자를 위한 전환 코드를 미리 쓰는 일**이고, 이 저장소가 이미 배운 것과 정반대다
(쓰지도 않는 테이블에 재생성 migration 을 쓰지 않는다 — 2단계 §4).

> **배포 순서 9번(원격 사용자 수 재확인)에서 `user_count > 0` 이면 배포를 중단하고
> 별도 전환 설계로 돌아간다.** 전환 로직을 미리 만들지 않는다.

| | 값 | 조치 |
|---|---|---|
| `SELECT COUNT(*) FROM users` | **0** | 그대로 진행. 전환 migration·전환 UI·전환 API **없음** |
| | **1 이상** | **배포 중단.** §14-2 를 설계로 승격시키고 결정을 다시 받는다 |

**왜 이 gate 가 성립하나**: OAuth 가 꺼져 있어 **아무도 계정을 만들 수 없다.** OAuth 를 켜는
것과 이 배포는 §15 에서 같은 창에 묶여 있으므로, 배포 직전의 0명은 **배포 순간까지 0명**이다.
gate 가 깨지는 유일한 경로는 "누군가 먼저 OAuth 를 켠 경우"이고, 그건 승인 사항이라
사용자만 할 수 있다.

**이 gate 의 비용**: 만약 1명이 있으면 그날 배포가 무산된다. 그 대가로 **쓰이지 않을 전환
코드와 그 테스트를 안 쓴다.** 사용자 0명인 지금 그 거래는 명백히 유리하다.

### 14-1. 경우 A — 구현 직전에도 0명 (**예상 경로**)

- 전환 migration 없음. 첫 사용자부터 새 정책이 적용된다.
- 스키마와 코드를 **같은 배포에** 반영하고, 그 배포 **전까지** OAuth 를 켜지 않는다.

### 14-2. 경우 B — 구현 직전에 1명 이상 (**배포 중단 후 별도 설계**)

아래는 그때 출발점으로 쓸 요구사항이지 **지금 만들 것이 아니다.** 그리고 이 목록이
**완전하지 않다는 것**이 §14-0 을 권고하는 이유이기도 하다 — 완전하게 만들려면 아래
전부를 설계하고 테스트해야 한다.

| 항목 | 요구사항 |
|---|---|
| 흐름 | 기존 사용자 로그인 성공 → 세션은 발급 → **정책 화면**을 앱이 띄운다 |
| **정책 수락 API** | `POST /api/policies/accept { pv }` — 인증 필요 · Origin 검사 · `signup` 버킷 · `pv` 현재 번들 대조 · `occurred_at`/`recorded_at` 둘 다 기록(§7-4) |
| **DB 기록** | 기존 `user_id` 에 `policy_events` **N행**(§8-0). 가입과 달리 **계정 생성이 없으므로 batch 는 이벤트만** |
| **서버 측 write 차단 조건** | 아래 §14-3 표. **클라이언트가 화면을 띄우는 것만으로는 방어가 아니다** — 구버전 PWA 는 그 화면을 모른다 |
| 정책 확인 전 허용 | 사전·연습·단어장 **읽기**(`GET /book`·`/me`·`/friends/:id/book`), 로그아웃, **계정 삭제** |
| 거부 시 | 로그아웃 또는 계정 삭제를 고르게 한다. 조용히 계속 쓰게 두지 않는다 |
| 기존 세션 | 유지한다. `session_version` 을 올리면 전원이 이유 없이 튕긴다 |
| 구버전 PWA | 새 API 를 모른다 → **서버가** 차단하고 "앱을 새로고침해 주세요"로 답한다 |
| 서비스워커 세대 | 자산 해시가 캐시 이름에 박혀 있어(`shhh-v11-<해시>`) 배포와 함께 갈린다 |
| **"이벤트 없음"을 정상으로 오인하지 않는 법** | `users.created_at` 과 정책 도입 시각을 비교한다. 도입 **후** 계정에 이벤트가 없으면 **버그**이고, 도입 **전** 계정은 전환 대상이다. `policy_events` 유무만으로는 못 가른다 |

### 14-3. 경우 B 의 서버 측 write 차단 표 — **`GET /friends` 를 빠뜨리지 않는다**

1판은 차단 목록을 `PUT /book`·`POST /friends`·`PUT /friends/:id`·`POST /friends/code`
넷으로 적었다. **`GET /api/friends` 가 빠져 있었고, 그것은 초대 코드를 INSERT 한다**(§1-1).

| 라우트 | 정책 미수락 계정에게 | 왜 |
|---|---|---|
| `GET /api/book` · `/api/me` | **허용** | 읽기. 자기 데이터를 못 보게 할 이유가 없다 |
| `GET /api/friends/:id/book` | 허용 | 읽기 |
| **`GET /api/friends`** | **차단(409 `policyRequired`)** | **`myCode()` 가 `invite_codes` 를 INSERT 한다.** 정책 미수락 계정에 새 데이터를 만들면 안 된다 |
| `PUT /api/book` | 차단 | 쓰기 |
| `POST /api/friends` · `POST /api/friends/code` | 차단 | 쓰기 |
| `PUT · DELETE /api/friends/:id` | 차단 | 쓰기 |
| `DELETE /api/session` (로그아웃) | **허용** | 나갈 길은 항상 열어 둔다 |
| `DELETE /api/me` (계정 삭제) | **허용** | 거부한 사람이 고를 수 있어야 하는 선택지다 |

> `GET /api/friends` 를 차단하면 친구 화면이 아예 안 뜬다. **그것이 의도다** — 정책을 수락하기
> 전에는 그 화면이 새 초대 코드를 만들지 않아야 한다. 화면에는 "먼저 확인해 주세요"를 띄운다.
> 대안(코드 생성만 막고 목록은 보여주기)은 `myCode()` 를 조건부로 만드는 일이라
> **읽기 경로에 분기를 하나 더 넣는다** — 경우 B 가 실제로 오면 그때 저울질한다.

**필요한 테스트**(경우 B 가 현실이 될 때): 위 표 전 행 × (수락 전/후) 왕복 · 구버전 PWA 가
`POST /policies/accept` 를 모르는 상태에서 쓰기를 시도 → 409 · 수락 후 즉시 쓰기 성공.

---

## 15. 배포·migration 순서 (실행하지 않는다)

**아래는 전부 별도 사용자 승인 사항이다**: 새 D1 생성 · 삭제 표식 저장소 생성 · migration 적용 ·
KV 바인딩 제거 · KV 삭제 · 시크릿 등록 · OAuth 활성화 · 배포 · push · Time Travel restore ·
백업 생성·복원.

| # | 단계 | 승인 |
|---|---|---|
| 1 | 법률 검토 결과 반영(§8 분기 확정) | — |
| 2 | 최종 스키마 확정(`policy_events` `CHECK`·CASCADE·ledger) | — |
| 3 | **회귀 테스트 먼저 작성** (`test-signup`·`test-policies`·`test-deletion-ledger`) | — |
| 4 | 로컬 migration (`--local`) | — |
| 5 | 로컬 가입·삭제·복원 시뮬레이션 | — |
| 6 | `privacy.html`·이용약관·`brand/NAVER-REVIEW.md` 를 **같은 변경 단위**로 수정 | — |
| 7 | 전체 `npm test` + `npm audit` | — |
| 8 | **사용자 승인** | ✅ |
| 9 | **원격 사용자 수 재확인 → release gate**(§14-0). **`user_count > 0` 이면 여기서 중단** | ✅ |
| 10 | 백업(저장소 밖·암호화 볼륨·24시간 뒤 삭제) | ✅ |
| 11 | 원격 migration (`0004` 포함 — 2026-08-17 원격 적용 완료) | ✅ |
| 11b | **삭제 표식 D1 생성 + 스키마** — §10-6 의 restore 금지를 **운영 문서에 먼저 적고** 만든다 | ✅ |
| 12 | 시크릿 등록 — `RL_KEY` · `DELETION_KEY` · OAuth 8개 | ✅ |
| 12b | **유지보수 모드 실동작 확인** — `MAINTENANCE=1` 로 한 번 켜고 `GET /cb`·`GET /friends` 가 503 인지 **찔러 본다**. 끄고 `/api/ready` 가 200 인지 확인 | ✅ |
| 13 | 배포 | ✅ |
| 14 | **배포 고유 주소**로 검증 | — |
| 15 | **canonical 주소**로 검증(`cf-cache-status`·`age` 까지 본다) | — |
| 16 | OAuth 실제 계정 테스트 | ✅ |
| 17 | 정책 이벤트가 실제로 **N행**(§8-0) 들어갔는지, 집합의 각 원소가 한 번씩인지 확인 | — |
| 18 | 삭제 절차 검증 — **테스트 전용 계정으로만.** ⚠️ **복원 절차는 §10-8-0 의 금지가 풀린 뒤에만** — **⑤ 지금 이 순간 임차증 0건이 확인되지 않았고 · ⑦ 옛 배포 차단(D1~D12)이 증명되지 않았고 · ⑨ T8 이 아직 「200」**인 동안에는 라이브에서 시연조차 하지 않는다 | ✅ |
| 19 | 구버전 서비스워커 확인(설치형 PWA 실기기) | — |
| 20 | 롤백 경로 확인 후 백업 폐기 | ✅ |

> **라이브 계정 삭제 시험은 사용자 데이터가 생긴 뒤에는 하지 않는다.** 18번은 테스트 전용
> 계정과 승인된 환경에서만 한다. 지금(0명)이 그 시험을 하기 가장 싼 시점이다.

---

## 16. 아키텍처 경계 — 바뀔 파일과 책임

새 계층을 만들지 않는다. 회원가입 하나 때문에 Worker 를 다시 쓰지 않는다.

| 파일 | 계층 | 이번에 지는 책임 | 새 파일인가 |
|---|---|---|---|
| `js/auth.js` | 입력·화면 | 게이트에 「가입하기/로그인」 분리 · 정책 화면 렌더 · 체크박스 · `signup_required` 처리 · 정책 해시 대조 결과에 따른 fail-closed | 기존 |
| `js/authApi.js` | 데이터·외부 접근 | `apiPolicies()` · `apiSignupStart()` 추가. **`fetch` 는 계속 이 파일에만** | 기존 |
| `worker/index.js` | 서버 경계 | `POST /signup/start` · `GET /policies` · `verifyProvider`/`findUser`/`createAccountWithPolicy` 분해 · **`RL_MAX` fallback 제거 + `signup` 등록**(§12-1) · **유지보수 모드 허용 목록**(§10-7) · 정책 상수 · 삭제 saga | 기존 |
| `worker/schema.sql` | DB | `policy_events` (분기 확정 후) | 기존 |
| `migrations/0005_*.sql` | DB | 같은 내용의 버전형 migration | **신규** |
| `policies/` | 정적 | 불변 정책 문서 + manifest | **신규** |
| `scripts/build.mjs` | 빌드 | `INCLUDE` 에 `policies` 추가 | 기존 |
| `service-worker.js` | 캐시 | `ASSETS` 에 현재 번들 파일 추가 | 기존 |
| `functions/api/[[path]].js` | 연결부 | **변경 없음**(로직을 복사하지 않는다) | 기존 |
| ledger DB(별도) | DB | `deletions` 테이블 | **신규**(승인 후) |
| `privacy.html` | 정적 | §14 불일치 8건 — 법률 근거 확정 후 같은 변경 단위 | 기존 |

정책 화면의 판단(무엇을 보여줄지·언제 fail-closed 할지)이 순수 함수로 떨어지면
`test-client` 가 화면 없이 잰다 — `syncPlan()` 과 같은 무늬다.

---

## 17. 사용자 결정 — 확정 현황 (2026-08-17)

| # | 결정 | 상태 |
|---|---|---|
| 1 | 가입 흐름 | ✅ **C안(로그인·가입 분리) 승인** |
| 2 | 임시 가입 상태 | ✅ **제거 승인** (방식 3) · **4판에서 의미 정정** — `pending_signups` 는 여전히 없고, OAuth 검증 **후의** replay tombstone 만 추가(§5-3 · 아래 결정 2 절) |
| 3 | 정책 이벤트 법적 분기 | ⏸ **외부 법률 검토까지 보류** |
| 4 | `policy_events` CASCADE | ⏸ **법률 검토 전까지 CASCADE 임시 유지 · migration 생성 금지** |
| 5 | 삭제 표식 저장소 | ⚠️ **별도 D1 조건부 승인** — 아래 결함 수정 **전에는 생성·구현 금지**. 2·5판이 그 수정이다(§10-1·§10-4·§10-5·§10-6·§10-6-0·§10-7) |
| **A** | **가입 state 기밀성** | ✅ **A안(AEAD 암호화) 확정 2026-08-17** — `SIGNUP_STATE_KEY` 전용 · 소비 표식은 `TOMBSTONE_KEY` 로 **분리** · 로그인 state 는 현행 유지. 명세 §5-4 · §5-3-5 |
| **B** | **정기 정리 수단** | ✅ **가) 정리 전용 Scheduled Worker 확정 2026-08-17** — Cron 무료 계정당 5개 확인됨. Pages Functions 에 넣지 않는다. 설계 §10-5-2 |
| **C** | **복원 외부 차단 경계** | ⚠️ **가) 조건부 확정 2026-08-17** — 「옛 배포 삭제 + Preview Access」를 **설계안으로만** 승인. **D1~D12 전부 증명 전에는 복원 금지 유지**하고, **실제 배포 삭제는 별도 최종 승인**. §10-8-1 |
| **D** | **전역 drain 착수 시점** | ✅ **나) 법률 검토 후 확정 2026-08-17** — 런타임 구현은 4단계. 단 **설계는 이 문서에서 완성한다**(§10-7 `restore_closed` · §10-8-0 ①~⑧ · §10-6-0) |
| **E** | **삭제 표식 보유기간** | ⚠️ **다) 임시 기술값 확정 2026-08-17** — 산식은 `max(Time Travel, 수동 백업) + 안전 여유`. **37일을 확정 보유기간·방침 문구로 쓰지 않는다.** §10-5 · 법률 `L14` |
| 6 | 삭제 실패 응답 | ✅ **pending 실패=X · 주 D1 삭제 실패=X · confirmed 실패=Y 승인.** 단 **reconciliation 은 유지보수 모드 안에서만**(§10-4) |

아래는 각 결정의 근거 표다. **확정된 것은 다시 묻지 않는다** — 근거 기록으로만 남긴다.

### 결정 1 — 가입 흐름 ✅ **C안 확정**

| | A: OAuth 전 가입 화면 | B: OAuth 후 가입 화면 | **C: 로그인·가입 완전 분리** |
|---|---|---|---|
| 쉬운 예시 | 문 앞에서 약관을 읽고 들어간다. **단골도 매번 읽는다** | 일단 들어온 뒤 신규면 서류를 쓴다. 그동안 **신분증을 맡아 둔다** | 문이 둘이다. 단골 문 / 신규 문 |
| 보안 | 좋음 | **가입 대기 토큰이 새 공격면** | 좋음 |
| 사용자 경험 | 기존 사용자가 매번 약관 화면 | 가장 매끄러움 | 신규가 로그인 문을 먼저 열면 왕복 1회 헛돔 |
| 개발·운영 | 낮음 | **높음**(테이블·만료·청소·복원 영향) | 낮음 |
| **Claude 권고** | | | ✅ **C** |

**권고 이유**: 임시 개인정보를 하나도 안 만들고, 정책 수락을 이미 있는 `state` 서명(지금 CSRF 를
막고 있는 그 자물쇠)에 실어 보내면 새 테이블도 새 비밀값도 필요 없다. 대가는 신규 사용자의
왕복 1회이고, 사용자 0명인 지금 그 비용은 사실상 0이다.

**결정 결과**: C안 확정. §5 의 흐름이 구현 대상이다.

### 결정 2 — 임시 가입 상태 ✅ **제거 확정**

| | 1: 암호화 쿠키 | 2: 서버 `pending_signups` | **3: 임시 상태 제거** |
|---|---|---|---|
| 쉬운 예시 | 봉인한 봉투를 손님이 들고 다닌다 | 프런트에 번호표를 맡긴다 | 맡길 것을 아예 안 만든다 |
| 보안 | 복사본 재사용을 완전히 막지 못함 | 1회용 보장 가능, 대신 상태가 는다 | 공격면 없음 |
| 개인정보 | 회원번호를 암호화해 보관 | 회원번호를 DB 에 보관 | **보관 없음** |
| 복원 영향 | 없음 | **Time Travel 이 pending 도 되살린다** | 없음 |
| **확정** | | | ✅ **3 — 임시 상태 없음** |

§6 의 방식 1·2 는 **구현하지 않는다.** 참고 기록으로만 남긴다.

#### ⚠️ 4판 — **결정 2 의 의미를 정정한다** (뒤집는 것이 아니라 범위를 명확히 한다)

**숨기지 않고 적는다: 4판은 주 D1 에 표 하나를 추가한다**(`consumed_signup_states`, §5-3).
3판까지의 "새 DB 테이블 0" 은 더 이상 사실이 아니다. **새 공격(위협 34)을 발견한 데 따른
4판 보안 정정**이다.

| | 결정 2 가 **거부한 것** | 4판이 **추가하는 것** |
|---|---|---|
| 무엇 | **`pending_signups`** — OAuth **전에** `provider_subject` 를 보관하는 표 | **replay tombstone** — OAuth 검증 **후에** state 의 중복 사용을 막는 표 |
| 언제 쓰나 | 제공자 인증 **전** (인증 없는 자리의 쓰기) | 제공자 인증 **성공 후**, 계정 생성 batch 안 (R4) |
| 무엇을 담나 | **회원번호(개인정보)** | `state_hash` + `expires_at` **둘뿐.** 누구인지 모른다 (R1·R2) |
| 개인정보 | 임시 보관 있음 | **없음** — 단방향 해시라 사람을 가리키지 않는다 |
| DoS 표면 | `/signup/start` 반복만으로 쓰기 발생 | **발생하지 않는다** — 인증을 통과해야 쓴다 (R4) |
| 왜 필요한가 | 가입 화면을 OAuth 뒤에 두려고 | **한 번의 약관 수락이 여러 계정의 증거가 되는 것을 막으려고**(위협 34) |

> **정정된 결정 2**: OAuth **전에** provider 정보를 보관하는 pending signup 은 **만들지 않는다.**
> 단, OAuth 검증 **뒤**에 가입 state 의 중복 사용을 막는 **최소 replay tombstone 은 사용한다.**

**결정 2 의 원래 근거(임시 개인정보를 안 만든다 · 인증 없는 자리에 쓰지 않는다 · 복원이
pending 을 되살리지 않는다)는 전부 그대로 유지된다.** R1·R2·R4 가 그 셋을 각각 지킨다.

### 결정 3 — 정책 이벤트의 법적 분기 ✅ **확정 2026-08-18 — 분기 ①(계약 이행)**

⚠️ **이 확정은 사용자가 공식 자료와 실제 사례를 검토해 내린 프로젝트 운영 결정이다.**
**외부 법률 검토 완료가 아니고 법적 적합성 보증도 아니다.** 근거로 채택한 것은
「개인정보 보호법」 **제15조 제1항 제4호**(계약의 이행)와 **제28조의8 제1항 제3호**
(계약 이행에 필요한 국외 처리위탁·보관)이다.

`requiredPolicyKinds` 는 **정확히 셋**이다 — `terms/accepted` · `privacy/presented` ·
`age14/attested`. 「개인정보 수집·이용 동의」·「국외 이전 동의」·「전체 동의」 체크박스를
**만들지 않는다.** 받지 않은 동의를 받았다고 기록하면 그 기록 자체가 거짓이 되기 때문이다.

아래는 결정 전의 판단 기록이다.

~~`privacy/presented`(계약 이행) 인가 `privacy/accepted`(동의) 인가, 국외 이전 별도 동의가
필요한가. 외부 법률 검토 전에는 구현을 보류한다는 것이 Claude 의 권고였다.~~

전달할 질문과 사실은 `docs/PRIVACY_LEGAL_REVIEW_PACKET.md` 에 있다.
**결정하지 않으면 막히는 것**: `policy_events` 의 `CHECK`, migration, 가입 화면 체크박스 수,
`privacy.html` §14 의 5·6번 문장.

### 결정 4 — `policy_events` 의 계정 삭제 시 처리 ✅ **확정 2026-08-18 — CASCADE**

**계정과 함께 삭제한다.** 탈퇴자에 대한 가명키 보존 구조를 만들지 않고, 정책 이벤트는
계정 존속 기간에만 보유한다. 개인정보 최소보유를 앞세운 선택이고,
`scripts/test-migrations.mjs` 의 「`users` 참조는 전부 CASCADE」 전수 검사와도 맞는다.
migration 은 `migrations/0005_policy_events_and_signup_states.sql` 이다.

아래는 결정 전의 비교 기록이다.

| | CASCADE(계정과 함께 삭제) | 보존(기간 확인 필요) |
|---|---|---|
| 개인정보 | 최소수집에 부합 | 탈퇴자 기록이 남는다 |
| 기술적 사실 | 지금 스키마 그대로 가능 | **`users` 외래키를 쓸 수 없다**(`test-migrations.mjs:96-103` 이 CASCADE 를 전수 강제). 가명 키로 바꾸거나 ledger 로 옮겨야 한다 |
| **확정(2026-08-18)** | ✅ **CASCADE** | — |

⛔ **이 결정을 뒤집으면 스키마가 통째로 달라진다.** 보존이 필요해지면 `users` 외래키를 쓸 수 없고,
가명 키로 바꾸거나 ledger 로 옮겨야 한다 — `policy_events` 재생성 migration 이 필요하다
(SQLite 는 `CHECK` 를 `ALTER` 로 못 바꾼다).

### 결정 5 — 삭제 표식 저장소 ⚠️ **별도 D1 조건부 승인 — 아래 수정 전 생성·구현 금지**

| | **별도 D1** | 새 전용 KV | R2 | 저장소 없이 복원 금지만 |
|---|---|---|---|---|
| 쉬운 예시 | 옆에 둔 작은 장부 | 포스트잇 벽(붙인 게 바로 안 보임) | 창고에 파일 하나씩 | 장부 없이 "복원 안 하기"로 약속 |
| 읽기 일관성 | **강함** | **읽기 전파가 eventual** | 강함 | — |
| SQL 조회 | ○ | ✗ | ✗ | — |
| 운영 편의 | `wrangler d1 execute` 한 줄 | 훑기 | 목록+읽기 | 없음 |
| 위험 | saga 관리 · **이 DB 자체의 Time Travel** | 위 + 조회 지연 | 위 | **복원이 꼭 필요한 사고에서 무력** |
| **확정** | ⚠️ **조건부 승인** | | | |

**승인 조건 — 아래가 문서에 반영되기 전에는 D1 생성도 구현도 하지 않는다**
(2판에서 전부 반영했다):

| # | 조건 | 반영된 곳 |
|---|---|---|
| C1 | 표식 DB 의 **restore 원칙 금지** 규정 | §10-6 |
| C2 | 불가피한 복원의 **내보내기 → 복원 → 합집합 병합 → 행 수·해시 대조** 절차 | §10-6 |
| C3 | reconciliation 을 **유지보수 모드 전용**으로 | §10-4 |
| C4 | 유지보수 모드를 **메서드가 아닌 라우트 허용 목록**으로 | §10-7 |
| C5 | pending/confirmed **만료 기준 분리 + 정리 실행 시점** | §10-5 |
| C6 | `attempts` **삭제** | §10-1 |
| C7 | 위 전부에 대한 **위협 항목과 회귀 테스트** | 위협 26·27·28·31 · 테스트 9·11~16 · M1~M8 |
| **C8** | **(3판 추가)** lease 의 증명 범위를 삭제 saga 로 한정하고, **주 D1 restore 금지 gate** 와 **pending 장기 잔존 규정**을 문서에 반영 | §10-8-0 · §10-9-6·§10-9-7 · §10-5-1 · 위협 32·33 · 테스트 T1~T8 |

> C8 은 **결정 5 를 다시 묻는 것이 아니다.** 조건부 승인의 취지("결함이 남은 채로 D1 을 만들지
> 않는다")를 3판에서 찾은 결함에 그대로 적용한 것이고, 결론은 그대로 **조건부 승인**이다.
> D1 생성은 여전히 별도 승인 사항이다.

### 결정 6 — 삭제 saga 실패 시 사용자 응답 ✅ **확정**

| 실패 지점 | 선택지 X (보수적) | 선택지 Y (낙관적) | 확정 |
|---|---|---|---|
| pending 기록 실패 | "지우지 못했어요" | 그냥 진행 | ✅ **X** — 표식 없이 지우면 복원 때 되살아난다 |
| 주 D1 삭제 실패 | "지우지 못했어요" · 세션 유지 | "지웠어요" | ✅ **X** — 2026-08-16 에 닫은 P0 와 같은 판단 |
| confirmed 기록 실패 | "지우지 못했어요" | **"계정을 지웠어요"** + reconciliation | ✅ **Y** — 계정은 **실제로 없다.** 여기서 실패라고 말하면 사용자가 재시도하는데 지울 것이 이미 없다 |
| reconciliation 실행 | 자동 주기 | 복원 직전 + 수동 | ✅ **유지보수 모드 안에서만.** 서비스가 도는 동안에는 어떤 이유로도 실행하지 않는다(위협 27) |

**마지막 줄이 2판에서 강화된 부분이다.** 1판의 "복원 직전 + 수동"은 **동시에 서비스가 도는
경우를 막지 못했다** — 그 창에서 삭제가 하나만 성공하면 표식 없는 삭제가 된다.

---

## 18. 이번 단계에서 하지 않은 것

`worker/index.js`·`worker/schema.sql`·`js/*.js`·`privacy.html` 수정 · 이용약관 작성 ·
policy manifest·정책 버전 파일 생성 · migration 생성 · 테스트 파일 생성 ·
**삭제 표식 D1 생성** · **유지보수 모드 구현** · **`RL_MAX` fallback 제거 구현** ·
별도 D1/KV/R2 생성 · 원격 D1 쓰기 · KV 값 조회·삭제 · 시크릿 변경 · OAuth 활성화 ·
배포 · `push` · 캐시 퍼지 · Time Travel restore · 백업 생성·복원 ·
`네이버검수-캡처/` 접근 · **법률 결론 단정**.

**2판과 3판에서 한 일은 문서 수정뿐**이다. `git diff --name-only` 가 그 사실을 증명한다 —
바뀐 파일은 `docs/*.md` · `CLAUDE.md` · `brand/NAVER-REVIEW.md` 뿐이고 `worker/` · `js/` ·
`migrations/` · `scripts/` 는 한 줄도 바뀌지 않았다.

**3·4판에서도 만들지 않은 것**: 전역 write drain 구현 · deletion lease 구현 · `maintenance`·
`write_leases` 테이블 · **`consumed_signup_states` 테이블**(§5-3 — **설계만**) ·
`scripts/test-signup.mjs`·`test-policies.mjs`·`test-deletion-ledger.mjs` · §13-5 의 T1~T39 코드.

---

## 19. 출처

| 출처 | 확인한 것 | 날짜 |
|---|---|---|
| Cloudflare D1 Worker API — `batch()` (문서 갱신일 2026-06-22) | 원문: "Batched statements are SQL transactions. If a statement in the sequence fails, then an error is returned for that specific statement, and **it aborts or rolls back the entire sequence**." · <https://developers.cloudflare.com/d1/worker-api/d1-database/> — HTTP 200 확인 | 2026-08-17 |
| Cloudflare D1 — Time Travel | 항상 켜짐·비활성화 불가 · 제자리 덮어쓰기 · bookmark 로 되돌리기 · 30일(유료)/7일(무료) | 2026-08-16(2단계 §19) |
| **Cloudflare D1 — Time Travel (4판 재확인)** | 원문: **"Queries in flight will be cancelled, and an error returned to the client."** · "Restoring a database to a specific point-in-time is a _destructive_ operation, and **overwrites the database in place**." · 복원 결과로 **이전 bookmark 가 주어져 되돌릴 수 있다** · Free **7일** / Paid **30일** · <https://developers.cloudflare.com/d1/reference/time-travel/> | **2026-08-17 (4판)** |
| 이 저장소 코드 | `worker/index.js:244·327·369·465·492·511-526·700-703·712·751·781·830-839·948` · `worker/schema.sql` · `scripts/test-migrations.mjs:96-103` · `scripts/build.mjs` · `service-worker.js` | 2026-08-17 |
| 라이브 `/api/ready` | HTTP 503 · `{"ok":true,"configReady":false,"db":true,"providers":[],"ready":false}` | 2026-08-17 |
| 옛 엣지 캐시 | `/CLAUDE.md`·`/worker/index.js` 가 `cf-cache-status: HIT`, `age 499879`, `s-maxage 604800` → 남은 수명 약 **29시간** | 2026-08-17 |
| **Cloudflare Pages — Preview deployments (5판)** | 원문: hash 주소는 "atomic and **may always be visited in the future**" · Access 는 "this will **only protect your preview deployments** … does **not** protect your `*.pages.dev` domain or custom domain" · `wrangler pages deployment delete` — "**The latest deployment for a branch cannot be deleted.**" · <https://developers.cloudflare.com/pages/configuration/preview-deployments/> | **2026-08-17 (5판)** |
| **Cloudflare Workers — Cron Triggers 한도 (5판)** | 원문: "Number of Cron Triggers per account | **5** | 250" · "CPU time per Cron Trigger | **10 ms** | 30 seconds (< 1 hour interval) 15 min (>= 1 hour interval)" · <https://developers.cloudflare.com/workers/platform/limits/> ⚠️ **무료 플랜도 지원한다.** 4판의 「무료 플랜 지원 미확인」은 틀렸다 | **2026-08-17 (5판)** |
| **Cloudflare D1 — 한도·요금 (5판)** | DB 개수 Free **10** / Paid 50,000 · rows written **Free 100,000/일** · rows read Free 5M/일 · <https://developers.cloudflare.com/d1/platform/limits/> · <https://developers.cloudflare.com/d1/platform/pricing/> | **2026-08-17 (5판)** |
| **Google OpenID Connect (5판)** | 원문: `sub` 는 "An identifier for the user, **unique among all Google Accounts and never reused**." ⚠️ **클라이언트별로 값이 갈린다는 서술이 없다** — 「제공자 ID 는 서비스 전용」이라는 단정의 근거가 되지 못한다 · <https://developers.google.com/identity/openid-connect/openid-connect> | **2026-08-17 (5판)** |
| **카카오·네이버 회원 식별자 (5판)** | ❌ **확인하지 못했다.** `developers.kakao.com` 은 리다이렉트, `developers.naver.com` 은 접근 실패. **비공식 자료로 확정하지 않는다** — 법률 자료 `L13` | **2026-08-17 (5판)** |
| **라이브 내부 파일 노출** | canonical 7개 경로가 **여전히 원문 200** · `cf-cache-status: HIT` · `s-maxage 604800`. **오리진은 깨끗하다**(쿼리를 붙이면 index fallback 9,994B). ⛔ 5판은 「남은 수명 약 24시간」이라 적었으나 **그 시각이 지난 뒤에도 그대로다** — 만료 예상은 완료 근거가 아니다 | 5판 2026-08-17 · **재측정 2026-08-18 09:10 KST** |
| 개인정보 보호법 제15조·제22조·제22조의2·제28조의8 | 2단계 §19 의 확인 내용을 그대로 인용. **이 문서에서 법률 판단을 새로 하지 않았다** | 2026-08-16 |

---

## 20. 4단계(구현) 착수 조건

**아래 조건이 전부 충족되기 전에는 4단계 구현 코드를 한 줄도 쓰지 않는다.**

| # | 조건 | 상태 |
|---|---|---|
| 1 | 설계 결함 9건 수정 | ✅ **완료(2판)** |
| 1b | **3판 결함 6건 수정**(§0-3) | ✅ **완료(3판)** |
| 1c | **4판 결함 8건 수정**(§0-4) | ✅ **완료(4판)** |
| 1d | **5판 결함 11건 수정**(§0-5) | ✅ **완료(5판)** |
| 2 | 결정 1·2·6 확정 | ✅ **완료** (결정 2 는 4판에서 **의미 정정** — §17) |
| 3 | 결정 5 의 승인 조건 C1~C8 반영 | ✅ **완료(2·3판)** — 다만 **D1 생성은 여전히 별도 승인** |
| 4 | **외부 법률 검토 — L1~L15**(L9 는 6개 하위 질문) | ❌ **대기.** 자료는 `docs/PRIVACY_LEGAL_REVIEW_PACKET.md` |
| 5 | 결정 3(법적 분기) 확정 | ❌ 4번에 종속 |
| 6 | 결정 4(CASCADE·보존) 확정 | ❌ 4번에 종속 |
| 7 | **플랜 확인** — Time Travel 7/30일 · **수동 백업 실태**(§10-5 의 `BACKUP_MAX_AGE`) | ❌ 대시보드·운영 확인. D1 개수(Free 10)·쓰기 한도(Free 10만/일)는 **공식 문서로 확인됨**(§19) |
| 9 | **§10-8-1 의 D1~D12** — 옛 배포 차단 증명 | ❌ **restore 금지 해제의 조건.** 4단계 구현과 별개로 남는다 |
| 8 | 구현 직전 `SELECT COUNT(*) FROM users` == 0 (§14-0 release gate) | ⏳ 구현 직전에 |

**4·5·6 이 설계와 구현을 가르는 선이다.** 그전에 만들 수 있는 것은 없다 —
`policy_events` 의 `CHECK` 가 정해지지 않으면 스키마도, 가입 화면 체크박스도, 테스트도
확정할 수 없고, 먼저 만들면 **쓰지도 않은 테이블에 재생성 migration 을 쓰게 된다**(2단계 §4).

### 20-1. **전역 write drain 은 언제 만드나** — 4판 정정 (**당시 기록**)

> ⚠️ **아래는 4판 시점(2026-08-17)의 기록이다. 현재 상태가 아니다.** 그날 「사용자 결정 대기」였던
> 것은 2026-08-18 에 **결정 A′ 구현 완료**로 닫혔다 — 현재 상태의 원본은 §21-0 이고, 무엇이 어디서
> 검증되는지는 §13-6 의 T6·T6b·T6c·T47b·T47d 다. 이 절을 지우지 않는 이유는 「법률과 독립이니
> 먼저 해도 된다」가 승인이 아니었다는 판단 근거가 여기에만 남기 때문이다.

3판의 완료 보고는 "전역 drain 은 법률과 무관하니 **먼저 착수할 수 있다**"고 적었다.
그런데 같은 문서 §20 은 "전부 충족 전에는 코드 한 줄도 금지"라고 적고 있었다. **충돌이다.**
게다가 **그 권고는 사용자가 승인한 것이 아니다.** 4판은 아래처럼만 기록한다.

| # | 사실 |
|---|---|
| 1 | **4단계 전체 구현은 외부 법률 검토와 결정 3·4 전까지 미착수**다. 이것은 확정이다 |
| 2 | **전역 drain 의 후보 비교 계획은 3단계 문서에 있다**(§10-9-8 의 A/B/C). 설계로서는 끝났다 |
| 3 | **실제 전역 drain 코드 실험을 법률 검토와 병행할지는 사용자 결정 대기**다 — 법률 결정과 기술적으로 독립인 것은 맞지만, 그것이 곧 「해도 된다」는 승인은 아니다 |
| 4 | ✅ **2026-08-17 사용자 결정: 「나) 법률 검토 후」.** 런타임 구현은 4단계에서 한다. **다만 설계는 이번 5판에서 완성했다** — `restore_closed`(§10-7) · 금지 해제 조건 ①~⑧(§10-8-0) · ledger 자기복원 해결(§10-6-0) · 옛 배포 차단(§10-8-1) |
| 5 | 현재 **사용자 0명 · OAuth 비활성**이므로, **주 D1 restore 금지**(§10-8-0)를 유지하는 것으로 위험이 통제된다 — 복원해야 할 사용자 데이터가 아직 없다 |
| 6 | **OAuth 활성화 전까지는 전역 drain 의 방식과 구현 시점을 확정해야 한다.** 실제 계정이 하나라도 생기면 「복원 불가」가 곧 데이터 손실 위험이 된다 |

**즉 3번이 §20-1 의 사용자 결정 항목이었고, 2026-08-17 에 「나) 법률 검토 후」로 확정됐다.**

---

## 21. 최종 판정

<!-- 현재상태:시작 -->

### 21-0. 현재 상태 (2026-08-22 · 사용자 결정 반영)

**이 표가 현재 상태의 원본이다.** 아래 21-1 부터는 **역사 기록**이고, 거기 적힌 판정은 그
시점의 것이다 — 지금 상태로 읽지 않는다. 단계 정의의 원본은 `CLAUDE.md` §1-1.

| 무엇 | 지금 |
|---|---|
| 3단계 설계 | **9판 완료 2026-08-22.** 8판의 완료 판정은 재감사로 철회됐고(§0-9 · 위협 52~56), **사용자 결정 0~7 이 확정되면서 다시 닫혔다**(§12-3-4). 확정된 것: 엣지 방어 **A** · B 보류 · Turnstile 은 공개 가입에만 · 세션 envelope 도입 · D1 수치는 추정치 · 잔여 위험 6종 인정 · 출시 조건 9가지 |
| 4단계 로컬 구현 | **완료 2026-08-22.** 위협 52~56 의 코드(`EDGE_GUARD` 계약 · 두 층 리미터 · 운영자 readiness)와 결정 3·4 의 코드(Turnstile · 세션 envelope · 호스트 잠금 미들웨어)가 T65~T72 와 함께 들어갔다. ⚠️ **테스트가 통과한다는 사실은 완료의 근거가 아니다** — 이 저장소는 같은 이유로 여섯 판 연속 잘못 선언했다(6판~11판) |
| 4단계 원격 반영 | **갈라 읽는다.** ✅ 안전 동기화는 실행됐다 — 배포 `19e69dee`(계정 라우트를 닫아 둔 세대) · `READY_KEY` 등록 · 옛 배포 15개 삭제 · preview `8e16c92e`. ⛔ 계정 인프라는 **아무것도 하지 않았다** — 원격 D1 쓰기 0건 · `0005` 미적용 · ledger D1 미생성 · 새 시크릿 미등록 · 도메인·WAF·Turnstile 위젯 미생성 |
| 남용 방어 | **A 로 확정됐고 아직 구성 전이다.** `EDGE_GUARD` 가 없으므로 계정 라우트는 **DB 를 만지기 전에 503**. 절차는 `docs/OPS_RUNBOOK.md` §13-2 |
| 전역 user-data drain | **결정 A′ 로 구현 완료 2026-08-18 · 8판에서 마지막 예외를 닫았다**(§10-9-6 · 위협 49 · T62) |
| 법률 | **L1~L15 미검토.** 폐쇄 베타 조건에 포함된다(결정 7) |
| 주 D1 restore | **금지.** §10-8-0 의 ①~⑨ 중 ⑤·⑦·⑨ 미충족. ⚠️ 앱 코드는 복원을 막지 못한다 |
| 운영 절차서 | `docs/OPS_RUNBOOK.md`. §13 에 A 안 활성화 절차와 외부 점검, §5-1 에 세션 키 교체의 대가, §15 에 배포 후 D1 실측 |
| 공개 계정·공개 회원가입 | **No-Go.** 폐쇄 베타는 **아래 9가지를 모두 충족한 뒤에만** 연다 |

**폐쇄 계정 베타 진입 조건 9가지**(결정 7):
① 공개 배포 안전 동기화 ② 외부 법률 검토와 필수 수정 ③ 세션 envelope 구현과 변이 테스트
④ 커스텀 도메인과 WAF 설정 ⑤ ledger D1 과 모든 시크릿 등록 ⑥ 모든 OAuth 콜백 URI 갱신
⑦ 실제 공개 주소 보안 검증 ⑧ iOS·Android 설치형 PWA 검증 ⑨ 옛 배포 주소 차단과 롤백 절차 검증

<!-- 현재상태:끝 -->

### 21-1. 설계가 여섯 판까지 온 기록

**판마다 닫은 것은 「직전 판이 완료라고 선언한 뒤에 발견된 자리」다.** 5판에서는 둘이 Critical 이었고,
6판에서는 넷 다 **구현이 설계보다 느슨했던 자리**였다 — 설계가 빠진 것이 아니라 코드가 덜 지켰다.

| 판 | 무엇을 닫았나 |
|---|---|
| 2판 | 설계 결함 9건(§0-2) |
| 3판 | quiescence 증명 범위 · 주 D1 복원 금지 gate · promote-only · ledger 병합(§0-3) |
| 4판 | 가입 state 재사용에 의한 정책 증거 다중 귀속(위협 34) · 실패한 삭제의 지연 재실행(위협 35)(§0-4) |
| **5판** | **복원 중 읽기 노출**(위협 36 · Critical) · **ledger 자기복원**(위협 37 · Critical) · **가입 정보 평문 통과**(§5-4) · **옛 배포 차단의 합격 조건**(§10-8-1) · **정리 수단 부재**(위협 38) · 보유기간 산식 · R5 원자성 · 표식 키 · 낡은 참조(§0-5) |
| **6판** | **`LEDGER` 미바인딩 fail-open**(위협 39 · High) · **인증 없는 임의 경로의 쓰기 증폭**(위협 40 · High) · **미등록 리미터 버킷의 조용한 완화**(위협 41) · **복원 사전점검이 인자로 우회됨**(위협 42 · High) · 「복원을 막는다」는 표현 정정 · 설정·시크릿 계약의 자동 검사(§0-6) |

**당시 사실 — 2026-08-17(5판을 쓰던 시점).** 아래 표는 그날 「설계 완료가 뜻하지 않는 것」을
적어 둔 것이다. **지금 상태가 아니다** — 구현 줄은 2026-08-18 에 닫혔고, 현재 상태는 §21-0 이 말한다.
그래도 지우지 않는다: 설계 완료를 구현 완료로 읽었던 자리가 어디였는지가 이 표에 남아 있다.

| 아직 아닌 것 | 그날의 사실 |
|---|---|
| 구현 | **코드 0줄.** 가입 UI·API·`policy_events`·`consumed_signup_states`·migration·`restore_closed`·삭제 표식 D1·**정리 Worker**·`user-data drain`·테스트 파일 **전부 없다** |
| 법률 | **L1~L15 미검토.** 결정 3·4 가 열려 있고, 그 답이 스키마를 바꾼다 |
| 복원 안전성 | **§10-8-0 의 ①~⑧ 이 전부 충족되기 전까지 주 D1 restore 금지.** 5판에서 조건이 4개에서 **8개로 늘었다** — 설계가 진전되면서 필요한 것이 더 드러났기 때문이다. **설계가 끝났다고 복원이 안전해진 것이 아니다** |
| 옛 배포 차단 | **§10-8-1 의 D1~D12 미증명.** 실제 배포 삭제는 **별도 최종 승인** 사항이고 **되돌릴 수 없다** |
| 운영 반영 | 원격 D1·시크릿·OAuth·배포 — 이 문서 작업으로는 **아무것도 바뀌지 않았다** |

**공개 OAuth 활성화와 계정 출시는 No-Go 다** — 이것은 2026-08-18 에도 그대로다(§21-0).

> ⛔ **당시 사실 — 2026-08-17.** 이 판정은 **설계 문서에 대한 것**이었다. 그날 기준으로
> 코드·법률·배포·출시는 각각 별개의 완료 조건을 가지며 **어느 것도 완료되지 않았다.**
> 그중 코드는 2026-08-18 에 로컬로 닫혔다. 나머지 셋은 그대로다.

> ⚠️ **판이 여섯까지 온 것 자체가 기록이다.** 2·3·4·5판이 각각 「완료」를 선언했고 그때마다
> 다음 재감사가 Critical 또는 High 결함을 찾았다 — **6판도 그렇게 나왔다**(4단계 「로컬 구현
> 완료」 선언 뒤의 재감사에서 High 셋). **6판이 마지막이라는 보장은 없다.**
> 원격 반영 전에 한 번 더 독립 재감사를 받는 것을 권한다 — 특히 §0-6 의 라우트 분류표와
> 재개방 판정은 **새로 쓴 것이라 아직 아무에게도 검토받지 않았다.**
