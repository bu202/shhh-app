# 임시/응답 덤프
scripts/last-response.txt
scripts/all-resp.xml

# OS
.DS_Store

# 로그
*.log
node_modules/
todo-100.txt

# 이 기기에서만 쓰는 Claude Code 설정과 개인 복구 메모
.claude/settings.local.json
CLAUDE.local.md

# wrangler 의 로컬 KV 시뮬레이션. `kv key list` 를 --remote 없이 돌리면 생긴다
worker/.wrangler/
.wrangler/

# 배포 산출물. `npm run build` 가 만든다 — 레포에 두면 두 벌이 어긋난다
dist/

# 정리 Worker 의 **실제** 배포 설정. 템플릿은 worker/cleanup/wrangler.example.jsonc 이고,
# 여기에는 만들어진 ledger D1 의 database_id 가 들어간다(비밀값은 아니지만, 저장소에 두면
# 다음 사람이 템플릿과 실제 설정을 구분하지 못한다 — docs/OPS_RUNBOOK.md §3).
worker/cleanup/wrangler.jsonc
